/**
 * React v0.10.0
 */
!function(e){if("object"==typeof exports)module.exports=e();else if("function"==typeof define&&define.amd)define(e);else{var f;"undefined"!=typeof window?f=window:"undefined"!=typeof global?f=global:"undefined"!=typeof self&&(f=self),f.React=e()}}(function(){var define,module,exports;return (function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);throw new Error("Cannot find module '"+o+"'")}var f=n[o]={exports:{}};t[o][0].call(f.exports,function(e){var n=t[o][1][e];return s(n?n:e)},f,f.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule AutoFocusMixin
 * @typechecks static-only
 */

"use strict";

var focusNode = _dereq_("./focusNode");

var AutoFocusMixin = {
  componentDidMount: function() {
    if (this.props.autoFocus) {
      focusNode(this.getDOMNode());
    }
  }
};

module.exports = AutoFocusMixin;

},{"./focusNode":100}],2:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule CSSProperty
 */

"use strict";

/**
 * CSS properties which accept numbers but are not in units of "px".
 */
var isUnitlessNumber = {
  columnCount: true,
  fillOpacity: true,
  flex: true,
  flexGrow: true,
  flexShrink: true,
  fontWeight: true,
  lineClamp: true,
  lineHeight: true,
  opacity: true,
  order: true,
  orphans: true,
  widows: true,
  zIndex: true,
  zoom: true
};

/**
 * @param {string} prefix vendor-specific prefix, eg: Webkit
 * @param {string} key style name, eg: transitionDuration
 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
 * WebkitTransitionDuration
 */
function prefixKey(prefix, key) {
  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
}

/**
 * Support style names that may come passed in prefixed by adding permutations
 * of vendor prefixes.
 */
var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
// infinite loop, because it iterates over the newly added props too.
Object.keys(isUnitlessNumber).forEach(function(prop) {
  prefixes.forEach(function(prefix) {
    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
  });
});

/**
 * Most style properties can be unset by doing .style[prop] = '' but IE8
 * doesn't like doing that with shorthand properties so for the properties that
 * IE8 breaks on, which are listed here, we instead unset each of the
 * individual properties. See http://bugs.jquery.com/ticket/12385.
 * The 4-value 'clock' properties like margin, padding, border-width seem to
 * behave without any problems. Curiously, list-style works too without any
 * special prodding.
 */
var shorthandPropertyExpansions = {
  background: {
    backgroundImage: true,
    backgroundPosition: true,
    backgroundRepeat: true,
    backgroundColor: true
  },
  border: {
    borderWidth: true,
    borderStyle: true,
    borderColor: true
  },
  borderBottom: {
    borderBottomWidth: true,
    borderBottomStyle: true,
    borderBottomColor: true
  },
  borderLeft: {
    borderLeftWidth: true,
    borderLeftStyle: true,
    borderLeftColor: true
  },
  borderRight: {
    borderRightWidth: true,
    borderRightStyle: true,
    borderRightColor: true
  },
  borderTop: {
    borderTopWidth: true,
    borderTopStyle: true,
    borderTopColor: true
  },
  font: {
    fontStyle: true,
    fontVariant: true,
    fontWeight: true,
    fontSize: true,
    lineHeight: true,
    fontFamily: true
  }
};

var CSSProperty = {
  isUnitlessNumber: isUnitlessNumber,
  shorthandPropertyExpansions: shorthandPropertyExpansions
};

module.exports = CSSProperty;

},{}],3:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule CSSPropertyOperations
 * @typechecks static-only
 */

"use strict";

var CSSProperty = _dereq_("./CSSProperty");

var dangerousStyleValue = _dereq_("./dangerousStyleValue");
var escapeTextForBrowser = _dereq_("./escapeTextForBrowser");
var hyphenate = _dereq_("./hyphenate");
var memoizeStringOnly = _dereq_("./memoizeStringOnly");

var processStyleName = memoizeStringOnly(function(styleName) {
  return escapeTextForBrowser(hyphenate(styleName));
});

/**
 * Operations for dealing with CSS properties.
 */
var CSSPropertyOperations = {

  /**
   * Serializes a mapping of style properties for use as inline styles:
   *
   *   > createMarkupForStyles({width: '200px', height: 0})
   *   "width:200px;height:0;"
   *
   * Undefined values are ignored so that declarative programming is easier.
   *
   * @param {object} styles
   * @return {?string}
   */
  createMarkupForStyles: function(styles) {
    var serialized = '';
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      var styleValue = styles[styleName];
      if (styleValue != null) {
        serialized += processStyleName(styleName) + ':';
        serialized += dangerousStyleValue(styleName, styleValue) + ';';
      }
    }
    return serialized || null;
  },

  /**
   * Sets the value for multiple styles on a node.  If a value is specified as
   * '' (empty string), the corresponding style property will be unset.
   *
   * @param {DOMElement} node
   * @param {object} styles
   */
  setValueForStyles: function(node, styles) {
    var style = node.style;
    for (var styleName in styles) {
      if (!styles.hasOwnProperty(styleName)) {
        continue;
      }
      var styleValue = dangerousStyleValue(styleName, styles[styleName]);
      if (styleValue) {
        style[styleName] = styleValue;
      } else {
        var expansion = CSSProperty.shorthandPropertyExpansions[styleName];
        if (expansion) {
          // Shorthand property that IE8 won't like unsetting, so unset each
          // component to placate it
          for (var individualStyleName in expansion) {
            style[individualStyleName] = '';
          }
        } else {
          style[styleName] = '';
        }
      }
    }
  }

};

module.exports = CSSPropertyOperations;

},{"./CSSProperty":2,"./dangerousStyleValue":95,"./escapeTextForBrowser":98,"./hyphenate":110,"./memoizeStringOnly":120}],4:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ChangeEventPlugin
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventPluginHub = _dereq_("./EventPluginHub");
var EventPropagators = _dereq_("./EventPropagators");
var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");
var ReactUpdates = _dereq_("./ReactUpdates");
var SyntheticEvent = _dereq_("./SyntheticEvent");

var isEventSupported = _dereq_("./isEventSupported");
var isTextInputElement = _dereq_("./isTextInputElement");
var keyOf = _dereq_("./keyOf");

var topLevelTypes = EventConstants.topLevelTypes;

var eventTypes = {
  change: {
    phasedRegistrationNames: {
      bubbled: keyOf({onChange: null}),
      captured: keyOf({onChangeCapture: null})
    },
    dependencies: [
      topLevelTypes.topBlur,
      topLevelTypes.topChange,
      topLevelTypes.topClick,
      topLevelTypes.topFocus,
      topLevelTypes.topInput,
      topLevelTypes.topKeyDown,
      topLevelTypes.topKeyUp,
      topLevelTypes.topSelectionChange
    ]
  }
};

/**
 * For IE shims
 */
var activeElement = null;
var activeElementID = null;
var activeElementValue = null;
var activeElementValueProp = null;

/**
 * SECTION: handle `change` event
 */
function shouldUseChangeEvent(elem) {
  return (
    elem.nodeName === 'SELECT' ||
    (elem.nodeName === 'INPUT' && elem.type === 'file')
  );
}

var doesChangeEventBubble = false;
if (ExecutionEnvironment.canUseDOM) {
  // See `handleChange` comment below
  doesChangeEventBubble = isEventSupported('change') && (
    !('documentMode' in document) || document.documentMode > 8
  );
}

function manualDispatchChangeEvent(nativeEvent) {
  var event = SyntheticEvent.getPooled(
    eventTypes.change,
    activeElementID,
    nativeEvent
  );
  EventPropagators.accumulateTwoPhaseDispatches(event);

  // If change and propertychange bubbled, we'd just bind to it like all the
  // other events and have it go through ReactEventTopLevelCallback. Since it
  // doesn't, we manually listen for the events and so we have to enqueue and
  // process the abstract event manually.
  //
  // Batching is necessary here in order to ensure that all event handlers run
  // before the next rerender (including event handlers attached to ancestor
  // elements instead of directly on the input). Without this, controlled
  // components don't work properly in conjunction with event bubbling because
  // the component is rerendered and the value reverted before all the event
  // handlers can run. See https://github.com/facebook/react/issues/708.
  ReactUpdates.batchedUpdates(runEventInBatch, event);
}

function runEventInBatch(event) {
  EventPluginHub.enqueueEvents(event);
  EventPluginHub.processEventQueue();
}

function startWatchingForChangeEventIE8(target, targetID) {
  activeElement = target;
  activeElementID = targetID;
  activeElement.attachEvent('onchange', manualDispatchChangeEvent);
}

function stopWatchingForChangeEventIE8() {
  if (!activeElement) {
    return;
  }
  activeElement.detachEvent('onchange', manualDispatchChangeEvent);
  activeElement = null;
  activeElementID = null;
}

function getTargetIDForChangeEvent(
    topLevelType,
    topLevelTarget,
    topLevelTargetID) {
  if (topLevelType === topLevelTypes.topChange) {
    return topLevelTargetID;
  }
}
function handleEventsForChangeEventIE8(
    topLevelType,
    topLevelTarget,
    topLevelTargetID) {
  if (topLevelType === topLevelTypes.topFocus) {
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForChangeEventIE8();
    startWatchingForChangeEventIE8(topLevelTarget, topLevelTargetID);
  } else if (topLevelType === topLevelTypes.topBlur) {
    stopWatchingForChangeEventIE8();
  }
}


/**
 * SECTION: handle `input` event
 */
var isInputEventSupported = false;
if (ExecutionEnvironment.canUseDOM) {
  // IE9 claims to support the input event but fails to trigger it when
  // deleting text, so we ignore its input events
  isInputEventSupported = isEventSupported('input') && (
    !('documentMode' in document) || document.documentMode > 9
  );
}

/**
 * (For old IE.) Replacement getter/setter for the `value` property that gets
 * set on the active element.
 */
var newValueProp =  {
  get: function() {
    return activeElementValueProp.get.call(this);
  },
  set: function(val) {
    // Cast to a string so we can do equality checks.
    activeElementValue = '' + val;
    activeElementValueProp.set.call(this, val);
  }
};

/**
 * (For old IE.) Starts tracking propertychange events on the passed-in element
 * and override the value property so that we can distinguish user events from
 * value changes in JS.
 */
function startWatchingForValueChange(target, targetID) {
  activeElement = target;
  activeElementID = targetID;
  activeElementValue = target.value;
  activeElementValueProp = Object.getOwnPropertyDescriptor(
    target.constructor.prototype,
    'value'
  );

  Object.defineProperty(activeElement, 'value', newValueProp);
  activeElement.attachEvent('onpropertychange', handlePropertyChange);
}

/**
 * (For old IE.) Removes the event listeners from the currently-tracked element,
 * if any exists.
 */
function stopWatchingForValueChange() {
  if (!activeElement) {
    return;
  }

  // delete restores the original property definition
  delete activeElement.value;
  activeElement.detachEvent('onpropertychange', handlePropertyChange);

  activeElement = null;
  activeElementID = null;
  activeElementValue = null;
  activeElementValueProp = null;
}

/**
 * (For old IE.) Handles a propertychange event, sending a `change` event if
 * the value of the active element has changed.
 */
function handlePropertyChange(nativeEvent) {
  if (nativeEvent.propertyName !== 'value') {
    return;
  }
  var value = nativeEvent.srcElement.value;
  if (value === activeElementValue) {
    return;
  }
  activeElementValue = value;

  manualDispatchChangeEvent(nativeEvent);
}

/**
 * If a `change` event should be fired, returns the target's ID.
 */
function getTargetIDForInputEvent(
    topLevelType,
    topLevelTarget,
    topLevelTargetID) {
  if (topLevelType === topLevelTypes.topInput) {
    // In modern browsers (i.e., not IE8 or IE9), the input event is exactly
    // what we want so fall through here and trigger an abstract event
    return topLevelTargetID;
  }
}

// For IE8 and IE9.
function handleEventsForInputEventIE(
    topLevelType,
    topLevelTarget,
    topLevelTargetID) {
  if (topLevelType === topLevelTypes.topFocus) {
    // In IE8, we can capture almost all .value changes by adding a
    // propertychange handler and looking for events with propertyName
    // equal to 'value'
    // In IE9, propertychange fires for most input events but is buggy and
    // doesn't fire when text is deleted, but conveniently, selectionchange
    // appears to fire in all of the remaining cases so we catch those and
    // forward the event if the value has changed
    // In either case, we don't want to call the event handler if the value
    // is changed from JS so we redefine a setter for `.value` that updates
    // our activeElementValue variable, allowing us to ignore those changes
    //
    // stopWatching() should be a noop here but we call it just in case we
    // missed a blur event somehow.
    stopWatchingForValueChange();
    startWatchingForValueChange(topLevelTarget, topLevelTargetID);
  } else if (topLevelType === topLevelTypes.topBlur) {
    stopWatchingForValueChange();
  }
}

// For IE8 and IE9.
function getTargetIDForInputEventIE(
    topLevelType,
    topLevelTarget,
    topLevelTargetID) {
  if (topLevelType === topLevelTypes.topSelectionChange ||
      topLevelType === topLevelTypes.topKeyUp ||
      topLevelType === topLevelTypes.topKeyDown) {
    // On the selectionchange event, the target is just document which isn't
    // helpful for us so just check activeElement instead.
    //
    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
    // propertychange on the first input event after setting `value` from a
    // script and fires only keydown, keypress, keyup. Catching keyup usually
    // gets it and catching keydown lets us fire an event for the first
    // keystroke if user does a key repeat (it'll be a little delayed: right
    // before the second keystroke). Other input methods (e.g., paste) seem to
    // fire selectionchange normally.
    if (activeElement && activeElement.value !== activeElementValue) {
      activeElementValue = activeElement.value;
      return activeElementID;
    }
  }
}


/**
 * SECTION: handle `click` event
 */
function shouldUseClickEvent(elem) {
  // Use the `click` event to detect changes to checkbox and radio inputs.
  // This approach works across all browsers, whereas `change` does not fire
  // until `blur` in IE8.
  return (
    elem.nodeName === 'INPUT' &&
    (elem.type === 'checkbox' || elem.type === 'radio')
  );
}

function getTargetIDForClickEvent(
    topLevelType,
    topLevelTarget,
    topLevelTargetID) {
  if (topLevelType === topLevelTypes.topClick) {
    return topLevelTargetID;
  }
}

/**
 * This plugin creates an `onChange` event that normalizes change events
 * across form elements. This event fires at a time when it's possible to
 * change the element's value without seeing a flicker.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - select
 */
var ChangeEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {

    var getTargetIDFunc, handleEventFunc;
    if (shouldUseChangeEvent(topLevelTarget)) {
      if (doesChangeEventBubble) {
        getTargetIDFunc = getTargetIDForChangeEvent;
      } else {
        handleEventFunc = handleEventsForChangeEventIE8;
      }
    } else if (isTextInputElement(topLevelTarget)) {
      if (isInputEventSupported) {
        getTargetIDFunc = getTargetIDForInputEvent;
      } else {
        getTargetIDFunc = getTargetIDForInputEventIE;
        handleEventFunc = handleEventsForInputEventIE;
      }
    } else if (shouldUseClickEvent(topLevelTarget)) {
      getTargetIDFunc = getTargetIDForClickEvent;
    }

    if (getTargetIDFunc) {
      var targetID = getTargetIDFunc(
        topLevelType,
        topLevelTarget,
        topLevelTargetID
      );
      if (targetID) {
        var event = SyntheticEvent.getPooled(
          eventTypes.change,
          targetID,
          nativeEvent
        );
        EventPropagators.accumulateTwoPhaseDispatches(event);
        return event;
      }
    }

    if (handleEventFunc) {
      handleEventFunc(
        topLevelType,
        topLevelTarget,
        topLevelTargetID
      );
    }
  }

};

module.exports = ChangeEventPlugin;

},{"./EventConstants":14,"./EventPluginHub":16,"./EventPropagators":19,"./ExecutionEnvironment":20,"./ReactUpdates":71,"./SyntheticEvent":78,"./isEventSupported":113,"./isTextInputElement":115,"./keyOf":119}],5:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ClientReactRootIndex
 * @typechecks
 */

"use strict";

var nextReactRootIndex = 0;

var ClientReactRootIndex = {
  createReactRootIndex: function() {
    return nextReactRootIndex++;
  }
};

module.exports = ClientReactRootIndex;

},{}],6:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule CompositionEventPlugin
 * @typechecks static-only
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventPropagators = _dereq_("./EventPropagators");
var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");
var ReactInputSelection = _dereq_("./ReactInputSelection");
var SyntheticCompositionEvent = _dereq_("./SyntheticCompositionEvent");

var getTextContentAccessor = _dereq_("./getTextContentAccessor");
var keyOf = _dereq_("./keyOf");

var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
var START_KEYCODE = 229;

var useCompositionEvent = (
  ExecutionEnvironment.canUseDOM &&
  'CompositionEvent' in window
);

// In IE9+, we have access to composition events, but the data supplied
// by the native compositionend event may be incorrect. In Korean, for example,
// the compositionend event contains only one character regardless of
// how many characters have been composed since compositionstart.
// We therefore use the fallback data while still using the native
// events as triggers.
var useFallbackData = (
  !useCompositionEvent ||
  'documentMode' in document && document.documentMode > 8
);

var topLevelTypes = EventConstants.topLevelTypes;
var currentComposition = null;

// Events and their corresponding property names.
var eventTypes = {
  compositionEnd: {
    phasedRegistrationNames: {
      bubbled: keyOf({onCompositionEnd: null}),
      captured: keyOf({onCompositionEndCapture: null})
    },
    dependencies: [
      topLevelTypes.topBlur,
      topLevelTypes.topCompositionEnd,
      topLevelTypes.topKeyDown,
      topLevelTypes.topKeyPress,
      topLevelTypes.topKeyUp,
      topLevelTypes.topMouseDown
    ]
  },
  compositionStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({onCompositionStart: null}),
      captured: keyOf({onCompositionStartCapture: null})
    },
    dependencies: [
      topLevelTypes.topBlur,
      topLevelTypes.topCompositionStart,
      topLevelTypes.topKeyDown,
      topLevelTypes.topKeyPress,
      topLevelTypes.topKeyUp,
      topLevelTypes.topMouseDown
    ]
  },
  compositionUpdate: {
    phasedRegistrationNames: {
      bubbled: keyOf({onCompositionUpdate: null}),
      captured: keyOf({onCompositionUpdateCapture: null})
    },
    dependencies: [
      topLevelTypes.topBlur,
      topLevelTypes.topCompositionUpdate,
      topLevelTypes.topKeyDown,
      topLevelTypes.topKeyPress,
      topLevelTypes.topKeyUp,
      topLevelTypes.topMouseDown
    ]
  }
};

/**
 * Translate native top level events into event types.
 *
 * @param {string} topLevelType
 * @return {object}
 */
function getCompositionEventType(topLevelType) {
  switch (topLevelType) {
    case topLevelTypes.topCompositionStart:
      return eventTypes.compositionStart;
    case topLevelTypes.topCompositionEnd:
      return eventTypes.compositionEnd;
    case topLevelTypes.topCompositionUpdate:
      return eventTypes.compositionUpdate;
  }
}

/**
 * Does our fallback best-guess model think this event signifies that
 * composition has begun?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackStart(topLevelType, nativeEvent) {
  return (
    topLevelType === topLevelTypes.topKeyDown &&
    nativeEvent.keyCode === START_KEYCODE
  );
}

/**
 * Does our fallback mode think that this event is the end of composition?
 *
 * @param {string} topLevelType
 * @param {object} nativeEvent
 * @return {boolean}
 */
function isFallbackEnd(topLevelType, nativeEvent) {
  switch (topLevelType) {
    case topLevelTypes.topKeyUp:
      // Command keys insert or clear IME input.
      return (END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1);
    case topLevelTypes.topKeyDown:
      // Expect IME keyCode on each keydown. If we get any other
      // code we must have exited earlier.
      return (nativeEvent.keyCode !== START_KEYCODE);
    case topLevelTypes.topKeyPress:
    case topLevelTypes.topMouseDown:
    case topLevelTypes.topBlur:
      // Events are not possible without cancelling IME.
      return true;
    default:
      return false;
  }
}

/**
 * Helper class stores information about selection and document state
 * so we can figure out what changed at a later date.
 *
 * @param {DOMEventTarget} root
 */
function FallbackCompositionState(root) {
  this.root = root;
  this.startSelection = ReactInputSelection.getSelection(root);
  this.startValue = this.getText();
}

/**
 * Get current text of input.
 *
 * @return {string}
 */
FallbackCompositionState.prototype.getText = function() {
  return this.root.value || this.root[getTextContentAccessor()];
};

/**
 * Text that has changed since the start of composition.
 *
 * @return {string}
 */
FallbackCompositionState.prototype.getData = function() {
  var endValue = this.getText();
  var prefixLength = this.startSelection.start;
  var suffixLength = this.startValue.length - this.startSelection.end;

  return endValue.substr(
    prefixLength,
    endValue.length - suffixLength - prefixLength
  );
};

/**
 * This plugin creates `onCompositionStart`, `onCompositionUpdate` and
 * `onCompositionEnd` events on inputs, textareas and contentEditable
 * nodes.
 */
var CompositionEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {

    var eventType;
    var data;

    if (useCompositionEvent) {
      eventType = getCompositionEventType(topLevelType);
    } else if (!currentComposition) {
      if (isFallbackStart(topLevelType, nativeEvent)) {
        eventType = eventTypes.compositionStart;
      }
    } else if (isFallbackEnd(topLevelType, nativeEvent)) {
      eventType = eventTypes.compositionEnd;
    }

    if (useFallbackData) {
      // The current composition is stored statically and must not be
      // overwritten while composition continues.
      if (!currentComposition && eventType === eventTypes.compositionStart) {
        currentComposition = new FallbackCompositionState(topLevelTarget);
      } else if (eventType === eventTypes.compositionEnd) {
        if (currentComposition) {
          data = currentComposition.getData();
          currentComposition = null;
        }
      }
    }

    if (eventType) {
      var event = SyntheticCompositionEvent.getPooled(
        eventType,
        topLevelTargetID,
        nativeEvent
      );
      if (data) {
        // Inject data generated from fallback path into the synthetic event.
        // This matches the property of native CompositionEventInterface.
        event.data = data;
      }
      EventPropagators.accumulateTwoPhaseDispatches(event);
      return event;
    }
  }
};

module.exports = CompositionEventPlugin;

},{"./EventConstants":14,"./EventPropagators":19,"./ExecutionEnvironment":20,"./ReactInputSelection":52,"./SyntheticCompositionEvent":76,"./getTextContentAccessor":108,"./keyOf":119}],7:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule DOMChildrenOperations
 * @typechecks static-only
 */

"use strict";

var Danger = _dereq_("./Danger");
var ReactMultiChildUpdateTypes = _dereq_("./ReactMultiChildUpdateTypes");

var getTextContentAccessor = _dereq_("./getTextContentAccessor");

/**
 * The DOM property to use when setting text content.
 *
 * @type {string}
 * @private
 */
var textContentAccessor = getTextContentAccessor();

/**
 * Inserts `childNode` as a child of `parentNode` at the `index`.
 *
 * @param {DOMElement} parentNode Parent node in which to insert.
 * @param {DOMElement} childNode Child node to insert.
 * @param {number} index Index at which to insert the child.
 * @internal
 */
function insertChildAt(parentNode, childNode, index) {
  var childNodes = parentNode.childNodes;
  if (childNodes[index] === childNode) {
    return;
  }
  // If `childNode` is already a child of `parentNode`, remove it so that
  // computing `childNodes[index]` takes into account the removal.
  if (childNode.parentNode === parentNode) {
    parentNode.removeChild(childNode);
  }
  if (index >= childNodes.length) {
    parentNode.appendChild(childNode);
  } else {
    parentNode.insertBefore(childNode, childNodes[index]);
  }
}

var updateTextContent;
if (textContentAccessor === 'textContent') {
  /**
   * Sets the text content of `node` to `text`.
   *
   * @param {DOMElement} node Node to change
   * @param {string} text New text content
   */
  updateTextContent = function(node, text) {
    node.textContent = text;
  };
} else {
  /**
   * Sets the text content of `node` to `text`.
   *
   * @param {DOMElement} node Node to change
   * @param {string} text New text content
   */
  updateTextContent = function(node, text) {
    // In order to preserve newlines correctly, we can't use .innerText to set
    // the contents (see #1080), so we empty the element then append a text node
    while (node.firstChild) {
      node.removeChild(node.firstChild);
    }
    if (text) {
      var doc = node.ownerDocument || document;
      node.appendChild(doc.createTextNode(text));
    }
  };
}

/**
 * Operations for updating with DOM children.
 */
var DOMChildrenOperations = {

  dangerouslyReplaceNodeWithMarkup: Danger.dangerouslyReplaceNodeWithMarkup,

  updateTextContent: updateTextContent,

  /**
   * Updates a component's children by processing a series of updates. The
   * update configurations are each expected to have a `parentNode` property.
   *
   * @param {array<object>} updates List of update configurations.
   * @param {array<string>} markupList List of markup strings.
   * @internal
   */
  processUpdates: function(updates, markupList) {
    var update;
    // Mapping from parent IDs to initial child orderings.
    var initialChildren = null;
    // List of children that will be moved or removed.
    var updatedChildren = null;

    for (var i = 0; update = updates[i]; i++) {
      if (update.type === ReactMultiChildUpdateTypes.MOVE_EXISTING ||
          update.type === ReactMultiChildUpdateTypes.REMOVE_NODE) {
        var updatedIndex = update.fromIndex;
        var updatedChild = update.parentNode.childNodes[updatedIndex];
        var parentID = update.parentID;

        initialChildren = initialChildren || {};
        initialChildren[parentID] = initialChildren[parentID] || [];
        initialChildren[parentID][updatedIndex] = updatedChild;

        updatedChildren = updatedChildren || [];
        updatedChildren.push(updatedChild);
      }
    }

    var renderedMarkup = Danger.dangerouslyRenderMarkup(markupList);

    // Remove updated children first so that `toIndex` is consistent.
    if (updatedChildren) {
      for (var j = 0; j < updatedChildren.length; j++) {
        updatedChildren[j].parentNode.removeChild(updatedChildren[j]);
      }
    }

    for (var k = 0; update = updates[k]; k++) {
      switch (update.type) {
        case ReactMultiChildUpdateTypes.INSERT_MARKUP:
          insertChildAt(
            update.parentNode,
            renderedMarkup[update.markupIndex],
            update.toIndex
          );
          break;
        case ReactMultiChildUpdateTypes.MOVE_EXISTING:
          insertChildAt(
            update.parentNode,
            initialChildren[update.parentID][update.fromIndex],
            update.toIndex
          );
          break;
        case ReactMultiChildUpdateTypes.TEXT_CONTENT:
          updateTextContent(
            update.parentNode,
            update.textContent
          );
          break;
        case ReactMultiChildUpdateTypes.REMOVE_NODE:
          // Already removed by the for-loop above.
          break;
      }
    }
  }

};

module.exports = DOMChildrenOperations;

},{"./Danger":10,"./ReactMultiChildUpdateTypes":58,"./getTextContentAccessor":108}],8:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule DOMProperty
 * @typechecks static-only
 */

/*jslint bitwise: true */

"use strict";

var invariant = _dereq_("./invariant");

var DOMPropertyInjection = {
  /**
   * Mapping from normalized, camelcased property names to a configuration that
   * specifies how the associated DOM property should be accessed or rendered.
   */
  MUST_USE_ATTRIBUTE: 0x1,
  MUST_USE_PROPERTY: 0x2,
  HAS_SIDE_EFFECTS: 0x4,
  HAS_BOOLEAN_VALUE: 0x8,
  HAS_POSITIVE_NUMERIC_VALUE: 0x10,

  /**
   * Inject some specialized knowledge about the DOM. This takes a config object
   * with the following properties:
   *
   * isCustomAttribute: function that given an attribute name will return true
   * if it can be inserted into the DOM verbatim. Useful for data-* or aria-*
   * attributes where it's impossible to enumerate all of the possible
   * attribute names,
   *
   * Properties: object mapping DOM property name to one of the
   * DOMPropertyInjection constants or null. If your attribute isn't in here,
   * it won't get written to the DOM.
   *
   * DOMAttributeNames: object mapping React attribute name to the DOM
   * attribute name. Attribute names not specified use the **lowercase**
   * normalized name.
   *
   * DOMPropertyNames: similar to DOMAttributeNames but for DOM properties.
   * Property names not specified use the normalized name.
   *
   * DOMMutationMethods: Properties that require special mutation methods. If
   * `value` is undefined, the mutation method should unset the property.
   *
   * @param {object} domPropertyConfig the config as described above.
   */
  injectDOMPropertyConfig: function(domPropertyConfig) {
    var Properties = domPropertyConfig.Properties || {};
    var DOMAttributeNames = domPropertyConfig.DOMAttributeNames || {};
    var DOMPropertyNames = domPropertyConfig.DOMPropertyNames || {};
    var DOMMutationMethods = domPropertyConfig.DOMMutationMethods || {};

    if (domPropertyConfig.isCustomAttribute) {
      DOMProperty._isCustomAttributeFunctions.push(
        domPropertyConfig.isCustomAttribute
      );
    }

    for (var propName in Properties) {
      ("production" !== "development" ? invariant(
        !DOMProperty.isStandardName[propName],
        'injectDOMPropertyConfig(...): You\'re trying to inject DOM property ' +
        '\'%s\' which has already been injected. You may be accidentally ' +
        'injecting the same DOM property config twice, or you may be ' +
        'injecting two configs that have conflicting property names.',
        propName
      ) : invariant(!DOMProperty.isStandardName[propName]));

      DOMProperty.isStandardName[propName] = true;

      var lowerCased = propName.toLowerCase();
      DOMProperty.getPossibleStandardName[lowerCased] = propName;

      var attributeName = DOMAttributeNames[propName];
      if (attributeName) {
        DOMProperty.getPossibleStandardName[attributeName] = propName;
      }

      DOMProperty.getAttributeName[propName] = attributeName || lowerCased;

      DOMProperty.getPropertyName[propName] =
        DOMPropertyNames[propName] || propName;

      var mutationMethod = DOMMutationMethods[propName];
      if (mutationMethod) {
        DOMProperty.getMutationMethod[propName] = mutationMethod;
      }

      var propConfig = Properties[propName];
      DOMProperty.mustUseAttribute[propName] =
        propConfig & DOMPropertyInjection.MUST_USE_ATTRIBUTE;
      DOMProperty.mustUseProperty[propName] =
        propConfig & DOMPropertyInjection.MUST_USE_PROPERTY;
      DOMProperty.hasSideEffects[propName] =
        propConfig & DOMPropertyInjection.HAS_SIDE_EFFECTS;
      DOMProperty.hasBooleanValue[propName] =
        propConfig & DOMPropertyInjection.HAS_BOOLEAN_VALUE;
      DOMProperty.hasPositiveNumericValue[propName] =
        propConfig & DOMPropertyInjection.HAS_POSITIVE_NUMERIC_VALUE;

      ("production" !== "development" ? invariant(
        !DOMProperty.mustUseAttribute[propName] ||
          !DOMProperty.mustUseProperty[propName],
        'DOMProperty: Cannot require using both attribute and property: %s',
        propName
      ) : invariant(!DOMProperty.mustUseAttribute[propName] ||
        !DOMProperty.mustUseProperty[propName]));
      ("production" !== "development" ? invariant(
        DOMProperty.mustUseProperty[propName] ||
          !DOMProperty.hasSideEffects[propName],
        'DOMProperty: Properties that have side effects must use property: %s',
        propName
      ) : invariant(DOMProperty.mustUseProperty[propName] ||
        !DOMProperty.hasSideEffects[propName]));
      ("production" !== "development" ? invariant(
        !DOMProperty.hasBooleanValue[propName] ||
          !DOMProperty.hasPositiveNumericValue[propName],
        'DOMProperty: Cannot have both boolean and positive numeric value: %s',
        propName
      ) : invariant(!DOMProperty.hasBooleanValue[propName] ||
        !DOMProperty.hasPositiveNumericValue[propName]));
    }
  }
};
var defaultValueCache = {};

/**
 * DOMProperty exports lookup objects that can be used like functions:
 *
 *   > DOMProperty.isValid['id']
 *   true
 *   > DOMProperty.isValid['foobar']
 *   undefined
 *
 * Although this may be confusing, it performs better in general.
 *
 * @see http://jsperf.com/key-exists
 * @see http://jsperf.com/key-missing
 */
var DOMProperty = {

  ID_ATTRIBUTE_NAME: 'data-reactid',

  /**
   * Checks whether a property name is a standard property.
   * @type {Object}
   */
  isStandardName: {},

  /**
   * Mapping from lowercase property names to the properly cased version, used
   * to warn in the case of missing properties.
   * @type {Object}
   */
  getPossibleStandardName: {},

  /**
   * Mapping from normalized names to attribute names that differ. Attribute
   * names are used when rendering markup or with `*Attribute()`.
   * @type {Object}
   */
  getAttributeName: {},

  /**
   * Mapping from normalized names to properties on DOM node instances.
   * (This includes properties that mutate due to external factors.)
   * @type {Object}
   */
  getPropertyName: {},

  /**
   * Mapping from normalized names to mutation methods. This will only exist if
   * mutation cannot be set simply by the property or `setAttribute()`.
   * @type {Object}
   */
  getMutationMethod: {},

  /**
   * Whether the property must be accessed and mutated as an object property.
   * @type {Object}
   */
  mustUseAttribute: {},

  /**
   * Whether the property must be accessed and mutated using `*Attribute()`.
   * (This includes anything that fails `<propName> in <element>`.)
   * @type {Object}
   */
  mustUseProperty: {},

  /**
   * Whether or not setting a value causes side effects such as triggering
   * resources to be loaded or text selection changes. We must ensure that
   * the value is only set if it has changed.
   * @type {Object}
   */
  hasSideEffects: {},

  /**
   * Whether the property should be removed when set to a falsey value.
   * @type {Object}
   */
  hasBooleanValue: {},

  /**
   * Whether the property must be positive numeric or parse as a positive
   * numeric and should be removed when set to a falsey value.
   * @type {Object}
   */
  hasPositiveNumericValue: {},

  /**
   * All of the isCustomAttribute() functions that have been injected.
   */
  _isCustomAttributeFunctions: [],

  /**
   * Checks whether a property name is a custom attribute.
   * @method
   */
  isCustomAttribute: function(attributeName) {
    for (var i = 0; i < DOMProperty._isCustomAttributeFunctions.length; i++) {
      var isCustomAttributeFn = DOMProperty._isCustomAttributeFunctions[i];
      if (isCustomAttributeFn(attributeName)) {
        return true;
      }
    }
    return false;
  },

  /**
   * Returns the default property value for a DOM property (i.e., not an
   * attribute). Most default values are '' or false, but not all. Worse yet,
   * some (in particular, `type`) vary depending on the type of element.
   *
   * TODO: Is it better to grab all the possible properties when creating an
   * element to avoid having to create the same element twice?
   */
  getDefaultValueForProperty: function(nodeName, prop) {
    var nodeDefaults = defaultValueCache[nodeName];
    var testElement;
    if (!nodeDefaults) {
      defaultValueCache[nodeName] = nodeDefaults = {};
    }
    if (!(prop in nodeDefaults)) {
      testElement = document.createElement(nodeName);
      nodeDefaults[prop] = testElement[prop];
    }
    return nodeDefaults[prop];
  },

  injection: DOMPropertyInjection
};

module.exports = DOMProperty;

},{"./invariant":112}],9:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule DOMPropertyOperations
 * @typechecks static-only
 */

"use strict";

var DOMProperty = _dereq_("./DOMProperty");

var escapeTextForBrowser = _dereq_("./escapeTextForBrowser");
var memoizeStringOnly = _dereq_("./memoizeStringOnly");
var warning = _dereq_("./warning");

function shouldIgnoreValue(name, value) {
  return value == null ||
    DOMProperty.hasBooleanValue[name] && !value ||
    DOMProperty.hasPositiveNumericValue[name] && (isNaN(value) || value < 1);
}

var processAttributeNameAndPrefix = memoizeStringOnly(function(name) {
  return escapeTextForBrowser(name) + '="';
});

if ("production" !== "development") {
  var reactProps = {
    children: true,
    dangerouslySetInnerHTML: true,
    key: true,
    ref: true
  };
  var warnedProperties = {};

  var warnUnknownProperty = function(name) {
    if (reactProps[name] || warnedProperties[name]) {
      return;
    }

    warnedProperties[name] = true;
    var lowerCasedName = name.toLowerCase();

    // data-* attributes should be lowercase; suggest the lowercase version
    var standardName = DOMProperty.isCustomAttribute(lowerCasedName) ?
      lowerCasedName : DOMProperty.getPossibleStandardName[lowerCasedName];

    // For now, only warn when we have a suggested correction. This prevents
    // logging too much when using transferPropsTo.
    ("production" !== "development" ? warning(
      standardName == null,
      'Unknown DOM property ' + name + '. Did you mean ' + standardName + '?'
    ) : null);

  };
}

/**
 * Operations for dealing with DOM properties.
 */
var DOMPropertyOperations = {

  /**
   * Creates markup for the ID property.
   *
   * @param {string} id Unescaped ID.
   * @return {string} Markup string.
   */
  createMarkupForID: function(id) {
    return processAttributeNameAndPrefix(DOMProperty.ID_ATTRIBUTE_NAME) +
      escapeTextForBrowser(id) + '"';
  },

  /**
   * Creates markup for a property.
   *
   * @param {string} name
   * @param {*} value
   * @return {?string} Markup string, or null if the property was invalid.
   */
  createMarkupForProperty: function(name, value) {
    if (DOMProperty.isStandardName[name]) {
      if (shouldIgnoreValue(name, value)) {
        return '';
      }
      var attributeName = DOMProperty.getAttributeName[name];
      if (DOMProperty.hasBooleanValue[name]) {
        return escapeTextForBrowser(attributeName);
      }
      return processAttributeNameAndPrefix(attributeName) +
        escapeTextForBrowser(value) + '"';
    } else if (DOMProperty.isCustomAttribute(name)) {
      if (value == null) {
        return '';
      }
      return processAttributeNameAndPrefix(name) +
        escapeTextForBrowser(value) + '"';
    } else if ("production" !== "development") {
      warnUnknownProperty(name);
    }
    return null;
  },

  /**
   * Sets the value for a property on a node.
   *
   * @param {DOMElement} node
   * @param {string} name
   * @param {*} value
   */
  setValueForProperty: function(node, name, value) {
    if (DOMProperty.isStandardName[name]) {
      var mutationMethod = DOMProperty.getMutationMethod[name];
      if (mutationMethod) {
        mutationMethod(node, value);
      } else if (shouldIgnoreValue(name, value)) {
        this.deleteValueForProperty(node, name);
      } else if (DOMProperty.mustUseAttribute[name]) {
        node.setAttribute(DOMProperty.getAttributeName[name], '' + value);
      } else {
        var propName = DOMProperty.getPropertyName[name];
        if (!DOMProperty.hasSideEffects[name] || node[propName] !== value) {
          node[propName] = value;
        }
      }
    } else if (DOMProperty.isCustomAttribute(name)) {
      if (value == null) {
        node.removeAttribute(DOMProperty.getAttributeName[name]);
      } else {
        node.setAttribute(name, '' + value);
      }
    } else if ("production" !== "development") {
      warnUnknownProperty(name);
    }
  },

  /**
   * Deletes the value for a property on a node.
   *
   * @param {DOMElement} node
   * @param {string} name
   */
  deleteValueForProperty: function(node, name) {
    if (DOMProperty.isStandardName[name]) {
      var mutationMethod = DOMProperty.getMutationMethod[name];
      if (mutationMethod) {
        mutationMethod(node, undefined);
      } else if (DOMProperty.mustUseAttribute[name]) {
        node.removeAttribute(DOMProperty.getAttributeName[name]);
      } else {
        var propName = DOMProperty.getPropertyName[name];
        var defaultValue = DOMProperty.getDefaultValueForProperty(
          node.nodeName,
          propName
        );
        if (!DOMProperty.hasSideEffects[name] ||
            node[propName] !== defaultValue) {
          node[propName] = defaultValue;
        }
      }
    } else if (DOMProperty.isCustomAttribute(name)) {
      node.removeAttribute(name);
    } else if ("production" !== "development") {
      warnUnknownProperty(name);
    }
  }

};

module.exports = DOMPropertyOperations;

},{"./DOMProperty":8,"./escapeTextForBrowser":98,"./memoizeStringOnly":120,"./warning":134}],10:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule Danger
 * @typechecks static-only
 */

/*jslint evil: true, sub: true */

"use strict";

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var createNodesFromMarkup = _dereq_("./createNodesFromMarkup");
var emptyFunction = _dereq_("./emptyFunction");
var getMarkupWrap = _dereq_("./getMarkupWrap");
var invariant = _dereq_("./invariant");

var OPEN_TAG_NAME_EXP = /^(<[^ \/>]+)/;
var RESULT_INDEX_ATTR = 'data-danger-index';

/**
 * Extracts the `nodeName` from a string of markup.
 *
 * NOTE: Extracting the `nodeName` does not require a regular expression match
 * because we make assumptions about React-generated markup (i.e. there are no
 * spaces surrounding the opening tag and there is at least one attribute).
 *
 * @param {string} markup String of markup.
 * @return {string} Node name of the supplied markup.
 * @see http://jsperf.com/extract-nodename
 */
function getNodeName(markup) {
  return markup.substring(1, markup.indexOf(' '));
}

var Danger = {

  /**
   * Renders markup into an array of nodes. The markup is expected to render
   * into a list of root nodes. Also, the length of `resultList` and
   * `markupList` should be the same.
   *
   * @param {array<string>} markupList List of markup strings to render.
   * @return {array<DOMElement>} List of rendered nodes.
   * @internal
   */
  dangerouslyRenderMarkup: function(markupList) {
    ("production" !== "development" ? invariant(
      ExecutionEnvironment.canUseDOM,
      'dangerouslyRenderMarkup(...): Cannot render markup in a Worker ' +
      'thread. This is likely a bug in the framework. Please report ' +
      'immediately.'
    ) : invariant(ExecutionEnvironment.canUseDOM));
    var nodeName;
    var markupByNodeName = {};
    // Group markup by `nodeName` if a wrap is necessary, else by '*'.
    for (var i = 0; i < markupList.length; i++) {
      ("production" !== "development" ? invariant(
        markupList[i],
        'dangerouslyRenderMarkup(...): Missing markup.'
      ) : invariant(markupList[i]));
      nodeName = getNodeName(markupList[i]);
      nodeName = getMarkupWrap(nodeName) ? nodeName : '*';
      markupByNodeName[nodeName] = markupByNodeName[nodeName] || [];
      markupByNodeName[nodeName][i] = markupList[i];
    }
    var resultList = [];
    var resultListAssignmentCount = 0;
    for (nodeName in markupByNodeName) {
      if (!markupByNodeName.hasOwnProperty(nodeName)) {
        continue;
      }
      var markupListByNodeName = markupByNodeName[nodeName];

      // This for-in loop skips the holes of the sparse array. The order of
      // iteration should follow the order of assignment, which happens to match
      // numerical index order, but we don't rely on that.
      for (var resultIndex in markupListByNodeName) {
        if (markupListByNodeName.hasOwnProperty(resultIndex)) {
          var markup = markupListByNodeName[resultIndex];

          // Push the requested markup with an additional RESULT_INDEX_ATTR
          // attribute.  If the markup does not start with a < character, it
          // will be discarded below (with an appropriate console.error).
          markupListByNodeName[resultIndex] = markup.replace(
            OPEN_TAG_NAME_EXP,
            // This index will be parsed back out below.
            '$1 ' + RESULT_INDEX_ATTR + '="' + resultIndex + '" '
          );
        }
      }

      // Render each group of markup with similar wrapping `nodeName`.
      var renderNodes = createNodesFromMarkup(
        markupListByNodeName.join(''),
        emptyFunction // Do nothing special with <script> tags.
      );

      for (i = 0; i < renderNodes.length; ++i) {
        var renderNode = renderNodes[i];
        if (renderNode.hasAttribute &&
            renderNode.hasAttribute(RESULT_INDEX_ATTR)) {

          resultIndex = +renderNode.getAttribute(RESULT_INDEX_ATTR);
          renderNode.removeAttribute(RESULT_INDEX_ATTR);

          ("production" !== "development" ? invariant(
            !resultList.hasOwnProperty(resultIndex),
            'Danger: Assigning to an already-occupied result index.'
          ) : invariant(!resultList.hasOwnProperty(resultIndex)));

          resultList[resultIndex] = renderNode;

          // This should match resultList.length and markupList.length when
          // we're done.
          resultListAssignmentCount += 1;

        } else if ("production" !== "development") {
          console.error(
            "Danger: Discarding unexpected node:",
            renderNode
          );
        }
      }
    }

    // Although resultList was populated out of order, it should now be a dense
    // array.
    ("production" !== "development" ? invariant(
      resultListAssignmentCount === resultList.length,
      'Danger: Did not assign to every index of resultList.'
    ) : invariant(resultListAssignmentCount === resultList.length));

    ("production" !== "development" ? invariant(
      resultList.length === markupList.length,
      'Danger: Expected markup to render %s nodes, but rendered %s.',
      markupList.length,
      resultList.length
    ) : invariant(resultList.length === markupList.length));

    return resultList;
  },

  /**
   * Replaces a node with a string of markup at its current position within its
   * parent. The markup must render into a single root node.
   *
   * @param {DOMElement} oldChild Child node to replace.
   * @param {string} markup Markup to render in place of the child node.
   * @internal
   */
  dangerouslyReplaceNodeWithMarkup: function(oldChild, markup) {
    ("production" !== "development" ? invariant(
      ExecutionEnvironment.canUseDOM,
      'dangerouslyReplaceNodeWithMarkup(...): Cannot render markup in a ' +
      'worker thread. This is likely a bug in the framework. Please report ' +
      'immediately.'
    ) : invariant(ExecutionEnvironment.canUseDOM));
    ("production" !== "development" ? invariant(markup, 'dangerouslyReplaceNodeWithMarkup(...): Missing markup.') : invariant(markup));
    ("production" !== "development" ? invariant(
      oldChild.tagName.toLowerCase() !== 'html',
      'dangerouslyReplaceNodeWithMarkup(...): Cannot replace markup of the ' +
      '<html> node. This is because browser quirks make this unreliable ' +
      'and/or slow. If you want to render to the root you must use ' +
      'server rendering. See renderComponentToString().'
    ) : invariant(oldChild.tagName.toLowerCase() !== 'html'));

    var newChild = createNodesFromMarkup(markup, emptyFunction)[0];
    oldChild.parentNode.replaceChild(newChild, oldChild);
  }

};

module.exports = Danger;

},{"./ExecutionEnvironment":20,"./createNodesFromMarkup":93,"./emptyFunction":96,"./getMarkupWrap":105,"./invariant":112}],11:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule DefaultDOMPropertyConfig
 */

/*jslint bitwise: true*/

"use strict";

var DOMProperty = _dereq_("./DOMProperty");

var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;
var MUST_USE_PROPERTY = DOMProperty.injection.MUST_USE_PROPERTY;
var HAS_BOOLEAN_VALUE = DOMProperty.injection.HAS_BOOLEAN_VALUE;
var HAS_SIDE_EFFECTS = DOMProperty.injection.HAS_SIDE_EFFECTS;
var HAS_POSITIVE_NUMERIC_VALUE =
  DOMProperty.injection.HAS_POSITIVE_NUMERIC_VALUE;

var DefaultDOMPropertyConfig = {
  isCustomAttribute: RegExp.prototype.test.bind(
    /^(data|aria)-[a-z_][a-z\d_.\-]*$/
  ),
  Properties: {
    /**
     * Standard Properties
     */
    accept: null,
    accessKey: null,
    action: null,
    allowFullScreen: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    allowTransparency: MUST_USE_ATTRIBUTE,
    alt: null,
    async: HAS_BOOLEAN_VALUE,
    autoComplete: null,
    // autoFocus is polyfilled/normalized by AutoFocusMixin
    // autoFocus: HAS_BOOLEAN_VALUE,
    autoPlay: HAS_BOOLEAN_VALUE,
    cellPadding: null,
    cellSpacing: null,
    charSet: MUST_USE_ATTRIBUTE,
    checked: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    className: MUST_USE_PROPERTY,
    cols: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
    colSpan: null,
    content: null,
    contentEditable: null,
    contextMenu: MUST_USE_ATTRIBUTE,
    controls: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    crossOrigin: null,
    data: null, // For `<object />` acts as `src`.
    dateTime: MUST_USE_ATTRIBUTE,
    defer: HAS_BOOLEAN_VALUE,
    dir: null,
    disabled: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    download: null,
    draggable: null,
    encType: null,
    form: MUST_USE_ATTRIBUTE,
    formNoValidate: HAS_BOOLEAN_VALUE,
    frameBorder: MUST_USE_ATTRIBUTE,
    height: MUST_USE_ATTRIBUTE,
    hidden: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    href: null,
    hrefLang: null,
    htmlFor: null,
    httpEquiv: null,
    icon: null,
    id: MUST_USE_PROPERTY,
    label: null,
    lang: null,
    list: null,
    loop: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    max: null,
    maxLength: MUST_USE_ATTRIBUTE,
    mediaGroup: null,
    method: null,
    min: null,
    multiple: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    muted: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    name: null,
    noValidate: HAS_BOOLEAN_VALUE,
    pattern: null,
    placeholder: null,
    poster: null,
    preload: null,
    radioGroup: null,
    readOnly: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    rel: null,
    required: HAS_BOOLEAN_VALUE,
    role: MUST_USE_ATTRIBUTE,
    rows: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
    rowSpan: null,
    sandbox: null,
    scope: null,
    scrollLeft: MUST_USE_PROPERTY,
    scrollTop: MUST_USE_PROPERTY,
    seamless: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
    selected: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
    size: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
    span: HAS_POSITIVE_NUMERIC_VALUE,
    spellCheck: null,
    src: null,
    srcDoc: MUST_USE_PROPERTY,
    srcSet: null,
    step: null,
    style: null,
    tabIndex: null,
    target: null,
    title: null,
    type: null,
    value: MUST_USE_PROPERTY | HAS_SIDE_EFFECTS,
    width: MUST_USE_ATTRIBUTE,
    wmode: MUST_USE_ATTRIBUTE,

    /**
     * Non-standard Properties
     */
    autoCapitalize: null, // Supported in Mobile Safari for keyboard hints
    autoCorrect: null, // Supported in Mobile Safari for keyboard hints
    property: null, // Supports OG in meta tags

    /**
     * SVG Properties
     */
    cx: MUST_USE_ATTRIBUTE,
    cy: MUST_USE_ATTRIBUTE,
    d: MUST_USE_ATTRIBUTE,
    fill: MUST_USE_ATTRIBUTE,
    fx: MUST_USE_ATTRIBUTE,
    fy: MUST_USE_ATTRIBUTE,
    gradientTransform: MUST_USE_ATTRIBUTE,
    gradientUnits: MUST_USE_ATTRIBUTE,
    offset: MUST_USE_ATTRIBUTE,
    points: MUST_USE_ATTRIBUTE,
    r: MUST_USE_ATTRIBUTE,
    rx: MUST_USE_ATTRIBUTE,
    ry: MUST_USE_ATTRIBUTE,
    spreadMethod: MUST_USE_ATTRIBUTE,
    stopColor: MUST_USE_ATTRIBUTE,
    stopOpacity: MUST_USE_ATTRIBUTE,
    stroke: MUST_USE_ATTRIBUTE,
    strokeLinecap: MUST_USE_ATTRIBUTE,
    strokeWidth: MUST_USE_ATTRIBUTE,
    textAnchor: MUST_USE_ATTRIBUTE,
    transform: MUST_USE_ATTRIBUTE,
    version: MUST_USE_ATTRIBUTE,
    viewBox: MUST_USE_ATTRIBUTE,
    x1: MUST_USE_ATTRIBUTE,
    x2: MUST_USE_ATTRIBUTE,
    x: MUST_USE_ATTRIBUTE,
    y1: MUST_USE_ATTRIBUTE,
    y2: MUST_USE_ATTRIBUTE,
    y: MUST_USE_ATTRIBUTE
  },
  DOMAttributeNames: {
    className: 'class',
    gradientTransform: 'gradientTransform',
    gradientUnits: 'gradientUnits',
    htmlFor: 'for',
    spreadMethod: 'spreadMethod',
    stopColor: 'stop-color',
    stopOpacity: 'stop-opacity',
    strokeLinecap: 'stroke-linecap',
    strokeWidth: 'stroke-width',
    textAnchor: 'text-anchor',
    viewBox: 'viewBox'
  },
  DOMPropertyNames: {
    autoCapitalize: 'autocapitalize',
    autoComplete: 'autocomplete',
    autoCorrect: 'autocorrect',
    autoFocus: 'autofocus',
    autoPlay: 'autoplay',
    encType: 'enctype',
    hrefLang: 'hreflang',
    radioGroup: 'radiogroup',
    spellCheck: 'spellcheck',
    srcDoc: 'srcdoc',
    srcSet: 'srcset'
  }
};

module.exports = DefaultDOMPropertyConfig;

},{"./DOMProperty":8}],12:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule DefaultEventPluginOrder
 */

"use strict";

 var keyOf = _dereq_("./keyOf");

/**
 * Module that is injectable into `EventPluginHub`, that specifies a
 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
 * plugins, without having to package every one of them. This is better than
 * having plugins be ordered in the same order that they are injected because
 * that ordering would be influenced by the packaging order.
 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
 */
var DefaultEventPluginOrder = [
  keyOf({ResponderEventPlugin: null}),
  keyOf({SimpleEventPlugin: null}),
  keyOf({TapEventPlugin: null}),
  keyOf({EnterLeaveEventPlugin: null}),
  keyOf({ChangeEventPlugin: null}),
  keyOf({SelectEventPlugin: null}),
  keyOf({CompositionEventPlugin: null}),
  keyOf({AnalyticsEventPlugin: null}),
  keyOf({MobileSafariClickEventPlugin: null})
];

module.exports = DefaultEventPluginOrder;

},{"./keyOf":119}],13:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EnterLeaveEventPlugin
 * @typechecks static-only
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventPropagators = _dereq_("./EventPropagators");
var SyntheticMouseEvent = _dereq_("./SyntheticMouseEvent");

var ReactMount = _dereq_("./ReactMount");
var keyOf = _dereq_("./keyOf");

var topLevelTypes = EventConstants.topLevelTypes;
var getFirstReactDOM = ReactMount.getFirstReactDOM;

var eventTypes = {
  mouseEnter: {
    registrationName: keyOf({onMouseEnter: null}),
    dependencies: [
      topLevelTypes.topMouseOut,
      topLevelTypes.topMouseOver
    ]
  },
  mouseLeave: {
    registrationName: keyOf({onMouseLeave: null}),
    dependencies: [
      topLevelTypes.topMouseOut,
      topLevelTypes.topMouseOver
    ]
  }
};

var extractedEvents = [null, null];

var EnterLeaveEventPlugin = {

  eventTypes: eventTypes,

  /**
   * For almost every interaction we care about, there will be both a top-level
   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
   * we do not extract duplicate events. However, moving the mouse into the
   * browser from outside will not fire a `mouseout` event. In this case, we use
   * the `mouseover` top-level event.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {
    if (topLevelType === topLevelTypes.topMouseOver &&
        (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
      return null;
    }
    if (topLevelType !== topLevelTypes.topMouseOut &&
        topLevelType !== topLevelTypes.topMouseOver) {
      // Must not be a mouse in or mouse out - ignoring.
      return null;
    }

    var win;
    if (topLevelTarget.window === topLevelTarget) {
      // `topLevelTarget` is probably a window object.
      win = topLevelTarget;
    } else {
      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
      var doc = topLevelTarget.ownerDocument;
      if (doc) {
        win = doc.defaultView || doc.parentWindow;
      } else {
        win = window;
      }
    }

    var from, to;
    if (topLevelType === topLevelTypes.topMouseOut) {
      from = topLevelTarget;
      to =
        getFirstReactDOM(nativeEvent.relatedTarget || nativeEvent.toElement) ||
        win;
    } else {
      from = win;
      to = topLevelTarget;
    }

    if (from === to) {
      // Nothing pertains to our managed components.
      return null;
    }

    var fromID = from ? ReactMount.getID(from) : '';
    var toID = to ? ReactMount.getID(to) : '';

    var leave = SyntheticMouseEvent.getPooled(
      eventTypes.mouseLeave,
      fromID,
      nativeEvent
    );
    leave.type = 'mouseleave';
    leave.target = from;
    leave.relatedTarget = to;

    var enter = SyntheticMouseEvent.getPooled(
      eventTypes.mouseEnter,
      toID,
      nativeEvent
    );
    enter.type = 'mouseenter';
    enter.target = to;
    enter.relatedTarget = from;

    EventPropagators.accumulateEnterLeaveDispatches(leave, enter, fromID, toID);

    extractedEvents[0] = leave;
    extractedEvents[1] = enter;

    return extractedEvents;
  }

};

module.exports = EnterLeaveEventPlugin;

},{"./EventConstants":14,"./EventPropagators":19,"./ReactMount":55,"./SyntheticMouseEvent":81,"./keyOf":119}],14:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EventConstants
 */

"use strict";

var keyMirror = _dereq_("./keyMirror");

var PropagationPhases = keyMirror({bubbled: null, captured: null});

/**
 * Types of raw signals from the browser caught at the top level.
 */
var topLevelTypes = keyMirror({
  topBlur: null,
  topChange: null,
  topClick: null,
  topCompositionEnd: null,
  topCompositionStart: null,
  topCompositionUpdate: null,
  topContextMenu: null,
  topCopy: null,
  topCut: null,
  topDoubleClick: null,
  topDrag: null,
  topDragEnd: null,
  topDragEnter: null,
  topDragExit: null,
  topDragLeave: null,
  topDragOver: null,
  topDragStart: null,
  topDrop: null,
  topError: null,
  topFocus: null,
  topInput: null,
  topKeyDown: null,
  topKeyPress: null,
  topKeyUp: null,
  topLoad: null,
  topMouseDown: null,
  topMouseMove: null,
  topMouseOut: null,
  topMouseOver: null,
  topMouseUp: null,
  topPaste: null,
  topReset: null,
  topScroll: null,
  topSelectionChange: null,
  topSubmit: null,
  topTouchCancel: null,
  topTouchEnd: null,
  topTouchMove: null,
  topTouchStart: null,
  topWheel: null
});

var EventConstants = {
  topLevelTypes: topLevelTypes,
  PropagationPhases: PropagationPhases
};

module.exports = EventConstants;

},{"./keyMirror":118}],15:[function(_dereq_,module,exports){
/**
 * @providesModule EventListener
 */

var emptyFunction = _dereq_("./emptyFunction");

/**
 * Upstream version of event listener. Does not take into account specific
 * nature of platform.
 */
var EventListener = {
  /**
   * Listen to DOM events during the bubble phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  listen: function(target, eventType, callback) {
    if (target.addEventListener) {
      target.addEventListener(eventType, callback, false);
      return {
        remove: function() {
          target.removeEventListener(eventType, callback, false);
        }
      };
    } else if (target.attachEvent) {
      target.attachEvent('on' + eventType, callback);
      return {
        remove: function() {
          target.detachEvent(eventType, callback);
        }
      };
    }
  },

  /**
   * Listen to DOM events during the capture phase.
   *
   * @param {DOMEventTarget} target DOM element to register listener on.
   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
   * @param {function} callback Callback function.
   * @return {object} Object with a `remove` method.
   */
  capture: function(target, eventType, callback) {
    if (!target.addEventListener) {
      if ("production" !== "development") {
        console.error(
          'Attempted to listen to events during the capture phase on a ' +
          'browser that does not support the capture phase. Your application ' +
          'will not receive some events.'
        );
      }
      return {
        remove: emptyFunction
      };
    } else {
      target.addEventListener(eventType, callback, true);
      return {
        remove: function() {
          target.removeEventListener(eventType, callback, true);
        }
      };
    }
  }
};

module.exports = EventListener;

},{"./emptyFunction":96}],16:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EventPluginHub
 */

"use strict";

var EventPluginRegistry = _dereq_("./EventPluginRegistry");
var EventPluginUtils = _dereq_("./EventPluginUtils");
var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var accumulate = _dereq_("./accumulate");
var forEachAccumulated = _dereq_("./forEachAccumulated");
var invariant = _dereq_("./invariant");
var isEventSupported = _dereq_("./isEventSupported");
var monitorCodeUse = _dereq_("./monitorCodeUse");

/**
 * Internal store for event listeners
 */
var listenerBank = {};

/**
 * Internal queue of events that have accumulated their dispatches and are
 * waiting to have their dispatches executed.
 */
var eventQueue = null;

/**
 * Dispatches an event and releases it back into the pool, unless persistent.
 *
 * @param {?object} event Synthetic event to be dispatched.
 * @private
 */
var executeDispatchesAndRelease = function(event) {
  if (event) {
    var executeDispatch = EventPluginUtils.executeDispatch;
    // Plugins can provide custom behavior when dispatching events.
    var PluginModule = EventPluginRegistry.getPluginModuleForEvent(event);
    if (PluginModule && PluginModule.executeDispatch) {
      executeDispatch = PluginModule.executeDispatch;
    }
    EventPluginUtils.executeDispatchesInOrder(event, executeDispatch);

    if (!event.isPersistent()) {
      event.constructor.release(event);
    }
  }
};

/**
 * - `InstanceHandle`: [required] Module that performs logical traversals of DOM
 *   hierarchy given ids of the logical DOM elements involved.
 */
var InstanceHandle = null;

function validateInstanceHandle() {
  var invalid = !InstanceHandle||
    !InstanceHandle.traverseTwoPhase ||
    !InstanceHandle.traverseEnterLeave;
  if (invalid) {
    throw new Error('InstanceHandle not injected before use!');
  }
}

/**
 * This is a unified interface for event plugins to be installed and configured.
 *
 * Event plugins can implement the following properties:
 *
 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
 *     Required. When a top-level event is fired, this method is expected to
 *     extract synthetic events that will in turn be queued and dispatched.
 *
 *   `eventTypes` {object}
 *     Optional, plugins that fire events must publish a mapping of registration
 *     names that are used to register listeners. Values of this mapping must
 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
 *
 *   `executeDispatch` {function(object, function, string)}
 *     Optional, allows plugins to override how an event gets dispatched. By
 *     default, the listener is simply invoked.
 *
 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
 *
 * @public
 */
var EventPluginHub = {

  /**
   * Methods for injecting dependencies.
   */
  injection: {

    /**
     * @param {object} InjectedMount
     * @public
     */
    injectMount: EventPluginUtils.injection.injectMount,

    /**
     * @param {object} InjectedInstanceHandle
     * @public
     */
    injectInstanceHandle: function(InjectedInstanceHandle) {
      InstanceHandle = InjectedInstanceHandle;
      if ("production" !== "development") {
        validateInstanceHandle();
      }
    },

    getInstanceHandle: function() {
      if ("production" !== "development") {
        validateInstanceHandle();
      }
      return InstanceHandle;
    },

    /**
     * @param {array} InjectedEventPluginOrder
     * @public
     */
    injectEventPluginOrder: EventPluginRegistry.injectEventPluginOrder,

    /**
     * @param {object} injectedNamesToPlugins Map from names to plugin modules.
     */
    injectEventPluginsByName: EventPluginRegistry.injectEventPluginsByName

  },

  eventNameDispatchConfigs: EventPluginRegistry.eventNameDispatchConfigs,

  registrationNameModules: EventPluginRegistry.registrationNameModules,

  /**
   * Stores `listener` at `listenerBank[registrationName][id]`. Is idempotent.
   *
   * @param {string} id ID of the DOM element.
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   * @param {?function} listener The callback to store.
   */
  putListener: function(id, registrationName, listener) {
    ("production" !== "development" ? invariant(
      ExecutionEnvironment.canUseDOM,
      'Cannot call putListener() in a non-DOM environment.'
    ) : invariant(ExecutionEnvironment.canUseDOM));
    ("production" !== "development" ? invariant(
      !listener || typeof listener === 'function',
      'Expected %s listener to be a function, instead got type %s',
      registrationName, typeof listener
    ) : invariant(!listener || typeof listener === 'function'));

    if ("production" !== "development") {
      // IE8 has no API for event capturing and the `onScroll` event doesn't
      // bubble.
      if (registrationName === 'onScroll' &&
          !isEventSupported('scroll', true)) {
        monitorCodeUse('react_no_scroll_event');
        console.warn('This browser doesn\'t support the `onScroll` event');
      }
    }
    var bankForRegistrationName =
      listenerBank[registrationName] || (listenerBank[registrationName] = {});
    bankForRegistrationName[id] = listener;
  },

  /**
   * @param {string} id ID of the DOM element.
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   * @return {?function} The stored callback.
   */
  getListener: function(id, registrationName) {
    var bankForRegistrationName = listenerBank[registrationName];
    return bankForRegistrationName && bankForRegistrationName[id];
  },

  /**
   * Deletes a listener from the registration bank.
   *
   * @param {string} id ID of the DOM element.
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   */
  deleteListener: function(id, registrationName) {
    var bankForRegistrationName = listenerBank[registrationName];
    if (bankForRegistrationName) {
      delete bankForRegistrationName[id];
    }
  },

  /**
   * Deletes all listeners for the DOM element with the supplied ID.
   *
   * @param {string} id ID of the DOM element.
   */
  deleteAllListeners: function(id) {
    for (var registrationName in listenerBank) {
      delete listenerBank[registrationName][id];
    }
  },

  /**
   * Allows registered plugins an opportunity to extract events from top-level
   * native browser events.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @internal
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {
    var events;
    var plugins = EventPluginRegistry.plugins;
    for (var i = 0, l = plugins.length; i < l; i++) {
      // Not every plugin in the ordering may be loaded at runtime.
      var possiblePlugin = plugins[i];
      if (possiblePlugin) {
        var extractedEvents = possiblePlugin.extractEvents(
          topLevelType,
          topLevelTarget,
          topLevelTargetID,
          nativeEvent
        );
        if (extractedEvents) {
          events = accumulate(events, extractedEvents);
        }
      }
    }
    return events;
  },

  /**
   * Enqueues a synthetic event that should be dispatched when
   * `processEventQueue` is invoked.
   *
   * @param {*} events An accumulation of synthetic events.
   * @internal
   */
  enqueueEvents: function(events) {
    if (events) {
      eventQueue = accumulate(eventQueue, events);
    }
  },

  /**
   * Dispatches all synthetic events on the event queue.
   *
   * @internal
   */
  processEventQueue: function() {
    // Set `eventQueue` to null before processing it so that we can tell if more
    // events get enqueued while processing.
    var processingEventQueue = eventQueue;
    eventQueue = null;
    forEachAccumulated(processingEventQueue, executeDispatchesAndRelease);
    ("production" !== "development" ? invariant(
      !eventQueue,
      'processEventQueue(): Additional events were enqueued while processing ' +
      'an event queue. Support for this has not yet been implemented.'
    ) : invariant(!eventQueue));
  },

  /**
   * These are needed for tests only. Do not use!
   */
  __purge: function() {
    listenerBank = {};
  },

  __getListenerBank: function() {
    return listenerBank;
  }

};

module.exports = EventPluginHub;

},{"./EventPluginRegistry":17,"./EventPluginUtils":18,"./ExecutionEnvironment":20,"./accumulate":87,"./forEachAccumulated":101,"./invariant":112,"./isEventSupported":113,"./monitorCodeUse":125}],17:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EventPluginRegistry
 * @typechecks static-only
 */

"use strict";

var invariant = _dereq_("./invariant");

/**
 * Injectable ordering of event plugins.
 */
var EventPluginOrder = null;

/**
 * Injectable mapping from names to event plugin modules.
 */
var namesToPlugins = {};

/**
 * Recomputes the plugin list using the injected plugins and plugin ordering.
 *
 * @private
 */
function recomputePluginOrdering() {
  if (!EventPluginOrder) {
    // Wait until an `EventPluginOrder` is injected.
    return;
  }
  for (var pluginName in namesToPlugins) {
    var PluginModule = namesToPlugins[pluginName];
    var pluginIndex = EventPluginOrder.indexOf(pluginName);
    ("production" !== "development" ? invariant(
      pluginIndex > -1,
      'EventPluginRegistry: Cannot inject event plugins that do not exist in ' +
      'the plugin ordering, `%s`.',
      pluginName
    ) : invariant(pluginIndex > -1));
    if (EventPluginRegistry.plugins[pluginIndex]) {
      continue;
    }
    ("production" !== "development" ? invariant(
      PluginModule.extractEvents,
      'EventPluginRegistry: Event plugins must implement an `extractEvents` ' +
      'method, but `%s` does not.',
      pluginName
    ) : invariant(PluginModule.extractEvents));
    EventPluginRegistry.plugins[pluginIndex] = PluginModule;
    var publishedEvents = PluginModule.eventTypes;
    for (var eventName in publishedEvents) {
      ("production" !== "development" ? invariant(
        publishEventForPlugin(
          publishedEvents[eventName],
          PluginModule,
          eventName
        ),
        'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.',
        eventName,
        pluginName
      ) : invariant(publishEventForPlugin(
        publishedEvents[eventName],
        PluginModule,
        eventName
      )));
    }
  }
}

/**
 * Publishes an event so that it can be dispatched by the supplied plugin.
 *
 * @param {object} dispatchConfig Dispatch configuration for the event.
 * @param {object} PluginModule Plugin publishing the event.
 * @return {boolean} True if the event was successfully published.
 * @private
 */
function publishEventForPlugin(dispatchConfig, PluginModule, eventName) {
  ("production" !== "development" ? invariant(
    !EventPluginRegistry.eventNameDispatchConfigs[eventName],
    'EventPluginHub: More than one plugin attempted to publish the same ' +
    'event name, `%s`.',
    eventName
  ) : invariant(!EventPluginRegistry.eventNameDispatchConfigs[eventName]));
  EventPluginRegistry.eventNameDispatchConfigs[eventName] = dispatchConfig;

  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
  if (phasedRegistrationNames) {
    for (var phaseName in phasedRegistrationNames) {
      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
        var phasedRegistrationName = phasedRegistrationNames[phaseName];
        publishRegistrationName(
          phasedRegistrationName,
          PluginModule,
          eventName
        );
      }
    }
    return true;
  } else if (dispatchConfig.registrationName) {
    publishRegistrationName(
      dispatchConfig.registrationName,
      PluginModule,
      eventName
    );
    return true;
  }
  return false;
}

/**
 * Publishes a registration name that is used to identify dispatched events and
 * can be used with `EventPluginHub.putListener` to register listeners.
 *
 * @param {string} registrationName Registration name to add.
 * @param {object} PluginModule Plugin publishing the event.
 * @private
 */
function publishRegistrationName(registrationName, PluginModule, eventName) {
  ("production" !== "development" ? invariant(
    !EventPluginRegistry.registrationNameModules[registrationName],
    'EventPluginHub: More than one plugin attempted to publish the same ' +
    'registration name, `%s`.',
    registrationName
  ) : invariant(!EventPluginRegistry.registrationNameModules[registrationName]));
  EventPluginRegistry.registrationNameModules[registrationName] = PluginModule;
  EventPluginRegistry.registrationNameDependencies[registrationName] =
    PluginModule.eventTypes[eventName].dependencies;
}

/**
 * Registers plugins so that they can extract and dispatch events.
 *
 * @see {EventPluginHub}
 */
var EventPluginRegistry = {

  /**
   * Ordered list of injected plugins.
   */
  plugins: [],

  /**
   * Mapping from event name to dispatch config
   */
  eventNameDispatchConfigs: {},

  /**
   * Mapping from registration name to plugin module
   */
  registrationNameModules: {},

  /**
   * Mapping from registration name to event name
   */
  registrationNameDependencies: {},

  /**
   * Injects an ordering of plugins (by plugin name). This allows the ordering
   * to be decoupled from injection of the actual plugins so that ordering is
   * always deterministic regardless of packaging, on-the-fly injection, etc.
   *
   * @param {array} InjectedEventPluginOrder
   * @internal
   * @see {EventPluginHub.injection.injectEventPluginOrder}
   */
  injectEventPluginOrder: function(InjectedEventPluginOrder) {
    ("production" !== "development" ? invariant(
      !EventPluginOrder,
      'EventPluginRegistry: Cannot inject event plugin ordering more than once.'
    ) : invariant(!EventPluginOrder));
    // Clone the ordering so it cannot be dynamically mutated.
    EventPluginOrder = Array.prototype.slice.call(InjectedEventPluginOrder);
    recomputePluginOrdering();
  },

  /**
   * Injects plugins to be used by `EventPluginHub`. The plugin names must be
   * in the ordering injected by `injectEventPluginOrder`.
   *
   * Plugins can be injected as part of page initialization or on-the-fly.
   *
   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
   * @internal
   * @see {EventPluginHub.injection.injectEventPluginsByName}
   */
  injectEventPluginsByName: function(injectedNamesToPlugins) {
    var isOrderingDirty = false;
    for (var pluginName in injectedNamesToPlugins) {
      if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
        continue;
      }
      var PluginModule = injectedNamesToPlugins[pluginName];
      if (namesToPlugins[pluginName] !== PluginModule) {
        ("production" !== "development" ? invariant(
          !namesToPlugins[pluginName],
          'EventPluginRegistry: Cannot inject two different event plugins ' +
          'using the same name, `%s`.',
          pluginName
        ) : invariant(!namesToPlugins[pluginName]));
        namesToPlugins[pluginName] = PluginModule;
        isOrderingDirty = true;
      }
    }
    if (isOrderingDirty) {
      recomputePluginOrdering();
    }
  },

  /**
   * Looks up the plugin for the supplied event.
   *
   * @param {object} event A synthetic event.
   * @return {?object} The plugin that created the supplied event.
   * @internal
   */
  getPluginModuleForEvent: function(event) {
    var dispatchConfig = event.dispatchConfig;
    if (dispatchConfig.registrationName) {
      return EventPluginRegistry.registrationNameModules[
        dispatchConfig.registrationName
      ] || null;
    }
    for (var phase in dispatchConfig.phasedRegistrationNames) {
      if (!dispatchConfig.phasedRegistrationNames.hasOwnProperty(phase)) {
        continue;
      }
      var PluginModule = EventPluginRegistry.registrationNameModules[
        dispatchConfig.phasedRegistrationNames[phase]
      ];
      if (PluginModule) {
        return PluginModule;
      }
    }
    return null;
  },

  /**
   * Exposed for unit testing.
   * @private
   */
  _resetEventPlugins: function() {
    EventPluginOrder = null;
    for (var pluginName in namesToPlugins) {
      if (namesToPlugins.hasOwnProperty(pluginName)) {
        delete namesToPlugins[pluginName];
      }
    }
    EventPluginRegistry.plugins.length = 0;

    var eventNameDispatchConfigs = EventPluginRegistry.eventNameDispatchConfigs;
    for (var eventName in eventNameDispatchConfigs) {
      if (eventNameDispatchConfigs.hasOwnProperty(eventName)) {
        delete eventNameDispatchConfigs[eventName];
      }
    }

    var registrationNameModules = EventPluginRegistry.registrationNameModules;
    for (var registrationName in registrationNameModules) {
      if (registrationNameModules.hasOwnProperty(registrationName)) {
        delete registrationNameModules[registrationName];
      }
    }
  }

};

module.exports = EventPluginRegistry;

},{"./invariant":112}],18:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EventPluginUtils
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");

var invariant = _dereq_("./invariant");

/**
 * Injected dependencies:
 */

/**
 * - `Mount`: [required] Module that can convert between React dom IDs and
 *   actual node references.
 */
var injection = {
  Mount: null,
  injectMount: function(InjectedMount) {
    injection.Mount = InjectedMount;
    if ("production" !== "development") {
      ("production" !== "development" ? invariant(
        InjectedMount && InjectedMount.getNode,
        'EventPluginUtils.injection.injectMount(...): Injected Mount module ' +
        'is missing getNode.'
      ) : invariant(InjectedMount && InjectedMount.getNode));
    }
  }
};

var topLevelTypes = EventConstants.topLevelTypes;

function isEndish(topLevelType) {
  return topLevelType === topLevelTypes.topMouseUp ||
         topLevelType === topLevelTypes.topTouchEnd ||
         topLevelType === topLevelTypes.topTouchCancel;
}

function isMoveish(topLevelType) {
  return topLevelType === topLevelTypes.topMouseMove ||
         topLevelType === topLevelTypes.topTouchMove;
}
function isStartish(topLevelType) {
  return topLevelType === topLevelTypes.topMouseDown ||
         topLevelType === topLevelTypes.topTouchStart;
}


var validateEventDispatches;
if ("production" !== "development") {
  validateEventDispatches = function(event) {
    var dispatchListeners = event._dispatchListeners;
    var dispatchIDs = event._dispatchIDs;

    var listenersIsArr = Array.isArray(dispatchListeners);
    var idsIsArr = Array.isArray(dispatchIDs);
    var IDsLen = idsIsArr ? dispatchIDs.length : dispatchIDs ? 1 : 0;
    var listenersLen = listenersIsArr ?
      dispatchListeners.length :
      dispatchListeners ? 1 : 0;

    ("production" !== "development" ? invariant(
      idsIsArr === listenersIsArr && IDsLen === listenersLen,
      'EventPluginUtils: Invalid `event`.'
    ) : invariant(idsIsArr === listenersIsArr && IDsLen === listenersLen));
  };
}

/**
 * Invokes `cb(event, listener, id)`. Avoids using call if no scope is
 * provided. The `(listener,id)` pair effectively forms the "dispatch" but are
 * kept separate to conserve memory.
 */
function forEachEventDispatch(event, cb) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchIDs = event._dispatchIDs;
  if ("production" !== "development") {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and IDs are two parallel arrays that are always in sync.
      cb(event, dispatchListeners[i], dispatchIDs[i]);
    }
  } else if (dispatchListeners) {
    cb(event, dispatchListeners, dispatchIDs);
  }
}

/**
 * Default implementation of PluginModule.executeDispatch().
 * @param {SyntheticEvent} SyntheticEvent to handle
 * @param {function} Application-level callback
 * @param {string} domID DOM id to pass to the callback.
 */
function executeDispatch(event, listener, domID) {
  event.currentTarget = injection.Mount.getNode(domID);
  var returnValue = listener(event, domID);
  event.currentTarget = null;
  return returnValue;
}

/**
 * Standard/simple iteration through an event's collected dispatches.
 */
function executeDispatchesInOrder(event, executeDispatch) {
  forEachEventDispatch(event, executeDispatch);
  event._dispatchListeners = null;
  event._dispatchIDs = null;
}

/**
 * Standard/simple iteration through an event's collected dispatches, but stops
 * at the first dispatch execution returning true, and returns that id.
 *
 * @return id of the first dispatch execution who's listener returns true, or
 * null if no listener returned true.
 */
function executeDispatchesInOrderStopAtTrue(event) {
  var dispatchListeners = event._dispatchListeners;
  var dispatchIDs = event._dispatchIDs;
  if ("production" !== "development") {
    validateEventDispatches(event);
  }
  if (Array.isArray(dispatchListeners)) {
    for (var i = 0; i < dispatchListeners.length; i++) {
      if (event.isPropagationStopped()) {
        break;
      }
      // Listeners and IDs are two parallel arrays that are always in sync.
      if (dispatchListeners[i](event, dispatchIDs[i])) {
        return dispatchIDs[i];
      }
    }
  } else if (dispatchListeners) {
    if (dispatchListeners(event, dispatchIDs)) {
      return dispatchIDs;
    }
  }
  return null;
}

/**
 * Execution of a "direct" dispatch - there must be at most one dispatch
 * accumulated on the event or it is considered an error. It doesn't really make
 * sense for an event with multiple dispatches (bubbled) to keep track of the
 * return values at each dispatch execution, but it does tend to make sense when
 * dealing with "direct" dispatches.
 *
 * @return The return value of executing the single dispatch.
 */
function executeDirectDispatch(event) {
  if ("production" !== "development") {
    validateEventDispatches(event);
  }
  var dispatchListener = event._dispatchListeners;
  var dispatchID = event._dispatchIDs;
  ("production" !== "development" ? invariant(
    !Array.isArray(dispatchListener),
    'executeDirectDispatch(...): Invalid `event`.'
  ) : invariant(!Array.isArray(dispatchListener)));
  var res = dispatchListener ?
    dispatchListener(event, dispatchID) :
    null;
  event._dispatchListeners = null;
  event._dispatchIDs = null;
  return res;
}

/**
 * @param {SyntheticEvent} event
 * @return {bool} True iff number of dispatches accumulated is greater than 0.
 */
function hasDispatches(event) {
  return !!event._dispatchListeners;
}

/**
 * General utilities that are useful in creating custom Event Plugins.
 */
var EventPluginUtils = {
  isEndish: isEndish,
  isMoveish: isMoveish,
  isStartish: isStartish,

  executeDirectDispatch: executeDirectDispatch,
  executeDispatch: executeDispatch,
  executeDispatchesInOrder: executeDispatchesInOrder,
  executeDispatchesInOrderStopAtTrue: executeDispatchesInOrderStopAtTrue,
  hasDispatches: hasDispatches,
  injection: injection,
  useTouchEvents: false
};

module.exports = EventPluginUtils;

},{"./EventConstants":14,"./invariant":112}],19:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule EventPropagators
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventPluginHub = _dereq_("./EventPluginHub");

var accumulate = _dereq_("./accumulate");
var forEachAccumulated = _dereq_("./forEachAccumulated");

var PropagationPhases = EventConstants.PropagationPhases;
var getListener = EventPluginHub.getListener;

/**
 * Some event types have a notion of different registration names for different
 * "phases" of propagation. This finds listeners by a given phase.
 */
function listenerAtPhase(id, event, propagationPhase) {
  var registrationName =
    event.dispatchConfig.phasedRegistrationNames[propagationPhase];
  return getListener(id, registrationName);
}

/**
 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
 * here, allows us to not have to bind or create functions for each event.
 * Mutating the event's members allows us to not have to create a wrapping
 * "dispatch" object that pairs the event with the listener.
 */
function accumulateDirectionalDispatches(domID, upwards, event) {
  if ("production" !== "development") {
    if (!domID) {
      throw new Error('Dispatching id must not be null');
    }
  }
  var phase = upwards ? PropagationPhases.bubbled : PropagationPhases.captured;
  var listener = listenerAtPhase(domID, event, phase);
  if (listener) {
    event._dispatchListeners = accumulate(event._dispatchListeners, listener);
    event._dispatchIDs = accumulate(event._dispatchIDs, domID);
  }
}

/**
 * Collect dispatches (must be entirely collected before dispatching - see unit
 * tests). Lazily allocate the array to conserve memory.  We must loop through
 * each event and perform the traversal for each one. We can not perform a
 * single traversal for the entire collection of events because each event may
 * have a different target.
 */
function accumulateTwoPhaseDispatchesSingle(event) {
  if (event && event.dispatchConfig.phasedRegistrationNames) {
    EventPluginHub.injection.getInstanceHandle().traverseTwoPhase(
      event.dispatchMarker,
      accumulateDirectionalDispatches,
      event
    );
  }
}


/**
 * Accumulates without regard to direction, does not look for phased
 * registration names. Same as `accumulateDirectDispatchesSingle` but without
 * requiring that the `dispatchMarker` be the same as the dispatched ID.
 */
function accumulateDispatches(id, ignoredDirection, event) {
  if (event && event.dispatchConfig.registrationName) {
    var registrationName = event.dispatchConfig.registrationName;
    var listener = getListener(id, registrationName);
    if (listener) {
      event._dispatchListeners = accumulate(event._dispatchListeners, listener);
      event._dispatchIDs = accumulate(event._dispatchIDs, id);
    }
  }
}

/**
 * Accumulates dispatches on an `SyntheticEvent`, but only for the
 * `dispatchMarker`.
 * @param {SyntheticEvent} event
 */
function accumulateDirectDispatchesSingle(event) {
  if (event && event.dispatchConfig.registrationName) {
    accumulateDispatches(event.dispatchMarker, null, event);
  }
}

function accumulateTwoPhaseDispatches(events) {
  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
}

function accumulateEnterLeaveDispatches(leave, enter, fromID, toID) {
  EventPluginHub.injection.getInstanceHandle().traverseEnterLeave(
    fromID,
    toID,
    accumulateDispatches,
    leave,
    enter
  );
}


function accumulateDirectDispatches(events) {
  forEachAccumulated(events, accumulateDirectDispatchesSingle);
}



/**
 * A small set of propagation patterns, each of which will accept a small amount
 * of information, and generate a set of "dispatch ready event objects" - which
 * are sets of events that have already been annotated with a set of dispatched
 * listener functions/ids. The API is designed this way to discourage these
 * propagation strategies from actually executing the dispatches, since we
 * always want to collect the entire set of dispatches before executing event a
 * single one.
 *
 * @constructor EventPropagators
 */
var EventPropagators = {
  accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
  accumulateDirectDispatches: accumulateDirectDispatches,
  accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches
};

module.exports = EventPropagators;

},{"./EventConstants":14,"./EventPluginHub":16,"./accumulate":87,"./forEachAccumulated":101}],20:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ExecutionEnvironment
 */

/*jslint evil: true */

"use strict";

var canUseDOM = typeof window !== 'undefined';

/**
 * Simple, lightweight module assisting with the detection and context of
 * Worker. Helps avoid circular dependencies and allows code to reason about
 * whether or not they are in a Worker, even if they never include the main
 * `ReactWorker` dependency.
 */
var ExecutionEnvironment = {

  canUseDOM: canUseDOM,

  canUseWorkers: typeof Worker !== 'undefined',

  canUseEventListeners:
    canUseDOM && (window.addEventListener || window.attachEvent),

  isInWorker: !canUseDOM // For now, this is true - might change in the future.

};

module.exports = ExecutionEnvironment;

},{}],21:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule LinkedValueUtils
 * @typechecks static-only
 */

"use strict";

var ReactPropTypes = _dereq_("./ReactPropTypes");

var invariant = _dereq_("./invariant");
var warning = _dereq_("./warning");

var hasReadOnlyValue = {
  'button': true,
  'checkbox': true,
  'image': true,
  'hidden': true,
  'radio': true,
  'reset': true,
  'submit': true
};

function _assertSingleLink(input) {
  ("production" !== "development" ? invariant(
      input.props.checkedLink == null || input.props.valueLink == null,
      'Cannot provide a checkedLink and a valueLink. If you want to use ' +
      'checkedLink, you probably don\'t want to use valueLink and vice versa.'
  ) : invariant(input.props.checkedLink == null || input.props.valueLink == null));
}
function _assertValueLink(input) {
  _assertSingleLink(input);
  ("production" !== "development" ? invariant(
    input.props.value == null && input.props.onChange == null,
    'Cannot provide a valueLink and a value or onChange event. If you want ' +
    'to use value or onChange, you probably don\'t want to use valueLink.'
  ) : invariant(input.props.value == null && input.props.onChange == null));
}

function _assertCheckedLink(input) {
  _assertSingleLink(input);
  ("production" !== "development" ? invariant(
    input.props.checked == null && input.props.onChange == null,
    'Cannot provide a checkedLink and a checked property or onChange event. ' +
    'If you want to use checked or onChange, you probably don\'t want to ' +
    'use checkedLink'
  ) : invariant(input.props.checked == null && input.props.onChange == null));
}

/**
 * @param {SyntheticEvent} e change event to handle
 */
function _handleLinkedValueChange(e) {
  /*jshint validthis:true */
  this.props.valueLink.requestChange(e.target.value);
}

/**
  * @param {SyntheticEvent} e change event to handle
  */
function _handleLinkedCheckChange(e) {
  /*jshint validthis:true */
  this.props.checkedLink.requestChange(e.target.checked);
}

/**
 * Provide a linked `value` attribute for controlled forms. You should not use
 * this outside of the ReactDOM controlled form components.
 */
var LinkedValueUtils = {
  Mixin: {
    propTypes: {
      value: function(props, propName, componentName) {
        if ("production" !== "development") {
          ("production" !== "development" ? warning(
            !props[propName] ||
            hasReadOnlyValue[props.type] ||
            props.onChange ||
            props.readOnly ||
            props.disabled,
            'You provided a `value` prop to a form field without an ' +
            '`onChange` handler. This will render a read-only field. If ' +
            'the field should be mutable use `defaultValue`. Otherwise, ' +
            'set either `onChange` or `readOnly`.'
          ) : null);
        }
      },
      checked: function(props, propName, componentName) {
        if ("production" !== "development") {
          ("production" !== "development" ? warning(
            !props[propName] ||
            props.onChange ||
            props.readOnly ||
            props.disabled,
            'You provided a `checked` prop to a form field without an ' +
            '`onChange` handler. This will render a read-only field. If ' +
            'the field should be mutable use `defaultChecked`. Otherwise, ' +
            'set either `onChange` or `readOnly`.'
          ) : null);
        }
      },
      onChange: ReactPropTypes.func
    }
  },

  /**
   * @param {ReactComponent} input Form component
   * @return {*} current value of the input either from value prop or link.
   */
  getValue: function(input) {
    if (input.props.valueLink) {
      _assertValueLink(input);
      return input.props.valueLink.value;
    }
    return input.props.value;
  },

  /**
   * @param {ReactComponent} input Form component
   * @return {*} current checked status of the input either from checked prop
   *             or link.
   */
  getChecked: function(input) {
    if (input.props.checkedLink) {
      _assertCheckedLink(input);
      return input.props.checkedLink.value;
    }
    return input.props.checked;
  },

  /**
   * @param {ReactComponent} input Form component
   * @return {function} change callback either from onChange prop or link.
   */
  getOnChange: function(input) {
    if (input.props.valueLink) {
      _assertValueLink(input);
      return _handleLinkedValueChange;
    } else if (input.props.checkedLink) {
      _assertCheckedLink(input);
      return _handleLinkedCheckChange;
    }
    return input.props.onChange;
  }
};

module.exports = LinkedValueUtils;

},{"./ReactPropTypes":64,"./invariant":112,"./warning":134}],22:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule MobileSafariClickEventPlugin
 * @typechecks static-only
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");

var emptyFunction = _dereq_("./emptyFunction");

var topLevelTypes = EventConstants.topLevelTypes;

/**
 * Mobile Safari does not fire properly bubble click events on non-interactive
 * elements, which means delegated click listeners do not fire. The workaround
 * for this bug involves attaching an empty click listener on the target node.
 *
 * This particular plugin works around the bug by attaching an empty click
 * listener on `touchstart` (which does fire on every element).
 */
var MobileSafariClickEventPlugin = {

  eventTypes: null,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {
    if (topLevelType === topLevelTypes.topTouchStart) {
      var target = nativeEvent.target;
      if (target && !target.onclick) {
        target.onclick = emptyFunction;
      }
    }
  }

};

module.exports = MobileSafariClickEventPlugin;

},{"./EventConstants":14,"./emptyFunction":96}],23:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule PooledClass
 */

"use strict";

var invariant = _dereq_("./invariant");

/**
 * Static poolers. Several custom versions for each potential number of
 * arguments. A completely generic pooler is easy to implement, but would
 * require accessing the `arguments` object. In each of these, `this` refers to
 * the Class itself, not an instance. If any others are needed, simply add them
 * here, or in their own files.
 */
var oneArgumentPooler = function(copyFieldsFrom) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, copyFieldsFrom);
    return instance;
  } else {
    return new Klass(copyFieldsFrom);
  }
};

var twoArgumentPooler = function(a1, a2) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2);
    return instance;
  } else {
    return new Klass(a1, a2);
  }
};

var threeArgumentPooler = function(a1, a2, a3) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2, a3);
    return instance;
  } else {
    return new Klass(a1, a2, a3);
  }
};

var fiveArgumentPooler = function(a1, a2, a3, a4, a5) {
  var Klass = this;
  if (Klass.instancePool.length) {
    var instance = Klass.instancePool.pop();
    Klass.call(instance, a1, a2, a3, a4, a5);
    return instance;
  } else {
    return new Klass(a1, a2, a3, a4, a5);
  }
};

var standardReleaser = function(instance) {
  var Klass = this;
  ("production" !== "development" ? invariant(
    instance instanceof Klass,
    'Trying to release an instance into a pool of a different type.'
  ) : invariant(instance instanceof Klass));
  if (instance.destructor) {
    instance.destructor();
  }
  if (Klass.instancePool.length < Klass.poolSize) {
    Klass.instancePool.push(instance);
  }
};

var DEFAULT_POOL_SIZE = 10;
var DEFAULT_POOLER = oneArgumentPooler;

/**
 * Augments `CopyConstructor` to be a poolable class, augmenting only the class
 * itself (statically) not adding any prototypical fields. Any CopyConstructor
 * you give this may have a `poolSize` property, and will look for a
 * prototypical `destructor` on instances (optional).
 *
 * @param {Function} CopyConstructor Constructor that can be used to reset.
 * @param {Function} pooler Customizable pooler.
 */
var addPoolingTo = function(CopyConstructor, pooler) {
  var NewKlass = CopyConstructor;
  NewKlass.instancePool = [];
  NewKlass.getPooled = pooler || DEFAULT_POOLER;
  if (!NewKlass.poolSize) {
    NewKlass.poolSize = DEFAULT_POOL_SIZE;
  }
  NewKlass.release = standardReleaser;
  return NewKlass;
};

var PooledClass = {
  addPoolingTo: addPoolingTo,
  oneArgumentPooler: oneArgumentPooler,
  twoArgumentPooler: twoArgumentPooler,
  threeArgumentPooler: threeArgumentPooler,
  fiveArgumentPooler: fiveArgumentPooler
};

module.exports = PooledClass;

},{"./invariant":112}],24:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule React
 */

"use strict";

var DOMPropertyOperations = _dereq_("./DOMPropertyOperations");
var EventPluginUtils = _dereq_("./EventPluginUtils");
var ReactChildren = _dereq_("./ReactChildren");
var ReactComponent = _dereq_("./ReactComponent");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactContext = _dereq_("./ReactContext");
var ReactCurrentOwner = _dereq_("./ReactCurrentOwner");
var ReactDOM = _dereq_("./ReactDOM");
var ReactDOMComponent = _dereq_("./ReactDOMComponent");
var ReactDefaultInjection = _dereq_("./ReactDefaultInjection");
var ReactInstanceHandles = _dereq_("./ReactInstanceHandles");
var ReactMount = _dereq_("./ReactMount");
var ReactMultiChild = _dereq_("./ReactMultiChild");
var ReactPerf = _dereq_("./ReactPerf");
var ReactPropTypes = _dereq_("./ReactPropTypes");
var ReactServerRendering = _dereq_("./ReactServerRendering");
var ReactTextComponent = _dereq_("./ReactTextComponent");

var onlyChild = _dereq_("./onlyChild");

ReactDefaultInjection.inject();

var React = {
  Children: {
    map: ReactChildren.map,
    forEach: ReactChildren.forEach,
    only: onlyChild
  },
  DOM: ReactDOM,
  PropTypes: ReactPropTypes,
  initializeTouchEvents: function(shouldUseTouch) {
    EventPluginUtils.useTouchEvents = shouldUseTouch;
  },
  createClass: ReactCompositeComponent.createClass,
  constructAndRenderComponent: ReactMount.constructAndRenderComponent,
  constructAndRenderComponentByID: ReactMount.constructAndRenderComponentByID,
  renderComponent: ReactPerf.measure(
    'React',
    'renderComponent',
    ReactMount.renderComponent
  ),
  renderComponentToString: ReactServerRendering.renderComponentToString,
  renderComponentToStaticMarkup:
    ReactServerRendering.renderComponentToStaticMarkup,
  unmountComponentAtNode: ReactMount.unmountComponentAtNode,
  isValidClass: ReactCompositeComponent.isValidClass,
  isValidComponent: ReactComponent.isValidComponent,
  withContext: ReactContext.withContext,
  __internals: {
    Component: ReactComponent,
    CurrentOwner: ReactCurrentOwner,
    DOMComponent: ReactDOMComponent,
    DOMPropertyOperations: DOMPropertyOperations,
    InstanceHandles: ReactInstanceHandles,
    Mount: ReactMount,
    MultiChild: ReactMultiChild,
    TextComponent: ReactTextComponent
  }
};

if ("production" !== "development") {
  var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");
  if (ExecutionEnvironment.canUseDOM &&
      window.top === window.self &&
      navigator.userAgent.indexOf('Chrome') > -1) {
    console.debug(
      'Download the React DevTools for a better development experience: ' +
      'http://fb.me/react-devtools'
    );
  }
}

// Version exists only in the open-source version of React, not in Facebook's
// internal version.
React.version = '0.10.0';

module.exports = React;

},{"./DOMPropertyOperations":9,"./EventPluginUtils":18,"./ExecutionEnvironment":20,"./ReactChildren":26,"./ReactComponent":27,"./ReactCompositeComponent":29,"./ReactContext":30,"./ReactCurrentOwner":31,"./ReactDOM":32,"./ReactDOMComponent":34,"./ReactDefaultInjection":44,"./ReactInstanceHandles":53,"./ReactMount":55,"./ReactMultiChild":57,"./ReactPerf":60,"./ReactPropTypes":64,"./ReactServerRendering":68,"./ReactTextComponent":70,"./onlyChild":128}],25:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactBrowserComponentMixin
 */

"use strict";

var ReactMount = _dereq_("./ReactMount");

var invariant = _dereq_("./invariant");

var ReactBrowserComponentMixin = {
  /**
   * Returns the DOM node rendered by this component.
   *
   * @return {DOMElement} The root node of this component.
   * @final
   * @protected
   */
  getDOMNode: function() {
    ("production" !== "development" ? invariant(
      this.isMounted(),
      'getDOMNode(): A component must be mounted to have a DOM node.'
    ) : invariant(this.isMounted()));
    return ReactMount.getNode(this._rootNodeID);
  }
};

module.exports = ReactBrowserComponentMixin;

},{"./ReactMount":55,"./invariant":112}],26:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactChildren
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");

var invariant = _dereq_("./invariant");
var traverseAllChildren = _dereq_("./traverseAllChildren");

var twoArgumentPooler = PooledClass.twoArgumentPooler;
var threeArgumentPooler = PooledClass.threeArgumentPooler;

/**
 * PooledClass representing the bookkeeping associated with performing a child
 * traversal. Allows avoiding binding callbacks.
 *
 * @constructor ForEachBookKeeping
 * @param {!function} forEachFunction Function to perform traversal with.
 * @param {?*} forEachContext Context to perform context with.
 */
function ForEachBookKeeping(forEachFunction, forEachContext) {
  this.forEachFunction = forEachFunction;
  this.forEachContext = forEachContext;
}
PooledClass.addPoolingTo(ForEachBookKeeping, twoArgumentPooler);

function forEachSingleChild(traverseContext, child, name, i) {
  var forEachBookKeeping = traverseContext;
  forEachBookKeeping.forEachFunction.call(
    forEachBookKeeping.forEachContext, child, i);
}

/**
 * Iterates through children that are typically specified as `props.children`.
 *
 * The provided forEachFunc(child, index) will be called for each
 * leaf child.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} forEachFunc.
 * @param {*} forEachContext Context for forEachContext.
 */
function forEachChildren(children, forEachFunc, forEachContext) {
  if (children == null) {
    return children;
  }

  var traverseContext =
    ForEachBookKeeping.getPooled(forEachFunc, forEachContext);
  traverseAllChildren(children, forEachSingleChild, traverseContext);
  ForEachBookKeeping.release(traverseContext);
}

/**
 * PooledClass representing the bookkeeping associated with performing a child
 * mapping. Allows avoiding binding callbacks.
 *
 * @constructor MapBookKeeping
 * @param {!*} mapResult Object containing the ordered map of results.
 * @param {!function} mapFunction Function to perform mapping with.
 * @param {?*} mapContext Context to perform mapping with.
 */
function MapBookKeeping(mapResult, mapFunction, mapContext) {
  this.mapResult = mapResult;
  this.mapFunction = mapFunction;
  this.mapContext = mapContext;
}
PooledClass.addPoolingTo(MapBookKeeping, threeArgumentPooler);

function mapSingleChildIntoContext(traverseContext, child, name, i) {
  var mapBookKeeping = traverseContext;
  var mapResult = mapBookKeeping.mapResult;
  var mappedChild =
    mapBookKeeping.mapFunction.call(mapBookKeeping.mapContext, child, i);
  // We found a component instance
  ("production" !== "development" ? invariant(
    !mapResult.hasOwnProperty(name),
    'ReactChildren.map(...): Encountered two children with the same key, ' +
    '`%s`. Children keys must be unique.',
    name
  ) : invariant(!mapResult.hasOwnProperty(name)));
  mapResult[name] = mappedChild;
}

/**
 * Maps children that are typically specified as `props.children`.
 *
 * The provided mapFunction(child, key, index) will be called for each
 * leaf child.
 *
 * TODO: This may likely break any calls to `ReactChildren.map` that were
 * previously relying on the fact that we guarded against null children.
 *
 * @param {?*} children Children tree container.
 * @param {function(*, int)} mapFunction.
 * @param {*} mapContext Context for mapFunction.
 * @return {object} Object containing the ordered map of results.
 */
function mapChildren(children, func, context) {
  if (children == null) {
    return children;
  }

  var mapResult = {};
  var traverseContext = MapBookKeeping.getPooled(mapResult, func, context);
  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
  MapBookKeeping.release(traverseContext);
  return mapResult;
}

var ReactChildren = {
  forEach: forEachChildren,
  map: mapChildren
};

module.exports = ReactChildren;

},{"./PooledClass":23,"./invariant":112,"./traverseAllChildren":133}],27:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactComponent
 */

"use strict";

var ReactCurrentOwner = _dereq_("./ReactCurrentOwner");
var ReactOwner = _dereq_("./ReactOwner");
var ReactUpdates = _dereq_("./ReactUpdates");

var invariant = _dereq_("./invariant");
var keyMirror = _dereq_("./keyMirror");
var merge = _dereq_("./merge");
var monitorCodeUse = _dereq_("./monitorCodeUse");

/**
 * Every React component is in one of these life cycles.
 */
var ComponentLifeCycle = keyMirror({
  /**
   * Mounted components have a DOM node representation and are capable of
   * receiving new props.
   */
  MOUNTED: null,
  /**
   * Unmounted components are inactive and cannot receive new props.
   */
  UNMOUNTED: null
});

/**
 * Warn if there's no key explicitly set on dynamic arrays of children or
 * object keys are not valid. This allows us to keep track of children between
 * updates.
 */

var ownerHasExplicitKeyWarning = {};
var ownerHasPropertyWarning = {};
var ownerHasMonitoredObjectMap = {};

var NUMERIC_PROPERTY_REGEX = /^\d+$/;

var injected = false;

/**
 * Optionally injectable environment dependent cleanup hook. (server vs.
 * browser etc). Example: A browser system caches DOM nodes based on component
 * ID and must remove that cache entry when this instance is unmounted.
 *
 * @private
 */
var unmountIDFromEnvironment = null;

/**
 * The "image" of a component tree, is the platform specific (typically
 * serialized) data that represents a tree of lower level UI building blocks.
 * On the web, this "image" is HTML markup which describes a construction of
 * low level `div` and `span` nodes. Other platforms may have different
 * encoding of this "image". This must be injected.
 *
 * @private
 */
var mountImageIntoNode = null;

/**
 * Warn if the component doesn't have an explicit key assigned to it.
 * This component is in an array. The array could grow and shrink or be
 * reordered. All children that haven't already been validated are required to
 * have a "key" property assigned to it.
 *
 * @internal
 * @param {ReactComponent} component Component that requires a key.
 */
function validateExplicitKey(component) {
  if (component.__keyValidated__ || component.props.key != null) {
    return;
  }
  component.__keyValidated__ = true;

  // We can't provide friendly warnings for top level components.
  if (!ReactCurrentOwner.current) {
    return;
  }

  // Name of the component whose render method tried to pass children.
  var currentName = ReactCurrentOwner.current.constructor.displayName;
  if (ownerHasExplicitKeyWarning.hasOwnProperty(currentName)) {
    return;
  }
  ownerHasExplicitKeyWarning[currentName] = true;

  var message = 'Each child in an array should have a unique "key" prop. ' +
                'Check the render method of ' + currentName + '.';

  var childOwnerName = null;
  if (!component.isOwnedBy(ReactCurrentOwner.current)) {
    // Name of the component that originally created this child.
    childOwnerName =
      component._owner &&
      component._owner.constructor.displayName;

    // Usually the current owner is the offender, but if it accepts
    // children as a property, it may be the creator of the child that's
    // responsible for assigning it a key.
    message += ' It was passed a child from ' + childOwnerName + '.';
  }

  message += ' See http://fb.me/react-warning-keys for more information.';
  monitorCodeUse('react_key_warning', {
    component: currentName,
    componentOwner: childOwnerName
  });
  console.warn(message);
}

/**
 * Warn if the key is being defined as an object property but has an incorrect
 * value.
 *
 * @internal
 * @param {string} name Property name of the key.
 * @param {ReactComponent} component Component that requires a key.
 */
function validatePropertyKey(name) {
  if (NUMERIC_PROPERTY_REGEX.test(name)) {
    // Name of the component whose render method tried to pass children.
    var currentName = ReactCurrentOwner.current.constructor.displayName;
    if (ownerHasPropertyWarning.hasOwnProperty(currentName)) {
      return;
    }
    ownerHasPropertyWarning[currentName] = true;

    monitorCodeUse('react_numeric_key_warning');
    console.warn(
      'Child objects should have non-numeric keys so ordering is preserved. ' +
      'Check the render method of ' + currentName + '. ' +
      'See http://fb.me/react-warning-keys for more information.'
    );
  }
}

/**
 * Log that we're using an object map. We're considering deprecating this
 * feature and replace it with proper Map and ImmutableMap data structures.
 *
 * @internal
 */
function monitorUseOfObjectMap() {
  // Name of the component whose render method tried to pass children.
  // We only use this to avoid spewing the logs. We lose additional
  // owner stacks but hopefully one level is enough to trace the source.
  var currentName = (ReactCurrentOwner.current &&
                    ReactCurrentOwner.current.constructor.displayName) || '';
  if (ownerHasMonitoredObjectMap.hasOwnProperty(currentName)) {
    return;
  }
  ownerHasMonitoredObjectMap[currentName] = true;
  monitorCodeUse('react_object_map_children');
}

/**
 * Ensure that every component either is passed in a static location, in an
 * array with an explicit keys property defined, or in an object literal
 * with valid key property.
 *
 * @internal
 * @param {*} component Statically passed child of any type.
 * @return {boolean}
 */
function validateChildKeys(component) {
  if (Array.isArray(component)) {
    for (var i = 0; i < component.length; i++) {
      var child = component[i];
      if (ReactComponent.isValidComponent(child)) {
        validateExplicitKey(child);
      }
    }
  } else if (ReactComponent.isValidComponent(component)) {
    // This component was passed in a valid location.
    component.__keyValidated__ = true;
  } else if (component && typeof component === 'object') {
    monitorUseOfObjectMap();
    for (var name in component) {
      validatePropertyKey(name, component);
    }
  }
}

/**
 * Components are the basic units of composition in React.
 *
 * Every component accepts a set of keyed input parameters known as "props" that
 * are initialized by the constructor. Once a component is mounted, the props
 * can be mutated using `setProps` or `replaceProps`.
 *
 * Every component is capable of the following operations:
 *
 *   `mountComponent`
 *     Initializes the component, renders markup, and registers event listeners.
 *
 *   `receiveComponent`
 *     Updates the rendered DOM nodes to match the given component.
 *
 *   `unmountComponent`
 *     Releases any resources allocated by this component.
 *
 * Components can also be "owned" by other components. Being owned by another
 * component means being constructed by that component. This is different from
 * being the child of a component, which means having a DOM representation that
 * is a child of the DOM representation of that component.
 *
 * @class ReactComponent
 */
var ReactComponent = {

  injection: {
    injectEnvironment: function(ReactComponentEnvironment) {
      ("production" !== "development" ? invariant(
        !injected,
        'ReactComponent: injectEnvironment() can only be called once.'
      ) : invariant(!injected));
      mountImageIntoNode = ReactComponentEnvironment.mountImageIntoNode;
      unmountIDFromEnvironment =
        ReactComponentEnvironment.unmountIDFromEnvironment;
      ReactComponent.BackendIDOperations =
        ReactComponentEnvironment.BackendIDOperations;
      ReactComponent.ReactReconcileTransaction =
        ReactComponentEnvironment.ReactReconcileTransaction;
      injected = true;
    }
  },

  /**
   * @param {?object} object
   * @return {boolean} True if `object` is a valid component.
   * @final
   */
  isValidComponent: function(object) {
    if (!object || !object.type || !object.type.prototype) {
      return false;
    }
    // This is the safer way of duck checking the type of instance this is.
    // The object can be a generic descriptor but the type property refers to
    // the constructor and it's prototype can be used to inspect the type that
    // will actually get mounted.
    var prototype = object.type.prototype;
    return (
      typeof prototype.mountComponentIntoNode === 'function' &&
      typeof prototype.receiveComponent === 'function'
    );
  },

  /**
   * @internal
   */
  LifeCycle: ComponentLifeCycle,

  /**
   * Injected module that provides ability to mutate individual properties.
   * Injected into the base class because many different subclasses need access
   * to this.
   *
   * @internal
   */
  BackendIDOperations: null,

  /**
   * React references `ReactReconcileTransaction` using this property in order
   * to allow dependency injection.
   *
   * @internal
   */
  ReactReconcileTransaction: null,

  /**
   * Base functionality for every ReactComponent constructor. Mixed into the
   * `ReactComponent` prototype, but exposed statically for easy access.
   *
   * @lends {ReactComponent.prototype}
   */
  Mixin: {

    /**
     * Checks whether or not this component is mounted.
     *
     * @return {boolean} True if mounted, false otherwise.
     * @final
     * @protected
     */
    isMounted: function() {
      return this._lifeCycleState === ComponentLifeCycle.MOUNTED;
    },

    /**
     * Sets a subset of the props.
     *
     * @param {object} partialProps Subset of the next props.
     * @param {?function} callback Called after props are updated.
     * @final
     * @public
     */
    setProps: function(partialProps, callback) {
      // Merge with `_pendingProps` if it exists, otherwise with existing props.
      this.replaceProps(
        merge(this._pendingProps || this.props, partialProps),
        callback
      );
    },

    /**
     * Replaces all of the props.
     *
     * @param {object} props New props.
     * @param {?function} callback Called after props are updated.
     * @final
     * @public
     */
    replaceProps: function(props, callback) {
      ("production" !== "development" ? invariant(
        this.isMounted(),
        'replaceProps(...): Can only update a mounted component.'
      ) : invariant(this.isMounted()));
      ("production" !== "development" ? invariant(
        this._mountDepth === 0,
        'replaceProps(...): You called `setProps` or `replaceProps` on a ' +
        'component with a parent. This is an anti-pattern since props will ' +
        'get reactively updated when rendered. Instead, change the owner\'s ' +
        '`render` method to pass the correct value as props to the component ' +
        'where it is created.'
      ) : invariant(this._mountDepth === 0));
      this._pendingProps = props;
      ReactUpdates.enqueueUpdate(this, callback);
    },

    /**
     * Base constructor for all React components.
     *
     * Subclasses that override this method should make sure to invoke
     * `ReactComponent.Mixin.construct.call(this, ...)`.
     *
     * @param {?object} initialProps
     * @param {*} children
     * @internal
     */
    construct: function(initialProps, children) {
      this.props = initialProps || {};
      // Record the component responsible for creating this component.
      this._owner = ReactCurrentOwner.current;
      // All components start unmounted.
      this._lifeCycleState = ComponentLifeCycle.UNMOUNTED;

      this._pendingProps = null;
      this._pendingCallbacks = null;

      // Unlike _pendingProps and _pendingCallbacks, we won't use null to
      // indicate that nothing is pending because it's possible for a component
      // to have a null owner. Instead, an owner change is pending when
      // this._owner !== this._pendingOwner.
      this._pendingOwner = this._owner;

      // Children can be more than one argument
      var childrenLength = arguments.length - 1;
      if (childrenLength === 1) {
        if ("production" !== "development") {
          validateChildKeys(children);
        }
        this.props.children = children;
      } else if (childrenLength > 1) {
        var childArray = Array(childrenLength);
        for (var i = 0; i < childrenLength; i++) {
          if ("production" !== "development") {
            validateChildKeys(arguments[i + 1]);
          }
          childArray[i] = arguments[i + 1];
        }
        this.props.children = childArray;
      }
    },

    /**
     * Initializes the component, renders markup, and registers event listeners.
     *
     * NOTE: This does not insert any nodes into the DOM.
     *
     * Subclasses that override this method should make sure to invoke
     * `ReactComponent.Mixin.mountComponent.call(this, ...)`.
     *
     * @param {string} rootID DOM ID of the root node.
     * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
     * @param {number} mountDepth number of components in the owner hierarchy.
     * @return {?string} Rendered markup to be inserted into the DOM.
     * @internal
     */
    mountComponent: function(rootID, transaction, mountDepth) {
      ("production" !== "development" ? invariant(
        !this.isMounted(),
        'mountComponent(%s, ...): Can only mount an unmounted component. ' +
        'Make sure to avoid storing components between renders or reusing a ' +
        'single component instance in multiple places.',
        rootID
      ) : invariant(!this.isMounted()));
      var props = this.props;
      if (props.ref != null) {
        ReactOwner.addComponentAsRefTo(this, props.ref, this._owner);
      }
      this._rootNodeID = rootID;
      this._lifeCycleState = ComponentLifeCycle.MOUNTED;
      this._mountDepth = mountDepth;
      // Effectively: return '';
    },

    /**
     * Releases any resources allocated by `mountComponent`.
     *
     * NOTE: This does not remove any nodes from the DOM.
     *
     * Subclasses that override this method should make sure to invoke
     * `ReactComponent.Mixin.unmountComponent.call(this)`.
     *
     * @internal
     */
    unmountComponent: function() {
      ("production" !== "development" ? invariant(
        this.isMounted(),
        'unmountComponent(): Can only unmount a mounted component.'
      ) : invariant(this.isMounted()));
      var props = this.props;
      if (props.ref != null) {
        ReactOwner.removeComponentAsRefFrom(this, props.ref, this._owner);
      }
      unmountIDFromEnvironment(this._rootNodeID);
      this._rootNodeID = null;
      this._lifeCycleState = ComponentLifeCycle.UNMOUNTED;
    },

    /**
     * Given a new instance of this component, updates the rendered DOM nodes
     * as if that instance was rendered instead.
     *
     * Subclasses that override this method should make sure to invoke
     * `ReactComponent.Mixin.receiveComponent.call(this, ...)`.
     *
     * @param {object} nextComponent Next set of properties.
     * @param {ReactReconcileTransaction} transaction
     * @internal
     */
    receiveComponent: function(nextComponent, transaction) {
      ("production" !== "development" ? invariant(
        this.isMounted(),
        'receiveComponent(...): Can only update a mounted component.'
      ) : invariant(this.isMounted()));
      this._pendingOwner = nextComponent._owner;
      this._pendingProps = nextComponent.props;
      this._performUpdateIfNecessary(transaction);
    },

    /**
     * Call `_performUpdateIfNecessary` within a new transaction.
     *
     * @internal
     */
    performUpdateIfNecessary: function() {
      var transaction = ReactComponent.ReactReconcileTransaction.getPooled();
      transaction.perform(this._performUpdateIfNecessary, this, transaction);
      ReactComponent.ReactReconcileTransaction.release(transaction);
    },

    /**
     * If `_pendingProps` is set, update the component.
     *
     * @param {ReactReconcileTransaction} transaction
     * @internal
     */
    _performUpdateIfNecessary: function(transaction) {
      if (this._pendingProps == null) {
        return;
      }
      var prevProps = this.props;
      var prevOwner = this._owner;
      this.props = this._pendingProps;
      this._owner = this._pendingOwner;
      this._pendingProps = null;
      this.updateComponent(transaction, prevProps, prevOwner);
    },

    /**
     * Updates the component's currently mounted representation.
     *
     * @param {ReactReconcileTransaction} transaction
     * @param {object} prevProps
     * @internal
     */
    updateComponent: function(transaction, prevProps, prevOwner) {
      var props = this.props;
      // If either the owner or a `ref` has changed, make sure the newest owner
      // has stored a reference to `this`, and the previous owner (if different)
      // has forgotten the reference to `this`.
      if (this._owner !== prevOwner || props.ref !== prevProps.ref) {
        if (prevProps.ref != null) {
          ReactOwner.removeComponentAsRefFrom(
            this, prevProps.ref, prevOwner
          );
        }
        // Correct, even if the owner is the same, and only the ref has changed.
        if (props.ref != null) {
          ReactOwner.addComponentAsRefTo(this, props.ref, this._owner);
        }
      }
    },

    /**
     * Mounts this component and inserts it into the DOM.
     *
     * @param {string} rootID DOM ID of the root node.
     * @param {DOMElement} container DOM element to mount into.
     * @param {boolean} shouldReuseMarkup If true, do not insert markup
     * @final
     * @internal
     * @see {ReactMount.renderComponent}
     */
    mountComponentIntoNode: function(rootID, container, shouldReuseMarkup) {
      var transaction = ReactComponent.ReactReconcileTransaction.getPooled();
      transaction.perform(
        this._mountComponentIntoNode,
        this,
        rootID,
        container,
        transaction,
        shouldReuseMarkup
      );
      ReactComponent.ReactReconcileTransaction.release(transaction);
    },

    /**
     * @param {string} rootID DOM ID of the root node.
     * @param {DOMElement} container DOM element to mount into.
     * @param {ReactReconcileTransaction} transaction
     * @param {boolean} shouldReuseMarkup If true, do not insert markup
     * @final
     * @private
     */
    _mountComponentIntoNode: function(
        rootID,
        container,
        transaction,
        shouldReuseMarkup) {
      var markup = this.mountComponent(rootID, transaction, 0);
      mountImageIntoNode(markup, container, shouldReuseMarkup);
    },

    /**
     * Checks if this component is owned by the supplied `owner` component.
     *
     * @param {ReactComponent} owner Component to check.
     * @return {boolean} True if `owners` owns this component.
     * @final
     * @internal
     */
    isOwnedBy: function(owner) {
      return this._owner === owner;
    },

    /**
     * Gets another component, that shares the same owner as this one, by ref.
     *
     * @param {string} ref of a sibling Component.
     * @return {?ReactComponent} the actual sibling Component.
     * @final
     * @internal
     */
    getSiblingByRef: function(ref) {
      var owner = this._owner;
      if (!owner || !owner.refs) {
        return null;
      }
      return owner.refs[ref];
    }
  }
};

module.exports = ReactComponent;

},{"./ReactCurrentOwner":31,"./ReactOwner":59,"./ReactUpdates":71,"./invariant":112,"./keyMirror":118,"./merge":121,"./monitorCodeUse":125}],28:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactComponentBrowserEnvironment
 */

/*jslint evil: true */

"use strict";

var ReactDOMIDOperations = _dereq_("./ReactDOMIDOperations");
var ReactMarkupChecksum = _dereq_("./ReactMarkupChecksum");
var ReactMount = _dereq_("./ReactMount");
var ReactPerf = _dereq_("./ReactPerf");
var ReactReconcileTransaction = _dereq_("./ReactReconcileTransaction");

var getReactRootElementInContainer = _dereq_("./getReactRootElementInContainer");
var invariant = _dereq_("./invariant");


var ELEMENT_NODE_TYPE = 1;
var DOC_NODE_TYPE = 9;


/**
 * Abstracts away all functionality of `ReactComponent` requires knowledge of
 * the browser context.
 */
var ReactComponentBrowserEnvironment = {
  ReactReconcileTransaction: ReactReconcileTransaction,

  BackendIDOperations: ReactDOMIDOperations,

  /**
   * If a particular environment requires that some resources be cleaned up,
   * specify this in the injected Mixin. In the DOM, we would likely want to
   * purge any cached node ID lookups.
   *
   * @private
   */
  unmountIDFromEnvironment: function(rootNodeID) {
    ReactMount.purgeID(rootNodeID);
  },

  /**
   * @param {string} markup Markup string to place into the DOM Element.
   * @param {DOMElement} container DOM Element to insert markup into.
   * @param {boolean} shouldReuseMarkup Should reuse the existing markup in the
   * container if possible.
   */
  mountImageIntoNode: ReactPerf.measure(
    'ReactComponentBrowserEnvironment',
    'mountImageIntoNode',
    function(markup, container, shouldReuseMarkup) {
      ("production" !== "development" ? invariant(
        container && (
          container.nodeType === ELEMENT_NODE_TYPE ||
            container.nodeType === DOC_NODE_TYPE
        ),
        'mountComponentIntoNode(...): Target container is not valid.'
      ) : invariant(container && (
        container.nodeType === ELEMENT_NODE_TYPE ||
          container.nodeType === DOC_NODE_TYPE
      )));

      if (shouldReuseMarkup) {
        if (ReactMarkupChecksum.canReuseMarkup(
          markup,
          getReactRootElementInContainer(container))) {
          return;
        } else {
          ("production" !== "development" ? invariant(
            container.nodeType !== DOC_NODE_TYPE,
            'You\'re trying to render a component to the document using ' +
            'server rendering but the checksum was invalid. This usually ' +
            'means you rendered a different component type or props on ' +
            'the client from the one on the server, or your render() ' +
            'methods are impure. React cannot handle this case due to ' +
            'cross-browser quirks by rendering at the document root. You ' +
            'should look for environment dependent code in your components ' +
            'and ensure the props are the same client and server side.'
          ) : invariant(container.nodeType !== DOC_NODE_TYPE));

          if ("production" !== "development") {
            console.warn(
              'React attempted to use reuse markup in a container but the ' +
              'checksum was invalid. This generally means that you are ' +
              'using server rendering and the markup generated on the ' +
              'server was not what the client was expecting. React injected' +
              'new markup to compensate which works but you have lost many ' +
              'of the benefits of server rendering. Instead, figure out ' +
              'why the markup being generated is different on the client ' +
              'or server.'
            );
          }
        }
      }

      ("production" !== "development" ? invariant(
        container.nodeType !== DOC_NODE_TYPE,
        'You\'re trying to render a component to the document but ' +
          'you didn\'t use server rendering. We can\'t do this ' +
          'without using server rendering due to cross-browser quirks. ' +
          'See renderComponentToString() for server rendering.'
      ) : invariant(container.nodeType !== DOC_NODE_TYPE));

      container.innerHTML = markup;
    }
  )
};

module.exports = ReactComponentBrowserEnvironment;

},{"./ReactDOMIDOperations":36,"./ReactMarkupChecksum":54,"./ReactMount":55,"./ReactPerf":60,"./ReactReconcileTransaction":66,"./getReactRootElementInContainer":107,"./invariant":112}],29:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactCompositeComponent
 */

"use strict";

var ReactComponent = _dereq_("./ReactComponent");
var ReactContext = _dereq_("./ReactContext");
var ReactCurrentOwner = _dereq_("./ReactCurrentOwner");
var ReactErrorUtils = _dereq_("./ReactErrorUtils");
var ReactOwner = _dereq_("./ReactOwner");
var ReactPerf = _dereq_("./ReactPerf");
var ReactPropTransferer = _dereq_("./ReactPropTransferer");
var ReactPropTypeLocations = _dereq_("./ReactPropTypeLocations");
var ReactPropTypeLocationNames = _dereq_("./ReactPropTypeLocationNames");
var ReactUpdates = _dereq_("./ReactUpdates");

var instantiateReactComponent = _dereq_("./instantiateReactComponent");
var invariant = _dereq_("./invariant");
var keyMirror = _dereq_("./keyMirror");
var merge = _dereq_("./merge");
var mixInto = _dereq_("./mixInto");
var monitorCodeUse = _dereq_("./monitorCodeUse");
var objMap = _dereq_("./objMap");
var shouldUpdateReactComponent = _dereq_("./shouldUpdateReactComponent");
var warning = _dereq_("./warning");

/**
 * Policies that describe methods in `ReactCompositeComponentInterface`.
 */
var SpecPolicy = keyMirror({
  /**
   * These methods may be defined only once by the class specification or mixin.
   */
  DEFINE_ONCE: null,
  /**
   * These methods may be defined by both the class specification and mixins.
   * Subsequent definitions will be chained. These methods must return void.
   */
  DEFINE_MANY: null,
  /**
   * These methods are overriding the base ReactCompositeComponent class.
   */
  OVERRIDE_BASE: null,
  /**
   * These methods are similar to DEFINE_MANY, except we assume they return
   * objects. We try to merge the keys of the return values of all the mixed in
   * functions. If there is a key conflict we throw.
   */
  DEFINE_MANY_MERGED: null
});


var injectedMixins = [];

/**
 * Composite components are higher-level components that compose other composite
 * or native components.
 *
 * To create a new type of `ReactCompositeComponent`, pass a specification of
 * your new class to `React.createClass`. The only requirement of your class
 * specification is that you implement a `render` method.
 *
 *   var MyComponent = React.createClass({
 *     render: function() {
 *       return <div>Hello World</div>;
 *     }
 *   });
 *
 * The class specification supports a specific protocol of methods that have
 * special meaning (e.g. `render`). See `ReactCompositeComponentInterface` for
 * more the comprehensive protocol. Any other properties and methods in the
 * class specification will available on the prototype.
 *
 * @interface ReactCompositeComponentInterface
 * @internal
 */
var ReactCompositeComponentInterface = {

  /**
   * An array of Mixin objects to include when defining your component.
   *
   * @type {array}
   * @optional
   */
  mixins: SpecPolicy.DEFINE_MANY,

  /**
   * An object containing properties and methods that should be defined on
   * the component's constructor instead of its prototype (static methods).
   *
   * @type {object}
   * @optional
   */
  statics: SpecPolicy.DEFINE_MANY,

  /**
   * Definition of prop types for this component.
   *
   * @type {object}
   * @optional
   */
  propTypes: SpecPolicy.DEFINE_MANY,

  /**
   * Definition of context types for this component.
   *
   * @type {object}
   * @optional
   */
  contextTypes: SpecPolicy.DEFINE_MANY,

  /**
   * Definition of context types this component sets for its children.
   *
   * @type {object}
   * @optional
   */
  childContextTypes: SpecPolicy.DEFINE_MANY,

  // ==== Definition methods ====

  /**
   * Invoked when the component is mounted. Values in the mapping will be set on
   * `this.props` if that prop is not specified (i.e. using an `in` check).
   *
   * This method is invoked before `getInitialState` and therefore cannot rely
   * on `this.state` or use `this.setState`.
   *
   * @return {object}
   * @optional
   */
  getDefaultProps: SpecPolicy.DEFINE_MANY_MERGED,

  /**
   * Invoked once before the component is mounted. The return value will be used
   * as the initial value of `this.state`.
   *
   *   getInitialState: function() {
   *     return {
   *       isOn: false,
   *       fooBaz: new BazFoo()
   *     }
   *   }
   *
   * @return {object}
   * @optional
   */
  getInitialState: SpecPolicy.DEFINE_MANY_MERGED,

  /**
   * @return {object}
   * @optional
   */
  getChildContext: SpecPolicy.DEFINE_MANY_MERGED,

  /**
   * Uses props from `this.props` and state from `this.state` to render the
   * structure of the component.
   *
   * No guarantees are made about when or how often this method is invoked, so
   * it must not have side effects.
   *
   *   render: function() {
   *     var name = this.props.name;
   *     return <div>Hello, {name}!</div>;
   *   }
   *
   * @return {ReactComponent}
   * @nosideeffects
   * @required
   */
  render: SpecPolicy.DEFINE_ONCE,



  // ==== Delegate methods ====

  /**
   * Invoked when the component is initially created and about to be mounted.
   * This may have side effects, but any external subscriptions or data created
   * by this method must be cleaned up in `componentWillUnmount`.
   *
   * @optional
   */
  componentWillMount: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked when the component has been mounted and has a DOM representation.
   * However, there is no guarantee that the DOM node is in the document.
   *
   * Use this as an opportunity to operate on the DOM when the component has
   * been mounted (initialized and rendered) for the first time.
   *
   * @param {DOMElement} rootNode DOM element representing the component.
   * @optional
   */
  componentDidMount: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked before the component receives new props.
   *
   * Use this as an opportunity to react to a prop transition by updating the
   * state using `this.setState`. Current props are accessed via `this.props`.
   *
   *   componentWillReceiveProps: function(nextProps, nextContext) {
   *     this.setState({
   *       likesIncreasing: nextProps.likeCount > this.props.likeCount
   *     });
   *   }
   *
   * NOTE: There is no equivalent `componentWillReceiveState`. An incoming prop
   * transition may cause a state change, but the opposite is not true. If you
   * need it, you are probably looking for `componentWillUpdate`.
   *
   * @param {object} nextProps
   * @optional
   */
  componentWillReceiveProps: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked while deciding if the component should be updated as a result of
   * receiving new props, state and/or context.
   *
   * Use this as an opportunity to `return false` when you're certain that the
   * transition to the new props/state/context will not require a component
   * update.
   *
   *   shouldComponentUpdate: function(nextProps, nextState, nextContext) {
   *     return !equal(nextProps, this.props) ||
   *       !equal(nextState, this.state) ||
   *       !equal(nextContext, this.context);
   *   }
   *
   * @param {object} nextProps
   * @param {?object} nextState
   * @param {?object} nextContext
   * @return {boolean} True if the component should update.
   * @optional
   */
  shouldComponentUpdate: SpecPolicy.DEFINE_ONCE,

  /**
   * Invoked when the component is about to update due to a transition from
   * `this.props`, `this.state` and `this.context` to `nextProps`, `nextState`
   * and `nextContext`.
   *
   * Use this as an opportunity to perform preparation before an update occurs.
   *
   * NOTE: You **cannot** use `this.setState()` in this method.
   *
   * @param {object} nextProps
   * @param {?object} nextState
   * @param {?object} nextContext
   * @param {ReactReconcileTransaction} transaction
   * @optional
   */
  componentWillUpdate: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked when the component's DOM representation has been updated.
   *
   * Use this as an opportunity to operate on the DOM when the component has
   * been updated.
   *
   * @param {object} prevProps
   * @param {?object} prevState
   * @param {?object} prevContext
   * @param {DOMElement} rootNode DOM element representing the component.
   * @optional
   */
  componentDidUpdate: SpecPolicy.DEFINE_MANY,

  /**
   * Invoked when the component is about to be removed from its parent and have
   * its DOM representation destroyed.
   *
   * Use this as an opportunity to deallocate any external resources.
   *
   * NOTE: There is no `componentDidUnmount` since your component will have been
   * destroyed by that point.
   *
   * @optional
   */
  componentWillUnmount: SpecPolicy.DEFINE_MANY,



  // ==== Advanced methods ====

  /**
   * Updates the component's currently mounted DOM representation.
   *
   * By default, this implements React's rendering and reconciliation algorithm.
   * Sophisticated clients may wish to override this.
   *
   * @param {ReactReconcileTransaction} transaction
   * @internal
   * @overridable
   */
  updateComponent: SpecPolicy.OVERRIDE_BASE

};

/**
 * Mapping from class specification keys to special processing functions.
 *
 * Although these are declared like instance properties in the specification
 * when defining classes using `React.createClass`, they are actually static
 * and are accessible on the constructor instead of the prototype. Despite
 * being static, they must be defined outside of the "statics" key under
 * which all other static methods are defined.
 */
var RESERVED_SPEC_KEYS = {
  displayName: function(ConvenienceConstructor, displayName) {
    ConvenienceConstructor.componentConstructor.displayName = displayName;
  },
  mixins: function(ConvenienceConstructor, mixins) {
    if (mixins) {
      for (var i = 0; i < mixins.length; i++) {
        mixSpecIntoComponent(ConvenienceConstructor, mixins[i]);
      }
    }
  },
  childContextTypes: function(ConvenienceConstructor, childContextTypes) {
    var Constructor = ConvenienceConstructor.componentConstructor;
    validateTypeDef(
      Constructor,
      childContextTypes,
      ReactPropTypeLocations.childContext
    );
    Constructor.childContextTypes = merge(
      Constructor.childContextTypes,
      childContextTypes
    );
  },
  contextTypes: function(ConvenienceConstructor, contextTypes) {
    var Constructor = ConvenienceConstructor.componentConstructor;
    validateTypeDef(
      Constructor,
      contextTypes,
      ReactPropTypeLocations.context
    );
    Constructor.contextTypes = merge(Constructor.contextTypes, contextTypes);
  },
  propTypes: function(ConvenienceConstructor, propTypes) {
    var Constructor = ConvenienceConstructor.componentConstructor;
    validateTypeDef(
      Constructor,
      propTypes,
      ReactPropTypeLocations.prop
    );
    Constructor.propTypes = merge(Constructor.propTypes, propTypes);
  },
  statics: function(ConvenienceConstructor, statics) {
    mixStaticSpecIntoComponent(ConvenienceConstructor, statics);
  }
};

function validateTypeDef(Constructor, typeDef, location) {
  for (var propName in typeDef) {
    if (typeDef.hasOwnProperty(propName)) {
      ("production" !== "development" ? invariant(
        typeof typeDef[propName] == 'function',
        '%s: %s type `%s` is invalid; it must be a function, usually from ' +
        'React.PropTypes.',
        Constructor.displayName || 'ReactCompositeComponent',
        ReactPropTypeLocationNames[location],
        propName
      ) : invariant(typeof typeDef[propName] == 'function'));
    }
  }
}

function validateMethodOverride(proto, name) {
  var specPolicy = ReactCompositeComponentInterface[name];

  // Disallow overriding of base class methods unless explicitly allowed.
  if (ReactCompositeComponentMixin.hasOwnProperty(name)) {
    ("production" !== "development" ? invariant(
      specPolicy === SpecPolicy.OVERRIDE_BASE,
      'ReactCompositeComponentInterface: You are attempting to override ' +
      '`%s` from your class specification. Ensure that your method names ' +
      'do not overlap with React methods.',
      name
    ) : invariant(specPolicy === SpecPolicy.OVERRIDE_BASE));
  }

  // Disallow defining methods more than once unless explicitly allowed.
  if (proto.hasOwnProperty(name)) {
    ("production" !== "development" ? invariant(
      specPolicy === SpecPolicy.DEFINE_MANY ||
      specPolicy === SpecPolicy.DEFINE_MANY_MERGED,
      'ReactCompositeComponentInterface: You are attempting to define ' +
      '`%s` on your component more than once. This conflict may be due ' +
      'to a mixin.',
      name
    ) : invariant(specPolicy === SpecPolicy.DEFINE_MANY ||
    specPolicy === SpecPolicy.DEFINE_MANY_MERGED));
  }
}

function validateLifeCycleOnReplaceState(instance) {
  var compositeLifeCycleState = instance._compositeLifeCycleState;
  ("production" !== "development" ? invariant(
    instance.isMounted() ||
      compositeLifeCycleState === CompositeLifeCycle.MOUNTING,
    'replaceState(...): Can only update a mounted or mounting component.'
  ) : invariant(instance.isMounted() ||
    compositeLifeCycleState === CompositeLifeCycle.MOUNTING));
  ("production" !== "development" ? invariant(compositeLifeCycleState !== CompositeLifeCycle.RECEIVING_STATE,
    'replaceState(...): Cannot update during an existing state transition ' +
    '(such as within `render`). This could potentially cause an infinite ' +
    'loop so it is forbidden.'
  ) : invariant(compositeLifeCycleState !== CompositeLifeCycle.RECEIVING_STATE));
  ("production" !== "development" ? invariant(compositeLifeCycleState !== CompositeLifeCycle.UNMOUNTING,
    'replaceState(...): Cannot update while unmounting component. This ' +
    'usually means you called setState() on an unmounted component.'
  ) : invariant(compositeLifeCycleState !== CompositeLifeCycle.UNMOUNTING));
}

/**
 * Custom version of `mixInto` which handles policy validation and reserved
 * specification keys when building `ReactCompositeComponent` classses.
 */
function mixSpecIntoComponent(ConvenienceConstructor, spec) {
  ("production" !== "development" ? invariant(
    !isValidClass(spec),
    'ReactCompositeComponent: You\'re attempting to ' +
    'use a component class as a mixin. Instead, just use a regular object.'
  ) : invariant(!isValidClass(spec)));
  ("production" !== "development" ? invariant(
    !ReactComponent.isValidComponent(spec),
    'ReactCompositeComponent: You\'re attempting to ' +
    'use a component as a mixin. Instead, just use a regular object.'
  ) : invariant(!ReactComponent.isValidComponent(spec)));

  var Constructor = ConvenienceConstructor.componentConstructor;
  var proto = Constructor.prototype;
  for (var name in spec) {
    var property = spec[name];
    if (!spec.hasOwnProperty(name)) {
      continue;
    }

    validateMethodOverride(proto, name);

    if (RESERVED_SPEC_KEYS.hasOwnProperty(name)) {
      RESERVED_SPEC_KEYS[name](ConvenienceConstructor, property);
    } else {
      // Setup methods on prototype:
      // The following member methods should not be automatically bound:
      // 1. Expected ReactCompositeComponent methods (in the "interface").
      // 2. Overridden methods (that were mixed in).
      var isCompositeComponentMethod = name in ReactCompositeComponentInterface;
      var isInherited = name in proto;
      var markedDontBind = property && property.__reactDontBind;
      var isFunction = typeof property === 'function';
      var shouldAutoBind =
        isFunction &&
        !isCompositeComponentMethod &&
        !isInherited &&
        !markedDontBind;

      if (shouldAutoBind) {
        if (!proto.__reactAutoBindMap) {
          proto.__reactAutoBindMap = {};
        }
        proto.__reactAutoBindMap[name] = property;
        proto[name] = property;
      } else {
        if (isInherited) {
          // For methods which are defined more than once, call the existing
          // methods before calling the new property.
          if (ReactCompositeComponentInterface[name] ===
              SpecPolicy.DEFINE_MANY_MERGED) {
            proto[name] = createMergedResultFunction(proto[name], property);
          } else {
            proto[name] = createChainedFunction(proto[name], property);
          }
        } else {
          proto[name] = property;
        }
      }
    }
  }
}

function mixStaticSpecIntoComponent(ConvenienceConstructor, statics) {
  if (!statics) {
    return;
  }
  for (var name in statics) {
    var property = statics[name];
    if (!statics.hasOwnProperty(name)) {
      return;
    }

    var isInherited = name in ConvenienceConstructor;
    var result = property;
    if (isInherited) {
      var existingProperty = ConvenienceConstructor[name];
      var existingType = typeof existingProperty;
      var propertyType = typeof property;
      ("production" !== "development" ? invariant(
        existingType === 'function' && propertyType === 'function',
        'ReactCompositeComponent: You are attempting to define ' +
        '`%s` on your component more than once, but that is only supported ' +
        'for functions, which are chained together. This conflict may be ' +
        'due to a mixin.',
        name
      ) : invariant(existingType === 'function' && propertyType === 'function'));
      result = createChainedFunction(existingProperty, property);
    }
    ConvenienceConstructor[name] = result;
    ConvenienceConstructor.componentConstructor[name] = result;
  }
}

/**
 * Merge two objects, but throw if both contain the same key.
 *
 * @param {object} one The first object, which is mutated.
 * @param {object} two The second object
 * @return {object} one after it has been mutated to contain everything in two.
 */
function mergeObjectsWithNoDuplicateKeys(one, two) {
  ("production" !== "development" ? invariant(
    one && two && typeof one === 'object' && typeof two === 'object',
    'mergeObjectsWithNoDuplicateKeys(): Cannot merge non-objects'
  ) : invariant(one && two && typeof one === 'object' && typeof two === 'object'));

  objMap(two, function(value, key) {
    ("production" !== "development" ? invariant(
      one[key] === undefined,
      'mergeObjectsWithNoDuplicateKeys(): ' +
      'Tried to merge two objects with the same key: %s',
      key
    ) : invariant(one[key] === undefined));
    one[key] = value;
  });
  return one;
}

/**
 * Creates a function that invokes two functions and merges their return values.
 *
 * @param {function} one Function to invoke first.
 * @param {function} two Function to invoke second.
 * @return {function} Function that invokes the two argument functions.
 * @private
 */
function createMergedResultFunction(one, two) {
  return function mergedResult() {
    var a = one.apply(this, arguments);
    var b = two.apply(this, arguments);
    if (a == null) {
      return b;
    } else if (b == null) {
      return a;
    }
    return mergeObjectsWithNoDuplicateKeys(a, b);
  };
}

/**
 * Creates a function that invokes two functions and ignores their return vales.
 *
 * @param {function} one Function to invoke first.
 * @param {function} two Function to invoke second.
 * @return {function} Function that invokes the two argument functions.
 * @private
 */
function createChainedFunction(one, two) {
  return function chainedFunction() {
    one.apply(this, arguments);
    two.apply(this, arguments);
  };
}

if ("production" !== "development") {

  var unmountedPropertyWhitelist = {
    constructor: true,
    construct: true,
    isOwnedBy: true, // should be deprecated but can have code mod (internal)
    type: true,
    props: true,
    // currently private but belong on the descriptor and are valid for use
    // inside the framework:
    __keyValidated__: true,
    _owner: true,
    _currentContext: true
  };

  var componentInstanceProperties = {
    __keyValidated__: true,
    __keySetters: true,
    _compositeLifeCycleState: true,
    _currentContext: true,
    _defaultProps: true,
    _instance: true,
    _lifeCycleState: true,
    _mountDepth: true,
    _owner: true,
    _pendingCallbacks: true,
    _pendingContext: true,
    _pendingForceUpdate: true,
    _pendingOwner: true,
    _pendingProps: true,
    _pendingState: true,
    _renderedComponent: true,
    _rootNodeID: true,
    context: true,
    props: true,
    refs: true,
    state: true,

    // These are known instance properties coming from other sources
    _pendingQueries: true,
    _queryPropListeners: true,
    queryParams: true

  };

  var hasWarnedOnComponentType = {};

  var warningStackCounter = 0;

  var issueMembraneWarning = function(instance, key) {
    var isWhitelisted = unmountedPropertyWhitelist.hasOwnProperty(key);
    if (warningStackCounter > 0 || isWhitelisted) {
      return;
    }
    var name = instance.constructor.displayName || 'Unknown';
    var owner = ReactCurrentOwner.current;
    var ownerName = (owner && owner.constructor.displayName) || 'Unknown';
    var warningKey = key + '|' + name + '|' + ownerName;
    if (hasWarnedOnComponentType.hasOwnProperty(warningKey)) {
      // We have already warned for this combination. Skip it this time.
      return;
    }
    hasWarnedOnComponentType[warningKey] = true;

    var context = owner ? ' in ' + ownerName + '.' : ' at the top level.';
    var staticMethodExample = '<' + name + ' />.type.' + key + '(...)';

    monitorCodeUse('react_descriptor_property_access', { component: name });
    console.warn(
      'Invalid access to component property "' + key + '" on ' + name +
      context + ' See http://fb.me/react-warning-descriptors .' +
      ' Use a static method instead: ' + staticMethodExample
    );
  };

  var wrapInMembraneFunction = function(fn, thisBinding) {
    if (fn.__reactMembraneFunction && fn.__reactMembraneSelf === thisBinding) {
      return fn.__reactMembraneFunction;
    }
    return fn.__reactMembraneFunction = function() {
      /**
       * By getting this function, you've already received a warning. The
       * internals of this function will likely cause more warnings. To avoid
       * Spamming too much we disable any warning triggered inside of this
       * stack.
       */
      warningStackCounter++;
      try {
        // If the this binding is unchanged, we defer to the real component.
        // This is important to keep some referential integrity in the
        // internals. E.g. owner equality check.
        var self = this === thisBinding ? this.__realComponentInstance : this;
        return fn.apply(self, arguments);
      } finally {
        warningStackCounter--;
      }
    };
  };

  var defineMembraneProperty = function(membrane, prototype, key) {
    Object.defineProperty(membrane, key, {

      configurable: false,
      enumerable: true,

      get: function() {
        if (this === membrane) {
          // We're allowed to access the prototype directly.
          return prototype[key];
        }
        issueMembraneWarning(this, key);

        var realValue = this.__realComponentInstance[key];
        // If the real value is a function, we need to provide a wrapper that
        // disables nested warnings. The properties type and constructors are
        // expected to the be constructors and therefore is often use with an
        // equality check and we shouldn't try to rebind those.
        if (typeof realValue === 'function' &&
            key !== 'type' &&
            key !== 'constructor') {
          return wrapInMembraneFunction(realValue, this);
        }
        return realValue;
      },

      set: function(value) {
        if (this === membrane) {
          // We're allowed to set a value on the prototype directly.
          prototype[key] = value;
          return;
        }
        issueMembraneWarning(this, key);
        this.__realComponentInstance[key] = value;
      }

    });
  };

  /**
   * Creates a membrane prototype which wraps the original prototype. If any
   * property is accessed in an unmounted state, a warning is issued.
   *
   * @param {object} prototype Original prototype.
   * @return {object} The membrane prototype.
   * @private
   */
  var createMountWarningMembrane = function(prototype) {
    var membrane = {};
    var key;
    for (key in prototype) {
      defineMembraneProperty(membrane, prototype, key);
    }
    // These are properties that goes into the instance but not the prototype.
    // We can create the membrane on the prototype even though this will
    // result in a faulty hasOwnProperty check it's better perf.
    for (key in componentInstanceProperties) {
      if (componentInstanceProperties.hasOwnProperty(key) &&
          !(key in prototype)) {
        defineMembraneProperty(membrane, prototype, key);
      }
    }
    return membrane;
  };

  /**
   * Creates a membrane constructor which wraps the component that gets mounted.
   *
   * @param {function} constructor Original constructor.
   * @return {function} The membrane constructor.
   * @private
   */
  var createDescriptorProxy = function(constructor) {
    try {
      var ProxyConstructor = function() {
        this.__realComponentInstance = new constructor();

        // We can only safely pass through known instance variables. Unknown
        // expandos are not safe. Use the real mounted instance to avoid this
        // problem if it blows something up.
        Object.freeze(this);
      };

      ProxyConstructor.prototype = createMountWarningMembrane(
        constructor.prototype
      );

      return ProxyConstructor;
    } catch(x) {
      // In IE8 define property will fail on non-DOM objects. If anything in
      // the membrane creation fails, we'll bail out and just use the plain
      // constructor without warnings.
      return constructor;
    }
  };

}

/**
 * `ReactCompositeComponent` maintains an auxiliary life cycle state in
 * `this._compositeLifeCycleState` (which can be null).
 *
 * This is different from the life cycle state maintained by `ReactComponent` in
 * `this._lifeCycleState`. The following diagram shows how the states overlap in
 * time. There are times when the CompositeLifeCycle is null - at those times it
 * is only meaningful to look at ComponentLifeCycle alone.
 *
 * Top Row: ReactComponent.ComponentLifeCycle
 * Low Row: ReactComponent.CompositeLifeCycle
 *
 * +-------+------------------------------------------------------+--------+
 * |  UN   |                    MOUNTED                           |   UN   |
 * |MOUNTED|                                                      | MOUNTED|
 * +-------+------------------------------------------------------+--------+
 * |       ^--------+   +------+   +------+   +------+   +--------^        |
 * |       |        |   |      |   |      |   |      |   |        |        |
 * |    0--|MOUNTING|-0-|RECEIV|-0-|RECEIV|-0-|RECEIV|-0-|   UN   |--->0   |
 * |       |        |   |PROPS |   | PROPS|   | STATE|   |MOUNTING|        |
 * |       |        |   |      |   |      |   |      |   |        |        |
 * |       |        |   |      |   |      |   |      |   |        |        |
 * |       +--------+   +------+   +------+   +------+   +--------+        |
 * |       |                                                      |        |
 * +-------+------------------------------------------------------+--------+
 */
var CompositeLifeCycle = keyMirror({
  /**
   * Components in the process of being mounted respond to state changes
   * differently.
   */
  MOUNTING: null,
  /**
   * Components in the process of being unmounted are guarded against state
   * changes.
   */
  UNMOUNTING: null,
  /**
   * Components that are mounted and receiving new props respond to state
   * changes differently.
   */
  RECEIVING_PROPS: null,
  /**
   * Components that are mounted and receiving new state are guarded against
   * additional state changes.
   */
  RECEIVING_STATE: null
});

/**
 * @lends {ReactCompositeComponent.prototype}
 */
var ReactCompositeComponentMixin = {

  /**
   * Base constructor for all composite component.
   *
   * @param {?object} initialProps
   * @param {*} children
   * @final
   * @internal
   */
  construct: function(initialProps, children) {
    // Children can be either an array or more than one argument
    ReactComponent.Mixin.construct.apply(this, arguments);
    ReactOwner.Mixin.construct.apply(this, arguments);

    this.state = null;
    this._pendingState = null;

    this.context = null;
    this._currentContext = ReactContext.current;
    this._pendingContext = null;

    // The descriptor that was used to instantiate this component. Will be
    // set by the instantiator instead of the constructor since this
    // constructor is currently used by both instances and descriptors.
    this._descriptor = null;

    this._compositeLifeCycleState = null;
  },

  /**
   * Components in the intermediate state now has cyclic references. To avoid
   * breaking JSON serialization we expose a custom JSON format.
   * @return {object} JSON compatible representation.
   * @internal
   * @final
   */
  toJSON: function() {
    return { type: this.type, props: this.props };
  },

  /**
   * Checks whether or not this composite component is mounted.
   * @return {boolean} True if mounted, false otherwise.
   * @protected
   * @final
   */
  isMounted: function() {
    return ReactComponent.Mixin.isMounted.call(this) &&
      this._compositeLifeCycleState !== CompositeLifeCycle.MOUNTING;
  },

  /**
   * Initializes the component, renders markup, and registers event listeners.
   *
   * @param {string} rootID DOM ID of the root node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {number} mountDepth number of components in the owner hierarchy
   * @return {?string} Rendered markup to be inserted into the DOM.
   * @final
   * @internal
   */
  mountComponent: ReactPerf.measure(
    'ReactCompositeComponent',
    'mountComponent',
    function(rootID, transaction, mountDepth) {
      ReactComponent.Mixin.mountComponent.call(
        this,
        rootID,
        transaction,
        mountDepth
      );
      this._compositeLifeCycleState = CompositeLifeCycle.MOUNTING;

      this.context = this._processContext(this._currentContext);
      this._defaultProps = this.getDefaultProps ? this.getDefaultProps() : null;
      this.props = this._processProps(this.props);

      if (this.__reactAutoBindMap) {
        this._bindAutoBindMethods();
      }

      this.state = this.getInitialState ? this.getInitialState() : null;
      ("production" !== "development" ? invariant(
        typeof this.state === 'object' && !Array.isArray(this.state),
        '%s.getInitialState(): must return an object or null',
        this.constructor.displayName || 'ReactCompositeComponent'
      ) : invariant(typeof this.state === 'object' && !Array.isArray(this.state)));

      this._pendingState = null;
      this._pendingForceUpdate = false;

      if (this.componentWillMount) {
        this.componentWillMount();
        // When mounting, calls to `setState` by `componentWillMount` will set
        // `this._pendingState` without triggering a re-render.
        if (this._pendingState) {
          this.state = this._pendingState;
          this._pendingState = null;
        }
      }

      this._renderedComponent = instantiateReactComponent(
        this._renderValidatedComponent()
      );

      // Done with mounting, `setState` will now trigger UI changes.
      this._compositeLifeCycleState = null;
      var markup = this._renderedComponent.mountComponent(
        rootID,
        transaction,
        mountDepth + 1
      );
      if (this.componentDidMount) {
        transaction.getReactMountReady().enqueue(this, this.componentDidMount);
      }
      return markup;
    }
  ),

  /**
   * Releases any resources allocated by `mountComponent`.
   *
   * @final
   * @internal
   */
  unmountComponent: function() {
    this._compositeLifeCycleState = CompositeLifeCycle.UNMOUNTING;
    if (this.componentWillUnmount) {
      this.componentWillUnmount();
    }
    this._compositeLifeCycleState = null;

    this._defaultProps = null;

    this._renderedComponent.unmountComponent();
    this._renderedComponent = null;

    ReactComponent.Mixin.unmountComponent.call(this);

    // Some existing components rely on this.props even after they've been
    // destroyed (in event handlers).
    // TODO: this.props = null;
    // TODO: this.state = null;
  },

  /**
   * Sets a subset of the state. Always use this or `replaceState` to mutate
   * state. You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * There is no guarantee that calls to `setState` will run synchronously,
   * as they may eventually be batched together.  You can provide an optional
   * callback that will be executed when the call to setState is actually
   * completed.
   *
   * @param {object} partialState Next partial state to be merged with state.
   * @param {?function} callback Called after state is updated.
   * @final
   * @protected
   */
  setState: function(partialState, callback) {
    ("production" !== "development" ? invariant(
      typeof partialState === 'object' || partialState == null,
      'setState(...): takes an object of state variables to update.'
    ) : invariant(typeof partialState === 'object' || partialState == null));
    if ("production" !== "development") {
      ("production" !== "development" ? warning(
        partialState != null,
        'setState(...): You passed an undefined or null state object; ' +
        'instead, use forceUpdate().'
      ) : null);
    }
    // Merge with `_pendingState` if it exists, otherwise with existing state.
    this.replaceState(
      merge(this._pendingState || this.state, partialState),
      callback
    );
  },

  /**
   * Replaces all of the state. Always use this or `setState` to mutate state.
   * You should treat `this.state` as immutable.
   *
   * There is no guarantee that `this.state` will be immediately updated, so
   * accessing `this.state` after calling this method may return the old value.
   *
   * @param {object} completeState Next state.
   * @param {?function} callback Called after state is updated.
   * @final
   * @protected
   */
  replaceState: function(completeState, callback) {
    validateLifeCycleOnReplaceState(this);
    this._pendingState = completeState;
    ReactUpdates.enqueueUpdate(this, callback);
  },

  /**
   * Filters the context object to only contain keys specified in
   * `contextTypes`, and asserts that they are valid.
   *
   * @param {object} context
   * @return {?object}
   * @private
   */
  _processContext: function(context) {
    var maskedContext = null;
    var contextTypes = this.constructor.contextTypes;
    if (contextTypes) {
      maskedContext = {};
      for (var contextName in contextTypes) {
        maskedContext[contextName] = context[contextName];
      }
      if ("production" !== "development") {
        this._checkPropTypes(
          contextTypes,
          maskedContext,
          ReactPropTypeLocations.context
        );
      }
    }
    return maskedContext;
  },

  /**
   * @param {object} currentContext
   * @return {object}
   * @private
   */
  _processChildContext: function(currentContext) {
    var childContext = this.getChildContext && this.getChildContext();
    var displayName = this.constructor.displayName || 'ReactCompositeComponent';
    if (childContext) {
      ("production" !== "development" ? invariant(
        typeof this.constructor.childContextTypes === 'object',
        '%s.getChildContext(): childContextTypes must be defined in order to ' +
        'use getChildContext().',
        displayName
      ) : invariant(typeof this.constructor.childContextTypes === 'object'));
      if ("production" !== "development") {
        this._checkPropTypes(
          this.constructor.childContextTypes,
          childContext,
          ReactPropTypeLocations.childContext
        );
      }
      for (var name in childContext) {
        ("production" !== "development" ? invariant(
          name in this.constructor.childContextTypes,
          '%s.getChildContext(): key "%s" is not defined in childContextTypes.',
          displayName,
          name
        ) : invariant(name in this.constructor.childContextTypes));
      }
      return merge(currentContext, childContext);
    }
    return currentContext;
  },

  /**
   * Processes props by setting default values for unspecified props and
   * asserting that the props are valid. Does not mutate its argument; returns
   * a new props object with defaults merged in.
   *
   * @param {object} newProps
   * @return {object}
   * @private
   */
  _processProps: function(newProps) {
    var props = merge(newProps);
    var defaultProps = this._defaultProps;
    for (var propName in defaultProps) {
      if (typeof props[propName] === 'undefined') {
        props[propName] = defaultProps[propName];
      }
    }
    if ("production" !== "development") {
      var propTypes = this.constructor.propTypes;
      if (propTypes) {
        this._checkPropTypes(propTypes, props, ReactPropTypeLocations.prop);
      }
    }
    return props;
  },

  /**
   * Assert that the props are valid
   *
   * @param {object} propTypes Map of prop name to a ReactPropType
   * @param {object} props
   * @param {string} location e.g. "prop", "context", "child context"
   * @private
   */
  _checkPropTypes: function(propTypes, props, location) {
    var componentName = this.constructor.displayName;
    for (var propName in propTypes) {
      if (propTypes.hasOwnProperty(propName)) {
        propTypes[propName](props, propName, componentName, location);
      }
    }
  },

  performUpdateIfNecessary: function() {
    var compositeLifeCycleState = this._compositeLifeCycleState;
    // Do not trigger a state transition if we are in the middle of mounting or
    // receiving props because both of those will already be doing this.
    if (compositeLifeCycleState === CompositeLifeCycle.MOUNTING ||
        compositeLifeCycleState === CompositeLifeCycle.RECEIVING_PROPS) {
      return;
    }
    ReactComponent.Mixin.performUpdateIfNecessary.call(this);
  },

  /**
   * If any of `_pendingProps`, `_pendingState`, or `_pendingForceUpdate` is
   * set, update the component.
   *
   * @param {ReactReconcileTransaction} transaction
   * @internal
   */
  _performUpdateIfNecessary: function(transaction) {
    if (this._pendingProps == null &&
        this._pendingState == null &&
        this._pendingContext == null &&
        !this._pendingForceUpdate) {
      return;
    }

    var nextFullContext = this._pendingContext || this._currentContext;
    var nextContext = this._processContext(nextFullContext);
    this._pendingContext = null;

    var nextProps = this.props;
    if (this._pendingProps != null) {
      nextProps = this._processProps(this._pendingProps);
      this._pendingProps = null;

      this._compositeLifeCycleState = CompositeLifeCycle.RECEIVING_PROPS;
      if (this.componentWillReceiveProps) {
        this.componentWillReceiveProps(nextProps, nextContext);
      }
    }

    this._compositeLifeCycleState = CompositeLifeCycle.RECEIVING_STATE;

    // Unlike props, state, and context, we specifically don't want to set
    // _pendingOwner to null here because it's possible for a component to have
    // a null owner, so we instead make `this._owner === this._pendingOwner`
    // mean that there's no owner change pending.
    var nextOwner = this._pendingOwner;

    var nextState = this._pendingState || this.state;
    this._pendingState = null;

    try {
      if (this._pendingForceUpdate ||
          !this.shouldComponentUpdate ||
          this.shouldComponentUpdate(nextProps, nextState, nextContext)) {
        this._pendingForceUpdate = false;
        // Will set `this.props`, `this.state` and `this.context`.
        this._performComponentUpdate(
          nextProps,
          nextOwner,
          nextState,
          nextFullContext,
          nextContext,
          transaction
        );
      } else {
        // If it's determined that a component should not update, we still want
        // to set props and state.
        this.props = nextProps;
        this._owner = nextOwner;
        this.state = nextState;
        this._currentContext = nextFullContext;
        this.context = nextContext;
      }
    } finally {
      this._compositeLifeCycleState = null;
    }
  },

  /**
   * Merges new props and state, notifies delegate methods of update and
   * performs update.
   *
   * @param {object} nextProps Next object to set as properties.
   * @param {?ReactComponent} nextOwner Next component to set as owner
   * @param {?object} nextState Next object to set as state.
   * @param {?object} nextFullContext Next object to set as _currentContext.
   * @param {?object} nextContext Next object to set as context.
   * @param {ReactReconcileTransaction} transaction
   * @private
   */
  _performComponentUpdate: function(
    nextProps,
    nextOwner,
    nextState,
    nextFullContext,
    nextContext,
    transaction
  ) {
    var prevProps = this.props;
    var prevOwner = this._owner;
    var prevState = this.state;
    var prevContext = this.context;

    if (this.componentWillUpdate) {
      this.componentWillUpdate(nextProps, nextState, nextContext);
    }

    this.props = nextProps;
    this._owner = nextOwner;
    this.state = nextState;
    this._currentContext = nextFullContext;
    this.context = nextContext;

    this.updateComponent(
      transaction,
      prevProps,
      prevOwner,
      prevState,
      prevContext
    );

    if (this.componentDidUpdate) {
      transaction.getReactMountReady().enqueue(
        this,
        this.componentDidUpdate.bind(this, prevProps, prevState, prevContext)
      );
    }
  },

  receiveComponent: function(nextComponent, transaction) {
    if (nextComponent === this._descriptor) {
      // Since props and context are immutable after the component is
      // mounted, we can do a cheap identity compare here to determine
      // if this is a superfluous reconcile.
      return;
    }

    // Update the descriptor that was last used by this component instance
    this._descriptor = nextComponent;

    this._pendingContext = nextComponent._currentContext;
    ReactComponent.Mixin.receiveComponent.call(
      this,
      nextComponent,
      transaction
    );
  },

  /**
   * Updates the component's currently mounted DOM representation.
   *
   * By default, this implements React's rendering and reconciliation algorithm.
   * Sophisticated clients may wish to override this.
   *
   * @param {ReactReconcileTransaction} transaction
   * @param {object} prevProps
   * @param {?ReactComponent} prevOwner
   * @param {?object} prevState
   * @param {?object} prevContext
   * @internal
   * @overridable
   */
  updateComponent: ReactPerf.measure(
    'ReactCompositeComponent',
    'updateComponent',
    function(transaction, prevProps, prevOwner, prevState, prevContext) {
      ReactComponent.Mixin.updateComponent.call(
        this,
        transaction,
        prevProps,
        prevOwner
      );


      var prevComponentInstance = this._renderedComponent;
      var nextComponent = this._renderValidatedComponent();
      if (shouldUpdateReactComponent(prevComponentInstance, nextComponent)) {
        prevComponentInstance.receiveComponent(nextComponent, transaction);
      } else {
        // These two IDs are actually the same! But nothing should rely on that.
        var thisID = this._rootNodeID;
        var prevComponentID = prevComponentInstance._rootNodeID;
        prevComponentInstance.unmountComponent();
        this._renderedComponent = instantiateReactComponent(nextComponent);
        var nextMarkup = this._renderedComponent.mountComponent(
          thisID,
          transaction,
          this._mountDepth + 1
        );
        ReactComponent.BackendIDOperations.dangerouslyReplaceNodeWithMarkupByID(
          prevComponentID,
          nextMarkup
        );
      }
    }
  ),

  /**
   * Forces an update. This should only be invoked when it is known with
   * certainty that we are **not** in a DOM transaction.
   *
   * You may want to call this when you know that some deeper aspect of the
   * component's state has changed but `setState` was not called.
   *
   * This will not invoke `shouldUpdateComponent`, but it will invoke
   * `componentWillUpdate` and `componentDidUpdate`.
   *
   * @param {?function} callback Called after update is complete.
   * @final
   * @protected
   */
  forceUpdate: function(callback) {
    var compositeLifeCycleState = this._compositeLifeCycleState;
    ("production" !== "development" ? invariant(
      this.isMounted() ||
        compositeLifeCycleState === CompositeLifeCycle.MOUNTING,
      'forceUpdate(...): Can only force an update on mounted or mounting ' +
        'components.'
    ) : invariant(this.isMounted() ||
      compositeLifeCycleState === CompositeLifeCycle.MOUNTING));
    ("production" !== "development" ? invariant(
      compositeLifeCycleState !== CompositeLifeCycle.RECEIVING_STATE &&
      compositeLifeCycleState !== CompositeLifeCycle.UNMOUNTING,
      'forceUpdate(...): Cannot force an update while unmounting component ' +
      'or during an existing state transition (such as within `render`).'
    ) : invariant(compositeLifeCycleState !== CompositeLifeCycle.RECEIVING_STATE &&
    compositeLifeCycleState !== CompositeLifeCycle.UNMOUNTING));
    this._pendingForceUpdate = true;
    ReactUpdates.enqueueUpdate(this, callback);
  },

  /**
   * @private
   */
  _renderValidatedComponent: ReactPerf.measure(
    'ReactCompositeComponent',
    '_renderValidatedComponent',
    function() {
      var renderedComponent;
      var previousContext = ReactContext.current;
      ReactContext.current = this._processChildContext(this._currentContext);
      ReactCurrentOwner.current = this;
      try {
        renderedComponent = this.render();
      } finally {
        ReactContext.current = previousContext;
        ReactCurrentOwner.current = null;
      }
      ("production" !== "development" ? invariant(
        ReactComponent.isValidComponent(renderedComponent),
        '%s.render(): A valid ReactComponent must be returned. You may have ' +
          'returned null, undefined, an array, or some other invalid object.',
        this.constructor.displayName || 'ReactCompositeComponent'
      ) : invariant(ReactComponent.isValidComponent(renderedComponent)));
      return renderedComponent;
    }
  ),

  /**
   * @private
   */
  _bindAutoBindMethods: function() {
    for (var autoBindKey in this.__reactAutoBindMap) {
      if (!this.__reactAutoBindMap.hasOwnProperty(autoBindKey)) {
        continue;
      }
      var method = this.__reactAutoBindMap[autoBindKey];
      this[autoBindKey] = this._bindAutoBindMethod(ReactErrorUtils.guard(
        method,
        this.constructor.displayName + '.' + autoBindKey
      ));
    }
  },

  /**
   * Binds a method to the component.
   *
   * @param {function} method Method to be bound.
   * @private
   */
  _bindAutoBindMethod: function(method) {
    var component = this;
    var boundMethod = function() {
      return method.apply(component, arguments);
    };
    if ("production" !== "development") {
      boundMethod.__reactBoundContext = component;
      boundMethod.__reactBoundMethod = method;
      boundMethod.__reactBoundArguments = null;
      var componentName = component.constructor.displayName;
      var _bind = boundMethod.bind;
      boundMethod.bind = function(newThis ) {var args=Array.prototype.slice.call(arguments,1);
        // User is trying to bind() an autobound method; we effectively will
        // ignore the value of "this" that the user is trying to use, so
        // let's warn.
        if (newThis !== component && newThis !== null) {
          monitorCodeUse('react_bind_warning', { component: componentName });
          console.warn(
            'bind(): React component methods may only be bound to the ' +
            'component instance. See ' + componentName
          );
        } else if (!args.length) {
          monitorCodeUse('react_bind_warning', { component: componentName });
          console.warn(
            'bind(): You are binding a component method to the component. ' +
            'React does this for you automatically in a high-performance ' +
            'way, so you can safely remove this call. See ' + componentName
          );
          return boundMethod;
        }
        var reboundMethod = _bind.apply(boundMethod, arguments);
        reboundMethod.__reactBoundContext = component;
        reboundMethod.__reactBoundMethod = method;
        reboundMethod.__reactBoundArguments = args;
        return reboundMethod;
      };
    }
    return boundMethod;
  }
};

var ReactCompositeComponentBase = function() {};
mixInto(ReactCompositeComponentBase, ReactComponent.Mixin);
mixInto(ReactCompositeComponentBase, ReactOwner.Mixin);
mixInto(ReactCompositeComponentBase, ReactPropTransferer.Mixin);
mixInto(ReactCompositeComponentBase, ReactCompositeComponentMixin);

/**
 * Checks if a value is a valid component constructor.
 *
 * @param {*}
 * @return {boolean}
 * @public
 */
function isValidClass(componentClass) {
  return componentClass instanceof Function &&
         'componentConstructor' in componentClass &&
         componentClass.componentConstructor instanceof Function;
}
/**
 * Module for creating composite components.
 *
 * @class ReactCompositeComponent
 * @extends ReactComponent
 * @extends ReactOwner
 * @extends ReactPropTransferer
 */
var ReactCompositeComponent = {

  LifeCycle: CompositeLifeCycle,

  Base: ReactCompositeComponentBase,

  /**
   * Creates a composite component class given a class specification.
   *
   * @param {object} spec Class specification (which must define `render`).
   * @return {function} Component constructor function.
   * @public
   */
  createClass: function(spec) {
    var Constructor = function() {};
    Constructor.prototype = new ReactCompositeComponentBase();
    Constructor.prototype.constructor = Constructor;

    var DescriptorConstructor = Constructor;

    var ConvenienceConstructor = function(props, children) {
      var descriptor = new DescriptorConstructor();
      descriptor.construct.apply(descriptor, arguments);
      return descriptor;
    };
    ConvenienceConstructor.componentConstructor = Constructor;
    Constructor.ConvenienceConstructor = ConvenienceConstructor;
    ConvenienceConstructor.originalSpec = spec;

    injectedMixins.forEach(
      mixSpecIntoComponent.bind(null, ConvenienceConstructor)
    );

    mixSpecIntoComponent(ConvenienceConstructor, spec);

    ("production" !== "development" ? invariant(
      Constructor.prototype.render,
      'createClass(...): Class specification must implement a `render` method.'
    ) : invariant(Constructor.prototype.render));

    if ("production" !== "development") {
      if (Constructor.prototype.componentShouldUpdate) {
        monitorCodeUse(
          'react_component_should_update_warning',
          { component: spec.displayName }
        );
        console.warn(
          (spec.displayName || 'A component') + ' has a method called ' +
          'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' +
          'The name is phrased as a question because the function is ' +
          'expected to return a value.'
         );
      }
    }

    // Expose the convience constructor on the prototype so that it can be
    // easily accessed on descriptors. E.g. <Foo />.type === Foo.type and for
    // static methods like <Foo />.type.staticMethod();
    // This should not be named constructor since this may not be the function
    // that created the descriptor, and it may not even be a constructor.
    ConvenienceConstructor.type = Constructor;
    Constructor.prototype.type = Constructor;

    // Reduce time spent doing lookups by setting these on the prototype.
    for (var methodName in ReactCompositeComponentInterface) {
      if (!Constructor.prototype[methodName]) {
        Constructor.prototype[methodName] = null;
      }
    }

    if ("production" !== "development") {
      // In DEV the convenience constructor generates a proxy to another
      // instance around it to warn about access to properties on the
      // descriptor.
      DescriptorConstructor = createDescriptorProxy(Constructor);
    }

    return ConvenienceConstructor;
  },

  isValidClass: isValidClass,

  injection: {
    injectMixin: function(mixin) {
      injectedMixins.push(mixin);
    }
  }
};

module.exports = ReactCompositeComponent;

},{"./ReactComponent":27,"./ReactContext":30,"./ReactCurrentOwner":31,"./ReactErrorUtils":47,"./ReactOwner":59,"./ReactPerf":60,"./ReactPropTransferer":61,"./ReactPropTypeLocationNames":62,"./ReactPropTypeLocations":63,"./ReactUpdates":71,"./instantiateReactComponent":111,"./invariant":112,"./keyMirror":118,"./merge":121,"./mixInto":124,"./monitorCodeUse":125,"./objMap":126,"./shouldUpdateReactComponent":131,"./warning":134}],30:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactContext
 */

"use strict";

var merge = _dereq_("./merge");

/**
 * Keeps track of the current context.
 *
 * The context is automatically passed down the component ownership hierarchy
 * and is accessible via `this.context` on ReactCompositeComponents.
 */
var ReactContext = {

  /**
   * @internal
   * @type {object}
   */
  current: {},

  /**
   * Temporarily extends the current context while executing scopedCallback.
   *
   * A typical use case might look like
   *
   *  render: function() {
   *    var children = ReactContext.withContext({foo: 'foo'} () => (
   *
   *    ));
   *    return <div>{children}</div>;
   *  }
   *
   * @param {object} newContext New context to merge into the existing context
   * @param {function} scopedCallback Callback to run with the new context
   * @return {ReactComponent|array<ReactComponent>}
   */
  withContext: function(newContext, scopedCallback) {
    var result;
    var previousContext = ReactContext.current;
    ReactContext.current = merge(previousContext, newContext);
    try {
      result = scopedCallback();
    } finally {
      ReactContext.current = previousContext;
    }
    return result;
  }

};

module.exports = ReactContext;

},{"./merge":121}],31:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactCurrentOwner
 */

"use strict";

/**
 * Keeps track of the current owner.
 *
 * The current owner is the component who should own any components that are
 * currently being constructed.
 *
 * The depth indicate how many composite components are above this render level.
 */
var ReactCurrentOwner = {

  /**
   * @internal
   * @type {ReactComponent}
   */
  current: null

};

module.exports = ReactCurrentOwner;

},{}],32:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOM
 * @typechecks static-only
 */

"use strict";

var ReactDOMComponent = _dereq_("./ReactDOMComponent");

var mergeInto = _dereq_("./mergeInto");
var objMapKeyVal = _dereq_("./objMapKeyVal");

/**
 * Creates a new React class that is idempotent and capable of containing other
 * React components. It accepts event listeners and DOM properties that are
 * valid according to `DOMProperty`.
 *
 *  - Event listeners: `onClick`, `onMouseDown`, etc.
 *  - DOM properties: `className`, `name`, `title`, etc.
 *
 * The `style` property functions differently from the DOM API. It accepts an
 * object mapping of style properties to values.
 *
 * @param {string} tag Tag name (e.g. `div`).
 * @param {boolean} omitClose True if the close tag should be omitted.
 * @private
 */
function createDOMComponentClass(tag, omitClose) {
  var Constructor = function() {};
  Constructor.prototype = new ReactDOMComponent(tag, omitClose);
  Constructor.prototype.constructor = Constructor;
  Constructor.displayName = tag;

  var ConvenienceConstructor = function(props, children) {
    var instance = new Constructor();
    instance.construct.apply(instance, arguments);
    return instance;
  };

  // Expose the constructor on the ConvenienceConstructor and prototype so that
  // it can be easily easily accessed on descriptors.
  // E.g. <div />.type === div.type
  ConvenienceConstructor.type = Constructor;
  Constructor.prototype.type = Constructor;

  Constructor.ConvenienceConstructor = ConvenienceConstructor;
  ConvenienceConstructor.componentConstructor = Constructor;
  return ConvenienceConstructor;
}

/**
 * Creates a mapping from supported HTML tags to `ReactDOMComponent` classes.
 * This is also accessible via `React.DOM`.
 *
 * @public
 */
var ReactDOM = objMapKeyVal({
  a: false,
  abbr: false,
  address: false,
  area: true,
  article: false,
  aside: false,
  audio: false,
  b: false,
  base: true,
  bdi: false,
  bdo: false,
  big: false,
  blockquote: false,
  body: false,
  br: true,
  button: false,
  canvas: false,
  caption: false,
  cite: false,
  code: false,
  col: true,
  colgroup: false,
  data: false,
  datalist: false,
  dd: false,
  del: false,
  details: false,
  dfn: false,
  div: false,
  dl: false,
  dt: false,
  em: false,
  embed: true,
  fieldset: false,
  figcaption: false,
  figure: false,
  footer: false,
  form: false, // NOTE: Injected, see `ReactDOMForm`.
  h1: false,
  h2: false,
  h3: false,
  h4: false,
  h5: false,
  h6: false,
  head: false,
  header: false,
  hr: true,
  html: false,
  i: false,
  iframe: false,
  img: true,
  input: true,
  ins: false,
  kbd: false,
  keygen: true,
  label: false,
  legend: false,
  li: false,
  link: true,
  main: false,
  map: false,
  mark: false,
  menu: false,
  menuitem: false, // NOTE: Close tag should be omitted, but causes problems.
  meta: true,
  meter: false,
  nav: false,
  noscript: false,
  object: false,
  ol: false,
  optgroup: false,
  option: false,
  output: false,
  p: false,
  param: true,
  pre: false,
  progress: false,
  q: false,
  rp: false,
  rt: false,
  ruby: false,
  s: false,
  samp: false,
  script: false,
  section: false,
  select: false,
  small: false,
  source: true,
  span: false,
  strong: false,
  style: false,
  sub: false,
  summary: false,
  sup: false,
  table: false,
  tbody: false,
  td: false,
  textarea: false, // NOTE: Injected, see `ReactDOMTextarea`.
  tfoot: false,
  th: false,
  thead: false,
  time: false,
  title: false,
  tr: false,
  track: true,
  u: false,
  ul: false,
  'var': false,
  video: false,
  wbr: true,

  // SVG
  circle: false,
  defs: false,
  g: false,
  line: false,
  linearGradient: false,
  path: false,
  polygon: false,
  polyline: false,
  radialGradient: false,
  rect: false,
  stop: false,
  svg: false,
  text: false
}, createDOMComponentClass);

var injection = {
  injectComponentClasses: function(componentClasses) {
    mergeInto(ReactDOM, componentClasses);
  }
};

ReactDOM.injection = injection;

module.exports = ReactDOM;

},{"./ReactDOMComponent":34,"./mergeInto":123,"./objMapKeyVal":127}],33:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMButton
 */

"use strict";

var AutoFocusMixin = _dereq_("./AutoFocusMixin");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");

var keyMirror = _dereq_("./keyMirror");

// Store a reference to the <button> `ReactDOMComponent`.
var button = ReactDOM.button;

var mouseListenerNames = keyMirror({
  onClick: true,
  onDoubleClick: true,
  onMouseDown: true,
  onMouseMove: true,
  onMouseUp: true,
  onClickCapture: true,
  onDoubleClickCapture: true,
  onMouseDownCapture: true,
  onMouseMoveCapture: true,
  onMouseUpCapture: true
});

/**
 * Implements a <button> native component that does not receive mouse events
 * when `disabled` is set.
 */
var ReactDOMButton = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMButton',

  mixins: [AutoFocusMixin, ReactBrowserComponentMixin],

  render: function() {
    var props = {};

    // Copy the props; except the mouse listeners if we're disabled
    for (var key in this.props) {
      if (this.props.hasOwnProperty(key) &&
          (!this.props.disabled || !mouseListenerNames[key])) {
        props[key] = this.props[key];
      }
    }

    return button(props, this.props.children);
  }

});

module.exports = ReactDOMButton;

},{"./AutoFocusMixin":1,"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./keyMirror":118}],34:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMComponent
 * @typechecks static-only
 */

"use strict";

var CSSPropertyOperations = _dereq_("./CSSPropertyOperations");
var DOMProperty = _dereq_("./DOMProperty");
var DOMPropertyOperations = _dereq_("./DOMPropertyOperations");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactComponent = _dereq_("./ReactComponent");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var ReactMount = _dereq_("./ReactMount");
var ReactMultiChild = _dereq_("./ReactMultiChild");
var ReactPerf = _dereq_("./ReactPerf");

var escapeTextForBrowser = _dereq_("./escapeTextForBrowser");
var invariant = _dereq_("./invariant");
var keyOf = _dereq_("./keyOf");
var merge = _dereq_("./merge");
var mixInto = _dereq_("./mixInto");

var deleteListener = ReactEventEmitter.deleteListener;
var listenTo = ReactEventEmitter.listenTo;
var registrationNameModules = ReactEventEmitter.registrationNameModules;

// For quickly matching children type, to test if can be treated as content.
var CONTENT_TYPES = {'string': true, 'number': true};

var STYLE = keyOf({style: null});

var ELEMENT_NODE_TYPE = 1;

/**
 * @param {?object} props
 */
function assertValidProps(props) {
  if (!props) {
    return;
  }
  // Note the use of `==` which checks for null or undefined.
  ("production" !== "development" ? invariant(
    props.children == null || props.dangerouslySetInnerHTML == null,
    'Can only set one of `children` or `props.dangerouslySetInnerHTML`.'
  ) : invariant(props.children == null || props.dangerouslySetInnerHTML == null));
  ("production" !== "development" ? invariant(
    props.style == null || typeof props.style === 'object',
    'The `style` prop expects a mapping from style properties to values, ' +
    'not a string.'
  ) : invariant(props.style == null || typeof props.style === 'object'));
}

function putListener(id, registrationName, listener, transaction) {
  var container = ReactMount.findReactContainerForID(id);
  if (container) {
    var doc = container.nodeType === ELEMENT_NODE_TYPE ?
      container.ownerDocument :
      container;
    listenTo(registrationName, doc);
  }
  transaction.getPutListenerQueue().enqueuePutListener(
    id,
    registrationName,
    listener
  );
}


/**
 * @constructor ReactDOMComponent
 * @extends ReactComponent
 * @extends ReactMultiChild
 */
function ReactDOMComponent(tag, omitClose) {
  this._tagOpen = '<' + tag;
  this._tagClose = omitClose ? '' : '</' + tag + '>';
  this.tagName = tag.toUpperCase();
}

ReactDOMComponent.Mixin = {

  /**
   * Generates root tag markup then recurses. This method has side effects and
   * is not idempotent.
   *
   * @internal
   * @param {string} rootID The root DOM ID for this node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {number} mountDepth number of components in the owner hierarchy
   * @return {string} The computed markup.
   */
  mountComponent: ReactPerf.measure(
    'ReactDOMComponent',
    'mountComponent',
    function(rootID, transaction, mountDepth) {
      ReactComponent.Mixin.mountComponent.call(
        this,
        rootID,
        transaction,
        mountDepth
      );
      assertValidProps(this.props);
      return (
        this._createOpenTagMarkupAndPutListeners(transaction) +
        this._createContentMarkup(transaction) +
        this._tagClose
      );
    }
  ),

  /**
   * Creates markup for the open tag and all attributes.
   *
   * This method has side effects because events get registered.
   *
   * Iterating over object properties is faster than iterating over arrays.
   * @see http://jsperf.com/obj-vs-arr-iteration
   *
   * @private
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @return {string} Markup of opening tag.
   */
  _createOpenTagMarkupAndPutListeners: function(transaction) {
    var props = this.props;
    var ret = this._tagOpen;

    for (var propKey in props) {
      if (!props.hasOwnProperty(propKey)) {
        continue;
      }
      var propValue = props[propKey];
      if (propValue == null) {
        continue;
      }
      if (registrationNameModules[propKey]) {
        putListener(this._rootNodeID, propKey, propValue, transaction);
      } else {
        if (propKey === STYLE) {
          if (propValue) {
            propValue = props.style = merge(props.style);
          }
          propValue = CSSPropertyOperations.createMarkupForStyles(propValue);
        }
        var markup =
          DOMPropertyOperations.createMarkupForProperty(propKey, propValue);
        if (markup) {
          ret += ' ' + markup;
        }
      }
    }

    // For static pages, no need to put React ID and checksum. Saves lots of
    // bytes.
    if (transaction.renderToStaticMarkup) {
      return ret + '>';
    }

    var markupForID = DOMPropertyOperations.createMarkupForID(this._rootNodeID);
    return ret + ' ' + markupForID + '>';
  },

  /**
   * Creates markup for the content between the tags.
   *
   * @private
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @return {string} Content markup.
   */
  _createContentMarkup: function(transaction) {
    // Intentional use of != to avoid catching zero/false.
    var innerHTML = this.props.dangerouslySetInnerHTML;
    if (innerHTML != null) {
      if (innerHTML.__html != null) {
        return innerHTML.__html;
      }
    } else {
      var contentToUse =
        CONTENT_TYPES[typeof this.props.children] ? this.props.children : null;
      var childrenToUse = contentToUse != null ? null : this.props.children;
      if (contentToUse != null) {
        return escapeTextForBrowser(contentToUse);
      } else if (childrenToUse != null) {
        var mountImages = this.mountChildren(
          childrenToUse,
          transaction
        );
        return mountImages.join('');
      }
    }
    return '';
  },

  receiveComponent: function(nextComponent, transaction) {
    if (nextComponent === this) {
      // Since props and context are immutable after the component is
      // mounted, we can do a cheap identity compare here to determine
      // if this is a superfluous reconcile.

      // TODO: compare the descriptor
      return;
    }

    assertValidProps(nextComponent.props);
    ReactComponent.Mixin.receiveComponent.call(
      this,
      nextComponent,
      transaction
    );
  },

  /**
   * Updates a native DOM component after it has already been allocated and
   * attached to the DOM. Reconciles the root DOM node, then recurses.
   *
   * @param {ReactReconcileTransaction} transaction
   * @param {object} prevProps
   * @internal
   * @overridable
   */
  updateComponent: ReactPerf.measure(
    'ReactDOMComponent',
    'updateComponent',
    function(transaction, prevProps, prevOwner) {
      ReactComponent.Mixin.updateComponent.call(
        this,
        transaction,
        prevProps,
        prevOwner
      );
      this._updateDOMProperties(prevProps, transaction);
      this._updateDOMChildren(prevProps, transaction);
    }
  ),

  /**
   * Reconciles the properties by detecting differences in property values and
   * updating the DOM as necessary. This function is probably the single most
   * critical path for performance optimization.
   *
   * TODO: Benchmark whether checking for changed values in memory actually
   *       improves performance (especially statically positioned elements).
   * TODO: Benchmark the effects of putting this at the top since 99% of props
   *       do not change for a given reconciliation.
   * TODO: Benchmark areas that can be improved with caching.
   *
   * @private
   * @param {object} lastProps
   * @param {ReactReconcileTransaction} transaction
   */
  _updateDOMProperties: function(lastProps, transaction) {
    var nextProps = this.props;
    var propKey;
    var styleName;
    var styleUpdates;
    for (propKey in lastProps) {
      if (nextProps.hasOwnProperty(propKey) ||
         !lastProps.hasOwnProperty(propKey)) {
        continue;
      }
      if (propKey === STYLE) {
        var lastStyle = lastProps[propKey];
        for (styleName in lastStyle) {
          if (lastStyle.hasOwnProperty(styleName)) {
            styleUpdates = styleUpdates || {};
            styleUpdates[styleName] = '';
          }
        }
      } else if (registrationNameModules[propKey]) {
        deleteListener(this._rootNodeID, propKey);
      } else if (
          DOMProperty.isStandardName[propKey] ||
          DOMProperty.isCustomAttribute(propKey)) {
        ReactComponent.BackendIDOperations.deletePropertyByID(
          this._rootNodeID,
          propKey
        );
      }
    }
    for (propKey in nextProps) {
      var nextProp = nextProps[propKey];
      var lastProp = lastProps[propKey];
      if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp) {
        continue;
      }
      if (propKey === STYLE) {
        if (nextProp) {
          nextProp = nextProps.style = merge(nextProp);
        }
        if (lastProp) {
          // Unset styles on `lastProp` but not on `nextProp`.
          for (styleName in lastProp) {
            if (lastProp.hasOwnProperty(styleName) &&
                !nextProp.hasOwnProperty(styleName)) {
              styleUpdates = styleUpdates || {};
              styleUpdates[styleName] = '';
            }
          }
          // Update styles that changed since `lastProp`.
          for (styleName in nextProp) {
            if (nextProp.hasOwnProperty(styleName) &&
                lastProp[styleName] !== nextProp[styleName]) {
              styleUpdates = styleUpdates || {};
              styleUpdates[styleName] = nextProp[styleName];
            }
          }
        } else {
          // Relies on `updateStylesByID` not mutating `styleUpdates`.
          styleUpdates = nextProp;
        }
      } else if (registrationNameModules[propKey]) {
        putListener(this._rootNodeID, propKey, nextProp, transaction);
      } else if (
          DOMProperty.isStandardName[propKey] ||
          DOMProperty.isCustomAttribute(propKey)) {
        ReactComponent.BackendIDOperations.updatePropertyByID(
          this._rootNodeID,
          propKey,
          nextProp
        );
      }
    }
    if (styleUpdates) {
      ReactComponent.BackendIDOperations.updateStylesByID(
        this._rootNodeID,
        styleUpdates
      );
    }
  },

  /**
   * Reconciles the children with the various properties that affect the
   * children content.
   *
   * @param {object} lastProps
   * @param {ReactReconcileTransaction} transaction
   */
  _updateDOMChildren: function(lastProps, transaction) {
    var nextProps = this.props;

    var lastContent =
      CONTENT_TYPES[typeof lastProps.children] ? lastProps.children : null;
    var nextContent =
      CONTENT_TYPES[typeof nextProps.children] ? nextProps.children : null;

    var lastHtml =
      lastProps.dangerouslySetInnerHTML &&
      lastProps.dangerouslySetInnerHTML.__html;
    var nextHtml =
      nextProps.dangerouslySetInnerHTML &&
      nextProps.dangerouslySetInnerHTML.__html;

    // Note the use of `!=` which checks for null or undefined.
    var lastChildren = lastContent != null ? null : lastProps.children;
    var nextChildren = nextContent != null ? null : nextProps.children;

    // If we're switching from children to content/html or vice versa, remove
    // the old content
    var lastHasContentOrHtml = lastContent != null || lastHtml != null;
    var nextHasContentOrHtml = nextContent != null || nextHtml != null;
    if (lastChildren != null && nextChildren == null) {
      this.updateChildren(null, transaction);
    } else if (lastHasContentOrHtml && !nextHasContentOrHtml) {
      this.updateTextContent('');
    }

    if (nextContent != null) {
      if (lastContent !== nextContent) {
        this.updateTextContent('' + nextContent);
      }
    } else if (nextHtml != null) {
      if (lastHtml !== nextHtml) {
        ReactComponent.BackendIDOperations.updateInnerHTMLByID(
          this._rootNodeID,
          nextHtml
        );
      }
    } else if (nextChildren != null) {
      this.updateChildren(nextChildren, transaction);
    }
  },

  /**
   * Destroys all event registrations for this instance. Does not remove from
   * the DOM. That must be done by the parent.
   *
   * @internal
   */
  unmountComponent: function() {
    this.unmountChildren();
    ReactEventEmitter.deleteAllListeners(this._rootNodeID);
    ReactComponent.Mixin.unmountComponent.call(this);
  }

};

mixInto(ReactDOMComponent, ReactComponent.Mixin);
mixInto(ReactDOMComponent, ReactDOMComponent.Mixin);
mixInto(ReactDOMComponent, ReactMultiChild.Mixin);
mixInto(ReactDOMComponent, ReactBrowserComponentMixin);

module.exports = ReactDOMComponent;

},{"./CSSPropertyOperations":3,"./DOMProperty":8,"./DOMPropertyOperations":9,"./ReactBrowserComponentMixin":25,"./ReactComponent":27,"./ReactEventEmitter":48,"./ReactMount":55,"./ReactMultiChild":57,"./ReactPerf":60,"./escapeTextForBrowser":98,"./invariant":112,"./keyOf":119,"./merge":121,"./mixInto":124}],35:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMForm
 */

"use strict";

var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var EventConstants = _dereq_("./EventConstants");

// Store a reference to the <form> `ReactDOMComponent`.
var form = ReactDOM.form;

/**
 * Since onSubmit doesn't bubble OR capture on the top level in IE8, we need
 * to capture it on the <form> element itself. There are lots of hacks we could
 * do to accomplish this, but the most reliable is to make <form> a
 * composite component and use `componentDidMount` to attach the event handlers.
 */
var ReactDOMForm = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMForm',

  mixins: [ReactBrowserComponentMixin],

  render: function() {
    // TODO: Instead of using `ReactDOM` directly, we should use JSX. However,
    // `jshint` fails to parse JSX so in order for linting to work in the open
    // source repo, we need to just use `ReactDOM.form`.
    return this.transferPropsTo(form(null, this.props.children));
  },

  componentDidMount: function() {
    ReactEventEmitter.trapBubbledEvent(
      EventConstants.topLevelTypes.topReset,
      'reset',
      this.getDOMNode()
    );
    ReactEventEmitter.trapBubbledEvent(
      EventConstants.topLevelTypes.topSubmit,
      'submit',
      this.getDOMNode()
    );
  }
});

module.exports = ReactDOMForm;

},{"./EventConstants":14,"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./ReactEventEmitter":48}],36:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMIDOperations
 * @typechecks static-only
 */

/*jslint evil: true */

"use strict";

var CSSPropertyOperations = _dereq_("./CSSPropertyOperations");
var DOMChildrenOperations = _dereq_("./DOMChildrenOperations");
var DOMPropertyOperations = _dereq_("./DOMPropertyOperations");
var ReactMount = _dereq_("./ReactMount");
var ReactPerf = _dereq_("./ReactPerf");

var invariant = _dereq_("./invariant");

/**
 * Errors for properties that should not be updated with `updatePropertyById()`.
 *
 * @type {object}
 * @private
 */
var INVALID_PROPERTY_ERRORS = {
  dangerouslySetInnerHTML:
    '`dangerouslySetInnerHTML` must be set using `updateInnerHTMLByID()`.',
  style: '`style` must be set using `updateStylesByID()`.'
};

var useWhitespaceWorkaround;

/**
 * Operations used to process updates to DOM nodes. This is made injectable via
 * `ReactComponent.BackendIDOperations`.
 */
var ReactDOMIDOperations = {

  /**
   * Updates a DOM node with new property values. This should only be used to
   * update DOM properties in `DOMProperty`.
   *
   * @param {string} id ID of the node to update.
   * @param {string} name A valid property name, see `DOMProperty`.
   * @param {*} value New value of the property.
   * @internal
   */
  updatePropertyByID: ReactPerf.measure(
    'ReactDOMIDOperations',
    'updatePropertyByID',
    function(id, name, value) {
      var node = ReactMount.getNode(id);
      ("production" !== "development" ? invariant(
        !INVALID_PROPERTY_ERRORS.hasOwnProperty(name),
        'updatePropertyByID(...): %s',
        INVALID_PROPERTY_ERRORS[name]
      ) : invariant(!INVALID_PROPERTY_ERRORS.hasOwnProperty(name)));

      // If we're updating to null or undefined, we should remove the property
      // from the DOM node instead of inadvertantly setting to a string. This
      // brings us in line with the same behavior we have on initial render.
      if (value != null) {
        DOMPropertyOperations.setValueForProperty(node, name, value);
      } else {
        DOMPropertyOperations.deleteValueForProperty(node, name);
      }
    }
  ),

  /**
   * Updates a DOM node to remove a property. This should only be used to remove
   * DOM properties in `DOMProperty`.
   *
   * @param {string} id ID of the node to update.
   * @param {string} name A property name to remove, see `DOMProperty`.
   * @internal
   */
  deletePropertyByID: ReactPerf.measure(
    'ReactDOMIDOperations',
    'deletePropertyByID',
    function(id, name, value) {
      var node = ReactMount.getNode(id);
      ("production" !== "development" ? invariant(
        !INVALID_PROPERTY_ERRORS.hasOwnProperty(name),
        'updatePropertyByID(...): %s',
        INVALID_PROPERTY_ERRORS[name]
      ) : invariant(!INVALID_PROPERTY_ERRORS.hasOwnProperty(name)));
      DOMPropertyOperations.deleteValueForProperty(node, name, value);
    }
  ),

  /**
   * Updates a DOM node with new style values. If a value is specified as '',
   * the corresponding style property will be unset.
   *
   * @param {string} id ID of the node to update.
   * @param {object} styles Mapping from styles to values.
   * @internal
   */
  updateStylesByID: ReactPerf.measure(
    'ReactDOMIDOperations',
    'updateStylesByID',
    function(id, styles) {
      var node = ReactMount.getNode(id);
      CSSPropertyOperations.setValueForStyles(node, styles);
    }
  ),

  /**
   * Updates a DOM node's innerHTML.
   *
   * @param {string} id ID of the node to update.
   * @param {string} html An HTML string.
   * @internal
   */
  updateInnerHTMLByID: ReactPerf.measure(
    'ReactDOMIDOperations',
    'updateInnerHTMLByID',
    function(id, html) {
      var node = ReactMount.getNode(id);

      // IE8: When updating a just created node with innerHTML only leading
      // whitespace is removed. When updating an existing node with innerHTML
      // whitespace in root TextNodes is also collapsed.
      // @see quirksmode.org/bugreports/archives/2004/11/innerhtml_and_t.html

      if (useWhitespaceWorkaround === undefined) {
        // Feature detection; only IE8 is known to behave improperly like this.
        var temp = document.createElement('div');
        temp.innerHTML = ' ';
        useWhitespaceWorkaround = temp.innerHTML === '';
      }

      if (useWhitespaceWorkaround) {
        // Magic theory: IE8 supposedly differentiates between added and updated
        // nodes when processing innerHTML, innerHTML on updated nodes suffers
        // from worse whitespace behavior. Re-adding a node like this triggers
        // the initial and more favorable whitespace behavior.
        node.parentNode.replaceChild(node, node);
      }

      if (useWhitespaceWorkaround && html.match(/^[ \r\n\t\f]/)) {
        // Recover leading whitespace by temporarily prepending any character.
        // \uFEFF has the potential advantage of being zero-width/invisible.
        node.innerHTML = '\uFEFF' + html;
        node.firstChild.deleteData(0, 1);
      } else {
        node.innerHTML = html;
      }
    }
  ),

  /**
   * Updates a DOM node's text content set by `props.content`.
   *
   * @param {string} id ID of the node to update.
   * @param {string} content Text content.
   * @internal
   */
  updateTextContentByID: ReactPerf.measure(
    'ReactDOMIDOperations',
    'updateTextContentByID',
    function(id, content) {
      var node = ReactMount.getNode(id);
      DOMChildrenOperations.updateTextContent(node, content);
    }
  ),

  /**
   * Replaces a DOM node that exists in the document with markup.
   *
   * @param {string} id ID of child to be replaced.
   * @param {string} markup Dangerous markup to inject in place of child.
   * @internal
   * @see {Danger.dangerouslyReplaceNodeWithMarkup}
   */
  dangerouslyReplaceNodeWithMarkupByID: ReactPerf.measure(
    'ReactDOMIDOperations',
    'dangerouslyReplaceNodeWithMarkupByID',
    function(id, markup) {
      var node = ReactMount.getNode(id);
      DOMChildrenOperations.dangerouslyReplaceNodeWithMarkup(node, markup);
    }
  ),

  /**
   * Updates a component's children by processing a series of updates.
   *
   * @param {array<object>} updates List of update configurations.
   * @param {array<string>} markup List of markup strings.
   * @internal
   */
  dangerouslyProcessChildrenUpdates: ReactPerf.measure(
    'ReactDOMIDOperations',
    'dangerouslyProcessChildrenUpdates',
    function(updates, markup) {
      for (var i = 0; i < updates.length; i++) {
        updates[i].parentNode = ReactMount.getNode(updates[i].parentID);
      }
      DOMChildrenOperations.processUpdates(updates, markup);
    }
  )
};

module.exports = ReactDOMIDOperations;

},{"./CSSPropertyOperations":3,"./DOMChildrenOperations":7,"./DOMPropertyOperations":9,"./ReactMount":55,"./ReactPerf":60,"./invariant":112}],37:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMImg
 */

"use strict";

var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var EventConstants = _dereq_("./EventConstants");

// Store a reference to the <img> `ReactDOMComponent`.
var img = ReactDOM.img;

/**
 * Since onLoad doesn't bubble OR capture on the top level in IE8, we need to
 * capture it on the <img> element itself. There are lots of hacks we could do
 * to accomplish this, but the most reliable is to make <img> a composite
 * component and use `componentDidMount` to attach the event handlers.
 */
var ReactDOMImg = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMImg',
  tagName: 'IMG',

  mixins: [ReactBrowserComponentMixin],

  render: function() {
    return img(this.props);
  },

  componentDidMount: function() {
    var node = this.getDOMNode();
    ReactEventEmitter.trapBubbledEvent(
      EventConstants.topLevelTypes.topLoad,
      'load',
      node
    );
    ReactEventEmitter.trapBubbledEvent(
      EventConstants.topLevelTypes.topError,
      'error',
      node
    );
  }
});

module.exports = ReactDOMImg;

},{"./EventConstants":14,"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./ReactEventEmitter":48}],38:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMInput
 */

"use strict";

var AutoFocusMixin = _dereq_("./AutoFocusMixin");
var DOMPropertyOperations = _dereq_("./DOMPropertyOperations");
var LinkedValueUtils = _dereq_("./LinkedValueUtils");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");
var ReactMount = _dereq_("./ReactMount");

var invariant = _dereq_("./invariant");
var merge = _dereq_("./merge");

// Store a reference to the <input> `ReactDOMComponent`.
var input = ReactDOM.input;

var instancesByReactID = {};

/**
 * Implements an <input> native component that allows setting these optional
 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
 *
 * If `checked` or `value` are not supplied (or null/undefined), user actions
 * that affect the checked state or value will trigger updates to the element.
 *
 * If they are supplied (and not null/undefined), the rendered element will not
 * trigger updates to the element. Instead, the props must change in order for
 * the rendered element to be updated.
 *
 * The rendered element will be initialized as unchecked (or `defaultChecked`)
 * with an empty value (or `defaultValue`).
 *
 * @see http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
 */
var ReactDOMInput = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMInput',

  mixins: [AutoFocusMixin, LinkedValueUtils.Mixin, ReactBrowserComponentMixin],

  getInitialState: function() {
    var defaultValue = this.props.defaultValue;
    return {
      checked: this.props.defaultChecked || false,
      value: defaultValue != null ? defaultValue : null
    };
  },

  shouldComponentUpdate: function() {
    // Defer any updates to this component during the `onChange` handler.
    return !this._isChanging;
  },

  render: function() {
    // Clone `this.props` so we don't mutate the input.
    var props = merge(this.props);

    props.defaultChecked = null;
    props.defaultValue = null;

    var value = LinkedValueUtils.getValue(this);
    props.value = value != null ? value : this.state.value;

    var checked = LinkedValueUtils.getChecked(this);
    props.checked = checked != null ? checked : this.state.checked;

    props.onChange = this._handleChange;

    return input(props, this.props.children);
  },

  componentDidMount: function() {
    var id = ReactMount.getID(this.getDOMNode());
    instancesByReactID[id] = this;
  },

  componentWillUnmount: function() {
    var rootNode = this.getDOMNode();
    var id = ReactMount.getID(rootNode);
    delete instancesByReactID[id];
  },

  componentDidUpdate: function(prevProps, prevState, prevContext) {
    var rootNode = this.getDOMNode();
    if (this.props.checked != null) {
      DOMPropertyOperations.setValueForProperty(
        rootNode,
        'checked',
        this.props.checked || false
      );
    }

    var value = LinkedValueUtils.getValue(this);
    if (value != null) {
      // Cast `value` to a string to ensure the value is set correctly. While
      // browsers typically do this as necessary, jsdom doesn't.
      DOMPropertyOperations.setValueForProperty(rootNode, 'value', '' + value);
    }
  },

  _handleChange: function(event) {
    var returnValue;
    var onChange = LinkedValueUtils.getOnChange(this);
    if (onChange) {
      this._isChanging = true;
      returnValue = onChange.call(this, event);
      this._isChanging = false;
    }
    this.setState({
      checked: event.target.checked,
      value: event.target.value
    });

    var name = this.props.name;
    if (this.props.type === 'radio' && name != null) {
      var rootNode = this.getDOMNode();
      var queryRoot = rootNode;

      while (queryRoot.parentNode) {
        queryRoot = queryRoot.parentNode;
      }

      // If `rootNode.form` was non-null, then we could try `form.elements`,
      // but that sometimes behaves strangely in IE8. We could also try using
      // `form.getElementsByName`, but that will only return direct children
      // and won't include inputs that use the HTML5 `form=` attribute. Since
      // the input might not even be in a form, let's just use the global
      // `querySelectorAll` to ensure we don't miss anything.
      var group = queryRoot.querySelectorAll(
        'input[name=' + JSON.stringify('' + name) + '][type="radio"]');

      for (var i = 0, groupLen = group.length; i < groupLen; i++) {
        var otherNode = group[i];
        if (otherNode === rootNode ||
            otherNode.form !== rootNode.form) {
          continue;
        }
        var otherID = ReactMount.getID(otherNode);
        ("production" !== "development" ? invariant(
          otherID,
          'ReactDOMInput: Mixing React and non-React radio inputs with the ' +
          'same `name` is not supported.'
        ) : invariant(otherID));
        var otherInstance = instancesByReactID[otherID];
        ("production" !== "development" ? invariant(
          otherInstance,
          'ReactDOMInput: Unknown radio button ID %s.',
          otherID
        ) : invariant(otherInstance));
        // In some cases, this will actually change the `checked` state value.
        // In other cases, there's no change but this forces a reconcile upon
        // which componentDidUpdate will reset the DOM property to whatever it
        // should be.
        otherInstance.setState({
          checked: false
        });
      }
    }

    return returnValue;
  }

});

module.exports = ReactDOMInput;

},{"./AutoFocusMixin":1,"./DOMPropertyOperations":9,"./LinkedValueUtils":21,"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./ReactMount":55,"./invariant":112,"./merge":121}],39:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMOption
 */

"use strict";

var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");

var warning = _dereq_("./warning");

// Store a reference to the <option> `ReactDOMComponent`.
var option = ReactDOM.option;

/**
 * Implements an <option> native component that warns when `selected` is set.
 */
var ReactDOMOption = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMOption',

  mixins: [ReactBrowserComponentMixin],

  componentWillMount: function() {
    // TODO (yungsters): Remove support for `selected` in <option>.
    if ("production" !== "development") {
      ("production" !== "development" ? warning(
        this.props.selected == null,
        'Use the `defaultValue` or `value` props on <select> instead of ' +
        'setting `selected` on <option>.'
      ) : null);
    }
  },

  render: function() {
    return option(this.props, this.props.children);
  }

});

module.exports = ReactDOMOption;

},{"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./warning":134}],40:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMSelect
 */

"use strict";

var AutoFocusMixin = _dereq_("./AutoFocusMixin");
var LinkedValueUtils = _dereq_("./LinkedValueUtils");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");

var invariant = _dereq_("./invariant");
var merge = _dereq_("./merge");

// Store a reference to the <select> `ReactDOMComponent`.
var select = ReactDOM.select;

/**
 * Validation function for `value` and `defaultValue`.
 * @private
 */
function selectValueType(props, propName, componentName) {
  if (props[propName] == null) {
    return;
  }
  if (props.multiple) {
    ("production" !== "development" ? invariant(
      Array.isArray(props[propName]),
      'The `%s` prop supplied to <select> must be an array if `multiple` is ' +
      'true.',
      propName
    ) : invariant(Array.isArray(props[propName])));
  } else {
    ("production" !== "development" ? invariant(
      !Array.isArray(props[propName]),
      'The `%s` prop supplied to <select> must be a scalar value if ' +
      '`multiple` is false.',
      propName
    ) : invariant(!Array.isArray(props[propName])));
  }
}

/**
 * If `value` is supplied, updates <option> elements on mount and update.
 * @param {ReactComponent} component Instance of ReactDOMSelect
 * @param {?*} propValue For uncontrolled components, null/undefined. For
 * controlled components, a string (or with `multiple`, a list of strings).
 * @private
 */
function updateOptions(component, propValue) {
  var multiple = component.props.multiple;
  var value = propValue != null ? propValue : component.state.value;
  var options = component.getDOMNode().options;
  var selectedValue, i, l;
  if (multiple) {
    selectedValue = {};
    for (i = 0, l = value.length; i < l; ++i) {
      selectedValue['' + value[i]] = true;
    }
  } else {
    selectedValue = '' + value;
  }
  for (i = 0, l = options.length; i < l; i++) {
    var selected = multiple ?
      selectedValue.hasOwnProperty(options[i].value) :
      options[i].value === selectedValue;

    if (selected !== options[i].selected) {
      options[i].selected = selected;
    }
  }
}

/**
 * Implements a <select> native component that allows optionally setting the
 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
 * string. If `multiple` is true, the prop must be an array of strings.
 *
 * If `value` is not supplied (or null/undefined), user actions that change the
 * selected option will trigger updates to the rendered options.
 *
 * If it is supplied (and not null/undefined), the rendered options will not
 * update in response to user actions. Instead, the `value` prop must change in
 * order for the rendered options to update.
 *
 * If `defaultValue` is provided, any options with the supplied values will be
 * selected.
 */
var ReactDOMSelect = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMSelect',

  mixins: [AutoFocusMixin, LinkedValueUtils.Mixin, ReactBrowserComponentMixin],

  propTypes: {
    defaultValue: selectValueType,
    value: selectValueType
  },

  getInitialState: function() {
    return {value: this.props.defaultValue || (this.props.multiple ? [] : '')};
  },

  componentWillReceiveProps: function(nextProps) {
    if (!this.props.multiple && nextProps.multiple) {
      this.setState({value: [this.state.value]});
    } else if (this.props.multiple && !nextProps.multiple) {
      this.setState({value: this.state.value[0]});
    }
  },

  shouldComponentUpdate: function() {
    // Defer any updates to this component during the `onChange` handler.
    return !this._isChanging;
  },

  render: function() {
    // Clone `this.props` so we don't mutate the input.
    var props = merge(this.props);

    props.onChange = this._handleChange;
    props.value = null;

    return select(props, this.props.children);
  },

  componentDidMount: function() {
    updateOptions(this, LinkedValueUtils.getValue(this));
  },

  componentDidUpdate: function() {
    var value = LinkedValueUtils.getValue(this);
    if (value != null) {
      updateOptions(this, value);
    }
  },

  _handleChange: function(event) {
    var returnValue;
    var onChange = LinkedValueUtils.getOnChange(this);
    if (onChange) {
      this._isChanging = true;
      returnValue = onChange.call(this, event);
      this._isChanging = false;
    }

    var selectedValue;
    if (this.props.multiple) {
      selectedValue = [];
      var options = event.target.options;
      for (var i = 0, l = options.length; i < l; i++) {
        if (options[i].selected) {
          selectedValue.push(options[i].value);
        }
      }
    } else {
      selectedValue = event.target.value;
    }

    this.setState({value: selectedValue});
    return returnValue;
  }

});

module.exports = ReactDOMSelect;

},{"./AutoFocusMixin":1,"./LinkedValueUtils":21,"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./invariant":112,"./merge":121}],41:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMSelection
 */

"use strict";

var getNodeForCharacterOffset = _dereq_("./getNodeForCharacterOffset");
var getTextContentAccessor = _dereq_("./getTextContentAccessor");

/**
 * Get the appropriate anchor and focus node/offset pairs for IE.
 *
 * The catch here is that IE's selection API doesn't provide information
 * about whether the selection is forward or backward, so we have to
 * behave as though it's always forward.
 *
 * IE text differs from modern selection in that it behaves as though
 * block elements end with a new line. This means character offsets will
 * differ between the two APIs.
 *
 * @param {DOMElement} node
 * @return {object}
 */
function getIEOffsets(node) {
  var selection = document.selection;
  var selectedRange = selection.createRange();
  var selectedLength = selectedRange.text.length;

  // Duplicate selection so we can move range without breaking user selection.
  var fromStart = selectedRange.duplicate();
  fromStart.moveToElementText(node);
  fromStart.setEndPoint('EndToStart', selectedRange);

  var startOffset = fromStart.text.length;
  var endOffset = startOffset + selectedLength;

  return {
    start: startOffset,
    end: endOffset
  };
}

/**
 * @param {DOMElement} node
 * @return {?object}
 */
function getModernOffsets(node) {
  var selection = window.getSelection();

  if (selection.rangeCount === 0) {
    return null;
  }

  var anchorNode = selection.anchorNode;
  var anchorOffset = selection.anchorOffset;
  var focusNode = selection.focusNode;
  var focusOffset = selection.focusOffset;

  var currentRange = selection.getRangeAt(0);
  var rangeLength = currentRange.toString().length;

  var tempRange = currentRange.cloneRange();
  tempRange.selectNodeContents(node);
  tempRange.setEnd(currentRange.startContainer, currentRange.startOffset);

  var start = tempRange.toString().length;
  var end = start + rangeLength;

  // Detect whether the selection is backward.
  var detectionRange = document.createRange();
  detectionRange.setStart(anchorNode, anchorOffset);
  detectionRange.setEnd(focusNode, focusOffset);
  var isBackward = detectionRange.collapsed;
  detectionRange.detach();

  return {
    start: isBackward ? end : start,
    end: isBackward ? start : end
  };
}

/**
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setIEOffsets(node, offsets) {
  var range = document.selection.createRange().duplicate();
  var start, end;

  if (typeof offsets.end === 'undefined') {
    start = offsets.start;
    end = start;
  } else if (offsets.start > offsets.end) {
    start = offsets.end;
    end = offsets.start;
  } else {
    start = offsets.start;
    end = offsets.end;
  }

  range.moveToElementText(node);
  range.moveStart('character', start);
  range.setEndPoint('EndToStart', range);
  range.moveEnd('character', end - start);
  range.select();
}

/**
 * In modern non-IE browsers, we can support both forward and backward
 * selections.
 *
 * Note: IE10+ supports the Selection object, but it does not support
 * the `extend` method, which means that even in modern IE, it's not possible
 * to programatically create a backward selection. Thus, for all IE
 * versions, we use the old IE API to create our selections.
 *
 * @param {DOMElement|DOMTextNode} node
 * @param {object} offsets
 */
function setModernOffsets(node, offsets) {
  var selection = window.getSelection();

  var length = node[getTextContentAccessor()].length;
  var start = Math.min(offsets.start, length);
  var end = typeof offsets.end === 'undefined' ?
            start : Math.min(offsets.end, length);

  // IE 11 uses modern selection, but doesn't support the extend method.
  // Flip backward selections, so we can set with a single range.
  if (!selection.extend && start > end) {
    var temp = end;
    end = start;
    start = temp;
  }

  var startMarker = getNodeForCharacterOffset(node, start);
  var endMarker = getNodeForCharacterOffset(node, end);

  if (startMarker && endMarker) {
    var range = document.createRange();
    range.setStart(startMarker.node, startMarker.offset);
    selection.removeAllRanges();

    if (start > end) {
      selection.addRange(range);
      selection.extend(endMarker.node, endMarker.offset);
    } else {
      range.setEnd(endMarker.node, endMarker.offset);
      selection.addRange(range);
    }

    range.detach();
  }
}

var ReactDOMSelection = {
  /**
   * @param {DOMElement} node
   */
  getOffsets: function(node) {
    var getOffsets = document.selection ? getIEOffsets : getModernOffsets;
    return getOffsets(node);
  },

  /**
   * @param {DOMElement|DOMTextNode} node
   * @param {object} offsets
   */
  setOffsets: function(node, offsets) {
    var setOffsets = document.selection ? setIEOffsets : setModernOffsets;
    setOffsets(node, offsets);
  }
};

module.exports = ReactDOMSelection;

},{"./getNodeForCharacterOffset":106,"./getTextContentAccessor":108}],42:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDOMTextarea
 */

"use strict";

var AutoFocusMixin = _dereq_("./AutoFocusMixin");
var DOMPropertyOperations = _dereq_("./DOMPropertyOperations");
var LinkedValueUtils = _dereq_("./LinkedValueUtils");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");

var invariant = _dereq_("./invariant");
var merge = _dereq_("./merge");

var warning = _dereq_("./warning");

// Store a reference to the <textarea> `ReactDOMComponent`.
var textarea = ReactDOM.textarea;

/**
 * Implements a <textarea> native component that allows setting `value`, and
 * `defaultValue`. This differs from the traditional DOM API because value is
 * usually set as PCDATA children.
 *
 * If `value` is not supplied (or null/undefined), user actions that affect the
 * value will trigger updates to the element.
 *
 * If `value` is supplied (and not null/undefined), the rendered element will
 * not trigger updates to the element. Instead, the `value` prop must change in
 * order for the rendered element to be updated.
 *
 * The rendered element will be initialized with an empty value, the prop
 * `defaultValue` if specified, or the children content (deprecated).
 */
var ReactDOMTextarea = ReactCompositeComponent.createClass({
  displayName: 'ReactDOMTextarea',

  mixins: [AutoFocusMixin, LinkedValueUtils.Mixin, ReactBrowserComponentMixin],

  getInitialState: function() {
    var defaultValue = this.props.defaultValue;
    // TODO (yungsters): Remove support for children content in <textarea>.
    var children = this.props.children;
    if (children != null) {
      if ("production" !== "development") {
        ("production" !== "development" ? warning(
          false,
          'Use the `defaultValue` or `value` props instead of setting ' +
          'children on <textarea>.'
        ) : null);
      }
      ("production" !== "development" ? invariant(
        defaultValue == null,
        'If you supply `defaultValue` on a <textarea>, do not pass children.'
      ) : invariant(defaultValue == null));
      if (Array.isArray(children)) {
        ("production" !== "development" ? invariant(
          children.length <= 1,
          '<textarea> can only have at most one child.'
        ) : invariant(children.length <= 1));
        children = children[0];
      }

      defaultValue = '' + children;
    }
    if (defaultValue == null) {
      defaultValue = '';
    }
    var value = LinkedValueUtils.getValue(this);
    return {
      // We save the initial value so that `ReactDOMComponent` doesn't update
      // `textContent` (unnecessary since we update value).
      // The initial value can be a boolean or object so that's why it's
      // forced to be a string.
      initialValue: '' + (value != null ? value : defaultValue),
      value: defaultValue
    };
  },

  shouldComponentUpdate: function() {
    // Defer any updates to this component during the `onChange` handler.
    return !this._isChanging;
  },

  render: function() {
    // Clone `this.props` so we don't mutate the input.
    var props = merge(this.props);
    var value = LinkedValueUtils.getValue(this);

    ("production" !== "development" ? invariant(
      props.dangerouslySetInnerHTML == null,
      '`dangerouslySetInnerHTML` does not make sense on <textarea>.'
    ) : invariant(props.dangerouslySetInnerHTML == null));

    props.defaultValue = null;
    props.value = value != null ? value : this.state.value;
    props.onChange = this._handleChange;

    // Always set children to the same thing. In IE9, the selection range will
    // get reset if `textContent` is mutated.
    return textarea(props, this.state.initialValue);
  },

  componentDidUpdate: function(prevProps, prevState, prevContext) {
    var value = LinkedValueUtils.getValue(this);
    if (value != null) {
      var rootNode = this.getDOMNode();
      // Cast `value` to a string to ensure the value is set correctly. While
      // browsers typically do this as necessary, jsdom doesn't.
      DOMPropertyOperations.setValueForProperty(rootNode, 'value', '' + value);
    }
  },

  _handleChange: function(event) {
    var returnValue;
    var onChange = LinkedValueUtils.getOnChange(this);
    if (onChange) {
      this._isChanging = true;
      returnValue = onChange.call(this, event);
      this._isChanging = false;
    }
    this.setState({value: event.target.value});
    return returnValue;
  }

});

module.exports = ReactDOMTextarea;

},{"./AutoFocusMixin":1,"./DOMPropertyOperations":9,"./LinkedValueUtils":21,"./ReactBrowserComponentMixin":25,"./ReactCompositeComponent":29,"./ReactDOM":32,"./invariant":112,"./merge":121,"./warning":134}],43:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDefaultBatchingStrategy
 */

"use strict";

var ReactUpdates = _dereq_("./ReactUpdates");
var Transaction = _dereq_("./Transaction");

var emptyFunction = _dereq_("./emptyFunction");
var mixInto = _dereq_("./mixInto");

var RESET_BATCHED_UPDATES = {
  initialize: emptyFunction,
  close: function() {
    ReactDefaultBatchingStrategy.isBatchingUpdates = false;
  }
};

var FLUSH_BATCHED_UPDATES = {
  initialize: emptyFunction,
  close: ReactUpdates.flushBatchedUpdates.bind(ReactUpdates)
};

var TRANSACTION_WRAPPERS = [FLUSH_BATCHED_UPDATES, RESET_BATCHED_UPDATES];

function ReactDefaultBatchingStrategyTransaction() {
  this.reinitializeTransaction();
}

mixInto(ReactDefaultBatchingStrategyTransaction, Transaction.Mixin);
mixInto(ReactDefaultBatchingStrategyTransaction, {
  getTransactionWrappers: function() {
    return TRANSACTION_WRAPPERS;
  }
});

var transaction = new ReactDefaultBatchingStrategyTransaction();

var ReactDefaultBatchingStrategy = {
  isBatchingUpdates: false,

  /**
   * Call the provided function in a context within which calls to `setState`
   * and friends are batched such that components aren't updated unnecessarily.
   */
  batchedUpdates: function(callback, param) {
    var alreadyBatchingUpdates = ReactDefaultBatchingStrategy.isBatchingUpdates;

    ReactDefaultBatchingStrategy.isBatchingUpdates = true;

    // The code is written this way to avoid extra allocations
    if (alreadyBatchingUpdates) {
      callback(param);
    } else {
      transaction.perform(callback, null, param);
    }
  }
};

module.exports = ReactDefaultBatchingStrategy;

},{"./ReactUpdates":71,"./Transaction":85,"./emptyFunction":96,"./mixInto":124}],44:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDefaultInjection
 */

"use strict";

var ReactInjection = _dereq_("./ReactInjection");

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var DefaultDOMPropertyConfig = _dereq_("./DefaultDOMPropertyConfig");

var ChangeEventPlugin = _dereq_("./ChangeEventPlugin");
var ClientReactRootIndex = _dereq_("./ClientReactRootIndex");
var CompositionEventPlugin = _dereq_("./CompositionEventPlugin");
var DefaultEventPluginOrder = _dereq_("./DefaultEventPluginOrder");
var EnterLeaveEventPlugin = _dereq_("./EnterLeaveEventPlugin");
var MobileSafariClickEventPlugin = _dereq_("./MobileSafariClickEventPlugin");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactComponentBrowserEnvironment =
  _dereq_("./ReactComponentBrowserEnvironment");
var ReactEventTopLevelCallback = _dereq_("./ReactEventTopLevelCallback");
var ReactDOM = _dereq_("./ReactDOM");
var ReactDOMButton = _dereq_("./ReactDOMButton");
var ReactDOMForm = _dereq_("./ReactDOMForm");
var ReactDOMImg = _dereq_("./ReactDOMImg");
var ReactDOMInput = _dereq_("./ReactDOMInput");
var ReactDOMOption = _dereq_("./ReactDOMOption");
var ReactDOMSelect = _dereq_("./ReactDOMSelect");
var ReactDOMTextarea = _dereq_("./ReactDOMTextarea");
var ReactInstanceHandles = _dereq_("./ReactInstanceHandles");
var ReactMount = _dereq_("./ReactMount");
var SelectEventPlugin = _dereq_("./SelectEventPlugin");
var ServerReactRootIndex = _dereq_("./ServerReactRootIndex");
var SimpleEventPlugin = _dereq_("./SimpleEventPlugin");

var ReactDefaultBatchingStrategy = _dereq_("./ReactDefaultBatchingStrategy");

var createFullPageComponent = _dereq_("./createFullPageComponent");

function inject() {
  ReactInjection.EventEmitter.injectTopLevelCallbackCreator(
    ReactEventTopLevelCallback
  );

  /**
   * Inject modules for resolving DOM hierarchy and plugin ordering.
   */
  ReactInjection.EventPluginHub.injectEventPluginOrder(DefaultEventPluginOrder);
  ReactInjection.EventPluginHub.injectInstanceHandle(ReactInstanceHandles);
  ReactInjection.EventPluginHub.injectMount(ReactMount);

  /**
   * Some important event plugins included by default (without having to require
   * them).
   */
  ReactInjection.EventPluginHub.injectEventPluginsByName({
    SimpleEventPlugin: SimpleEventPlugin,
    EnterLeaveEventPlugin: EnterLeaveEventPlugin,
    ChangeEventPlugin: ChangeEventPlugin,
    CompositionEventPlugin: CompositionEventPlugin,
    MobileSafariClickEventPlugin: MobileSafariClickEventPlugin,
    SelectEventPlugin: SelectEventPlugin
  });

  ReactInjection.DOM.injectComponentClasses({
    button: ReactDOMButton,
    form: ReactDOMForm,
    img: ReactDOMImg,
    input: ReactDOMInput,
    option: ReactDOMOption,
    select: ReactDOMSelect,
    textarea: ReactDOMTextarea,

    html: createFullPageComponent(ReactDOM.html),
    head: createFullPageComponent(ReactDOM.head),
    title: createFullPageComponent(ReactDOM.title),
    body: createFullPageComponent(ReactDOM.body)
  });


  // This needs to happen after createFullPageComponent() otherwise the mixin
  // gets double injected.
  ReactInjection.CompositeComponent.injectMixin(ReactBrowserComponentMixin);

  ReactInjection.DOMProperty.injectDOMPropertyConfig(DefaultDOMPropertyConfig);

  ReactInjection.Updates.injectBatchingStrategy(
    ReactDefaultBatchingStrategy
  );

  ReactInjection.RootIndex.injectCreateReactRootIndex(
    ExecutionEnvironment.canUseDOM ?
      ClientReactRootIndex.createReactRootIndex :
      ServerReactRootIndex.createReactRootIndex
  );

  ReactInjection.Component.injectEnvironment(ReactComponentBrowserEnvironment);

  if ("production" !== "development") {
    var url = (ExecutionEnvironment.canUseDOM && window.location.href) || '';
    if ((/[?&]react_perf\b/).test(url)) {
      var ReactDefaultPerf = _dereq_("./ReactDefaultPerf");
      ReactDefaultPerf.start();
    }
  }
}

module.exports = {
  inject: inject
};

},{"./ChangeEventPlugin":4,"./ClientReactRootIndex":5,"./CompositionEventPlugin":6,"./DefaultDOMPropertyConfig":11,"./DefaultEventPluginOrder":12,"./EnterLeaveEventPlugin":13,"./ExecutionEnvironment":20,"./MobileSafariClickEventPlugin":22,"./ReactBrowserComponentMixin":25,"./ReactComponentBrowserEnvironment":28,"./ReactDOM":32,"./ReactDOMButton":33,"./ReactDOMForm":35,"./ReactDOMImg":37,"./ReactDOMInput":38,"./ReactDOMOption":39,"./ReactDOMSelect":40,"./ReactDOMTextarea":42,"./ReactDefaultBatchingStrategy":43,"./ReactDefaultPerf":45,"./ReactEventTopLevelCallback":50,"./ReactInjection":51,"./ReactInstanceHandles":53,"./ReactMount":55,"./SelectEventPlugin":72,"./ServerReactRootIndex":73,"./SimpleEventPlugin":74,"./createFullPageComponent":92}],45:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDefaultPerf
 * @typechecks static-only
 */

"use strict";

var DOMProperty = _dereq_("./DOMProperty");
var ReactDefaultPerfAnalysis = _dereq_("./ReactDefaultPerfAnalysis");
var ReactMount = _dereq_("./ReactMount");
var ReactPerf = _dereq_("./ReactPerf");

var performanceNow = _dereq_("./performanceNow");

function roundFloat(val) {
  return Math.floor(val * 100) / 100;
}

var ReactDefaultPerf = {
  _allMeasurements: [], // last item in the list is the current one
  _injected: false,

  start: function() {
    if (!ReactDefaultPerf._injected) {
      ReactPerf.injection.injectMeasure(ReactDefaultPerf.measure);
    }

    ReactDefaultPerf._allMeasurements.length = 0;
    ReactPerf.enableMeasure = true;
  },

  stop: function() {
    ReactPerf.enableMeasure = false;
  },

  getLastMeasurements: function() {
    return ReactDefaultPerf._allMeasurements;
  },

  printExclusive: function(measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getExclusiveSummary(measurements);
    console.table(summary.map(function(item) {
      return {
        'Component class name': item.componentName,
        'Total inclusive time (ms)': roundFloat(item.inclusive),
        'Total exclusive time (ms)': roundFloat(item.exclusive),
        'Exclusive time per instance (ms)': roundFloat(item.exclusive / item.count),
        'Instances': item.count
      };
    }));
    console.log(
      'Total time:',
      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
    );
  },

  printInclusive: function(measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(measurements);
    console.table(summary.map(function(item) {
      return {
        'Owner > component': item.componentName,
        'Inclusive time (ms)': roundFloat(item.time),
        'Instances': item.count
      };
    }));
    console.log(
      'Total time:',
      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
    );
  },

  printWasted: function(measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(
      measurements,
      true
    );
    console.table(summary.map(function(item) {
      return {
        'Owner > component': item.componentName,
        'Wasted time (ms)': item.time,
        'Instances': item.count
      };
    }));
    console.log(
      'Total time:',
      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
    );
  },

  printDOM: function(measurements) {
    measurements = measurements || ReactDefaultPerf._allMeasurements;
    var summary = ReactDefaultPerfAnalysis.getDOMSummary(measurements);
    console.table(summary.map(function(item) {
      var result = {};
      result[DOMProperty.ID_ATTRIBUTE_NAME] = item.id;
      result['type'] = item.type;
      result['args'] = JSON.stringify(item.args);
      return result;
    }));
    console.log(
      'Total time:',
      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
    );
  },

  _recordWrite: function(id, fnName, totalTime, args) {
    // TODO: totalTime isn't that useful since it doesn't count paints/reflows
    var writes =
      ReactDefaultPerf
        ._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1]
        .writes;
    writes[id] = writes[id] || [];
    writes[id].push({
      type: fnName,
      time: totalTime,
      args: args
    });
  },

  measure: function(moduleName, fnName, func) {
    return function() {var args=Array.prototype.slice.call(arguments,0);
      var totalTime;
      var rv;
      var start;

      if (fnName === '_renderNewRootComponent' ||
          fnName === 'flushBatchedUpdates') {
        // A "measurement" is a set of metrics recorded for each flush. We want
        // to group the metrics for a given flush together so we can look at the
        // components that rendered and the DOM operations that actually
        // happened to determine the amount of "wasted work" performed.
        ReactDefaultPerf._allMeasurements.push({
          exclusive: {},
          inclusive: {},
          counts: {},
          writes: {},
          displayNames: {},
          totalTime: 0
        });
        start = performanceNow();
        rv = func.apply(this, args);
        ReactDefaultPerf._allMeasurements[
          ReactDefaultPerf._allMeasurements.length - 1
        ].totalTime = performanceNow() - start;
        return rv;
      } else if (moduleName === 'ReactDOMIDOperations' ||
        moduleName === 'ReactComponentBrowserEnvironment') {
        start = performanceNow();
        rv = func.apply(this, args);
        totalTime = performanceNow() - start;

        if (fnName === 'mountImageIntoNode') {
          var mountID = ReactMount.getID(args[1]);
          ReactDefaultPerf._recordWrite(mountID, fnName, totalTime, args[0]);
        } else if (fnName === 'dangerouslyProcessChildrenUpdates') {
          // special format
          args[0].forEach(function(update) {
            var writeArgs = {};
            if (update.fromIndex !== null) {
              writeArgs.fromIndex = update.fromIndex;
            }
            if (update.toIndex !== null) {
              writeArgs.toIndex = update.toIndex;
            }
            if (update.textContent !== null) {
              writeArgs.textContent = update.textContent;
            }
            if (update.markupIndex !== null) {
              writeArgs.markup = args[1][update.markupIndex];
            }
            ReactDefaultPerf._recordWrite(
              update.parentID,
              update.type,
              totalTime,
              writeArgs
            );
          });
        } else {
          // basic format
          ReactDefaultPerf._recordWrite(
            args[0],
            fnName,
            totalTime,
            Array.prototype.slice.call(args, 1)
          );
        }
        return rv;
      } else if (moduleName === 'ReactCompositeComponent' && (
        fnName === 'mountComponent' ||
        fnName === 'updateComponent' || // TODO: receiveComponent()?
        fnName === '_renderValidatedComponent')) {

        var rootNodeID = fnName === 'mountComponent' ?
          args[0] :
          this._rootNodeID;
        var isRender = fnName === '_renderValidatedComponent';
        var entry = ReactDefaultPerf._allMeasurements[
          ReactDefaultPerf._allMeasurements.length - 1
        ];

        if (isRender) {
          entry.counts[rootNodeID] = entry.counts[rootNodeID] || 0;
          entry.counts[rootNodeID] += 1;
        }

        start = performanceNow();
        rv = func.apply(this, args);
        totalTime = performanceNow() - start;

        var typeOfLog = isRender ? entry.exclusive : entry.inclusive;
        typeOfLog[rootNodeID] = typeOfLog[rootNodeID] || 0;
        typeOfLog[rootNodeID] += totalTime;

        entry.displayNames[rootNodeID] = {
          current: this.constructor.displayName,
          owner: this._owner ? this._owner.constructor.displayName : '<root>'
        };

        return rv;
      } else {
        return func.apply(this, args);
      }
    };
  }
};

module.exports = ReactDefaultPerf;

},{"./DOMProperty":8,"./ReactDefaultPerfAnalysis":46,"./ReactMount":55,"./ReactPerf":60,"./performanceNow":129}],46:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactDefaultPerfAnalysis
 */

var merge = _dereq_("./merge");

// Don't try to save users less than 1.2ms (a number I made up)
var DONT_CARE_THRESHOLD = 1.2;
var DOM_OPERATION_TYPES = {
  'mountImageIntoNode': 'set innerHTML',
  INSERT_MARKUP: 'set innerHTML',
  MOVE_EXISTING: 'move',
  REMOVE_NODE: 'remove',
  TEXT_CONTENT: 'set textContent',
  'updatePropertyByID': 'update attribute',
  'deletePropertyByID': 'delete attribute',
  'updateStylesByID': 'update styles',
  'updateInnerHTMLByID': 'set innerHTML',
  'dangerouslyReplaceNodeWithMarkupByID': 'replace'
};

function getTotalTime(measurements) {
  // TODO: return number of DOM ops? could be misleading.
  // TODO: measure dropped frames after reconcile?
  // TODO: log total time of each reconcile and the top-level component
  // class that triggered it.
  var totalTime = 0;
  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    totalTime += measurement.totalTime;
  }
  return totalTime;
}

function getDOMSummary(measurements) {
  var items = [];
  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    var id;

    for (id in measurement.writes) {
      measurement.writes[id].forEach(function(write) {
        items.push({
          id: id,
          type: DOM_OPERATION_TYPES[write.type] || write.type,
          args: write.args
        });
      });
    }
  }
  return items;
}

function getExclusiveSummary(measurements) {
  var candidates = {};
  var displayName;

  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    var allIDs = merge(measurement.exclusive, measurement.inclusive);

    for (var id in allIDs) {
      displayName = measurement.displayNames[id].current;

      candidates[displayName] = candidates[displayName] || {
        componentName: displayName,
        inclusive: 0,
        exclusive: 0,
        count: 0
      };
      if (measurement.exclusive[id]) {
        candidates[displayName].exclusive += measurement.exclusive[id];
      }
      if (measurement.inclusive[id]) {
        candidates[displayName].inclusive += measurement.inclusive[id];
      }
      if (measurement.counts[id]) {
        candidates[displayName].count += measurement.counts[id];
      }
    }
  }

  // Now make a sorted array with the results.
  var arr = [];
  for (displayName in candidates) {
    if (candidates[displayName].exclusive >= DONT_CARE_THRESHOLD) {
      arr.push(candidates[displayName]);
    }
  }

  arr.sort(function(a, b) {
    return b.exclusive - a.exclusive;
  });

  return arr;
}

function getInclusiveSummary(measurements, onlyClean) {
  var candidates = {};
  var inclusiveKey;

  for (var i = 0; i < measurements.length; i++) {
    var measurement = measurements[i];
    var allIDs = merge(measurement.exclusive, measurement.inclusive);
    var cleanComponents;

    if (onlyClean) {
      cleanComponents = getUnchangedComponents(measurement);
    }

    for (var id in allIDs) {
      if (onlyClean && !cleanComponents[id]) {
        continue;
      }

      var displayName = measurement.displayNames[id];

      // Inclusive time is not useful for many components without knowing where
      // they are instantiated. So we aggregate inclusive time with both the
      // owner and current displayName as the key.
      inclusiveKey = displayName.owner + ' > ' + displayName.current;

      candidates[inclusiveKey] = candidates[inclusiveKey] || {
        componentName: inclusiveKey,
        time: 0,
        count: 0
      };

      if (measurement.inclusive[id]) {
        candidates[inclusiveKey].time += measurement.inclusive[id];
      }
      if (measurement.counts[id]) {
        candidates[inclusiveKey].count += measurement.counts[id];
      }
    }
  }

  // Now make a sorted array with the results.
  var arr = [];
  for (inclusiveKey in candidates) {
    if (candidates[inclusiveKey].time >= DONT_CARE_THRESHOLD) {
      arr.push(candidates[inclusiveKey]);
    }
  }

  arr.sort(function(a, b) {
    return b.time - a.time;
  });

  return arr;
}

function getUnchangedComponents(measurement) {
  // For a given reconcile, look at which components did not actually
  // render anything to the DOM and return a mapping of their ID to
  // the amount of time it took to render the entire subtree.
  var cleanComponents = {};
  var dirtyLeafIDs = Object.keys(measurement.writes);
  var allIDs = merge(measurement.exclusive, measurement.inclusive);

  for (var id in allIDs) {
    var isDirty = false;
    // For each component that rendered, see if a component that triggerd
    // a DOM op is in its subtree.
    for (var i = 0; i < dirtyLeafIDs.length; i++) {
      if (dirtyLeafIDs[i].indexOf(id) === 0) {
        isDirty = true;
        break;
      }
    }
    if (!isDirty && measurement.counts[id] > 0) {
      cleanComponents[id] = true;
    }
  }
  return cleanComponents;
}

var ReactDefaultPerfAnalysis = {
  getExclusiveSummary: getExclusiveSummary,
  getInclusiveSummary: getInclusiveSummary,
  getDOMSummary: getDOMSummary,
  getTotalTime: getTotalTime
};

module.exports = ReactDefaultPerfAnalysis;

},{"./merge":121}],47:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactErrorUtils
 * @typechecks
 */

"use strict";

var ReactErrorUtils = {
  /**
   * Creates a guarded version of a function. This is supposed to make debugging
   * of event handlers easier. To aid debugging with the browser's debugger,
   * this currently simply returns the original function.
   *
   * @param {function} func Function to be executed
   * @param {string} name The name of the guard
   * @return {function}
   */
  guard: function(func, name) {
    return func;
  }
};

module.exports = ReactErrorUtils;

},{}],48:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactEventEmitter
 * @typechecks static-only
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventListener = _dereq_("./EventListener");
var EventPluginHub = _dereq_("./EventPluginHub");
var EventPluginRegistry = _dereq_("./EventPluginRegistry");
var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");
var ReactEventEmitterMixin = _dereq_("./ReactEventEmitterMixin");
var ViewportMetrics = _dereq_("./ViewportMetrics");

var invariant = _dereq_("./invariant");
var isEventSupported = _dereq_("./isEventSupported");
var merge = _dereq_("./merge");

/**
 * Summary of `ReactEventEmitter` event handling:
 *
 *  - Top-level delegation is used to trap native browser events. We normalize
 *    and de-duplicate events to account for browser quirks.
 *
 *  - Forward these native events (with the associated top-level type used to
 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
 *    to extract any synthetic events.
 *
 *  - The `EventPluginHub` will then process each event by annotating them with
 *    "dispatches", a sequence of listeners and IDs that care about that event.
 *
 *  - The `EventPluginHub` then dispatches the events.
 *
 * Overview of React and the event system:
 *
 *                   .
 * +------------+    .
 * |    DOM     |    .
 * +------------+    .                         +-----------+
 *       +           .               +--------+|SimpleEvent|
 *       |           .               |         |Plugin     |
 * +-----|------+    .               v         +-----------+
 * |     |      |    .    +--------------+                    +------------+
 * |     +-----------.--->|EventPluginHub|                    |    Event   |
 * |            |    .    |              |     +-----------+  | Propagators|
 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
 * |            |    .    |              |     +-----------+  |  utilities |
 * |     +-----------.--->|              |                    +------------+
 * |     |      |    .    +--------------+
 * +-----|------+    .                ^        +-----------+
 *       |           .                |        |Enter/Leave|
 *       +           .                +-------+|Plugin     |
 * +-------------+   .                         +-----------+
 * | application |   .
 * |-------------|   .
 * |             |   .
 * |             |   .
 * +-------------+   .
 *                   .
 *    React Core     .  General Purpose Event Plugin System
 */

var alreadyListeningTo = {};
var isMonitoringScrollValue = false;
var reactTopListenersCounter = 0;

// For events like 'submit' which don't consistently bubble (which we trap at a
// lower node than `document`), binding at `document` would cause duplicate
// events so we don't include them here
var topEventMapping = {
  topBlur: 'blur',
  topChange: 'change',
  topClick: 'click',
  topCompositionEnd: 'compositionend',
  topCompositionStart: 'compositionstart',
  topCompositionUpdate: 'compositionupdate',
  topContextMenu: 'contextmenu',
  topCopy: 'copy',
  topCut: 'cut',
  topDoubleClick: 'dblclick',
  topDrag: 'drag',
  topDragEnd: 'dragend',
  topDragEnter: 'dragenter',
  topDragExit: 'dragexit',
  topDragLeave: 'dragleave',
  topDragOver: 'dragover',
  topDragStart: 'dragstart',
  topDrop: 'drop',
  topFocus: 'focus',
  topInput: 'input',
  topKeyDown: 'keydown',
  topKeyPress: 'keypress',
  topKeyUp: 'keyup',
  topMouseDown: 'mousedown',
  topMouseMove: 'mousemove',
  topMouseOut: 'mouseout',
  topMouseOver: 'mouseover',
  topMouseUp: 'mouseup',
  topPaste: 'paste',
  topScroll: 'scroll',
  topSelectionChange: 'selectionchange',
  topTouchCancel: 'touchcancel',
  topTouchEnd: 'touchend',
  topTouchMove: 'touchmove',
  topTouchStart: 'touchstart',
  topWheel: 'wheel'
};

/**
 * To ensure no conflicts with other potential React instances on the page
 */
var topListenersIDKey = "_reactListenersID" + String(Math.random()).slice(2);

function getListeningForDocument(mountAt) {
  if (mountAt[topListenersIDKey] == null) {
    mountAt[topListenersIDKey] = reactTopListenersCounter++;
    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
  }
  return alreadyListeningTo[mountAt[topListenersIDKey]];
}

/**
 * Traps top-level events by using event bubbling.
 *
 * @param {string} topLevelType Record from `EventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {DOMEventTarget} element Element on which to attach listener.
 * @internal
 */
function trapBubbledEvent(topLevelType, handlerBaseName, element) {
  EventListener.listen(
    element,
    handlerBaseName,
    ReactEventEmitter.TopLevelCallbackCreator.createTopLevelCallback(
      topLevelType
    )
  );
}

/**
 * Traps a top-level event by using event capturing.
 *
 * @param {string} topLevelType Record from `EventConstants`.
 * @param {string} handlerBaseName Event name (e.g. "click").
 * @param {DOMEventTarget} element Element on which to attach listener.
 * @internal
 */
function trapCapturedEvent(topLevelType, handlerBaseName, element) {
  EventListener.capture(
    element,
    handlerBaseName,
    ReactEventEmitter.TopLevelCallbackCreator.createTopLevelCallback(
      topLevelType
    )
  );
}

/**
 * `ReactEventEmitter` is used to attach top-level event listeners. For example:
 *
 *   ReactEventEmitter.putListener('myID', 'onClick', myFunction);
 *
 * This would allocate a "registration" of `('onClick', myFunction)` on 'myID'.
 *
 * @internal
 */
var ReactEventEmitter = merge(ReactEventEmitterMixin, {

  /**
   * React references `ReactEventTopLevelCallback` using this property in order
   * to allow dependency injection.
   */
  TopLevelCallbackCreator: null,

  injection: {
    /**
     * @param {function} TopLevelCallbackCreator
     */
    injectTopLevelCallbackCreator: function(TopLevelCallbackCreator) {
      ReactEventEmitter.TopLevelCallbackCreator = TopLevelCallbackCreator;
    }
  },

  /**
   * Sets whether or not any created callbacks should be enabled.
   *
   * @param {boolean} enabled True if callbacks should be enabled.
   */
  setEnabled: function(enabled) {
    ("production" !== "development" ? invariant(
      ExecutionEnvironment.canUseDOM,
      'setEnabled(...): Cannot toggle event listening in a Worker thread. ' +
      'This is likely a bug in the framework. Please report immediately.'
    ) : invariant(ExecutionEnvironment.canUseDOM));
    if (ReactEventEmitter.TopLevelCallbackCreator) {
      ReactEventEmitter.TopLevelCallbackCreator.setEnabled(enabled);
    }
  },

  /**
   * @return {boolean} True if callbacks are enabled.
   */
  isEnabled: function() {
    return !!(
      ReactEventEmitter.TopLevelCallbackCreator &&
      ReactEventEmitter.TopLevelCallbackCreator.isEnabled()
    );
  },

  /**
   * We listen for bubbled touch events on the document object.
   *
   * Firefox v8.01 (and possibly others) exhibited strange behavior when
   * mounting `onmousemove` events at some node that was not the document
   * element. The symptoms were that if your mouse is not moving over something
   * contained within that mount point (for example on the background) the
   * top-level listeners for `onmousemove` won't be called. However, if you
   * register the `mousemove` on the document object, then it will of course
   * catch all `mousemove`s. This along with iOS quirks, justifies restricting
   * top-level listeners to the document object only, at least for these
   * movement types of events and possibly all events.
   *
   * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
   *
   * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
   * they bubble to document.
   *
   * @param {string} registrationName Name of listener (e.g. `onClick`).
   * @param {DOMDocument} contentDocument Document which owns the container
   */
  listenTo: function(registrationName, contentDocument) {
    var mountAt = contentDocument;
    var isListening = getListeningForDocument(mountAt);
    var dependencies = EventPluginRegistry.
      registrationNameDependencies[registrationName];

    var topLevelTypes = EventConstants.topLevelTypes;
    for (var i = 0, l = dependencies.length; i < l; i++) {
      var dependency = dependencies[i];
      if (!isListening[dependency]) {
        var topLevelType = topLevelTypes[dependency];

        if (topLevelType === topLevelTypes.topWheel) {
          if (isEventSupported('wheel')) {
            trapBubbledEvent(topLevelTypes.topWheel, 'wheel', mountAt);
          } else if (isEventSupported('mousewheel')) {
            trapBubbledEvent(topLevelTypes.topWheel, 'mousewheel', mountAt);
          } else {
            // Firefox needs to capture a different mouse scroll event.
            // @see http://www.quirksmode.org/dom/events/tests/scroll.html
            trapBubbledEvent(
              topLevelTypes.topWheel,
              'DOMMouseScroll',
              mountAt);
          }
        } else if (topLevelType === topLevelTypes.topScroll) {

          if (isEventSupported('scroll', true)) {
            trapCapturedEvent(topLevelTypes.topScroll, 'scroll', mountAt);
          } else {
            trapBubbledEvent(topLevelTypes.topScroll, 'scroll', window);
          }
        } else if (topLevelType === topLevelTypes.topFocus ||
            topLevelType === topLevelTypes.topBlur) {

          if (isEventSupported('focus', true)) {
            trapCapturedEvent(topLevelTypes.topFocus, 'focus', mountAt);
            trapCapturedEvent(topLevelTypes.topBlur, 'blur', mountAt);
          } else if (isEventSupported('focusin')) {
            // IE has `focusin` and `focusout` events which bubble.
            // @see http://www.quirksmode.org/blog/archives/2008/04/delegating_the.html
            trapBubbledEvent(topLevelTypes.topFocus, 'focusin', mountAt);
            trapBubbledEvent(topLevelTypes.topBlur, 'focusout', mountAt);
          }

          // to make sure blur and focus event listeners are only attached once
          isListening[topLevelTypes.topBlur] = true;
          isListening[topLevelTypes.topFocus] = true;
        } else if (topEventMapping[dependency]) {
          trapBubbledEvent(topLevelType, topEventMapping[dependency], mountAt);
        }

        isListening[dependency] = true;
      }
    }
  },

  /**
   * Listens to window scroll and resize events. We cache scroll values so that
   * application code can access them without triggering reflows.
   *
   * NOTE: Scroll events do not bubble.
   *
   * @see http://www.quirksmode.org/dom/events/scroll.html
   */
  ensureScrollValueMonitoring: function(){
    if (!isMonitoringScrollValue) {
      var refresh = ViewportMetrics.refreshScrollValues;
      EventListener.listen(window, 'scroll', refresh);
      EventListener.listen(window, 'resize', refresh);
      isMonitoringScrollValue = true;
    }
  },

  eventNameDispatchConfigs: EventPluginHub.eventNameDispatchConfigs,

  registrationNameModules: EventPluginHub.registrationNameModules,

  putListener: EventPluginHub.putListener,

  getListener: EventPluginHub.getListener,

  deleteListener: EventPluginHub.deleteListener,

  deleteAllListeners: EventPluginHub.deleteAllListeners,

  trapBubbledEvent: trapBubbledEvent,

  trapCapturedEvent: trapCapturedEvent

});

module.exports = ReactEventEmitter;

},{"./EventConstants":14,"./EventListener":15,"./EventPluginHub":16,"./EventPluginRegistry":17,"./ExecutionEnvironment":20,"./ReactEventEmitterMixin":49,"./ViewportMetrics":86,"./invariant":112,"./isEventSupported":113,"./merge":121}],49:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactEventEmitterMixin
 */

"use strict";

var EventPluginHub = _dereq_("./EventPluginHub");
var ReactUpdates = _dereq_("./ReactUpdates");

function runEventQueueInBatch(events) {
  EventPluginHub.enqueueEvents(events);
  EventPluginHub.processEventQueue();
}

var ReactEventEmitterMixin = {

  /**
   * Streams a fired top-level event to `EventPluginHub` where plugins have the
   * opportunity to create `ReactEvent`s to be dispatched.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {object} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native environment event.
   */
  handleTopLevel: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {
    var events = EventPluginHub.extractEvents(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent
    );

    // Event queue being processed in the same cycle allows `preventDefault`.
    ReactUpdates.batchedUpdates(runEventQueueInBatch, events);
  }
};

module.exports = ReactEventEmitterMixin;

},{"./EventPluginHub":16,"./ReactUpdates":71}],50:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactEventTopLevelCallback
 * @typechecks static-only
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var ReactInstanceHandles = _dereq_("./ReactInstanceHandles");
var ReactMount = _dereq_("./ReactMount");

var getEventTarget = _dereq_("./getEventTarget");
var mixInto = _dereq_("./mixInto");

/**
 * @type {boolean}
 * @private
 */
var _topLevelListenersEnabled = true;

/**
 * Finds the parent React component of `node`.
 *
 * @param {*} node
 * @return {?DOMEventTarget} Parent container, or `null` if the specified node
 *                           is not nested.
 */
function findParent(node) {
  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
  // traversal, but caching is difficult to do correctly without using a
  // mutation observer to listen for all DOM changes.
  var nodeID = ReactMount.getID(node);
  var rootID = ReactInstanceHandles.getReactRootIDFromNodeID(nodeID);
  var container = ReactMount.findReactContainerForID(rootID);
  var parent = ReactMount.getFirstReactDOM(container);
  return parent;
}

/**
 * Calls ReactEventEmitter.handleTopLevel for each node stored in bookKeeping's
 * ancestor list. Separated from createTopLevelCallback to avoid try/finally
 * deoptimization.
 *
 * @param {string} topLevelType
 * @param {DOMEvent} nativeEvent
 * @param {TopLevelCallbackBookKeeping} bookKeeping
 */
function handleTopLevelImpl(topLevelType, nativeEvent, bookKeeping) {
  var topLevelTarget = ReactMount.getFirstReactDOM(
    getEventTarget(nativeEvent)
  ) || window;

  // Loop through the hierarchy, in case there's any nested components.
  // It's important that we build the array of ancestors before calling any
  // event handlers, because event handlers can modify the DOM, leading to
  // inconsistencies with ReactMount's node cache. See #1105.
  var ancestor = topLevelTarget;
  while (ancestor) {
    bookKeeping.ancestors.push(ancestor);
    ancestor = findParent(ancestor);
  }

  for (var i = 0, l = bookKeeping.ancestors.length; i < l; i++) {
    topLevelTarget = bookKeeping.ancestors[i];
    var topLevelTargetID = ReactMount.getID(topLevelTarget) || '';
    ReactEventEmitter.handleTopLevel(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent
    );
  }
}

// Used to store ancestor hierarchy in top level callback
function TopLevelCallbackBookKeeping() {
  this.ancestors = [];
}
mixInto(TopLevelCallbackBookKeeping, {
  destructor: function() {
    this.ancestors.length = 0;
  }
});
PooledClass.addPoolingTo(TopLevelCallbackBookKeeping);

/**
 * Top-level callback creator used to implement event handling using delegation.
 * This is used via dependency injection.
 */
var ReactEventTopLevelCallback = {

  /**
   * Sets whether or not any created callbacks should be enabled.
   *
   * @param {boolean} enabled True if callbacks should be enabled.
   */
  setEnabled: function(enabled) {
    _topLevelListenersEnabled = !!enabled;
  },

  /**
   * @return {boolean} True if callbacks are enabled.
   */
  isEnabled: function() {
    return _topLevelListenersEnabled;
  },

  /**
   * Creates a callback for the supplied `topLevelType` that could be added as
   * a listener to the document. The callback computes a `topLevelTarget` which
   * should be the root node of a mounted React component where the listener
   * is attached.
   *
   * @param {string} topLevelType Record from `EventConstants`.
   * @return {function} Callback for handling top-level events.
   */
  createTopLevelCallback: function(topLevelType) {
    return function(nativeEvent) {
      if (!_topLevelListenersEnabled) {
        return;
      }

      var bookKeeping = TopLevelCallbackBookKeeping.getPooled();
      try {
        handleTopLevelImpl(topLevelType, nativeEvent, bookKeeping);
      } finally {
        TopLevelCallbackBookKeeping.release(bookKeeping);
      }
    };
  }

};

module.exports = ReactEventTopLevelCallback;

},{"./PooledClass":23,"./ReactEventEmitter":48,"./ReactInstanceHandles":53,"./ReactMount":55,"./getEventTarget":104,"./mixInto":124}],51:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactInjection
 */

"use strict";

var DOMProperty = _dereq_("./DOMProperty");
var EventPluginHub = _dereq_("./EventPluginHub");
var ReactComponent = _dereq_("./ReactComponent");
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");
var ReactDOM = _dereq_("./ReactDOM");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var ReactPerf = _dereq_("./ReactPerf");
var ReactRootIndex = _dereq_("./ReactRootIndex");
var ReactUpdates = _dereq_("./ReactUpdates");

var ReactInjection = {
  Component: ReactComponent.injection,
  CompositeComponent: ReactCompositeComponent.injection,
  DOMProperty: DOMProperty.injection,
  EventPluginHub: EventPluginHub.injection,
  DOM: ReactDOM.injection,
  EventEmitter: ReactEventEmitter.injection,
  Perf: ReactPerf.injection,
  RootIndex: ReactRootIndex.injection,
  Updates: ReactUpdates.injection
};

module.exports = ReactInjection;

},{"./DOMProperty":8,"./EventPluginHub":16,"./ReactComponent":27,"./ReactCompositeComponent":29,"./ReactDOM":32,"./ReactEventEmitter":48,"./ReactPerf":60,"./ReactRootIndex":67,"./ReactUpdates":71}],52:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactInputSelection
 */

"use strict";

var ReactDOMSelection = _dereq_("./ReactDOMSelection");

var containsNode = _dereq_("./containsNode");
var focusNode = _dereq_("./focusNode");
var getActiveElement = _dereq_("./getActiveElement");

function isInDocument(node) {
  return containsNode(document.documentElement, node);
}

/**
 * @ReactInputSelection: React input selection module. Based on Selection.js,
 * but modified to be suitable for react and has a couple of bug fixes (doesn't
 * assume buttons have range selections allowed).
 * Input selection module for React.
 */
var ReactInputSelection = {

  hasSelectionCapabilities: function(elem) {
    return elem && (
      (elem.nodeName === 'INPUT' && elem.type === 'text') ||
      elem.nodeName === 'TEXTAREA' ||
      elem.contentEditable === 'true'
    );
  },

  getSelectionInformation: function() {
    var focusedElem = getActiveElement();
    return {
      focusedElem: focusedElem,
      selectionRange:
          ReactInputSelection.hasSelectionCapabilities(focusedElem) ?
          ReactInputSelection.getSelection(focusedElem) :
          null
    };
  },

  /**
   * @restoreSelection: If any selection information was potentially lost,
   * restore it. This is useful when performing operations that could remove dom
   * nodes and place them back in, resulting in focus being lost.
   */
  restoreSelection: function(priorSelectionInformation) {
    var curFocusedElem = getActiveElement();
    var priorFocusedElem = priorSelectionInformation.focusedElem;
    var priorSelectionRange = priorSelectionInformation.selectionRange;
    if (curFocusedElem !== priorFocusedElem &&
        isInDocument(priorFocusedElem)) {
      if (ReactInputSelection.hasSelectionCapabilities(priorFocusedElem)) {
        ReactInputSelection.setSelection(
          priorFocusedElem,
          priorSelectionRange
        );
      }
      focusNode(priorFocusedElem);
    }
  },

  /**
   * @getSelection: Gets the selection bounds of a focused textarea, input or
   * contentEditable node.
   * -@input: Look up selection bounds of this input
   * -@return {start: selectionStart, end: selectionEnd}
   */
  getSelection: function(input) {
    var selection;

    if ('selectionStart' in input) {
      // Modern browser with input or textarea.
      selection = {
        start: input.selectionStart,
        end: input.selectionEnd
      };
    } else if (document.selection && input.nodeName === 'INPUT') {
      // IE8 input.
      var range = document.selection.createRange();
      // There can only be one selection per document in IE, so it must
      // be in our element.
      if (range.parentElement() === input) {
        selection = {
          start: -range.moveStart('character', -input.value.length),
          end: -range.moveEnd('character', -input.value.length)
        };
      }
    } else {
      // Content editable or old IE textarea.
      selection = ReactDOMSelection.getOffsets(input);
    }

    return selection || {start: 0, end: 0};
  },

  /**
   * @setSelection: Sets the selection bounds of a textarea or input and focuses
   * the input.
   * -@input     Set selection bounds of this input or textarea
   * -@offsets   Object of same form that is returned from get*
   */
  setSelection: function(input, offsets) {
    var start = offsets.start;
    var end = offsets.end;
    if (typeof end === 'undefined') {
      end = start;
    }

    if ('selectionStart' in input) {
      input.selectionStart = start;
      input.selectionEnd = Math.min(end, input.value.length);
    } else if (document.selection && input.nodeName === 'INPUT') {
      var range = input.createTextRange();
      range.collapse(true);
      range.moveStart('character', start);
      range.moveEnd('character', end - start);
      range.select();
    } else {
      ReactDOMSelection.setOffsets(input, offsets);
    }
  }
};

module.exports = ReactInputSelection;

},{"./ReactDOMSelection":41,"./containsNode":89,"./focusNode":100,"./getActiveElement":102}],53:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactInstanceHandles
 * @typechecks static-only
 */

"use strict";

var ReactRootIndex = _dereq_("./ReactRootIndex");

var invariant = _dereq_("./invariant");

var SEPARATOR = '.';
var SEPARATOR_LENGTH = SEPARATOR.length;

/**
 * Maximum depth of traversals before we consider the possibility of a bad ID.
 */
var MAX_TREE_DEPTH = 100;

/**
 * Creates a DOM ID prefix to use when mounting React components.
 *
 * @param {number} index A unique integer
 * @return {string} React root ID.
 * @internal
 */
function getReactRootIDString(index) {
  return SEPARATOR + index.toString(36);
}

/**
 * Checks if a character in the supplied ID is a separator or the end.
 *
 * @param {string} id A React DOM ID.
 * @param {number} index Index of the character to check.
 * @return {boolean} True if the character is a separator or end of the ID.
 * @private
 */
function isBoundary(id, index) {
  return id.charAt(index) === SEPARATOR || index === id.length;
}

/**
 * Checks if the supplied string is a valid React DOM ID.
 *
 * @param {string} id A React DOM ID, maybe.
 * @return {boolean} True if the string is a valid React DOM ID.
 * @private
 */
function isValidID(id) {
  return id === '' || (
    id.charAt(0) === SEPARATOR && id.charAt(id.length - 1) !== SEPARATOR
  );
}

/**
 * Checks if the first ID is an ancestor of or equal to the second ID.
 *
 * @param {string} ancestorID
 * @param {string} descendantID
 * @return {boolean} True if `ancestorID` is an ancestor of `descendantID`.
 * @internal
 */
function isAncestorIDOf(ancestorID, descendantID) {
  return (
    descendantID.indexOf(ancestorID) === 0 &&
    isBoundary(descendantID, ancestorID.length)
  );
}

/**
 * Gets the parent ID of the supplied React DOM ID, `id`.
 *
 * @param {string} id ID of a component.
 * @return {string} ID of the parent, or an empty string.
 * @private
 */
function getParentID(id) {
  return id ? id.substr(0, id.lastIndexOf(SEPARATOR)) : '';
}

/**
 * Gets the next DOM ID on the tree path from the supplied `ancestorID` to the
 * supplied `destinationID`. If they are equal, the ID is returned.
 *
 * @param {string} ancestorID ID of an ancestor node of `destinationID`.
 * @param {string} destinationID ID of the destination node.
 * @return {string} Next ID on the path from `ancestorID` to `destinationID`.
 * @private
 */
function getNextDescendantID(ancestorID, destinationID) {
  ("production" !== "development" ? invariant(
    isValidID(ancestorID) && isValidID(destinationID),
    'getNextDescendantID(%s, %s): Received an invalid React DOM ID.',
    ancestorID,
    destinationID
  ) : invariant(isValidID(ancestorID) && isValidID(destinationID)));
  ("production" !== "development" ? invariant(
    isAncestorIDOf(ancestorID, destinationID),
    'getNextDescendantID(...): React has made an invalid assumption about ' +
    'the DOM hierarchy. Expected `%s` to be an ancestor of `%s`.',
    ancestorID,
    destinationID
  ) : invariant(isAncestorIDOf(ancestorID, destinationID)));
  if (ancestorID === destinationID) {
    return ancestorID;
  }
  // Skip over the ancestor and the immediate separator. Traverse until we hit
  // another separator or we reach the end of `destinationID`.
  var start = ancestorID.length + SEPARATOR_LENGTH;
  for (var i = start; i < destinationID.length; i++) {
    if (isBoundary(destinationID, i)) {
      break;
    }
  }
  return destinationID.substr(0, i);
}

/**
 * Gets the nearest common ancestor ID of two IDs.
 *
 * Using this ID scheme, the nearest common ancestor ID is the longest common
 * prefix of the two IDs that immediately preceded a "marker" in both strings.
 *
 * @param {string} oneID
 * @param {string} twoID
 * @return {string} Nearest common ancestor ID, or the empty string if none.
 * @private
 */
function getFirstCommonAncestorID(oneID, twoID) {
  var minLength = Math.min(oneID.length, twoID.length);
  if (minLength === 0) {
    return '';
  }
  var lastCommonMarkerIndex = 0;
  // Use `<=` to traverse until the "EOL" of the shorter string.
  for (var i = 0; i <= minLength; i++) {
    if (isBoundary(oneID, i) && isBoundary(twoID, i)) {
      lastCommonMarkerIndex = i;
    } else if (oneID.charAt(i) !== twoID.charAt(i)) {
      break;
    }
  }
  var longestCommonID = oneID.substr(0, lastCommonMarkerIndex);
  ("production" !== "development" ? invariant(
    isValidID(longestCommonID),
    'getFirstCommonAncestorID(%s, %s): Expected a valid React DOM ID: %s',
    oneID,
    twoID,
    longestCommonID
  ) : invariant(isValidID(longestCommonID)));
  return longestCommonID;
}

/**
 * Traverses the parent path between two IDs (either up or down). The IDs must
 * not be the same, and there must exist a parent path between them. If the
 * callback returns `false`, traversal is stopped.
 *
 * @param {?string} start ID at which to start traversal.
 * @param {?string} stop ID at which to end traversal.
 * @param {function} cb Callback to invoke each ID with.
 * @param {?boolean} skipFirst Whether or not to skip the first node.
 * @param {?boolean} skipLast Whether or not to skip the last node.
 * @private
 */
function traverseParentPath(start, stop, cb, arg, skipFirst, skipLast) {
  start = start || '';
  stop = stop || '';
  ("production" !== "development" ? invariant(
    start !== stop,
    'traverseParentPath(...): Cannot traverse from and to the same ID, `%s`.',
    start
  ) : invariant(start !== stop));
  var traverseUp = isAncestorIDOf(stop, start);
  ("production" !== "development" ? invariant(
    traverseUp || isAncestorIDOf(start, stop),
    'traverseParentPath(%s, %s, ...): Cannot traverse from two IDs that do ' +
    'not have a parent path.',
    start,
    stop
  ) : invariant(traverseUp || isAncestorIDOf(start, stop)));
  // Traverse from `start` to `stop` one depth at a time.
  var depth = 0;
  var traverse = traverseUp ? getParentID : getNextDescendantID;
  for (var id = start; /* until break */; id = traverse(id, stop)) {
    var ret;
    if ((!skipFirst || id !== start) && (!skipLast || id !== stop)) {
      ret = cb(id, traverseUp, arg);
    }
    if (ret === false || id === stop) {
      // Only break //after// visiting `stop`.
      break;
    }
    ("production" !== "development" ? invariant(
      depth++ < MAX_TREE_DEPTH,
      'traverseParentPath(%s, %s, ...): Detected an infinite loop while ' +
      'traversing the React DOM ID tree. This may be due to malformed IDs: %s',
      start, stop
    ) : invariant(depth++ < MAX_TREE_DEPTH));
  }
}

/**
 * Manages the IDs assigned to DOM representations of React components. This
 * uses a specific scheme in order to traverse the DOM efficiently (e.g. in
 * order to simulate events).
 *
 * @internal
 */
var ReactInstanceHandles = {

  /**
   * Constructs a React root ID
   * @return {string} A React root ID.
   */
  createReactRootID: function() {
    return getReactRootIDString(ReactRootIndex.createReactRootIndex());
  },

  /**
   * Constructs a React ID by joining a root ID with a name.
   *
   * @param {string} rootID Root ID of a parent component.
   * @param {string} name A component's name (as flattened children).
   * @return {string} A React ID.
   * @internal
   */
  createReactID: function(rootID, name) {
    return rootID + name;
  },

  /**
   * Gets the DOM ID of the React component that is the root of the tree that
   * contains the React component with the supplied DOM ID.
   *
   * @param {string} id DOM ID of a React component.
   * @return {?string} DOM ID of the React component that is the root.
   * @internal
   */
  getReactRootIDFromNodeID: function(id) {
    if (id && id.charAt(0) === SEPARATOR && id.length > 1) {
      var index = id.indexOf(SEPARATOR, 1);
      return index > -1 ? id.substr(0, index) : id;
    }
    return null;
  },

  /**
   * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
   * should would receive a `mouseEnter` or `mouseLeave` event.
   *
   * NOTE: Does not invoke the callback on the nearest common ancestor because
   * nothing "entered" or "left" that element.
   *
   * @param {string} leaveID ID being left.
   * @param {string} enterID ID being entered.
   * @param {function} cb Callback to invoke on each entered/left ID.
   * @param {*} upArg Argument to invoke the callback with on left IDs.
   * @param {*} downArg Argument to invoke the callback with on entered IDs.
   * @internal
   */
  traverseEnterLeave: function(leaveID, enterID, cb, upArg, downArg) {
    var ancestorID = getFirstCommonAncestorID(leaveID, enterID);
    if (ancestorID !== leaveID) {
      traverseParentPath(leaveID, ancestorID, cb, upArg, false, true);
    }
    if (ancestorID !== enterID) {
      traverseParentPath(ancestorID, enterID, cb, downArg, true, false);
    }
  },

  /**
   * Simulates the traversal of a two-phase, capture/bubble event dispatch.
   *
   * NOTE: This traversal happens on IDs without touching the DOM.
   *
   * @param {string} targetID ID of the target node.
   * @param {function} cb Callback to invoke.
   * @param {*} arg Argument to invoke the callback with.
   * @internal
   */
  traverseTwoPhase: function(targetID, cb, arg) {
    if (targetID) {
      traverseParentPath('', targetID, cb, arg, true, false);
      traverseParentPath(targetID, '', cb, arg, false, true);
    }
  },

  /**
   * Traverse a node ID, calling the supplied `cb` for each ancestor ID. For
   * example, passing `.0.$row-0.1` would result in `cb` getting called
   * with `.0`, `.0.$row-0`, and `.0.$row-0.1`.
   *
   * NOTE: This traversal happens on IDs without touching the DOM.
   *
   * @param {string} targetID ID of the target node.
   * @param {function} cb Callback to invoke.
   * @param {*} arg Argument to invoke the callback with.
   * @internal
   */
  traverseAncestors: function(targetID, cb, arg) {
    traverseParentPath('', targetID, cb, arg, true, false);
  },

  /**
   * Exposed for unit testing.
   * @private
   */
  _getFirstCommonAncestorID: getFirstCommonAncestorID,

  /**
   * Exposed for unit testing.
   * @private
   */
  _getNextDescendantID: getNextDescendantID,

  isAncestorIDOf: isAncestorIDOf,

  SEPARATOR: SEPARATOR

};

module.exports = ReactInstanceHandles;

},{"./ReactRootIndex":67,"./invariant":112}],54:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactMarkupChecksum
 */

"use strict";

var adler32 = _dereq_("./adler32");

var ReactMarkupChecksum = {
  CHECKSUM_ATTR_NAME: 'data-react-checksum',

  /**
   * @param {string} markup Markup string
   * @return {string} Markup string with checksum attribute attached
   */
  addChecksumToMarkup: function(markup) {
    var checksum = adler32(markup);
    return markup.replace(
      '>',
      ' ' + ReactMarkupChecksum.CHECKSUM_ATTR_NAME + '="' + checksum + '">'
    );
  },

  /**
   * @param {string} markup to use
   * @param {DOMElement} element root React element
   * @returns {boolean} whether or not the markup is the same
   */
  canReuseMarkup: function(markup, element) {
    var existingChecksum = element.getAttribute(
      ReactMarkupChecksum.CHECKSUM_ATTR_NAME
    );
    existingChecksum = existingChecksum && parseInt(existingChecksum, 10);
    var markupChecksum = adler32(markup);
    return markupChecksum === existingChecksum;
  }
};

module.exports = ReactMarkupChecksum;

},{"./adler32":88}],55:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactMount
 */

"use strict";

var DOMProperty = _dereq_("./DOMProperty");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var ReactInstanceHandles = _dereq_("./ReactInstanceHandles");
var ReactPerf = _dereq_("./ReactPerf");

var containsNode = _dereq_("./containsNode");
var getReactRootElementInContainer = _dereq_("./getReactRootElementInContainer");
var instantiateReactComponent = _dereq_("./instantiateReactComponent");
var invariant = _dereq_("./invariant");
var shouldUpdateReactComponent = _dereq_("./shouldUpdateReactComponent");

var SEPARATOR = ReactInstanceHandles.SEPARATOR;

var ATTR_NAME = DOMProperty.ID_ATTRIBUTE_NAME;
var nodeCache = {};

var ELEMENT_NODE_TYPE = 1;
var DOC_NODE_TYPE = 9;

/** Mapping from reactRootID to React component instance. */
var instancesByReactRootID = {};

/** Mapping from reactRootID to `container` nodes. */
var containersByReactRootID = {};

if ("production" !== "development") {
  /** __DEV__-only mapping from reactRootID to root elements. */
  var rootElementsByReactRootID = {};
}

// Used to store breadth-first search state in findComponentRoot.
var findComponentRootReusableArray = [];

/**
 * @param {DOMElement} container DOM element that may contain a React component.
 * @return {?string} A "reactRoot" ID, if a React component is rendered.
 */
function getReactRootID(container) {
  var rootElement = getReactRootElementInContainer(container);
  return rootElement && ReactMount.getID(rootElement);
}

/**
 * Accessing node[ATTR_NAME] or calling getAttribute(ATTR_NAME) on a form
 * element can return its control whose name or ID equals ATTR_NAME. All
 * DOM nodes support `getAttributeNode` but this can also get called on
 * other objects so just return '' if we're given something other than a
 * DOM node (such as window).
 *
 * @param {?DOMElement|DOMWindow|DOMDocument|DOMTextNode} node DOM node.
 * @return {string} ID of the supplied `domNode`.
 */
function getID(node) {
  var id = internalGetID(node);
  if (id) {
    if (nodeCache.hasOwnProperty(id)) {
      var cached = nodeCache[id];
      if (cached !== node) {
        ("production" !== "development" ? invariant(
          !isValid(cached, id),
          'ReactMount: Two valid but unequal nodes with the same `%s`: %s',
          ATTR_NAME, id
        ) : invariant(!isValid(cached, id)));

        nodeCache[id] = node;
      }
    } else {
      nodeCache[id] = node;
    }
  }

  return id;
}

function internalGetID(node) {
  // If node is something like a window, document, or text node, none of
  // which support attributes or a .getAttribute method, gracefully return
  // the empty string, as if the attribute were missing.
  return node && node.getAttribute && node.getAttribute(ATTR_NAME) || '';
}

/**
 * Sets the React-specific ID of the given node.
 *
 * @param {DOMElement} node The DOM node whose ID will be set.
 * @param {string} id The value of the ID attribute.
 */
function setID(node, id) {
  var oldID = internalGetID(node);
  if (oldID !== id) {
    delete nodeCache[oldID];
  }
  node.setAttribute(ATTR_NAME, id);
  nodeCache[id] = node;
}

/**
 * Finds the node with the supplied React-generated DOM ID.
 *
 * @param {string} id A React-generated DOM ID.
 * @return {DOMElement} DOM node with the suppled `id`.
 * @internal
 */
function getNode(id) {
  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
    nodeCache[id] = ReactMount.findReactNodeByID(id);
  }
  return nodeCache[id];
}

/**
 * A node is "valid" if it is contained by a currently mounted container.
 *
 * This means that the node does not have to be contained by a document in
 * order to be considered valid.
 *
 * @param {?DOMElement} node The candidate DOM node.
 * @param {string} id The expected ID of the node.
 * @return {boolean} Whether the node is contained by a mounted container.
 */
function isValid(node, id) {
  if (node) {
    ("production" !== "development" ? invariant(
      internalGetID(node) === id,
      'ReactMount: Unexpected modification of `%s`',
      ATTR_NAME
    ) : invariant(internalGetID(node) === id));

    var container = ReactMount.findReactContainerForID(id);
    if (container && containsNode(container, node)) {
      return true;
    }
  }

  return false;
}

/**
 * Causes the cache to forget about one React-specific ID.
 *
 * @param {string} id The ID to forget.
 */
function purgeID(id) {
  delete nodeCache[id];
}

var deepestNodeSoFar = null;
function findDeepestCachedAncestorImpl(ancestorID) {
  var ancestor = nodeCache[ancestorID];
  if (ancestor && isValid(ancestor, ancestorID)) {
    deepestNodeSoFar = ancestor;
  } else {
    // This node isn't populated in the cache, so presumably none of its
    // descendants are. Break out of the loop.
    return false;
  }
}

/**
 * Return the deepest cached node whose ID is a prefix of `targetID`.
 */
function findDeepestCachedAncestor(targetID) {
  deepestNodeSoFar = null;
  ReactInstanceHandles.traverseAncestors(
    targetID,
    findDeepestCachedAncestorImpl
  );

  var foundNode = deepestNodeSoFar;
  deepestNodeSoFar = null;
  return foundNode;
}

/**
 * Mounting is the process of initializing a React component by creatings its
 * representative DOM elements and inserting them into a supplied `container`.
 * Any prior content inside `container` is destroyed in the process.
 *
 *   ReactMount.renderComponent(
 *     component,
 *     document.getElementById('container')
 *   );
 *
 *   <div id="container">                   <-- Supplied `container`.
 *     <div data-reactid=".3">              <-- Rendered reactRoot of React
 *       // ...                                 component.
 *     </div>
 *   </div>
 *
 * Inside of `container`, the first element rendered is the "reactRoot".
 */
var ReactMount = {
  /** Time spent generating markup. */
  totalInstantiationTime: 0,

  /** Time spent inserting markup into the DOM. */
  totalInjectionTime: 0,

  /** Whether support for touch events should be initialized. */
  useTouchEvents: false,

  /** Exposed for debugging purposes **/
  _instancesByReactRootID: instancesByReactRootID,

  /**
   * This is a hook provided to support rendering React components while
   * ensuring that the apparent scroll position of its `container` does not
   * change.
   *
   * @param {DOMElement} container The `container` being rendered into.
   * @param {function} renderCallback This must be called once to do the render.
   */
  scrollMonitor: function(container, renderCallback) {
    renderCallback();
  },

  /**
   * Take a component that's already mounted into the DOM and replace its props
   * @param {ReactComponent} prevComponent component instance already in the DOM
   * @param {ReactComponent} nextComponent component instance to render
   * @param {DOMElement} container container to render into
   * @param {?function} callback function triggered on completion
   */
  _updateRootComponent: function(
      prevComponent,
      nextComponent,
      container,
      callback) {
    var nextProps = nextComponent.props;
    ReactMount.scrollMonitor(container, function() {
      prevComponent.replaceProps(nextProps, callback);
    });

    if ("production" !== "development") {
      // Record the root element in case it later gets transplanted.
      rootElementsByReactRootID[getReactRootID(container)] =
        getReactRootElementInContainer(container);
    }

    return prevComponent;
  },

  /**
   * Register a component into the instance map and starts scroll value
   * monitoring
   * @param {ReactComponent} nextComponent component instance to render
   * @param {DOMElement} container container to render into
   * @return {string} reactRoot ID prefix
   */
  _registerComponent: function(nextComponent, container) {
    ("production" !== "development" ? invariant(
      container && (
        container.nodeType === ELEMENT_NODE_TYPE ||
        container.nodeType === DOC_NODE_TYPE
      ),
      '_registerComponent(...): Target container is not a DOM element.'
    ) : invariant(container && (
      container.nodeType === ELEMENT_NODE_TYPE ||
      container.nodeType === DOC_NODE_TYPE
    )));

    ReactEventEmitter.ensureScrollValueMonitoring();

    var reactRootID = ReactMount.registerContainer(container);
    instancesByReactRootID[reactRootID] = nextComponent;
    return reactRootID;
  },

  /**
   * Render a new component into the DOM.
   * @param {ReactComponent} nextComponent component instance to render
   * @param {DOMElement} container container to render into
   * @param {boolean} shouldReuseMarkup if we should skip the markup insertion
   * @return {ReactComponent} nextComponent
   */
  _renderNewRootComponent: ReactPerf.measure(
    'ReactMount',
    '_renderNewRootComponent',
    function(
        nextComponent,
        container,
        shouldReuseMarkup) {

      var componentInstance = instantiateReactComponent(nextComponent);
      var reactRootID = ReactMount._registerComponent(
        componentInstance,
        container
      );
      componentInstance.mountComponentIntoNode(
        reactRootID,
        container,
        shouldReuseMarkup
      );

      if ("production" !== "development") {
        // Record the root element in case it later gets transplanted.
        rootElementsByReactRootID[reactRootID] =
          getReactRootElementInContainer(container);
      }

      return componentInstance;
    }
  ),

  /**
   * Renders a React component into the DOM in the supplied `container`.
   *
   * If the React component was previously rendered into `container`, this will
   * perform an update on it and only mutate the DOM as necessary to reflect the
   * latest React component.
   *
   * @param {ReactComponent} nextComponent Component instance to render.
   * @param {DOMElement} container DOM element to render into.
   * @param {?function} callback function triggered on completion
   * @return {ReactComponent} Component instance rendered in `container`.
   */
  renderComponent: function(nextComponent, container, callback) {
    var prevComponent = instancesByReactRootID[getReactRootID(container)];

    if (prevComponent) {
      if (shouldUpdateReactComponent(prevComponent, nextComponent)) {
        return ReactMount._updateRootComponent(
          prevComponent,
          nextComponent,
          container,
          callback
        );
      } else {
        ReactMount.unmountComponentAtNode(container);
      }
    }

    var reactRootElement = getReactRootElementInContainer(container);
    var containerHasReactMarkup =
      reactRootElement && ReactMount.isRenderedByReact(reactRootElement);

    var shouldReuseMarkup = containerHasReactMarkup && !prevComponent;

    var component = ReactMount._renderNewRootComponent(
      nextComponent,
      container,
      shouldReuseMarkup
    );
    callback && callback.call(component);
    return component;
  },

  /**
   * Constructs a component instance of `constructor` with `initialProps` and
   * renders it into the supplied `container`.
   *
   * @param {function} constructor React component constructor.
   * @param {?object} props Initial props of the component instance.
   * @param {DOMElement} container DOM element to render into.
   * @return {ReactComponent} Component instance rendered in `container`.
   */
  constructAndRenderComponent: function(constructor, props, container) {
    return ReactMount.renderComponent(constructor(props), container);
  },

  /**
   * Constructs a component instance of `constructor` with `initialProps` and
   * renders it into a container node identified by supplied `id`.
   *
   * @param {function} componentConstructor React component constructor
   * @param {?object} props Initial props of the component instance.
   * @param {string} id ID of the DOM element to render into.
   * @return {ReactComponent} Component instance rendered in the container node.
   */
  constructAndRenderComponentByID: function(constructor, props, id) {
    var domNode = document.getElementById(id);
    ("production" !== "development" ? invariant(
      domNode,
      'Tried to get element with id of "%s" but it is not present on the page.',
      id
    ) : invariant(domNode));
    return ReactMount.constructAndRenderComponent(constructor, props, domNode);
  },

  /**
   * Registers a container node into which React components will be rendered.
   * This also creates the "reactRoot" ID that will be assigned to the element
   * rendered within.
   *
   * @param {DOMElement} container DOM element to register as a container.
   * @return {string} The "reactRoot" ID of elements rendered within.
   */
  registerContainer: function(container) {
    var reactRootID = getReactRootID(container);
    if (reactRootID) {
      // If one exists, make sure it is a valid "reactRoot" ID.
      reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(reactRootID);
    }
    if (!reactRootID) {
      // No valid "reactRoot" ID found, create one.
      reactRootID = ReactInstanceHandles.createReactRootID();
    }
    containersByReactRootID[reactRootID] = container;
    return reactRootID;
  },

  /**
   * Unmounts and destroys the React component rendered in the `container`.
   *
   * @param {DOMElement} container DOM element containing a React component.
   * @return {boolean} True if a component was found in and unmounted from
   *                   `container`
   */
  unmountComponentAtNode: function(container) {
    var reactRootID = getReactRootID(container);
    var component = instancesByReactRootID[reactRootID];
    if (!component) {
      return false;
    }
    ReactMount.unmountComponentFromNode(component, container);
    delete instancesByReactRootID[reactRootID];
    delete containersByReactRootID[reactRootID];
    if ("production" !== "development") {
      delete rootElementsByReactRootID[reactRootID];
    }
    return true;
  },

  /**
   * Unmounts a component and removes it from the DOM.
   *
   * @param {ReactComponent} instance React component instance.
   * @param {DOMElement} container DOM element to unmount from.
   * @final
   * @internal
   * @see {ReactMount.unmountComponentAtNode}
   */
  unmountComponentFromNode: function(instance, container) {
    instance.unmountComponent();

    if (container.nodeType === DOC_NODE_TYPE) {
      container = container.documentElement;
    }

    // http://jsperf.com/emptying-a-node
    while (container.lastChild) {
      container.removeChild(container.lastChild);
    }
  },

  /**
   * Finds the container DOM element that contains React component to which the
   * supplied DOM `id` belongs.
   *
   * @param {string} id The ID of an element rendered by a React component.
   * @return {?DOMElement} DOM element that contains the `id`.
   */
  findReactContainerForID: function(id) {
    var reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(id);
    var container = containersByReactRootID[reactRootID];

    if ("production" !== "development") {
      var rootElement = rootElementsByReactRootID[reactRootID];
      if (rootElement && rootElement.parentNode !== container) {
        ("production" !== "development" ? invariant(
          // Call internalGetID here because getID calls isValid which calls
          // findReactContainerForID (this function).
          internalGetID(rootElement) === reactRootID,
          'ReactMount: Root element ID differed from reactRootID.'
        ) : invariant(// Call internalGetID here because getID calls isValid which calls
        // findReactContainerForID (this function).
        internalGetID(rootElement) === reactRootID));

        var containerChild = container.firstChild;
        if (containerChild &&
            reactRootID === internalGetID(containerChild)) {
          // If the container has a new child with the same ID as the old
          // root element, then rootElementsByReactRootID[reactRootID] is
          // just stale and needs to be updated. The case that deserves a
          // warning is when the container is empty.
          rootElementsByReactRootID[reactRootID] = containerChild;
        } else {
          console.warn(
            'ReactMount: Root element has been removed from its original ' +
            'container. New container:', rootElement.parentNode
          );
        }
      }
    }

    return container;
  },

  /**
   * Finds an element rendered by React with the supplied ID.
   *
   * @param {string} id ID of a DOM node in the React component.
   * @return {DOMElement} Root DOM node of the React component.
   */
  findReactNodeByID: function(id) {
    var reactRoot = ReactMount.findReactContainerForID(id);
    return ReactMount.findComponentRoot(reactRoot, id);
  },

  /**
   * True if the supplied `node` is rendered by React.
   *
   * @param {*} node DOM Element to check.
   * @return {boolean} True if the DOM Element appears to be rendered by React.
   * @internal
   */
  isRenderedByReact: function(node) {
    if (node.nodeType !== 1) {
      // Not a DOMElement, therefore not a React component
      return false;
    }
    var id = ReactMount.getID(node);
    return id ? id.charAt(0) === SEPARATOR : false;
  },

  /**
   * Traverses up the ancestors of the supplied node to find a node that is a
   * DOM representation of a React component.
   *
   * @param {*} node
   * @return {?DOMEventTarget}
   * @internal
   */
  getFirstReactDOM: function(node) {
    var current = node;
    while (current && current.parentNode !== current) {
      if (ReactMount.isRenderedByReact(current)) {
        return current;
      }
      current = current.parentNode;
    }
    return null;
  },

  /**
   * Finds a node with the supplied `targetID` inside of the supplied
   * `ancestorNode`.  Exploits the ID naming scheme to perform the search
   * quickly.
   *
   * @param {DOMEventTarget} ancestorNode Search from this root.
   * @pararm {string} targetID ID of the DOM representation of the component.
   * @return {DOMEventTarget} DOM node with the supplied `targetID`.
   * @internal
   */
  findComponentRoot: function(ancestorNode, targetID) {
    var firstChildren = findComponentRootReusableArray;
    var childIndex = 0;

    var deepestAncestor = findDeepestCachedAncestor(targetID) || ancestorNode;

    firstChildren[0] = deepestAncestor.firstChild;
    firstChildren.length = 1;

    while (childIndex < firstChildren.length) {
      var child = firstChildren[childIndex++];
      var targetChild;

      while (child) {
        var childID = ReactMount.getID(child);
        if (childID) {
          // Even if we find the node we're looking for, we finish looping
          // through its siblings to ensure they're cached so that we don't have
          // to revisit this node again. Otherwise, we make n^2 calls to getID
          // when visiting the many children of a single node in order.

          if (targetID === childID) {
            targetChild = child;
          } else if (ReactInstanceHandles.isAncestorIDOf(childID, targetID)) {
            // If we find a child whose ID is an ancestor of the given ID,
            // then we can be sure that we only want to search the subtree
            // rooted at this child, so we can throw out the rest of the
            // search state.
            firstChildren.length = childIndex = 0;
            firstChildren.push(child.firstChild);
          }

        } else {
          // If this child had no ID, then there's a chance that it was
          // injected automatically by the browser, as when a `<table>`
          // element sprouts an extra `<tbody>` child as a side effect of
          // `.innerHTML` parsing. Optimistically continue down this
          // branch, but not before examining the other siblings.
          firstChildren.push(child.firstChild);
        }

        child = child.nextSibling;
      }

      if (targetChild) {
        // Emptying firstChildren/findComponentRootReusableArray is
        // not necessary for correctness, but it helps the GC reclaim
        // any nodes that were left at the end of the search.
        firstChildren.length = 0;

        return targetChild;
      }
    }

    firstChildren.length = 0;

    ("production" !== "development" ? invariant(
      false,
      'findComponentRoot(..., %s): Unable to find element. This probably ' +
      'means the DOM was unexpectedly mutated (e.g., by the browser), ' +
      'usually due to forgetting a <tbody> when using tables or nesting <p> ' +
      'or <a> tags. Try inspecting the child nodes of the element with React ' +
      'ID `%s`.',
      targetID,
      ReactMount.getID(ancestorNode)
    ) : invariant(false));
  },


  /**
   * React ID utilities.
   */

  getReactRootID: getReactRootID,

  getID: getID,

  setID: setID,

  getNode: getNode,

  purgeID: purgeID
};

module.exports = ReactMount;

},{"./DOMProperty":8,"./ReactEventEmitter":48,"./ReactInstanceHandles":53,"./ReactPerf":60,"./containsNode":89,"./getReactRootElementInContainer":107,"./instantiateReactComponent":111,"./invariant":112,"./shouldUpdateReactComponent":131}],56:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactMountReady
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");

var mixInto = _dereq_("./mixInto");

/**
 * A specialized pseudo-event module to help keep track of components waiting to
 * be notified when their DOM representations are available for use.
 *
 * This implements `PooledClass`, so you should never need to instantiate this.
 * Instead, use `ReactMountReady.getPooled()`.
 *
 * @param {?array<function>} initialCollection
 * @class ReactMountReady
 * @implements PooledClass
 * @internal
 */
function ReactMountReady(initialCollection) {
  this._queue = initialCollection || null;
}

mixInto(ReactMountReady, {

  /**
   * Enqueues a callback to be invoked when `notifyAll` is invoked. This is used
   * to enqueue calls to `componentDidMount` and `componentDidUpdate`.
   *
   * @param {ReactComponent} component Component being rendered.
   * @param {function(DOMElement)} callback Invoked when `notifyAll` is invoked.
   * @internal
   */
  enqueue: function(component, callback) {
    this._queue = this._queue || [];
    this._queue.push({component: component, callback: callback});
  },

  /**
   * Invokes all enqueued callbacks and clears the queue. This is invoked after
   * the DOM representation of a component has been created or updated.
   *
   * @internal
   */
  notifyAll: function() {
    var queue = this._queue;
    if (queue) {
      this._queue = null;
      for (var i = 0, l = queue.length; i < l; i++) {
        var component = queue[i].component;
        var callback = queue[i].callback;
        callback.call(component);
      }
      queue.length = 0;
    }
  },

  /**
   * Resets the internal queue.
   *
   * @internal
   */
  reset: function() {
    this._queue = null;
  },

  /**
   * `PooledClass` looks for this.
   */
  destructor: function() {
    this.reset();
  }

});

PooledClass.addPoolingTo(ReactMountReady);

module.exports = ReactMountReady;

},{"./PooledClass":23,"./mixInto":124}],57:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactMultiChild
 * @typechecks static-only
 */

"use strict";

var ReactComponent = _dereq_("./ReactComponent");
var ReactMultiChildUpdateTypes = _dereq_("./ReactMultiChildUpdateTypes");

var flattenChildren = _dereq_("./flattenChildren");
var instantiateReactComponent = _dereq_("./instantiateReactComponent");
var shouldUpdateReactComponent = _dereq_("./shouldUpdateReactComponent");

/**
 * Updating children of a component may trigger recursive updates. The depth is
 * used to batch recursive updates to render markup more efficiently.
 *
 * @type {number}
 * @private
 */
var updateDepth = 0;

/**
 * Queue of update configuration objects.
 *
 * Each object has a `type` property that is in `ReactMultiChildUpdateTypes`.
 *
 * @type {array<object>}
 * @private
 */
var updateQueue = [];

/**
 * Queue of markup to be rendered.
 *
 * @type {array<string>}
 * @private
 */
var markupQueue = [];

/**
 * Enqueues markup to be rendered and inserted at a supplied index.
 *
 * @param {string} parentID ID of the parent component.
 * @param {string} markup Markup that renders into an element.
 * @param {number} toIndex Destination index.
 * @private
 */
function enqueueMarkup(parentID, markup, toIndex) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.INSERT_MARKUP,
    markupIndex: markupQueue.push(markup) - 1,
    textContent: null,
    fromIndex: null,
    toIndex: toIndex
  });
}

/**
 * Enqueues moving an existing element to another index.
 *
 * @param {string} parentID ID of the parent component.
 * @param {number} fromIndex Source index of the existing element.
 * @param {number} toIndex Destination index of the element.
 * @private
 */
function enqueueMove(parentID, fromIndex, toIndex) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.MOVE_EXISTING,
    markupIndex: null,
    textContent: null,
    fromIndex: fromIndex,
    toIndex: toIndex
  });
}

/**
 * Enqueues removing an element at an index.
 *
 * @param {string} parentID ID of the parent component.
 * @param {number} fromIndex Index of the element to remove.
 * @private
 */
function enqueueRemove(parentID, fromIndex) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.REMOVE_NODE,
    markupIndex: null,
    textContent: null,
    fromIndex: fromIndex,
    toIndex: null
  });
}

/**
 * Enqueues setting the text content.
 *
 * @param {string} parentID ID of the parent component.
 * @param {string} textContent Text content to set.
 * @private
 */
function enqueueTextContent(parentID, textContent) {
  // NOTE: Null values reduce hidden classes.
  updateQueue.push({
    parentID: parentID,
    parentNode: null,
    type: ReactMultiChildUpdateTypes.TEXT_CONTENT,
    markupIndex: null,
    textContent: textContent,
    fromIndex: null,
    toIndex: null
  });
}

/**
 * Processes any enqueued updates.
 *
 * @private
 */
function processQueue() {
  if (updateQueue.length) {
    ReactComponent.BackendIDOperations.dangerouslyProcessChildrenUpdates(
      updateQueue,
      markupQueue
    );
    clearQueue();
  }
}

/**
 * Clears any enqueued updates.
 *
 * @private
 */
function clearQueue() {
  updateQueue.length = 0;
  markupQueue.length = 0;
}

/**
 * ReactMultiChild are capable of reconciling multiple children.
 *
 * @class ReactMultiChild
 * @internal
 */
var ReactMultiChild = {

  /**
   * Provides common functionality for components that must reconcile multiple
   * children. This is used by `ReactDOMComponent` to mount, update, and
   * unmount child components.
   *
   * @lends {ReactMultiChild.prototype}
   */
  Mixin: {

    /**
     * Generates a "mount image" for each of the supplied children. In the case
     * of `ReactDOMComponent`, a mount image is a string of markup.
     *
     * @param {?object} nestedChildren Nested child maps.
     * @return {array} An array of mounted representations.
     * @internal
     */
    mountChildren: function(nestedChildren, transaction) {
      var children = flattenChildren(nestedChildren);
      var mountImages = [];
      var index = 0;
      this._renderedChildren = children;
      for (var name in children) {
        var child = children[name];
        if (children.hasOwnProperty(name)) {
          // The rendered children must be turned into instances as they're
          // mounted.
          var childInstance = instantiateReactComponent(child);
          children[name] = childInstance;
          // Inlined for performance, see `ReactInstanceHandles.createReactID`.
          var rootID = this._rootNodeID + name;
          var mountImage = childInstance.mountComponent(
            rootID,
            transaction,
            this._mountDepth + 1
          );
          childInstance._mountIndex = index;
          mountImages.push(mountImage);
          index++;
        }
      }
      return mountImages;
    },

    /**
     * Replaces any rendered children with a text content string.
     *
     * @param {string} nextContent String of content.
     * @internal
     */
    updateTextContent: function(nextContent) {
      updateDepth++;
      var errorThrown = true;
      try {
        var prevChildren = this._renderedChildren;
        // Remove any rendered children.
        for (var name in prevChildren) {
          if (prevChildren.hasOwnProperty(name)) {
            this._unmountChildByName(prevChildren[name], name);
          }
        }
        // Set new text content.
        this.setTextContent(nextContent);
        errorThrown = false;
      } finally {
        updateDepth--;
        if (!updateDepth) {
          errorThrown ? clearQueue() : processQueue();
        }
      }
    },

    /**
     * Updates the rendered children with new children.
     *
     * @param {?object} nextNestedChildren Nested child maps.
     * @param {ReactReconcileTransaction} transaction
     * @internal
     */
    updateChildren: function(nextNestedChildren, transaction) {
      updateDepth++;
      var errorThrown = true;
      try {
        this._updateChildren(nextNestedChildren, transaction);
        errorThrown = false;
      } finally {
        updateDepth--;
        if (!updateDepth) {
          errorThrown ? clearQueue() : processQueue();
        }
      }
    },

    /**
     * Improve performance by isolating this hot code path from the try/catch
     * block in `updateChildren`.
     *
     * @param {?object} nextNestedChildren Nested child maps.
     * @param {ReactReconcileTransaction} transaction
     * @final
     * @protected
     */
    _updateChildren: function(nextNestedChildren, transaction) {
      var nextChildren = flattenChildren(nextNestedChildren);
      var prevChildren = this._renderedChildren;
      if (!nextChildren && !prevChildren) {
        return;
      }
      var name;
      // `nextIndex` will increment for each child in `nextChildren`, but
      // `lastIndex` will be the last index visited in `prevChildren`.
      var lastIndex = 0;
      var nextIndex = 0;
      for (name in nextChildren) {
        if (!nextChildren.hasOwnProperty(name)) {
          continue;
        }
        var prevChild = prevChildren && prevChildren[name];
        var nextChild = nextChildren[name];
        if (shouldUpdateReactComponent(prevChild, nextChild)) {
          this.moveChild(prevChild, nextIndex, lastIndex);
          lastIndex = Math.max(prevChild._mountIndex, lastIndex);
          prevChild.receiveComponent(nextChild, transaction);
          prevChild._mountIndex = nextIndex;
        } else {
          if (prevChild) {
            // Update `lastIndex` before `_mountIndex` gets unset by unmounting.
            lastIndex = Math.max(prevChild._mountIndex, lastIndex);
            this._unmountChildByName(prevChild, name);
          }
          // The child must be instantiated before it's mounted.
          var nextChildInstance = instantiateReactComponent(nextChild);
          this._mountChildByNameAtIndex(
            nextChildInstance, name, nextIndex, transaction
          );
        }
        nextIndex++;
      }
      // Remove children that are no longer present.
      for (name in prevChildren) {
        if (prevChildren.hasOwnProperty(name) &&
            !(nextChildren && nextChildren[name])) {
          this._unmountChildByName(prevChildren[name], name);
        }
      }
    },

    /**
     * Unmounts all rendered children. This should be used to clean up children
     * when this component is unmounted.
     *
     * @internal
     */
    unmountChildren: function() {
      var renderedChildren = this._renderedChildren;
      for (var name in renderedChildren) {
        var renderedChild = renderedChildren[name];
        // TODO: When is this not true?
        if (renderedChild.unmountComponent) {
          renderedChild.unmountComponent();
        }
      }
      this._renderedChildren = null;
    },

    /**
     * Moves a child component to the supplied index.
     *
     * @param {ReactComponent} child Component to move.
     * @param {number} toIndex Destination index of the element.
     * @param {number} lastIndex Last index visited of the siblings of `child`.
     * @protected
     */
    moveChild: function(child, toIndex, lastIndex) {
      // If the index of `child` is less than `lastIndex`, then it needs to
      // be moved. Otherwise, we do not need to move it because a child will be
      // inserted or moved before `child`.
      if (child._mountIndex < lastIndex) {
        enqueueMove(this._rootNodeID, child._mountIndex, toIndex);
      }
    },

    /**
     * Creates a child component.
     *
     * @param {ReactComponent} child Component to create.
     * @param {string} mountImage Markup to insert.
     * @protected
     */
    createChild: function(child, mountImage) {
      enqueueMarkup(this._rootNodeID, mountImage, child._mountIndex);
    },

    /**
     * Removes a child component.
     *
     * @param {ReactComponent} child Child to remove.
     * @protected
     */
    removeChild: function(child) {
      enqueueRemove(this._rootNodeID, child._mountIndex);
    },

    /**
     * Sets this text content string.
     *
     * @param {string} textContent Text content to set.
     * @protected
     */
    setTextContent: function(textContent) {
      enqueueTextContent(this._rootNodeID, textContent);
    },

    /**
     * Mounts a child with the supplied name.
     *
     * NOTE: This is part of `updateChildren` and is here for readability.
     *
     * @param {ReactComponent} child Component to mount.
     * @param {string} name Name of the child.
     * @param {number} index Index at which to insert the child.
     * @param {ReactReconcileTransaction} transaction
     * @private
     */
    _mountChildByNameAtIndex: function(child, name, index, transaction) {
      // Inlined for performance, see `ReactInstanceHandles.createReactID`.
      var rootID = this._rootNodeID + name;
      var mountImage = child.mountComponent(
        rootID,
        transaction,
        this._mountDepth + 1
      );
      child._mountIndex = index;
      this.createChild(child, mountImage);
      this._renderedChildren = this._renderedChildren || {};
      this._renderedChildren[name] = child;
    },

    /**
     * Unmounts a rendered child by name.
     *
     * NOTE: This is part of `updateChildren` and is here for readability.
     *
     * @param {ReactComponent} child Component to unmount.
     * @param {string} name Name of the child in `this._renderedChildren`.
     * @private
     */
    _unmountChildByName: function(child, name) {
      // TODO: When is this not true?
      if (ReactComponent.isValidComponent(child)) {
        this.removeChild(child);
        child._mountIndex = null;
        child.unmountComponent();
        delete this._renderedChildren[name];
      }
    }

  }

};

module.exports = ReactMultiChild;

},{"./ReactComponent":27,"./ReactMultiChildUpdateTypes":58,"./flattenChildren":99,"./instantiateReactComponent":111,"./shouldUpdateReactComponent":131}],58:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactMultiChildUpdateTypes
 */

"use strict";

var keyMirror = _dereq_("./keyMirror");

/**
 * When a component's children are updated, a series of update configuration
 * objects are created in order to batch and serialize the required changes.
 *
 * Enumerates all the possible types of update configurations.
 *
 * @internal
 */
var ReactMultiChildUpdateTypes = keyMirror({
  INSERT_MARKUP: null,
  MOVE_EXISTING: null,
  REMOVE_NODE: null,
  TEXT_CONTENT: null
});

module.exports = ReactMultiChildUpdateTypes;

},{"./keyMirror":118}],59:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactOwner
 */

"use strict";

var emptyObject = _dereq_("./emptyObject");
var invariant = _dereq_("./invariant");

/**
 * ReactOwners are capable of storing references to owned components.
 *
 * All components are capable of //being// referenced by owner components, but
 * only ReactOwner components are capable of //referencing// owned components.
 * The named reference is known as a "ref".
 *
 * Refs are available when mounted and updated during reconciliation.
 *
 *   var MyComponent = React.createClass({
 *     render: function() {
 *       return (
 *         <div onClick={this.handleClick}>
 *           <CustomComponent ref="custom" />
 *         </div>
 *       );
 *     },
 *     handleClick: function() {
 *       this.refs.custom.handleClick();
 *     },
 *     componentDidMount: function() {
 *       this.refs.custom.initialize();
 *     }
 *   });
 *
 * Refs should rarely be used. When refs are used, they should only be done to
 * control data that is not handled by React's data flow.
 *
 * @class ReactOwner
 */
var ReactOwner = {

  /**
   * @param {?object} object
   * @return {boolean} True if `object` is a valid owner.
   * @final
   */
  isValidOwner: function(object) {
    return !!(
      object &&
      typeof object.attachRef === 'function' &&
      typeof object.detachRef === 'function'
    );
  },

  /**
   * Adds a component by ref to an owner component.
   *
   * @param {ReactComponent} component Component to reference.
   * @param {string} ref Name by which to refer to the component.
   * @param {ReactOwner} owner Component on which to record the ref.
   * @final
   * @internal
   */
  addComponentAsRefTo: function(component, ref, owner) {
    ("production" !== "development" ? invariant(
      ReactOwner.isValidOwner(owner),
      'addComponentAsRefTo(...): Only a ReactOwner can have refs. This ' +
      'usually means that you\'re trying to add a ref to a component that ' +
      'doesn\'t have an owner (that is, was not created inside of another ' +
      'component\'s `render` method). Try rendering this component inside of ' +
      'a new top-level component which will hold the ref.'
    ) : invariant(ReactOwner.isValidOwner(owner)));
    owner.attachRef(ref, component);
  },

  /**
   * Removes a component by ref from an owner component.
   *
   * @param {ReactComponent} component Component to dereference.
   * @param {string} ref Name of the ref to remove.
   * @param {ReactOwner} owner Component on which the ref is recorded.
   * @final
   * @internal
   */
  removeComponentAsRefFrom: function(component, ref, owner) {
    ("production" !== "development" ? invariant(
      ReactOwner.isValidOwner(owner),
      'removeComponentAsRefFrom(...): Only a ReactOwner can have refs. This ' +
      'usually means that you\'re trying to remove a ref to a component that ' +
      'doesn\'t have an owner (that is, was not created inside of another ' +
      'component\'s `render` method). Try rendering this component inside of ' +
      'a new top-level component which will hold the ref.'
    ) : invariant(ReactOwner.isValidOwner(owner)));
    // Check that `component` is still the current ref because we do not want to
    // detach the ref if another component stole it.
    if (owner.refs[ref] === component) {
      owner.detachRef(ref);
    }
  },

  /**
   * A ReactComponent must mix this in to have refs.
   *
   * @lends {ReactOwner.prototype}
   */
  Mixin: {

    construct: function() {
      this.refs = emptyObject;
    },

    /**
     * Lazily allocates the refs object and stores `component` as `ref`.
     *
     * @param {string} ref Reference name.
     * @param {component} component Component to store as `ref`.
     * @final
     * @private
     */
    attachRef: function(ref, component) {
      ("production" !== "development" ? invariant(
        component.isOwnedBy(this),
        'attachRef(%s, ...): Only a component\'s owner can store a ref to it.',
        ref
      ) : invariant(component.isOwnedBy(this)));
      var refs = this.refs === emptyObject ? (this.refs = {}) : this.refs;
      refs[ref] = component;
    },

    /**
     * Detaches a reference name.
     *
     * @param {string} ref Name to dereference.
     * @final
     * @private
     */
    detachRef: function(ref) {
      delete this.refs[ref];
    }

  }

};

module.exports = ReactOwner;

},{"./emptyObject":97,"./invariant":112}],60:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactPerf
 * @typechecks static-only
 */

"use strict";

/**
 * ReactPerf is a general AOP system designed to measure performance. This
 * module only has the hooks: see ReactDefaultPerf for the analysis tool.
 */
var ReactPerf = {
  /**
   * Boolean to enable/disable measurement. Set to false by default to prevent
   * accidental logging and perf loss.
   */
  enableMeasure: false,

  /**
   * Holds onto the measure function in use. By default, don't measure
   * anything, but we'll override this if we inject a measure function.
   */
  storedMeasure: _noMeasure,

  /**
   * Use this to wrap methods you want to measure. Zero overhead in production.
   *
   * @param {string} objName
   * @param {string} fnName
   * @param {function} func
   * @return {function}
   */
  measure: function(objName, fnName, func) {
    if ("production" !== "development") {
      var measuredFunc = null;
      return function() {
        if (ReactPerf.enableMeasure) {
          if (!measuredFunc) {
            measuredFunc = ReactPerf.storedMeasure(objName, fnName, func);
          }
          return measuredFunc.apply(this, arguments);
        }
        return func.apply(this, arguments);
      };
    }
    return func;
  },

  injection: {
    /**
     * @param {function} measure
     */
    injectMeasure: function(measure) {
      ReactPerf.storedMeasure = measure;
    }
  }
};

/**
 * Simply passes through the measured function, without measuring it.
 *
 * @param {string} objName
 * @param {string} fnName
 * @param {function} func
 * @return {function}
 */
function _noMeasure(objName, fnName, func) {
  return func;
}

module.exports = ReactPerf;

},{}],61:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactPropTransferer
 */

"use strict";

var emptyFunction = _dereq_("./emptyFunction");
var invariant = _dereq_("./invariant");
var joinClasses = _dereq_("./joinClasses");
var merge = _dereq_("./merge");

/**
 * Creates a transfer strategy that will merge prop values using the supplied
 * `mergeStrategy`. If a prop was previously unset, this just sets it.
 *
 * @param {function} mergeStrategy
 * @return {function}
 */
function createTransferStrategy(mergeStrategy) {
  return function(props, key, value) {
    if (!props.hasOwnProperty(key)) {
      props[key] = value;
    } else {
      props[key] = mergeStrategy(props[key], value);
    }
  };
}

/**
 * Transfer strategies dictate how props are transferred by `transferPropsTo`.
 * NOTE: if you add any more exceptions to this list you should be sure to
 * update `cloneWithProps()` accordingly.
 */
var TransferStrategies = {
  /**
   * Never transfer `children`.
   */
  children: emptyFunction,
  /**
   * Transfer the `className` prop by merging them.
   */
  className: createTransferStrategy(joinClasses),
  /**
   * Never transfer the `key` prop.
   */
  key: emptyFunction,
  /**
   * Never transfer the `ref` prop.
   */
  ref: emptyFunction,
  /**
   * Transfer the `style` prop (which is an object) by merging them.
   */
  style: createTransferStrategy(merge)
};

/**
 * ReactPropTransferer are capable of transferring props to another component
 * using a `transferPropsTo` method.
 *
 * @class ReactPropTransferer
 */
var ReactPropTransferer = {

  TransferStrategies: TransferStrategies,

  /**
   * Merge two props objects using TransferStrategies.
   *
   * @param {object} oldProps original props (they take precedence)
   * @param {object} newProps new props to merge in
   * @return {object} a new object containing both sets of props merged.
   */
  mergeProps: function(oldProps, newProps) {
    var props = merge(oldProps);

    for (var thisKey in newProps) {
      if (!newProps.hasOwnProperty(thisKey)) {
        continue;
      }

      var transferStrategy = TransferStrategies[thisKey];

      if (transferStrategy && TransferStrategies.hasOwnProperty(thisKey)) {
        transferStrategy(props, thisKey, newProps[thisKey]);
      } else if (!props.hasOwnProperty(thisKey)) {
        props[thisKey] = newProps[thisKey];
      }
    }

    return props;
  },

  /**
   * @lends {ReactPropTransferer.prototype}
   */
  Mixin: {

    /**
     * Transfer props from this component to a target component.
     *
     * Props that do not have an explicit transfer strategy will be transferred
     * only if the target component does not already have the prop set.
     *
     * This is usually used to pass down props to a returned root component.
     *
     * @param {ReactComponent} component Component receiving the properties.
     * @return {ReactComponent} The supplied `component`.
     * @final
     * @protected
     */
    transferPropsTo: function(component) {
      ("production" !== "development" ? invariant(
        component._owner === this,
        '%s: You can\'t call transferPropsTo() on a component that you ' +
        'don\'t own, %s. This usually means you are calling ' +
        'transferPropsTo() on a component passed in as props or children.',
        this.constructor.displayName,
        component.constructor.displayName
      ) : invariant(component._owner === this));

      component.props = ReactPropTransferer.mergeProps(
        component.props,
        this.props
      );

      return component;
    }

  }
};

module.exports = ReactPropTransferer;

},{"./emptyFunction":96,"./invariant":112,"./joinClasses":117,"./merge":121}],62:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactPropTypeLocationNames
 */

"use strict";

var ReactPropTypeLocationNames = {};

if ("production" !== "development") {
  ReactPropTypeLocationNames = {
    prop: 'prop',
    context: 'context',
    childContext: 'child context'
  };
}

module.exports = ReactPropTypeLocationNames;

},{}],63:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactPropTypeLocations
 */

"use strict";

var keyMirror = _dereq_("./keyMirror");

var ReactPropTypeLocations = keyMirror({
  prop: null,
  context: null,
  childContext: null
});

module.exports = ReactPropTypeLocations;

},{"./keyMirror":118}],64:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactPropTypes
 */

"use strict";

var ReactComponent = _dereq_("./ReactComponent");
var ReactPropTypeLocationNames = _dereq_("./ReactPropTypeLocationNames");

var warning = _dereq_("./warning");
var createObjectFrom = _dereq_("./createObjectFrom");

/**
 * Collection of methods that allow declaration and validation of props that are
 * supplied to React components. Example usage:
 *
 *   var Props = require('ReactPropTypes');
 *   var MyArticle = React.createClass({
 *     propTypes: {
 *       // An optional string prop named "description".
 *       description: Props.string,
 *
 *       // A required enum prop named "category".
 *       category: Props.oneOf(['News','Photos']).isRequired,
 *
 *       // A prop named "dialog" that requires an instance of Dialog.
 *       dialog: Props.instanceOf(Dialog).isRequired
 *     },
 *     render: function() { ... }
 *   });
 *
 * A more formal specification of how these methods are used:
 *
 *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
 *   decl := ReactPropTypes.{type}(.isRequired)?
 *
 * Each and every declaration produces a function with the same signature. This
 * allows the creation of custom validation functions. For example:
 *
 *   var Props = require('ReactPropTypes');
 *   var MyLink = React.createClass({
 *     propTypes: {
 *       // An optional string or URI prop named "href".
 *       href: function(props, propName, componentName) {
 *         var propValue = props[propName];
 *         warning(
 *           propValue == null ||
 *           typeof propValue === 'string' ||
 *           propValue instanceof URI,
 *           'Invalid `%s` supplied to `%s`, expected string or URI.',
 *           propName,
 *           componentName
 *         );
 *       }
 *     },
 *     render: function() { ... }
 *   });
 *
 * @internal
 */
var Props = {

  array: createPrimitiveTypeChecker('array'),
  bool: createPrimitiveTypeChecker('boolean'),
  func: createPrimitiveTypeChecker('function'),
  number: createPrimitiveTypeChecker('number'),
  object: createPrimitiveTypeChecker('object'),
  string: createPrimitiveTypeChecker('string'),

  shape: createShapeTypeChecker,
  oneOf: createEnumTypeChecker,
  oneOfType: createUnionTypeChecker,
  arrayOf: createArrayOfTypeChecker,

  instanceOf: createInstanceTypeChecker,

  renderable: createRenderableTypeChecker(),

  component: createComponentTypeChecker(),

  any: createAnyTypeChecker()
};

var ANONYMOUS = '<<anonymous>>';

function isRenderable(propValue) {
  switch(typeof propValue) {
    case 'number':
    case 'string':
      return true;
    case 'object':
      if (Array.isArray(propValue)) {
        return propValue.every(isRenderable);
      }
      if (ReactComponent.isValidComponent(propValue)) {
        return true;
      }
      for (var k in propValue) {
        if (!isRenderable(propValue[k])) {
          return false;
        }
      }
      return true;
    default:
      return false;
  }
}

// Equivalent of typeof but with special handling for arrays
function getPropType(propValue) {
  var propType = typeof propValue;
  if (propType === 'object' && Array.isArray(propValue)) {
    return 'array';
  }
  return propType;
}

function createAnyTypeChecker() {
  function validateAnyType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    return true; // is always valid
  }
  return createChainableTypeChecker(validateAnyType);
}

function createPrimitiveTypeChecker(expectedType) {
  function validatePrimitiveType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var propType = getPropType(propValue);
    var isValid = propType === expectedType;
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` of type `%s` supplied to `%s`, expected `%s`.',
        ReactPropTypeLocationNames[location],
        propName,
        propType,
        componentName,
        expectedType
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validatePrimitiveType);
}

function createEnumTypeChecker(expectedValues) {
  var expectedEnum = createObjectFrom(expectedValues);
  function validateEnumType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var isValid = expectedEnum[propValue];
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` supplied to `%s`, expected one of %s.',
        ReactPropTypeLocationNames[location],
        propName,
        componentName,
        JSON.stringify(Object.keys(expectedEnum))
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validateEnumType);
}

function createShapeTypeChecker(shapeTypes) {
  function validateShapeType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var propType = getPropType(propValue);
    var isValid = propType === 'object';
    if (isValid) {
      for (var key in shapeTypes) {
        var checker = shapeTypes[key];
        if (checker && !checker(propValue, key, componentName, location)) {
          return false;
        }
      }
    }
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` of type `%s` supplied to `%s`, expected `object`.',
        ReactPropTypeLocationNames[location],
        propName,
        propType,
        componentName
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validateShapeType);
}

function createInstanceTypeChecker(expectedClass) {
  function validateInstanceType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var isValid = propValue instanceof expectedClass;
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` supplied to `%s`, expected instance of `%s`.',
        ReactPropTypeLocationNames[location],
        propName,
        componentName,
        expectedClass.name || ANONYMOUS
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validateInstanceType);
}

function createArrayOfTypeChecker(propTypeChecker) {
  function validateArrayType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var isValid = Array.isArray(propValue);
    if (isValid) {
      for (var i = 0; i < propValue.length; i++) {
        if (!propTypeChecker(propValue, i, componentName, location)) {
          return false;
        }
      }
    }
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` supplied to `%s`, expected an array.',
        ReactPropTypeLocationNames[location],
        propName,
        componentName
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validateArrayType);
}

function createRenderableTypeChecker() {
  function validateRenderableType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var isValid = isRenderable(propValue);
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` supplied to `%s`, expected a renderable prop.',
        ReactPropTypeLocationNames[location],
        propName,
        componentName
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validateRenderableType);
}

function createComponentTypeChecker() {
  function validateComponentType(
    shouldWarn, propValue, propName, componentName, location
  ) {
    var isValid = ReactComponent.isValidComponent(propValue);
    if (shouldWarn) {
      ("production" !== "development" ? warning(
        isValid,
        'Invalid %s `%s` supplied to `%s`, expected a React component.',
        ReactPropTypeLocationNames[location],
        propName,
        componentName
      ) : null);
    }
    return isValid;
  }
  return createChainableTypeChecker(validateComponentType);
}

function createUnionTypeChecker(arrayOfValidators) {
  return function(props, propName, componentName, location) {
    var isValid = false;
    for (var ii = 0; ii < arrayOfValidators.length; ii++) {
      var validate = arrayOfValidators[ii];
      if (typeof validate.weak === 'function') {
        validate = validate.weak;
      }
      if (validate(props, propName, componentName, location)) {
        isValid = true;
        break;
      }
    }
    ("production" !== "development" ? warning(
      isValid,
      'Invalid %s `%s` supplied to `%s`.',
      ReactPropTypeLocationNames[location],
      propName,
      componentName || ANONYMOUS
    ) : null);
    return isValid;
  };
}

function createChainableTypeChecker(validate) {
  function checkType(
    isRequired, shouldWarn, props, propName, componentName, location
  ) {
    var propValue = props[propName];
    if (propValue != null) {
      // Only validate if there is a value to check.
      return validate(
        shouldWarn,
        propValue,
        propName,
        componentName || ANONYMOUS,
        location
      );
    } else {
      var isValid = !isRequired;
      if (shouldWarn) {
        ("production" !== "development" ? warning(
          isValid,
          'Required %s `%s` was not specified in `%s`.',
          ReactPropTypeLocationNames[location],
          propName,
          componentName || ANONYMOUS
        ) : null);
      }
      return isValid;
    }
  }

  var checker = checkType.bind(null, false, true);
  checker.weak = checkType.bind(null, false, false);
  checker.isRequired = checkType.bind(null, true, true);
  checker.weak.isRequired = checkType.bind(null, true, false);
  checker.isRequired.weak = checker.weak.isRequired;

  return checker;
}

module.exports = Props;

},{"./ReactComponent":27,"./ReactPropTypeLocationNames":62,"./createObjectFrom":94,"./warning":134}],65:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactPutListenerQueue
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");

var mixInto = _dereq_("./mixInto");

function ReactPutListenerQueue() {
  this.listenersToPut = [];
}

mixInto(ReactPutListenerQueue, {
  enqueuePutListener: function(rootNodeID, propKey, propValue) {
    this.listenersToPut.push({
      rootNodeID: rootNodeID,
      propKey: propKey,
      propValue: propValue
    });
  },

  putListeners: function() {
    for (var i = 0; i < this.listenersToPut.length; i++) {
      var listenerToPut = this.listenersToPut[i];
      ReactEventEmitter.putListener(
        listenerToPut.rootNodeID,
        listenerToPut.propKey,
        listenerToPut.propValue
      );
    }
  },

  reset: function() {
    this.listenersToPut.length = 0;
  },

  destructor: function() {
    this.reset();
  }
});

PooledClass.addPoolingTo(ReactPutListenerQueue);

module.exports = ReactPutListenerQueue;

},{"./PooledClass":23,"./ReactEventEmitter":48,"./mixInto":124}],66:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactReconcileTransaction
 * @typechecks static-only
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");
var ReactEventEmitter = _dereq_("./ReactEventEmitter");
var ReactInputSelection = _dereq_("./ReactInputSelection");
var ReactMountReady = _dereq_("./ReactMountReady");
var ReactPutListenerQueue = _dereq_("./ReactPutListenerQueue");
var Transaction = _dereq_("./Transaction");

var mixInto = _dereq_("./mixInto");

/**
 * Ensures that, when possible, the selection range (currently selected text
 * input) is not disturbed by performing the transaction.
 */
var SELECTION_RESTORATION = {
  /**
   * @return {Selection} Selection information.
   */
  initialize: ReactInputSelection.getSelectionInformation,
  /**
   * @param {Selection} sel Selection information returned from `initialize`.
   */
  close: ReactInputSelection.restoreSelection
};

/**
 * Suppresses events (blur/focus) that could be inadvertently dispatched due to
 * high level DOM manipulations (like temporarily removing a text input from the
 * DOM).
 */
var EVENT_SUPPRESSION = {
  /**
   * @return {boolean} The enabled status of `ReactEventEmitter` before the
   * reconciliation.
   */
  initialize: function() {
    var currentlyEnabled = ReactEventEmitter.isEnabled();
    ReactEventEmitter.setEnabled(false);
    return currentlyEnabled;
  },

  /**
   * @param {boolean} previouslyEnabled Enabled status of `ReactEventEmitter`
   *   before the reconciliation occured. `close` restores the previous value.
   */
  close: function(previouslyEnabled) {
    ReactEventEmitter.setEnabled(previouslyEnabled);
  }
};

/**
 * Provides a `ReactMountReady` queue for collecting `onDOMReady` callbacks
 * during the performing of the transaction.
 */
var ON_DOM_READY_QUEUEING = {
  /**
   * Initializes the internal `onDOMReady` queue.
   */
  initialize: function() {
    this.reactMountReady.reset();
  },

  /**
   * After DOM is flushed, invoke all registered `onDOMReady` callbacks.
   */
  close: function() {
    this.reactMountReady.notifyAll();
  }
};

var PUT_LISTENER_QUEUEING = {
  initialize: function() {
    this.putListenerQueue.reset();
  },

  close: function() {
    this.putListenerQueue.putListeners();
  }
};

/**
 * Executed within the scope of the `Transaction` instance. Consider these as
 * being member methods, but with an implied ordering while being isolated from
 * each other.
 */
var TRANSACTION_WRAPPERS = [
  PUT_LISTENER_QUEUEING,
  SELECTION_RESTORATION,
  EVENT_SUPPRESSION,
  ON_DOM_READY_QUEUEING
];

/**
 * Currently:
 * - The order that these are listed in the transaction is critical:
 * - Suppresses events.
 * - Restores selection range.
 *
 * Future:
 * - Restore document/overflow scroll positions that were unintentionally
 *   modified via DOM insertions above the top viewport boundary.
 * - Implement/integrate with customized constraint based layout system and keep
 *   track of which dimensions must be remeasured.
 *
 * @class ReactReconcileTransaction
 */
function ReactReconcileTransaction() {
  this.reinitializeTransaction();
  // Only server-side rendering really needs this option (see
  // `ReactServerRendering`), but server-side uses
  // `ReactServerRenderingTransaction` instead. This option is here so that it's
  // accessible and defaults to false when `ReactDOMComponent` and
  // `ReactTextComponent` checks it in `mountComponent`.`
  this.renderToStaticMarkup = false;
  this.reactMountReady = ReactMountReady.getPooled(null);
  this.putListenerQueue = ReactPutListenerQueue.getPooled();
}

var Mixin = {
  /**
   * @see Transaction
   * @abstract
   * @final
   * @return {array<object>} List of operation wrap proceedures.
   *   TODO: convert to array<TransactionWrapper>
   */
  getTransactionWrappers: function() {
    return TRANSACTION_WRAPPERS;
  },

  /**
   * @return {object} The queue to collect `onDOMReady` callbacks with.
   *   TODO: convert to ReactMountReady
   */
  getReactMountReady: function() {
    return this.reactMountReady;
  },

  getPutListenerQueue: function() {
    return this.putListenerQueue;
  },

  /**
   * `PooledClass` looks for this, and will invoke this before allowing this
   * instance to be resused.
   */
  destructor: function() {
    ReactMountReady.release(this.reactMountReady);
    this.reactMountReady = null;

    ReactPutListenerQueue.release(this.putListenerQueue);
    this.putListenerQueue = null;
  }
};


mixInto(ReactReconcileTransaction, Transaction.Mixin);
mixInto(ReactReconcileTransaction, Mixin);

PooledClass.addPoolingTo(ReactReconcileTransaction);

module.exports = ReactReconcileTransaction;

},{"./PooledClass":23,"./ReactEventEmitter":48,"./ReactInputSelection":52,"./ReactMountReady":56,"./ReactPutListenerQueue":65,"./Transaction":85,"./mixInto":124}],67:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactRootIndex
 * @typechecks
 */

"use strict";

var ReactRootIndexInjection = {
  /**
   * @param {function} _createReactRootIndex
   */
  injectCreateReactRootIndex: function(_createReactRootIndex) {
    ReactRootIndex.createReactRootIndex = _createReactRootIndex;
  }
};

var ReactRootIndex = {
  createReactRootIndex: null,
  injection: ReactRootIndexInjection
};

module.exports = ReactRootIndex;

},{}],68:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @typechecks static-only
 * @providesModule ReactServerRendering
 */
"use strict";

var ReactComponent = _dereq_("./ReactComponent");
var ReactInstanceHandles = _dereq_("./ReactInstanceHandles");
var ReactMarkupChecksum = _dereq_("./ReactMarkupChecksum");
var ReactServerRenderingTransaction =
  _dereq_("./ReactServerRenderingTransaction");

var instantiateReactComponent = _dereq_("./instantiateReactComponent");
var invariant = _dereq_("./invariant");

/**
 * @param {ReactComponent} component
 * @return {string} the HTML markup
 */
function renderComponentToString(component) {
  ("production" !== "development" ? invariant(
    ReactComponent.isValidComponent(component),
    'renderComponentToString(): You must pass a valid ReactComponent.'
  ) : invariant(ReactComponent.isValidComponent(component)));

  ("production" !== "development" ? invariant(
    !(arguments.length === 2 && typeof arguments[1] === 'function'),
    'renderComponentToString(): This function became synchronous and now ' +
    'returns the generated markup. Please remove the second parameter.'
  ) : invariant(!(arguments.length === 2 && typeof arguments[1] === 'function')));

  var transaction;
  try {
    var id = ReactInstanceHandles.createReactRootID();
    transaction = ReactServerRenderingTransaction.getPooled(false);

    return transaction.perform(function() {
      var componentInstance = instantiateReactComponent(component);
      var markup = componentInstance.mountComponent(id, transaction, 0);
      return ReactMarkupChecksum.addChecksumToMarkup(markup);
    }, null);
  } finally {
    ReactServerRenderingTransaction.release(transaction);
  }
}

/**
 * @param {ReactComponent} component
 * @return {string} the HTML markup, without the extra React ID and checksum
* (for generating static pages)
 */
function renderComponentToStaticMarkup(component) {
  ("production" !== "development" ? invariant(
    ReactComponent.isValidComponent(component),
    'renderComponentToStaticMarkup(): You must pass a valid ReactComponent.'
  ) : invariant(ReactComponent.isValidComponent(component)));

  var transaction;
  try {
    var id = ReactInstanceHandles.createReactRootID();
    transaction = ReactServerRenderingTransaction.getPooled(true);

    return transaction.perform(function() {
      var componentInstance = instantiateReactComponent(component);
      return componentInstance.mountComponent(id, transaction, 0);
    }, null);
  } finally {
    ReactServerRenderingTransaction.release(transaction);
  }
}

module.exports = {
  renderComponentToString: renderComponentToString,
  renderComponentToStaticMarkup: renderComponentToStaticMarkup
};

},{"./ReactComponent":27,"./ReactInstanceHandles":53,"./ReactMarkupChecksum":54,"./ReactServerRenderingTransaction":69,"./instantiateReactComponent":111,"./invariant":112}],69:[function(_dereq_,module,exports){
/**
 * Copyright 2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactServerRenderingTransaction
 * @typechecks
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");
var ReactMountReady = _dereq_("./ReactMountReady");
var ReactPutListenerQueue = _dereq_("./ReactPutListenerQueue");
var Transaction = _dereq_("./Transaction");

var emptyFunction = _dereq_("./emptyFunction");
var mixInto = _dereq_("./mixInto");

/**
 * Provides a `ReactMountReady` queue for collecting `onDOMReady` callbacks
 * during the performing of the transaction.
 */
var ON_DOM_READY_QUEUEING = {
  /**
   * Initializes the internal `onDOMReady` queue.
   */
  initialize: function() {
    this.reactMountReady.reset();
  },

  close: emptyFunction
};

var PUT_LISTENER_QUEUEING = {
  initialize: function() {
    this.putListenerQueue.reset();
  },

  close: emptyFunction
};

/**
 * Executed within the scope of the `Transaction` instance. Consider these as
 * being member methods, but with an implied ordering while being isolated from
 * each other.
 */
var TRANSACTION_WRAPPERS = [
  PUT_LISTENER_QUEUEING,
  ON_DOM_READY_QUEUEING
];

/**
 * @class ReactServerRenderingTransaction
 * @param {boolean} renderToStaticMarkup
 */
function ReactServerRenderingTransaction(renderToStaticMarkup) {
  this.reinitializeTransaction();
  this.renderToStaticMarkup = renderToStaticMarkup;
  this.reactMountReady = ReactMountReady.getPooled(null);
  this.putListenerQueue = ReactPutListenerQueue.getPooled();
}

var Mixin = {
  /**
   * @see Transaction
   * @abstract
   * @final
   * @return {array} Empty list of operation wrap proceedures.
   */
  getTransactionWrappers: function() {
    return TRANSACTION_WRAPPERS;
  },

  /**
   * @return {object} The queue to collect `onDOMReady` callbacks with.
   *   TODO: convert to ReactMountReady
   */
  getReactMountReady: function() {
    return this.reactMountReady;
  },

  getPutListenerQueue: function() {
    return this.putListenerQueue;
  },

  /**
   * `PooledClass` looks for this, and will invoke this before allowing this
   * instance to be resused.
   */
  destructor: function() {
    ReactMountReady.release(this.reactMountReady);
    this.reactMountReady = null;

    ReactPutListenerQueue.release(this.putListenerQueue);
    this.putListenerQueue = null;
  }
};


mixInto(ReactServerRenderingTransaction, Transaction.Mixin);
mixInto(ReactServerRenderingTransaction, Mixin);

PooledClass.addPoolingTo(ReactServerRenderingTransaction);

module.exports = ReactServerRenderingTransaction;

},{"./PooledClass":23,"./ReactMountReady":56,"./ReactPutListenerQueue":65,"./Transaction":85,"./emptyFunction":96,"./mixInto":124}],70:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactTextComponent
 * @typechecks static-only
 */

"use strict";

var DOMPropertyOperations = _dereq_("./DOMPropertyOperations");
var ReactBrowserComponentMixin = _dereq_("./ReactBrowserComponentMixin");
var ReactComponent = _dereq_("./ReactComponent");

var escapeTextForBrowser = _dereq_("./escapeTextForBrowser");
var mixInto = _dereq_("./mixInto");

/**
 * Text nodes violate a couple assumptions that React makes about components:
 *
 *  - When mounting text into the DOM, adjacent text nodes are merged.
 *  - Text nodes cannot be assigned a React root ID.
 *
 * This component is used to wrap strings in elements so that they can undergo
 * the same reconciliation that is applied to elements.
 *
 * TODO: Investigate representing React components in the DOM with text nodes.
 *
 * @class ReactTextComponent
 * @extends ReactComponent
 * @internal
 */
var ReactTextComponent = function(initialText) {
  this.construct({text: initialText});
};

/**
 * Used to clone the text descriptor object before it's mounted.
 *
 * @param {object} props
 * @return {object} A new ReactTextComponent instance
 */
ReactTextComponent.ConvenienceConstructor = function(props) {
  return new ReactTextComponent(props.text);
};

mixInto(ReactTextComponent, ReactComponent.Mixin);
mixInto(ReactTextComponent, ReactBrowserComponentMixin);
mixInto(ReactTextComponent, {

  /**
   * Creates the markup for this text node. This node is not intended to have
   * any features besides containing text content.
   *
   * @param {string} rootID DOM ID of the root node.
   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
   * @param {number} mountDepth number of components in the owner hierarchy
   * @return {string} Markup for this text node.
   * @internal
   */
  mountComponent: function(rootID, transaction, mountDepth) {
    ReactComponent.Mixin.mountComponent.call(
      this,
      rootID,
      transaction,
      mountDepth
    );

    var escapedText = escapeTextForBrowser(this.props.text);

    if (transaction.renderToStaticMarkup) {
      // Normally we'd wrap this in a `span` for the reasons stated above, but
      // since this is a situation where React won't take over (static pages),
      // we can simply return the text as it is.
      return escapedText;
    }

    return (
      '<span ' + DOMPropertyOperations.createMarkupForID(rootID) + '>' +
        escapedText +
      '</span>'
    );
  },

  /**
   * Updates this component by updating the text content.
   *
   * @param {object} nextComponent Contains the next text content.
   * @param {ReactReconcileTransaction} transaction
   * @internal
   */
  receiveComponent: function(nextComponent, transaction) {
    var nextProps = nextComponent.props;
    if (nextProps.text !== this.props.text) {
      this.props.text = nextProps.text;
      ReactComponent.BackendIDOperations.updateTextContentByID(
        this._rootNodeID,
        nextProps.text
      );
    }
  }

});

// Expose the constructor on itself and the prototype for consistency with other
// descriptors.
ReactTextComponent.type = ReactTextComponent;
ReactTextComponent.prototype.type = ReactTextComponent;

module.exports = ReactTextComponent;

},{"./DOMPropertyOperations":9,"./ReactBrowserComponentMixin":25,"./ReactComponent":27,"./escapeTextForBrowser":98,"./mixInto":124}],71:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ReactUpdates
 */

"use strict";

var ReactPerf = _dereq_("./ReactPerf");

var invariant = _dereq_("./invariant");

var dirtyComponents = [];

var batchingStrategy = null;

function ensureBatchingStrategy() {
  ("production" !== "development" ? invariant(batchingStrategy, 'ReactUpdates: must inject a batching strategy') : invariant(batchingStrategy));
}

function batchedUpdates(callback, param) {
  ensureBatchingStrategy();
  batchingStrategy.batchedUpdates(callback, param);
}

/**
 * Array comparator for ReactComponents by owner depth
 *
 * @param {ReactComponent} c1 first component you're comparing
 * @param {ReactComponent} c2 second component you're comparing
 * @return {number} Return value usable by Array.prototype.sort().
 */
function mountDepthComparator(c1, c2) {
  return c1._mountDepth - c2._mountDepth;
}

function runBatchedUpdates() {
  // Since reconciling a component higher in the owner hierarchy usually (not
  // always -- see shouldComponentUpdate()) will reconcile children, reconcile
  // them before their children by sorting the array.

  dirtyComponents.sort(mountDepthComparator);

  for (var i = 0; i < dirtyComponents.length; i++) {
    // If a component is unmounted before pending changes apply, ignore them
    // TODO: Queue unmounts in the same list to avoid this happening at all
    var component = dirtyComponents[i];
    if (component.isMounted()) {
      // If performUpdateIfNecessary happens to enqueue any new updates, we
      // shouldn't execute the callbacks until the next render happens, so
      // stash the callbacks first
      var callbacks = component._pendingCallbacks;
      component._pendingCallbacks = null;
      component.performUpdateIfNecessary();
      if (callbacks) {
        for (var j = 0; j < callbacks.length; j++) {
          callbacks[j].call(component);
        }
      }
    }
  }
}

function clearDirtyComponents() {
  dirtyComponents.length = 0;
}

var flushBatchedUpdates = ReactPerf.measure(
  'ReactUpdates',
  'flushBatchedUpdates',
  function() {
    // Run these in separate functions so the JIT can optimize
    try {
      runBatchedUpdates();
    } finally {
      clearDirtyComponents();
    }
  }
);

/**
 * Mark a component as needing a rerender, adding an optional callback to a
 * list of functions which will be executed once the rerender occurs.
 */
function enqueueUpdate(component, callback) {
  ("production" !== "development" ? invariant(
    !callback || typeof callback === "function",
    'enqueueUpdate(...): You called `setProps`, `replaceProps`, ' +
    '`setState`, `replaceState`, or `forceUpdate` with a callback that ' +
    'isn\'t callable.'
  ) : invariant(!callback || typeof callback === "function"));
  ensureBatchingStrategy();

  if (!batchingStrategy.isBatchingUpdates) {
    component.performUpdateIfNecessary();
    callback && callback.call(component);
    return;
  }

  dirtyComponents.push(component);

  if (callback) {
    if (component._pendingCallbacks) {
      component._pendingCallbacks.push(callback);
    } else {
      component._pendingCallbacks = [callback];
    }
  }
}

var ReactUpdatesInjection = {
  injectBatchingStrategy: function(_batchingStrategy) {
    ("production" !== "development" ? invariant(
      _batchingStrategy,
      'ReactUpdates: must provide a batching strategy'
    ) : invariant(_batchingStrategy));
    ("production" !== "development" ? invariant(
      typeof _batchingStrategy.batchedUpdates === 'function',
      'ReactUpdates: must provide a batchedUpdates() function'
    ) : invariant(typeof _batchingStrategy.batchedUpdates === 'function'));
    ("production" !== "development" ? invariant(
      typeof _batchingStrategy.isBatchingUpdates === 'boolean',
      'ReactUpdates: must provide an isBatchingUpdates boolean attribute'
    ) : invariant(typeof _batchingStrategy.isBatchingUpdates === 'boolean'));
    batchingStrategy = _batchingStrategy;
  }
};

var ReactUpdates = {
  batchedUpdates: batchedUpdates,
  enqueueUpdate: enqueueUpdate,
  flushBatchedUpdates: flushBatchedUpdates,
  injection: ReactUpdatesInjection
};

module.exports = ReactUpdates;

},{"./ReactPerf":60,"./invariant":112}],72:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SelectEventPlugin
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventPropagators = _dereq_("./EventPropagators");
var ReactInputSelection = _dereq_("./ReactInputSelection");
var SyntheticEvent = _dereq_("./SyntheticEvent");

var getActiveElement = _dereq_("./getActiveElement");
var isTextInputElement = _dereq_("./isTextInputElement");
var keyOf = _dereq_("./keyOf");
var shallowEqual = _dereq_("./shallowEqual");

var topLevelTypes = EventConstants.topLevelTypes;

var eventTypes = {
  select: {
    phasedRegistrationNames: {
      bubbled: keyOf({onSelect: null}),
      captured: keyOf({onSelectCapture: null})
    },
    dependencies: [
      topLevelTypes.topBlur,
      topLevelTypes.topContextMenu,
      topLevelTypes.topFocus,
      topLevelTypes.topKeyDown,
      topLevelTypes.topMouseDown,
      topLevelTypes.topMouseUp,
      topLevelTypes.topSelectionChange
    ]
  }
};

var activeElement = null;
var activeElementID = null;
var lastSelection = null;
var mouseDown = false;

/**
 * Get an object which is a unique representation of the current selection.
 *
 * The return value will not be consistent across nodes or browsers, but
 * two identical selections on the same node will return identical objects.
 *
 * @param {DOMElement} node
 * @param {object}
 */
function getSelection(node) {
  if ('selectionStart' in node &&
      ReactInputSelection.hasSelectionCapabilities(node)) {
    return {
      start: node.selectionStart,
      end: node.selectionEnd
    };
  } else if (document.selection) {
    var range = document.selection.createRange();
    return {
      parentElement: range.parentElement(),
      text: range.text,
      top: range.boundingTop,
      left: range.boundingLeft
    };
  } else {
    var selection = window.getSelection();
    return {
      anchorNode: selection.anchorNode,
      anchorOffset: selection.anchorOffset,
      focusNode: selection.focusNode,
      focusOffset: selection.focusOffset
    };
  }
}

/**
 * Poll selection to see whether it's changed.
 *
 * @param {object} nativeEvent
 * @return {?SyntheticEvent}
 */
function constructSelectEvent(nativeEvent) {
  // Ensure we have the right element, and that the user is not dragging a
  // selection (this matches native `select` event behavior). In HTML5, select
  // fires only on input and textarea thus if there's no focused element we
  // won't dispatch.
  if (mouseDown ||
      activeElement == null ||
      activeElement != getActiveElement()) {
    return;
  }

  // Only fire when selection has actually changed.
  var currentSelection = getSelection(activeElement);
  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
    lastSelection = currentSelection;

    var syntheticEvent = SyntheticEvent.getPooled(
      eventTypes.select,
      activeElementID,
      nativeEvent
    );

    syntheticEvent.type = 'select';
    syntheticEvent.target = activeElement;

    EventPropagators.accumulateTwoPhaseDispatches(syntheticEvent);

    return syntheticEvent;
  }
}

/**
 * This plugin creates an `onSelect` event that normalizes select events
 * across form elements.
 *
 * Supported elements are:
 * - input (see `isTextInputElement`)
 * - textarea
 * - contentEditable
 *
 * This differs from native browser implementations in the following ways:
 * - Fires on contentEditable fields as well as inputs.
 * - Fires for collapsed selection.
 * - Fires after user input.
 */
var SelectEventPlugin = {

  eventTypes: eventTypes,

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {

    switch (topLevelType) {
      // Track the input node that has focus.
      case topLevelTypes.topFocus:
        if (isTextInputElement(topLevelTarget) ||
            topLevelTarget.contentEditable === 'true') {
          activeElement = topLevelTarget;
          activeElementID = topLevelTargetID;
          lastSelection = null;
        }
        break;
      case topLevelTypes.topBlur:
        activeElement = null;
        activeElementID = null;
        lastSelection = null;
        break;

      // Don't fire the event while the user is dragging. This matches the
      // semantics of the native select event.
      case topLevelTypes.topMouseDown:
        mouseDown = true;
        break;
      case topLevelTypes.topContextMenu:
      case topLevelTypes.topMouseUp:
        mouseDown = false;
        return constructSelectEvent(nativeEvent);

      // Chrome and IE fire non-standard event when selection is changed (and
      // sometimes when it hasn't).
      // Firefox doesn't support selectionchange, so check selection status
      // after each key entry. The selection changes after keydown and before
      // keyup, but we check on keydown as well in the case of holding down a
      // key, when multiple keydown events are fired but only one keyup is.
      case topLevelTypes.topSelectionChange:
      case topLevelTypes.topKeyDown:
      case topLevelTypes.topKeyUp:
        return constructSelectEvent(nativeEvent);
    }
  }
};

module.exports = SelectEventPlugin;

},{"./EventConstants":14,"./EventPropagators":19,"./ReactInputSelection":52,"./SyntheticEvent":78,"./getActiveElement":102,"./isTextInputElement":115,"./keyOf":119,"./shallowEqual":130}],73:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ServerReactRootIndex
 * @typechecks
 */

"use strict";

/**
 * Size of the reactRoot ID space. We generate random numbers for React root
 * IDs and if there's a collision the events and DOM update system will
 * get confused. In the future we need a way to generate GUIDs but for
 * now this will work on a smaller scale.
 */
var GLOBAL_MOUNT_POINT_MAX = Math.pow(2, 53);

var ServerReactRootIndex = {
  createReactRootIndex: function() {
    return Math.ceil(Math.random() * GLOBAL_MOUNT_POINT_MAX);
  }
};

module.exports = ServerReactRootIndex;

},{}],74:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SimpleEventPlugin
 */

"use strict";

var EventConstants = _dereq_("./EventConstants");
var EventPluginUtils = _dereq_("./EventPluginUtils");
var EventPropagators = _dereq_("./EventPropagators");
var SyntheticClipboardEvent = _dereq_("./SyntheticClipboardEvent");
var SyntheticEvent = _dereq_("./SyntheticEvent");
var SyntheticFocusEvent = _dereq_("./SyntheticFocusEvent");
var SyntheticKeyboardEvent = _dereq_("./SyntheticKeyboardEvent");
var SyntheticMouseEvent = _dereq_("./SyntheticMouseEvent");
var SyntheticDragEvent = _dereq_("./SyntheticDragEvent");
var SyntheticTouchEvent = _dereq_("./SyntheticTouchEvent");
var SyntheticUIEvent = _dereq_("./SyntheticUIEvent");
var SyntheticWheelEvent = _dereq_("./SyntheticWheelEvent");

var invariant = _dereq_("./invariant");
var keyOf = _dereq_("./keyOf");

var topLevelTypes = EventConstants.topLevelTypes;

var eventTypes = {
  blur: {
    phasedRegistrationNames: {
      bubbled: keyOf({onBlur: true}),
      captured: keyOf({onBlurCapture: true})
    }
  },
  click: {
    phasedRegistrationNames: {
      bubbled: keyOf({onClick: true}),
      captured: keyOf({onClickCapture: true})
    }
  },
  contextMenu: {
    phasedRegistrationNames: {
      bubbled: keyOf({onContextMenu: true}),
      captured: keyOf({onContextMenuCapture: true})
    }
  },
  copy: {
    phasedRegistrationNames: {
      bubbled: keyOf({onCopy: true}),
      captured: keyOf({onCopyCapture: true})
    }
  },
  cut: {
    phasedRegistrationNames: {
      bubbled: keyOf({onCut: true}),
      captured: keyOf({onCutCapture: true})
    }
  },
  doubleClick: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDoubleClick: true}),
      captured: keyOf({onDoubleClickCapture: true})
    }
  },
  drag: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDrag: true}),
      captured: keyOf({onDragCapture: true})
    }
  },
  dragEnd: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDragEnd: true}),
      captured: keyOf({onDragEndCapture: true})
    }
  },
  dragEnter: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDragEnter: true}),
      captured: keyOf({onDragEnterCapture: true})
    }
  },
  dragExit: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDragExit: true}),
      captured: keyOf({onDragExitCapture: true})
    }
  },
  dragLeave: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDragLeave: true}),
      captured: keyOf({onDragLeaveCapture: true})
    }
  },
  dragOver: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDragOver: true}),
      captured: keyOf({onDragOverCapture: true})
    }
  },
  dragStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDragStart: true}),
      captured: keyOf({onDragStartCapture: true})
    }
  },
  drop: {
    phasedRegistrationNames: {
      bubbled: keyOf({onDrop: true}),
      captured: keyOf({onDropCapture: true})
    }
  },
  focus: {
    phasedRegistrationNames: {
      bubbled: keyOf({onFocus: true}),
      captured: keyOf({onFocusCapture: true})
    }
  },
  input: {
    phasedRegistrationNames: {
      bubbled: keyOf({onInput: true}),
      captured: keyOf({onInputCapture: true})
    }
  },
  keyDown: {
    phasedRegistrationNames: {
      bubbled: keyOf({onKeyDown: true}),
      captured: keyOf({onKeyDownCapture: true})
    }
  },
  keyPress: {
    phasedRegistrationNames: {
      bubbled: keyOf({onKeyPress: true}),
      captured: keyOf({onKeyPressCapture: true})
    }
  },
  keyUp: {
    phasedRegistrationNames: {
      bubbled: keyOf({onKeyUp: true}),
      captured: keyOf({onKeyUpCapture: true})
    }
  },
  load: {
    phasedRegistrationNames: {
      bubbled: keyOf({onLoad: true}),
      captured: keyOf({onLoadCapture: true})
    }
  },
  error: {
    phasedRegistrationNames: {
      bubbled: keyOf({onError: true}),
      captured: keyOf({onErrorCapture: true})
    }
  },
  // Note: We do not allow listening to mouseOver events. Instead, use the
  // onMouseEnter/onMouseLeave created by `EnterLeaveEventPlugin`.
  mouseDown: {
    phasedRegistrationNames: {
      bubbled: keyOf({onMouseDown: true}),
      captured: keyOf({onMouseDownCapture: true})
    }
  },
  mouseMove: {
    phasedRegistrationNames: {
      bubbled: keyOf({onMouseMove: true}),
      captured: keyOf({onMouseMoveCapture: true})
    }
  },
  mouseOut: {
    phasedRegistrationNames: {
      bubbled: keyOf({onMouseOut: true}),
      captured: keyOf({onMouseOutCapture: true})
    }
  },
  mouseOver: {
    phasedRegistrationNames: {
      bubbled: keyOf({onMouseOver: true}),
      captured: keyOf({onMouseOverCapture: true})
    }
  },
  mouseUp: {
    phasedRegistrationNames: {
      bubbled: keyOf({onMouseUp: true}),
      captured: keyOf({onMouseUpCapture: true})
    }
  },
  paste: {
    phasedRegistrationNames: {
      bubbled: keyOf({onPaste: true}),
      captured: keyOf({onPasteCapture: true})
    }
  },
  reset: {
    phasedRegistrationNames: {
      bubbled: keyOf({onReset: true}),
      captured: keyOf({onResetCapture: true})
    }
  },
  scroll: {
    phasedRegistrationNames: {
      bubbled: keyOf({onScroll: true}),
      captured: keyOf({onScrollCapture: true})
    }
  },
  submit: {
    phasedRegistrationNames: {
      bubbled: keyOf({onSubmit: true}),
      captured: keyOf({onSubmitCapture: true})
    }
  },
  touchCancel: {
    phasedRegistrationNames: {
      bubbled: keyOf({onTouchCancel: true}),
      captured: keyOf({onTouchCancelCapture: true})
    }
  },
  touchEnd: {
    phasedRegistrationNames: {
      bubbled: keyOf({onTouchEnd: true}),
      captured: keyOf({onTouchEndCapture: true})
    }
  },
  touchMove: {
    phasedRegistrationNames: {
      bubbled: keyOf({onTouchMove: true}),
      captured: keyOf({onTouchMoveCapture: true})
    }
  },
  touchStart: {
    phasedRegistrationNames: {
      bubbled: keyOf({onTouchStart: true}),
      captured: keyOf({onTouchStartCapture: true})
    }
  },
  wheel: {
    phasedRegistrationNames: {
      bubbled: keyOf({onWheel: true}),
      captured: keyOf({onWheelCapture: true})
    }
  }
};

var topLevelEventsToDispatchConfig = {
  topBlur:        eventTypes.blur,
  topClick:       eventTypes.click,
  topContextMenu: eventTypes.contextMenu,
  topCopy:        eventTypes.copy,
  topCut:         eventTypes.cut,
  topDoubleClick: eventTypes.doubleClick,
  topDrag:        eventTypes.drag,
  topDragEnd:     eventTypes.dragEnd,
  topDragEnter:   eventTypes.dragEnter,
  topDragExit:    eventTypes.dragExit,
  topDragLeave:   eventTypes.dragLeave,
  topDragOver:    eventTypes.dragOver,
  topDragStart:   eventTypes.dragStart,
  topDrop:        eventTypes.drop,
  topError:       eventTypes.error,
  topFocus:       eventTypes.focus,
  topInput:       eventTypes.input,
  topKeyDown:     eventTypes.keyDown,
  topKeyPress:    eventTypes.keyPress,
  topKeyUp:       eventTypes.keyUp,
  topLoad:        eventTypes.load,
  topMouseDown:   eventTypes.mouseDown,
  topMouseMove:   eventTypes.mouseMove,
  topMouseOut:    eventTypes.mouseOut,
  topMouseOver:   eventTypes.mouseOver,
  topMouseUp:     eventTypes.mouseUp,
  topPaste:       eventTypes.paste,
  topReset:       eventTypes.reset,
  topScroll:      eventTypes.scroll,
  topSubmit:      eventTypes.submit,
  topTouchCancel: eventTypes.touchCancel,
  topTouchEnd:    eventTypes.touchEnd,
  topTouchMove:   eventTypes.touchMove,
  topTouchStart:  eventTypes.touchStart,
  topWheel:       eventTypes.wheel
};

for (var topLevelType in topLevelEventsToDispatchConfig) {
  topLevelEventsToDispatchConfig[topLevelType].dependencies = [topLevelType];
}

var SimpleEventPlugin = {

  eventTypes: eventTypes,

  /**
   * Same as the default implementation, except cancels the event when return
   * value is false.
   *
   * @param {object} Event to be dispatched.
   * @param {function} Application-level callback.
   * @param {string} domID DOM ID to pass to the callback.
   */
  executeDispatch: function(event, listener, domID) {
    var returnValue = EventPluginUtils.executeDispatch(event, listener, domID);
    if (returnValue === false) {
      event.stopPropagation();
      event.preventDefault();
    }
  },

  /**
   * @param {string} topLevelType Record from `EventConstants`.
   * @param {DOMEventTarget} topLevelTarget The listening component root node.
   * @param {string} topLevelTargetID ID of `topLevelTarget`.
   * @param {object} nativeEvent Native browser event.
   * @return {*} An accumulation of synthetic events.
   * @see {EventPluginHub.extractEvents}
   */
  extractEvents: function(
      topLevelType,
      topLevelTarget,
      topLevelTargetID,
      nativeEvent) {
    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
    if (!dispatchConfig) {
      return null;
    }
    var EventConstructor;
    switch (topLevelType) {
      case topLevelTypes.topInput:
      case topLevelTypes.topLoad:
      case topLevelTypes.topError:
      case topLevelTypes.topReset:
      case topLevelTypes.topSubmit:
        // HTML Events
        // @see http://www.w3.org/TR/html5/index.html#events-0
        EventConstructor = SyntheticEvent;
        break;
      case topLevelTypes.topKeyDown:
      case topLevelTypes.topKeyPress:
      case topLevelTypes.topKeyUp:
        EventConstructor = SyntheticKeyboardEvent;
        break;
      case topLevelTypes.topBlur:
      case topLevelTypes.topFocus:
        EventConstructor = SyntheticFocusEvent;
        break;
      case topLevelTypes.topClick:
        // Firefox creates a click event on right mouse clicks. This removes the
        // unwanted click events.
        if (nativeEvent.button === 2) {
          return null;
        }
        /* falls through */
      case topLevelTypes.topContextMenu:
      case topLevelTypes.topDoubleClick:
      case topLevelTypes.topMouseDown:
      case topLevelTypes.topMouseMove:
      case topLevelTypes.topMouseOut:
      case topLevelTypes.topMouseOver:
      case topLevelTypes.topMouseUp:
        EventConstructor = SyntheticMouseEvent;
        break;
      case topLevelTypes.topDrag:
      case topLevelTypes.topDragEnd:
      case topLevelTypes.topDragEnter:
      case topLevelTypes.topDragExit:
      case topLevelTypes.topDragLeave:
      case topLevelTypes.topDragOver:
      case topLevelTypes.topDragStart:
      case topLevelTypes.topDrop:
        EventConstructor = SyntheticDragEvent;
        break;
      case topLevelTypes.topTouchCancel:
      case topLevelTypes.topTouchEnd:
      case topLevelTypes.topTouchMove:
      case topLevelTypes.topTouchStart:
        EventConstructor = SyntheticTouchEvent;
        break;
      case topLevelTypes.topScroll:
        EventConstructor = SyntheticUIEvent;
        break;
      case topLevelTypes.topWheel:
        EventConstructor = SyntheticWheelEvent;
        break;
      case topLevelTypes.topCopy:
      case topLevelTypes.topCut:
      case topLevelTypes.topPaste:
        EventConstructor = SyntheticClipboardEvent;
        break;
    }
    ("production" !== "development" ? invariant(
      EventConstructor,
      'SimpleEventPlugin: Unhandled event type, `%s`.',
      topLevelType
    ) : invariant(EventConstructor));
    var event = EventConstructor.getPooled(
      dispatchConfig,
      topLevelTargetID,
      nativeEvent
    );
    EventPropagators.accumulateTwoPhaseDispatches(event);
    return event;
  }

};

module.exports = SimpleEventPlugin;

},{"./EventConstants":14,"./EventPluginUtils":18,"./EventPropagators":19,"./SyntheticClipboardEvent":75,"./SyntheticDragEvent":77,"./SyntheticEvent":78,"./SyntheticFocusEvent":79,"./SyntheticKeyboardEvent":80,"./SyntheticMouseEvent":81,"./SyntheticTouchEvent":82,"./SyntheticUIEvent":83,"./SyntheticWheelEvent":84,"./invariant":112,"./keyOf":119}],75:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticClipboardEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticEvent = _dereq_("./SyntheticEvent");

/**
 * @interface Event
 * @see http://www.w3.org/TR/clipboard-apis/
 */
var ClipboardEventInterface = {
  clipboardData: function(event) {
    return (
      'clipboardData' in event ?
        event.clipboardData :
        window.clipboardData
    );
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticClipboardEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticEvent.augmentClass(SyntheticClipboardEvent, ClipboardEventInterface);

module.exports = SyntheticClipboardEvent;


},{"./SyntheticEvent":78}],76:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticCompositionEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticEvent = _dereq_("./SyntheticEvent");

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
 */
var CompositionEventInterface = {
  data: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticCompositionEvent(
  dispatchConfig,
  dispatchMarker,
  nativeEvent) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticEvent.augmentClass(
  SyntheticCompositionEvent,
  CompositionEventInterface
);

module.exports = SyntheticCompositionEvent;


},{"./SyntheticEvent":78}],77:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticDragEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticMouseEvent = _dereq_("./SyntheticMouseEvent");

/**
 * @interface DragEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var DragEventInterface = {
  dataTransfer: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticDragEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticMouseEvent.augmentClass(SyntheticDragEvent, DragEventInterface);

module.exports = SyntheticDragEvent;

},{"./SyntheticMouseEvent":81}],78:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticEvent
 * @typechecks static-only
 */

"use strict";

var PooledClass = _dereq_("./PooledClass");

var emptyFunction = _dereq_("./emptyFunction");
var getEventTarget = _dereq_("./getEventTarget");
var merge = _dereq_("./merge");
var mergeInto = _dereq_("./mergeInto");

/**
 * @interface Event
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var EventInterface = {
  type: null,
  target: getEventTarget,
  // currentTarget is set when dispatching; no use in copying it here
  currentTarget: emptyFunction.thatReturnsNull,
  eventPhase: null,
  bubbles: null,
  cancelable: null,
  timeStamp: function(event) {
    return event.timeStamp || Date.now();
  },
  defaultPrevented: null,
  isTrusted: null
};

/**
 * Synthetic events are dispatched by event plugins, typically in response to a
 * top-level event delegation handler.
 *
 * These systems should generally use pooling to reduce the frequency of garbage
 * collection. The system should check `isPersistent` to determine whether the
 * event should be released into the pool after being dispatched. Users that
 * need a persisted event should invoke `persist`.
 *
 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
 * normalizing browser quirks. Subclasses do not necessarily have to implement a
 * DOM interface; custom application-specific events can also subclass this.
 *
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 */
function SyntheticEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  this.dispatchConfig = dispatchConfig;
  this.dispatchMarker = dispatchMarker;
  this.nativeEvent = nativeEvent;

  var Interface = this.constructor.Interface;
  for (var propName in Interface) {
    if (!Interface.hasOwnProperty(propName)) {
      continue;
    }
    var normalize = Interface[propName];
    if (normalize) {
      this[propName] = normalize(nativeEvent);
    } else {
      this[propName] = nativeEvent[propName];
    }
  }

  var defaultPrevented = nativeEvent.defaultPrevented != null ?
    nativeEvent.defaultPrevented :
    nativeEvent.returnValue === false;
  if (defaultPrevented) {
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  } else {
    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
  }
  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
}

mergeInto(SyntheticEvent.prototype, {

  preventDefault: function() {
    this.defaultPrevented = true;
    var event = this.nativeEvent;
    event.preventDefault ? event.preventDefault() : event.returnValue = false;
    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
  },

  stopPropagation: function() {
    var event = this.nativeEvent;
    event.stopPropagation ? event.stopPropagation() : event.cancelBubble = true;
    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
  },

  /**
   * We release all dispatched `SyntheticEvent`s after each event loop, adding
   * them back into the pool. This allows a way to hold onto a reference that
   * won't be added back into the pool.
   */
  persist: function() {
    this.isPersistent = emptyFunction.thatReturnsTrue;
  },

  /**
   * Checks if this event should be released back into the pool.
   *
   * @return {boolean} True if this should not be released, false otherwise.
   */
  isPersistent: emptyFunction.thatReturnsFalse,

  /**
   * `PooledClass` looks for `destructor` on each instance it releases.
   */
  destructor: function() {
    var Interface = this.constructor.Interface;
    for (var propName in Interface) {
      this[propName] = null;
    }
    this.dispatchConfig = null;
    this.dispatchMarker = null;
    this.nativeEvent = null;
  }

});

SyntheticEvent.Interface = EventInterface;

/**
 * Helper to reduce boilerplate when creating subclasses.
 *
 * @param {function} Class
 * @param {?object} Interface
 */
SyntheticEvent.augmentClass = function(Class, Interface) {
  var Super = this;

  var prototype = Object.create(Super.prototype);
  mergeInto(prototype, Class.prototype);
  Class.prototype = prototype;
  Class.prototype.constructor = Class;

  Class.Interface = merge(Super.Interface, Interface);
  Class.augmentClass = Super.augmentClass;

  PooledClass.addPoolingTo(Class, PooledClass.threeArgumentPooler);
};

PooledClass.addPoolingTo(SyntheticEvent, PooledClass.threeArgumentPooler);

module.exports = SyntheticEvent;

},{"./PooledClass":23,"./emptyFunction":96,"./getEventTarget":104,"./merge":121,"./mergeInto":123}],79:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticFocusEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticUIEvent = _dereq_("./SyntheticUIEvent");

/**
 * @interface FocusEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var FocusEventInterface = {
  relatedTarget: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticFocusEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticUIEvent.augmentClass(SyntheticFocusEvent, FocusEventInterface);

module.exports = SyntheticFocusEvent;

},{"./SyntheticUIEvent":83}],80:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticKeyboardEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticUIEvent = _dereq_("./SyntheticUIEvent");

var getEventKey = _dereq_("./getEventKey");

/**
 * @interface KeyboardEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var KeyboardEventInterface = {
  key: getEventKey,
  location: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  repeat: null,
  locale: null,
  // Legacy Interface
  'char': null,
  charCode: null,
  keyCode: null,
  which: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticKeyboardEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticUIEvent.augmentClass(SyntheticKeyboardEvent, KeyboardEventInterface);

module.exports = SyntheticKeyboardEvent;

},{"./SyntheticUIEvent":83,"./getEventKey":103}],81:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticMouseEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticUIEvent = _dereq_("./SyntheticUIEvent");
var ViewportMetrics = _dereq_("./ViewportMetrics");

/**
 * @interface MouseEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var MouseEventInterface = {
  screenX: null,
  screenY: null,
  clientX: null,
  clientY: null,
  ctrlKey: null,
  shiftKey: null,
  altKey: null,
  metaKey: null,
  button: function(event) {
    // Webkit, Firefox, IE9+
    // which:  1 2 3
    // button: 0 1 2 (standard)
    var button = event.button;
    if ('which' in event) {
      return button;
    }
    // IE<9
    // which:  undefined
    // button: 0 0 0
    // button: 1 4 2 (onmouseup)
    return button === 2 ? 2 : button === 4 ? 1 : 0;
  },
  buttons: null,
  relatedTarget: function(event) {
    return event.relatedTarget || (
      event.fromElement === event.srcElement ?
        event.toElement :
        event.fromElement
    );
  },
  // "Proprietary" Interface.
  pageX: function(event) {
    return 'pageX' in event ?
      event.pageX :
      event.clientX + ViewportMetrics.currentScrollLeft;
  },
  pageY: function(event) {
    return 'pageY' in event ?
      event.pageY :
      event.clientY + ViewportMetrics.currentScrollTop;
  }
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticMouseEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticUIEvent.augmentClass(SyntheticMouseEvent, MouseEventInterface);

module.exports = SyntheticMouseEvent;

},{"./SyntheticUIEvent":83,"./ViewportMetrics":86}],82:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticTouchEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticUIEvent = _dereq_("./SyntheticUIEvent");

/**
 * @interface TouchEvent
 * @see http://www.w3.org/TR/touch-events/
 */
var TouchEventInterface = {
  touches: null,
  targetTouches: null,
  changedTouches: null,
  altKey: null,
  metaKey: null,
  ctrlKey: null,
  shiftKey: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticUIEvent}
 */
function SyntheticTouchEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticUIEvent.augmentClass(SyntheticTouchEvent, TouchEventInterface);

module.exports = SyntheticTouchEvent;

},{"./SyntheticUIEvent":83}],83:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticUIEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticEvent = _dereq_("./SyntheticEvent");

/**
 * @interface UIEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var UIEventInterface = {
  view: null,
  detail: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticEvent}
 */
function SyntheticUIEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticEvent.augmentClass(SyntheticUIEvent, UIEventInterface);

module.exports = SyntheticUIEvent;

},{"./SyntheticEvent":78}],84:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule SyntheticWheelEvent
 * @typechecks static-only
 */

"use strict";

var SyntheticMouseEvent = _dereq_("./SyntheticMouseEvent");

/**
 * @interface WheelEvent
 * @see http://www.w3.org/TR/DOM-Level-3-Events/
 */
var WheelEventInterface = {
  deltaX: function(event) {
    return (
      'deltaX' in event ? event.deltaX :
      // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
      'wheelDeltaX' in event ? -event.wheelDeltaX : 0
    );
  },
  deltaY: function(event) {
    return (
      'deltaY' in event ? event.deltaY :
      // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
      'wheelDeltaY' in event ? -event.wheelDeltaY :
      // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
      'wheelDelta' in event ? -event.wheelDelta : 0
    );
  },
  deltaZ: null,

  // Browsers without "deltaMode" is reporting in raw wheel delta where one
  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
  deltaMode: null
};

/**
 * @param {object} dispatchConfig Configuration used to dispatch this event.
 * @param {string} dispatchMarker Marker identifying the event target.
 * @param {object} nativeEvent Native browser event.
 * @extends {SyntheticMouseEvent}
 */
function SyntheticWheelEvent(dispatchConfig, dispatchMarker, nativeEvent) {
  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
}

SyntheticMouseEvent.augmentClass(SyntheticWheelEvent, WheelEventInterface);

module.exports = SyntheticWheelEvent;

},{"./SyntheticMouseEvent":81}],85:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule Transaction
 */

"use strict";

var invariant = _dereq_("./invariant");

/**
 * `Transaction` creates a black box that is able to wrap any method such that
 * certain invariants are maintained before and after the method is invoked
 * (Even if an exception is thrown while invoking the wrapped method). Whoever
 * instantiates a transaction can provide enforcers of the invariants at
 * creation time. The `Transaction` class itself will supply one additional
 * automatic invariant for you - the invariant that any transaction instance
 * should not be run while it is already being run. You would typically create a
 * single instance of a `Transaction` for reuse multiple times, that potentially
 * is used to wrap several different methods. Wrappers are extremely simple -
 * they only require implementing two methods.
 *
 * <pre>
 *                       wrappers (injected at creation time)
 *                                      +        +
 *                                      |        |
 *                    +-----------------|--------|--------------+
 *                    |                 v        |              |
 *                    |      +---------------+   |              |
 *                    |   +--|    wrapper1   |---|----+         |
 *                    |   |  +---------------+   v    |         |
 *                    |   |          +-------------+  |         |
 *                    |   |     +----|   wrapper2  |--------+   |
 *                    |   |     |    +-------------+  |     |   |
 *                    |   |     |                     |     |   |
 *                    |   v     v                     v     v   | wrapper
 *                    | +---+ +---+   +---------+   +---+ +---+ | invariants
 * perform(anyMethod) | |   | |   |   |         |   |   | |   | | maintained
 * +----------------->|-|---|-|---|-->|anyMethod|---|---|-|---|-|-------->
 *                    | |   | |   |   |         |   |   | |   | |
 *                    | |   | |   |   |         |   |   | |   | |
 *                    | |   | |   |   |         |   |   | |   | |
 *                    | +---+ +---+   +---------+   +---+ +---+ |
 *                    |  initialize                    close    |
 *                    +-----------------------------------------+
 * </pre>
 *
 * Bonus:
 * - Reports timing metrics by method name and wrapper index.
 *
 * Use cases:
 * - Preserving the input selection ranges before/after reconciliation.
 *   Restoring selection even in the event of an unexpected error.
 * - Deactivating events while rearranging the DOM, preventing blurs/focuses,
 *   while guaranteeing that afterwards, the event system is reactivated.
 * - Flushing a queue of collected DOM mutations to the main UI thread after a
 *   reconciliation takes place in a worker thread.
 * - Invoking any collected `componentDidUpdate` callbacks after rendering new
 *   content.
 * - (Future use case): Wrapping particular flushes of the `ReactWorker` queue
 *   to preserve the `scrollTop` (an automatic scroll aware DOM).
 * - (Future use case): Layout calculations before and after DOM upates.
 *
 * Transactional plugin API:
 * - A module that has an `initialize` method that returns any precomputation.
 * - and a `close` method that accepts the precomputation. `close` is invoked
 *   when the wrapped process is completed, or has failed.
 *
 * @param {Array<TransactionalWrapper>} transactionWrapper Wrapper modules
 * that implement `initialize` and `close`.
 * @return {Transaction} Single transaction for reuse in thread.
 *
 * @class Transaction
 */
var Mixin = {
  /**
   * Sets up this instance so that it is prepared for collecting metrics. Does
   * so such that this setup method may be used on an instance that is already
   * initialized, in a way that does not consume additional memory upon reuse.
   * That can be useful if you decide to make your subclass of this mixin a
   * "PooledClass".
   */
  reinitializeTransaction: function() {
    this.transactionWrappers = this.getTransactionWrappers();
    if (!this.wrapperInitData) {
      this.wrapperInitData = [];
    } else {
      this.wrapperInitData.length = 0;
    }
    if (!this.timingMetrics) {
      this.timingMetrics = {};
    }
    this.timingMetrics.methodInvocationTime = 0;
    if (!this.timingMetrics.wrapperInitTimes) {
      this.timingMetrics.wrapperInitTimes = [];
    } else {
      this.timingMetrics.wrapperInitTimes.length = 0;
    }
    if (!this.timingMetrics.wrapperCloseTimes) {
      this.timingMetrics.wrapperCloseTimes = [];
    } else {
      this.timingMetrics.wrapperCloseTimes.length = 0;
    }
    this._isInTransaction = false;
  },

  _isInTransaction: false,

  /**
   * @abstract
   * @return {Array<TransactionWrapper>} Array of transaction wrappers.
   */
  getTransactionWrappers: null,

  isInTransaction: function() {
    return !!this._isInTransaction;
  },

  /**
   * Executes the function within a safety window. Use this for the top level
   * methods that result in large amounts of computation/mutations that would
   * need to be safety checked.
   *
   * @param {function} method Member of scope to call.
   * @param {Object} scope Scope to invoke from.
   * @param {Object?=} args... Arguments to pass to the method (optional).
   *                           Helps prevent need to bind in many cases.
   * @return Return value from `method`.
   */
  perform: function(method, scope, a, b, c, d, e, f) {
    ("production" !== "development" ? invariant(
      !this.isInTransaction(),
      'Transaction.perform(...): Cannot initialize a transaction when there ' +
      'is already an outstanding transaction.'
    ) : invariant(!this.isInTransaction()));
    var memberStart = Date.now();
    var errorThrown;
    var ret;
    try {
      this._isInTransaction = true;
      // Catching errors makes debugging more difficult, so we start with
      // errorThrown set to true before setting it to false after calling
      // close -- if it's still set to true in the finally block, it means
      // one of these calls threw.
      errorThrown = true;
      this.initializeAll(0);
      ret = method.call(scope, a, b, c, d, e, f);
      errorThrown = false;
    } finally {
      var memberEnd = Date.now();
      this.methodInvocationTime += (memberEnd - memberStart);
      try {
        if (errorThrown) {
          // If `method` throws, prefer to show that stack trace over any thrown
          // by invoking `closeAll`.
          try {
            this.closeAll(0);
          } catch (err) {
          }
        } else {
          // Since `method` didn't throw, we don't want to silence the exception
          // here.
          this.closeAll(0);
        }
      } finally {
        this._isInTransaction = false;
      }
    }
    return ret;
  },

  initializeAll: function(startIndex) {
    var transactionWrappers = this.transactionWrappers;
    var wrapperInitTimes = this.timingMetrics.wrapperInitTimes;
    for (var i = startIndex; i < transactionWrappers.length; i++) {
      var initStart = Date.now();
      var wrapper = transactionWrappers[i];
      try {
        // Catching errors makes debugging more difficult, so we start with the
        // OBSERVED_ERROR state before overwriting it with the real return value
        // of initialize -- if it's still set to OBSERVED_ERROR in the finally
        // block, it means wrapper.initialize threw.
        this.wrapperInitData[i] = Transaction.OBSERVED_ERROR;
        this.wrapperInitData[i] = wrapper.initialize ?
          wrapper.initialize.call(this) :
          null;
      } finally {
        var curInitTime = wrapperInitTimes[i];
        var initEnd = Date.now();
        wrapperInitTimes[i] = (curInitTime || 0) + (initEnd - initStart);

        if (this.wrapperInitData[i] === Transaction.OBSERVED_ERROR) {
          // The initializer for wrapper i threw an error; initialize the
          // remaining wrappers but silence any exceptions from them to ensure
          // that the first error is the one to bubble up.
          try {
            this.initializeAll(i + 1);
          } catch (err) {
          }
        }
      }
    }
  },

  /**
   * Invokes each of `this.transactionWrappers.close[i]` functions, passing into
   * them the respective return values of `this.transactionWrappers.init[i]`
   * (`close`rs that correspond to initializers that failed will not be
   * invoked).
   */
  closeAll: function(startIndex) {
    ("production" !== "development" ? invariant(
      this.isInTransaction(),
      'Transaction.closeAll(): Cannot close transaction when none are open.'
    ) : invariant(this.isInTransaction()));
    var transactionWrappers = this.transactionWrappers;
    var wrapperCloseTimes = this.timingMetrics.wrapperCloseTimes;
    for (var i = startIndex; i < transactionWrappers.length; i++) {
      var wrapper = transactionWrappers[i];
      var closeStart = Date.now();
      var initData = this.wrapperInitData[i];
      var errorThrown;
      try {
        // Catching errors makes debugging more difficult, so we start with
        // errorThrown set to true before setting it to false after calling
        // close -- if it's still set to true in the finally block, it means
        // wrapper.close threw.
        errorThrown = true;
        if (initData !== Transaction.OBSERVED_ERROR) {
          wrapper.close && wrapper.close.call(this, initData);
        }
        errorThrown = false;
      } finally {
        var closeEnd = Date.now();
        var curCloseTime = wrapperCloseTimes[i];
        wrapperCloseTimes[i] = (curCloseTime || 0) + (closeEnd - closeStart);

        if (errorThrown) {
          // The closer for wrapper i threw an error; close the remaining
          // wrappers but silence any exceptions from them to ensure that the
          // first error is the one to bubble up.
          try {
            this.closeAll(i + 1);
          } catch (e) {
          }
        }
      }
    }
    this.wrapperInitData.length = 0;
  }
};

var Transaction = {

  Mixin: Mixin,

  /**
   * Token to look for to determine if an error occured.
   */
  OBSERVED_ERROR: {}

};

module.exports = Transaction;

},{"./invariant":112}],86:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule ViewportMetrics
 */

"use strict";

var getUnboundedScrollPosition = _dereq_("./getUnboundedScrollPosition");

var ViewportMetrics = {

  currentScrollLeft: 0,

  currentScrollTop: 0,

  refreshScrollValues: function() {
    var scrollPosition = getUnboundedScrollPosition(window);
    ViewportMetrics.currentScrollLeft = scrollPosition.x;
    ViewportMetrics.currentScrollTop = scrollPosition.y;
  }

};

module.exports = ViewportMetrics;

},{"./getUnboundedScrollPosition":109}],87:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule accumulate
 */

"use strict";

var invariant = _dereq_("./invariant");

/**
 * Accumulates items that must not be null or undefined.
 *
 * This is used to conserve memory by avoiding array allocations.
 *
 * @return {*|array<*>} An accumulation of items.
 */
function accumulate(current, next) {
  ("production" !== "development" ? invariant(
    next != null,
    'accumulate(...): Accumulated items must be not be null or undefined.'
  ) : invariant(next != null));
  if (current == null) {
    return next;
  } else {
    // Both are not empty. Warning: Never call x.concat(y) when you are not
    // certain that x is an Array (x could be a string with concat method).
    var currentIsArray = Array.isArray(current);
    var nextIsArray = Array.isArray(next);
    if (currentIsArray) {
      return current.concat(next);
    } else {
      if (nextIsArray) {
        return [current].concat(next);
      } else {
        return [current, next];
      }
    }
  }
}

module.exports = accumulate;

},{"./invariant":112}],88:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule adler32
 */

/* jslint bitwise:true */

"use strict";

var MOD = 65521;

// This is a clean-room implementation of adler32 designed for detecting
// if markup is not what we expect it to be. It does not need to be
// cryptographically strong, only reasonable good at detecting if markup
// generated on the server is different than that on the client.
function adler32(data) {
  var a = 1;
  var b = 0;
  for (var i = 0; i < data.length; i++) {
    a = (a + data.charCodeAt(i)) % MOD;
    b = (b + a) % MOD;
  }
  return a | (b << 16);
}

module.exports = adler32;

},{}],89:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule containsNode
 * @typechecks
 */

var isTextNode = _dereq_("./isTextNode");

/*jslint bitwise:true */

/**
 * Checks if a given DOM node contains or is another DOM node.
 *
 * @param {?DOMNode} outerNode Outer DOM node.
 * @param {?DOMNode} innerNode Inner DOM node.
 * @return {boolean} True if `outerNode` contains or is `innerNode`.
 */
function containsNode(outerNode, innerNode) {
  if (!outerNode || !innerNode) {
    return false;
  } else if (outerNode === innerNode) {
    return true;
  } else if (isTextNode(outerNode)) {
    return false;
  } else if (isTextNode(innerNode)) {
    return containsNode(outerNode, innerNode.parentNode);
  } else if (outerNode.contains) {
    return outerNode.contains(innerNode);
  } else if (outerNode.compareDocumentPosition) {
    return !!(outerNode.compareDocumentPosition(innerNode) & 16);
  } else {
    return false;
  }
}

module.exports = containsNode;

},{"./isTextNode":116}],90:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule copyProperties
 */

/**
 * Copy properties from one or more objects (up to 5) into the first object.
 * This is a shallow copy. It mutates the first object and also returns it.
 *
 * NOTE: `arguments` has a very significant performance penalty, which is why
 * we don't support unlimited arguments.
 */
function copyProperties(obj, a, b, c, d, e, f) {
  obj = obj || {};

  if ("production" !== "development") {
    if (f) {
      throw new Error('Too many arguments passed to copyProperties');
    }
  }

  var args = [a, b, c, d, e];
  var ii = 0, v;
  while (args[ii]) {
    v = args[ii++];
    for (var k in v) {
      obj[k] = v[k];
    }

    // IE ignores toString in object iteration.. See:
    // webreflection.blogspot.com/2007/07/quick-fix-internet-explorer-and.html
    if (v.hasOwnProperty && v.hasOwnProperty('toString') &&
        (typeof v.toString != 'undefined') && (obj.toString !== v.toString)) {
      obj.toString = v.toString;
    }
  }

  return obj;
}

module.exports = copyProperties;

},{}],91:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule createArrayFrom
 * @typechecks
 */

var toArray = _dereq_("./toArray");

/**
 * Perform a heuristic test to determine if an object is "array-like".
 *
 *   A monk asked Joshu, a Zen master, "Has a dog Buddha nature?"
 *   Joshu replied: "Mu."
 *
 * This function determines if its argument has "array nature": it returns
 * true if the argument is an actual array, an `arguments' object, or an
 * HTMLCollection (e.g. node.childNodes or node.getElementsByTagName()).
 *
 * It will return false for other array-like objects like Filelist.
 *
 * @param {*} obj
 * @return {boolean}
 */
function hasArrayNature(obj) {
  return (
    // not null/false
    !!obj &&
    // arrays are objects, NodeLists are functions in Safari
    (typeof obj == 'object' || typeof obj == 'function') &&
    // quacks like an array
    ('length' in obj) &&
    // not window
    !('setInterval' in obj) &&
    // no DOM node should be considered an array-like
    // a 'select' element has 'length' and 'item' properties on IE8
    (typeof obj.nodeType != 'number') &&
    (
      // a real array
      (// HTMLCollection/NodeList
      (Array.isArray(obj) ||
      // arguments
      ('callee' in obj) || 'item' in obj))
    )
  );
}

/**
 * Ensure that the argument is an array by wrapping it in an array if it is not.
 * Creates a copy of the argument if it is already an array.
 *
 * This is mostly useful idiomatically:
 *
 *   var createArrayFrom = require('createArrayFrom');
 *
 *   function takesOneOrMoreThings(things) {
 *     things = createArrayFrom(things);
 *     ...
 *   }
 *
 * This allows you to treat `things' as an array, but accept scalars in the API.
 *
 * If you need to convert an array-like object, like `arguments`, into an array
 * use toArray instead.
 *
 * @param {*} obj
 * @return {array}
 */
function createArrayFrom(obj) {
  if (!hasArrayNature(obj)) {
    return [obj];
  } else if (Array.isArray(obj)) {
    return obj.slice();
  } else {
    return toArray(obj);
  }
}

module.exports = createArrayFrom;

},{"./toArray":132}],92:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule createFullPageComponent
 * @typechecks
 */

"use strict";

// Defeat circular references by requiring this directly.
var ReactCompositeComponent = _dereq_("./ReactCompositeComponent");

var invariant = _dereq_("./invariant");

/**
 * Create a component that will throw an exception when unmounted.
 *
 * Components like <html> <head> and <body> can't be removed or added
 * easily in a cross-browser way, however it's valuable to be able to
 * take advantage of React's reconciliation for styling and <title>
 * management. So we just document it and throw in dangerous cases.
 *
 * @param {function} componentClass convenience constructor to wrap
 * @return {function} convenience constructor of new component
 */
function createFullPageComponent(componentClass) {
  var FullPageComponent = ReactCompositeComponent.createClass({
    displayName: 'ReactFullPageComponent' + (
      componentClass.componentConstructor.displayName || ''
    ),

    componentWillUnmount: function() {
      ("production" !== "development" ? invariant(
        false,
        '%s tried to unmount. Because of cross-browser quirks it is ' +
        'impossible to unmount some top-level components (eg <html>, <head>, ' +
        'and <body>) reliably and efficiently. To fix this, have a single ' +
        'top-level component that never unmounts render these elements.',
        this.constructor.displayName
      ) : invariant(false));
    },

    render: function() {
      return this.transferPropsTo(componentClass(null, this.props.children));
    }
  });

  return FullPageComponent;
}

module.exports = createFullPageComponent;

},{"./ReactCompositeComponent":29,"./invariant":112}],93:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule createNodesFromMarkup
 * @typechecks
 */

/*jslint evil: true, sub: true */

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var createArrayFrom = _dereq_("./createArrayFrom");
var getMarkupWrap = _dereq_("./getMarkupWrap");
var invariant = _dereq_("./invariant");

/**
 * Dummy container used to render all markup.
 */
var dummyNode =
  ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

/**
 * Pattern used by `getNodeName`.
 */
var nodeNamePattern = /^\s*<(\w+)/;

/**
 * Extracts the `nodeName` of the first element in a string of markup.
 *
 * @param {string} markup String of markup.
 * @return {?string} Node name of the supplied markup.
 */
function getNodeName(markup) {
  var nodeNameMatch = markup.match(nodeNamePattern);
  return nodeNameMatch && nodeNameMatch[1].toLowerCase();
}

/**
 * Creates an array containing the nodes rendered from the supplied markup. The
 * optionally supplied `handleScript` function will be invoked once for each
 * <script> element that is rendered. If no `handleScript` function is supplied,
 * an exception is thrown if any <script> elements are rendered.
 *
 * @param {string} markup A string of valid HTML markup.
 * @param {?function} handleScript Invoked once for each rendered <script>.
 * @return {array<DOMElement|DOMTextNode>} An array of rendered nodes.
 */
function createNodesFromMarkup(markup, handleScript) {
  var node = dummyNode;
  ("production" !== "development" ? invariant(!!dummyNode, 'createNodesFromMarkup dummy not initialized') : invariant(!!dummyNode));
  var nodeName = getNodeName(markup);

  var wrap = nodeName && getMarkupWrap(nodeName);
  if (wrap) {
    node.innerHTML = wrap[1] + markup + wrap[2];

    var wrapDepth = wrap[0];
    while (wrapDepth--) {
      node = node.lastChild;
    }
  } else {
    node.innerHTML = markup;
  }

  var scripts = node.getElementsByTagName('script');
  if (scripts.length) {
    ("production" !== "development" ? invariant(
      handleScript,
      'createNodesFromMarkup(...): Unexpected <script> element rendered.'
    ) : invariant(handleScript));
    createArrayFrom(scripts).forEach(handleScript);
  }

  var nodes = createArrayFrom(node.childNodes);
  while (node.lastChild) {
    node.removeChild(node.lastChild);
  }
  return nodes;
}

module.exports = createNodesFromMarkup;

},{"./ExecutionEnvironment":20,"./createArrayFrom":91,"./getMarkupWrap":105,"./invariant":112}],94:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule createObjectFrom
 */

/**
 * Construct an object from an array of keys
 * and optionally specified value or list of values.
 *
 *  >>> createObjectFrom(['a','b','c']);
 *  {a: true, b: true, c: true}
 *
 *  >>> createObjectFrom(['a','b','c'], false);
 *  {a: false, b: false, c: false}
 *
 *  >>> createObjectFrom(['a','b','c'], 'monkey');
 *  {c:'monkey', b:'monkey' c:'monkey'}
 *
 *  >>> createObjectFrom(['a','b','c'], [1,2,3]);
 *  {a: 1, b: 2, c: 3}
 *
 *  >>> createObjectFrom(['women', 'men'], [true, false]);
 *  {women: true, men: false}
 *
 * @param   Array   list of keys
 * @param   mixed   optional value or value array.  defaults true.
 * @returns object
 */
function createObjectFrom(keys, values /* = true */) {
  if ("production" !== "development") {
    if (!Array.isArray(keys)) {
      throw new TypeError('Must pass an array of keys.');
    }
  }

  var object = {};
  var isArray = Array.isArray(values);
  if (typeof values == 'undefined') {
    values = true;
  }

  for (var ii = keys.length; ii--;) {
    object[keys[ii]] = isArray ? values[ii] : values;
  }
  return object;
}

module.exports = createObjectFrom;

},{}],95:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule dangerousStyleValue
 * @typechecks static-only
 */

"use strict";

var CSSProperty = _dereq_("./CSSProperty");

/**
 * Convert a value into the proper css writable value. The `styleName` name
 * name should be logical (no hyphens), as specified
 * in `CSSProperty.isUnitlessNumber`.
 *
 * @param {string} styleName CSS property name such as `topMargin`.
 * @param {*} value CSS property value such as `10px`.
 * @return {string} Normalized style value with dimensions applied.
 */
function dangerousStyleValue(styleName, value) {
  // Note that we've removed escapeTextForBrowser() calls here since the
  // whole string will be escaped when the attribute is injected into
  // the markup. If you provide unsafe user data here they can inject
  // arbitrary CSS which may be problematic (I couldn't repro this):
  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
  // This is not an XSS hole but instead a potential CSS injection issue
  // which has lead to a greater discussion about how we're going to
  // trust URLs moving forward. See #2115901

  var isEmpty = value == null || typeof value === 'boolean' || value === '';
  if (isEmpty) {
    return '';
  }

  var isNonNumeric = isNaN(value);
  if (isNonNumeric || value === 0 || CSSProperty.isUnitlessNumber[styleName]) {
    return '' + value; // cast to string
  }

  return value + 'px';
}

module.exports = dangerousStyleValue;

},{"./CSSProperty":2}],96:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule emptyFunction
 */

var copyProperties = _dereq_("./copyProperties");

function makeEmptyFunction(arg) {
  return function() {
    return arg;
  };
}

/**
 * This function accepts and discards inputs; it has no side effects. This is
 * primarily useful idiomatically for overridable function endpoints which
 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
 */
function emptyFunction() {}

copyProperties(emptyFunction, {
  thatReturns: makeEmptyFunction,
  thatReturnsFalse: makeEmptyFunction(false),
  thatReturnsTrue: makeEmptyFunction(true),
  thatReturnsNull: makeEmptyFunction(null),
  thatReturnsThis: function() { return this; },
  thatReturnsArgument: function(arg) { return arg; }
});

module.exports = emptyFunction;

},{"./copyProperties":90}],97:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule emptyObject
 */

"use strict";

var emptyObject = {};

if ("production" !== "development") {
  Object.freeze(emptyObject);
}

module.exports = emptyObject;

},{}],98:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule escapeTextForBrowser
 * @typechecks static-only
 */

"use strict";

var ESCAPE_LOOKUP = {
  "&": "&amp;",
  ">": "&gt;",
  "<": "&lt;",
  "\"": "&quot;",
  "'": "&#x27;",
  "/": "&#x2f;"
};

var ESCAPE_REGEX = /[&><"'\/]/g;

function escaper(match) {
  return ESCAPE_LOOKUP[match];
}

/**
 * Escapes text to prevent scripting attacks.
 *
 * @param {*} text Text value to escape.
 * @return {string} An escaped string.
 */
function escapeTextForBrowser(text) {
  return ('' + text).replace(ESCAPE_REGEX, escaper);
}

module.exports = escapeTextForBrowser;

},{}],99:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule flattenChildren
 */

"use strict";

var invariant = _dereq_("./invariant");
var traverseAllChildren = _dereq_("./traverseAllChildren");

/**
 * @param {function} traverseContext Context passed through traversal.
 * @param {?ReactComponent} child React child component.
 * @param {!string} name String name of key path to child.
 */
function flattenSingleChildIntoContext(traverseContext, child, name) {
  // We found a component instance.
  var result = traverseContext;
  ("production" !== "development" ? invariant(
    !result.hasOwnProperty(name),
    'flattenChildren(...): Encountered two children with the same key, `%s`. ' +
    'Children keys must be unique.',
    name
  ) : invariant(!result.hasOwnProperty(name)));
  if (child != null) {
    result[name] = child;
  }
}

/**
 * Flattens children that are typically specified as `props.children`. Any null
 * children will not be included in the resulting object.
 * @return {!object} flattened children keyed by name.
 */
function flattenChildren(children) {
  if (children == null) {
    return children;
  }
  var result = {};
  traverseAllChildren(children, flattenSingleChildIntoContext, result);
  return result;
}

module.exports = flattenChildren;

},{"./invariant":112,"./traverseAllChildren":133}],100:[function(_dereq_,module,exports){
/**
 * Copyright 2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule focusNode
 */

"use strict";

/**
 * IE8 throws if an input/textarea is disabled and we try to focus it.
 * Focus only when necessary.
 *
 * @param {DOMElement} node input/textarea to focus
 */
function focusNode(node) {
  if (!node.disabled) {
    node.focus();
  }
}

module.exports = focusNode;

},{}],101:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule forEachAccumulated
 */

"use strict";

/**
 * @param {array} an "accumulation" of items which is either an Array or
 * a single item. Useful when paired with the `accumulate` module. This is a
 * simple utility that allows us to reason about a collection of items, but
 * handling the case when there is exactly one item (and we do not need to
 * allocate an array).
 */
var forEachAccumulated = function(arr, cb, scope) {
  if (Array.isArray(arr)) {
    arr.forEach(cb, scope);
  } else if (arr) {
    cb.call(scope, arr);
  }
};

module.exports = forEachAccumulated;

},{}],102:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getActiveElement
 * @typechecks
 */

/**
 * Same as document.activeElement but wraps in a try-catch block. In IE it is
 * not safe to call document.activeElement if there is nothing focused.
 *
 * The activeElement will be null only if the document body is not yet defined.
 */
function getActiveElement() /*?DOMElement*/ {
  try {
    return document.activeElement || document.body;
  } catch (e) {
    return document.body;
  }
}

module.exports = getActiveElement;

},{}],103:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getEventKey
 * @typechecks static-only
 */

"use strict";

/**
 * Normalization of deprecated HTML5 "key" values
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var normalizeKey = {
  'Esc': 'Escape',
  'Spacebar': ' ',
  'Left': 'ArrowLeft',
  'Up': 'ArrowUp',
  'Right': 'ArrowRight',
  'Down': 'ArrowDown',
  'Del': 'Delete',
  'Win': 'OS',
  'Menu': 'ContextMenu',
  'Apps': 'ContextMenu',
  'Scroll': 'ScrollLock',
  'MozPrintableKey': 'Unidentified'
};

/**
 * Translation from legacy "which/keyCode" to HTML5 "key"
 * Only special keys supported, all others depend on keyboard layout or browser
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
 */
var translateToKey = {
  8: 'Backspace',
  9: 'Tab',
  12: 'Clear',
  13: 'Enter',
  16: 'Shift',
  17: 'Control',
  18: 'Alt',
  19: 'Pause',
  20: 'CapsLock',
  27: 'Escape',
  32: ' ',
  33: 'PageUp',
  34: 'PageDown',
  35: 'End',
  36: 'Home',
  37: 'ArrowLeft',
  38: 'ArrowUp',
  39: 'ArrowRight',
  40: 'ArrowDown',
  45: 'Insert',
  46: 'Delete',
  112: 'F1', 113: 'F2', 114: 'F3', 115: 'F4', 116: 'F5', 117: 'F6',
  118: 'F7', 119: 'F8', 120: 'F9', 121: 'F10', 122: 'F11', 123: 'F12',
  144: 'NumLock',
  145: 'ScrollLock',
  224: 'Meta'
};

/**
 * @param {object} nativeEvent Native browser event.
 * @return {string} Normalized `key` property.
 */
function getEventKey(nativeEvent) {
  return 'key' in nativeEvent ?
    normalizeKey[nativeEvent.key] || nativeEvent.key :
    translateToKey[nativeEvent.which || nativeEvent.keyCode] || 'Unidentified';
}

module.exports = getEventKey;

},{}],104:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getEventTarget
 * @typechecks static-only
 */

"use strict";

/**
 * Gets the target node from a native browser event by accounting for
 * inconsistencies in browser DOM APIs.
 *
 * @param {object} nativeEvent Native browser event.
 * @return {DOMEventTarget} Target node.
 */
function getEventTarget(nativeEvent) {
  var target = nativeEvent.target || nativeEvent.srcElement || window;
  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
  // @see http://www.quirksmode.org/js/events_properties.html
  return target.nodeType === 3 ? target.parentNode : target;
}

module.exports = getEventTarget;

},{}],105:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getMarkupWrap
 */

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var invariant = _dereq_("./invariant");

/**
 * Dummy container used to detect which wraps are necessary.
 */
var dummyNode =
  ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

/**
 * Some browsers cannot use `innerHTML` to render certain elements standalone,
 * so we wrap them, render the wrapped nodes, then extract the desired node.
 *
 * In IE8, certain elements cannot render alone, so wrap all elements ('*').
 */
var shouldWrap = {
  // Force wrapping for SVG elements because if they get created inside a <div>,
  // they will be initialized in the wrong namespace (and will not display).
  'circle': true,
  'defs': true,
  'g': true,
  'line': true,
  'linearGradient': true,
  'path': true,
  'polygon': true,
  'polyline': true,
  'radialGradient': true,
  'rect': true,
  'stop': true,
  'text': true
};

var selectWrap = [1, '<select multiple="true">', '</select>'];
var tableWrap = [1, '<table>', '</table>'];
var trWrap = [3, '<table><tbody><tr>', '</tr></tbody></table>'];

var svgWrap = [1, '<svg>', '</svg>'];

var markupWrap = {
  '*': [1, '?<div>', '</div>'],

  'area': [1, '<map>', '</map>'],
  'col': [2, '<table><tbody></tbody><colgroup>', '</colgroup></table>'],
  'legend': [1, '<fieldset>', '</fieldset>'],
  'param': [1, '<object>', '</object>'],
  'tr': [2, '<table><tbody>', '</tbody></table>'],

  'optgroup': selectWrap,
  'option': selectWrap,

  'caption': tableWrap,
  'colgroup': tableWrap,
  'tbody': tableWrap,
  'tfoot': tableWrap,
  'thead': tableWrap,

  'td': trWrap,
  'th': trWrap,

  'circle': svgWrap,
  'defs': svgWrap,
  'g': svgWrap,
  'line': svgWrap,
  'linearGradient': svgWrap,
  'path': svgWrap,
  'polygon': svgWrap,
  'polyline': svgWrap,
  'radialGradient': svgWrap,
  'rect': svgWrap,
  'stop': svgWrap,
  'text': svgWrap
};

/**
 * Gets the markup wrap configuration for the supplied `nodeName`.
 *
 * NOTE: This lazily detects which wraps are necessary for the current browser.
 *
 * @param {string} nodeName Lowercase `nodeName`.
 * @return {?array} Markup wrap configuration, if applicable.
 */
function getMarkupWrap(nodeName) {
  ("production" !== "development" ? invariant(!!dummyNode, 'Markup wrapping node not initialized') : invariant(!!dummyNode));
  if (!markupWrap.hasOwnProperty(nodeName)) {
    nodeName = '*';
  }
  if (!shouldWrap.hasOwnProperty(nodeName)) {
    if (nodeName === '*') {
      dummyNode.innerHTML = '<link />';
    } else {
      dummyNode.innerHTML = '<' + nodeName + '></' + nodeName + '>';
    }
    shouldWrap[nodeName] = !dummyNode.firstChild;
  }
  return shouldWrap[nodeName] ? markupWrap[nodeName] : null;
}


module.exports = getMarkupWrap;

},{"./ExecutionEnvironment":20,"./invariant":112}],106:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getNodeForCharacterOffset
 */

"use strict";

/**
 * Given any node return the first leaf node without children.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {DOMElement|DOMTextNode}
 */
function getLeafNode(node) {
  while (node && node.firstChild) {
    node = node.firstChild;
  }
  return node;
}

/**
 * Get the next sibling within a container. This will walk up the
 * DOM if a node's siblings have been exhausted.
 *
 * @param {DOMElement|DOMTextNode} node
 * @return {?DOMElement|DOMTextNode}
 */
function getSiblingNode(node) {
  while (node) {
    if (node.nextSibling) {
      return node.nextSibling;
    }
    node = node.parentNode;
  }
}

/**
 * Get object describing the nodes which contain characters at offset.
 *
 * @param {DOMElement|DOMTextNode} root
 * @param {number} offset
 * @return {?object}
 */
function getNodeForCharacterOffset(root, offset) {
  var node = getLeafNode(root);
  var nodeStart = 0;
  var nodeEnd = 0;

  while (node) {
    if (node.nodeType == 3) {
      nodeEnd = nodeStart + node.textContent.length;

      if (nodeStart <= offset && nodeEnd >= offset) {
        return {
          node: node,
          offset: offset - nodeStart
        };
      }

      nodeStart = nodeEnd;
    }

    node = getLeafNode(getSiblingNode(node));
  }
}

module.exports = getNodeForCharacterOffset;

},{}],107:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getReactRootElementInContainer
 */

"use strict";

var DOC_NODE_TYPE = 9;

/**
 * @param {DOMElement|DOMDocument} container DOM element that may contain
 *                                           a React component
 * @return {?*} DOM element that may have the reactRoot ID, or null.
 */
function getReactRootElementInContainer(container) {
  if (!container) {
    return null;
  }

  if (container.nodeType === DOC_NODE_TYPE) {
    return container.documentElement;
  } else {
    return container.firstChild;
  }
}

module.exports = getReactRootElementInContainer;

},{}],108:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getTextContentAccessor
 */

"use strict";

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var contentKey = null;

/**
 * Gets the key used to access text content on a DOM node.
 *
 * @return {?string} Key used to access text content.
 * @internal
 */
function getTextContentAccessor() {
  if (!contentKey && ExecutionEnvironment.canUseDOM) {
    // Prefer textContent to innerText because many browsers support both but
    // SVG <text> elements don't support innerText even when <div> does.
    contentKey = 'textContent' in document.createElement('div') ?
      'textContent' :
      'innerText';
  }
  return contentKey;
}

module.exports = getTextContentAccessor;

},{"./ExecutionEnvironment":20}],109:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule getUnboundedScrollPosition
 * @typechecks
 */

"use strict";

/**
 * Gets the scroll position of the supplied element or window.
 *
 * The return values are unbounded, unlike `getScrollPosition`. This means they
 * may be negative or exceed the element boundaries (which is possible using
 * inertial scrolling).
 *
 * @param {DOMWindow|DOMElement} scrollable
 * @return {object} Map with `x` and `y` keys.
 */
function getUnboundedScrollPosition(scrollable) {
  if (scrollable === window) {
    return {
      x: window.pageXOffset || document.documentElement.scrollLeft,
      y: window.pageYOffset || document.documentElement.scrollTop
    };
  }
  return {
    x: scrollable.scrollLeft,
    y: scrollable.scrollTop
  };
}

module.exports = getUnboundedScrollPosition;

},{}],110:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule hyphenate
 * @typechecks
 */

var _uppercasePattern = /([A-Z])/g;

/**
 * Hyphenates a camelcased string, for example:
 *
 *   > hyphenate('backgroundColor')
 *   < "background-color"
 *
 * @param {string} string
 * @return {string}
 */
function hyphenate(string) {
  return string.replace(_uppercasePattern, '-$1').toLowerCase();
}

module.exports = hyphenate;

},{}],111:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule instantiateReactComponent
 * @typechecks static-only
 */

"use strict";

var warning = _dereq_("./warning");

/**
 * Validate a `componentDescriptor`. This should be exposed publicly in a follow
 * up diff.
 *
 * @param {object} descriptor
 * @return {boolean} Returns true if this is a valid descriptor of a Component.
 */
function isValidComponentDescriptor(descriptor) {
  return (
    typeof descriptor.constructor === 'function' &&
    typeof descriptor.constructor.prototype.construct === 'function' &&
    typeof descriptor.constructor.prototype.mountComponent === 'function' &&
    typeof descriptor.constructor.prototype.receiveComponent === 'function'
  );
}

/**
 * Given a `componentDescriptor` create an instance that will actually be
 * mounted. Currently it just extracts an existing clone from composite
 * components but this is an implementation detail which will change.
 *
 * @param {object} descriptor
 * @return {object} A new instance of componentDescriptor's constructor.
 * @protected
 */
function instantiateReactComponent(descriptor) {
  if ("production" !== "development") {
    ("production" !== "development" ? warning(
      isValidComponentDescriptor(descriptor),
      'Only React Components are valid for mounting.'
    ) : null);
    // We use the clone of a composite component instead of the original
    // instance. This allows us to warn you if you're are accessing the wrong
    // instance.
    var instance = descriptor.__realComponentInstance || descriptor;
    instance._descriptor = descriptor;
    return instance;
  }
  // In prod we don't clone, we simply use the same instance for unaffected
  // behavior. We have to keep the descriptor around for comparison later on.
  // This should ideally be accepted in the constructor of the instance but
  // since that is currently overloaded, we just manually attach it here.
  descriptor._descriptor = descriptor;
  return descriptor;
}

module.exports = instantiateReactComponent;

},{"./warning":134}],112:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule invariant
 */

"use strict";

/**
 * Use invariant() to assert state which your program assumes to be true.
 *
 * Provide sprintf-style format (only %s is supported) and arguments
 * to provide information about what broke and what you were
 * expecting.
 *
 * The invariant message will be stripped in production, but the invariant
 * will remain to ensure logic does not differ in production.
 */

var invariant = function(condition) {
  if (!condition) {
    var error = new Error(
      'Minified exception occured; use the non-minified dev environment for ' +
      'the full error message and additional helpful warnings.'
    );
    error.framesToPop = 1;
    throw error;
  }
};

if ("production" !== "development") {
  invariant = function(condition, format, a, b, c, d, e, f) {
    if (format === undefined) {
      throw new Error('invariant requires an error message argument');
    }

    if (!condition) {
      var args = [a, b, c, d, e, f];
      var argIndex = 0;
      var error = new Error(
        'Invariant Violation: ' +
        format.replace(/%s/g, function() { return args[argIndex++]; })
      );
      error.framesToPop = 1; // we don't care about invariant's own frame
      throw error;
    }
  };
}

module.exports = invariant;

},{}],113:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule isEventSupported
 */

"use strict";

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

var useHasFeature;
if (ExecutionEnvironment.canUseDOM) {
  useHasFeature =
    document.implementation &&
    document.implementation.hasFeature &&
    // always returns true in newer browsers as per the standard.
    // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature
    document.implementation.hasFeature('', '') !== true;
}

/**
 * Checks if an event is supported in the current execution environment.
 *
 * NOTE: This will not work correctly for non-generic events such as `change`,
 * `reset`, `load`, `error`, and `select`.
 *
 * Borrows from Modernizr.
 *
 * @param {string} eventNameSuffix Event name, e.g. "click".
 * @param {?boolean} capture Check if the capture phase is supported.
 * @return {boolean} True if the event is supported.
 * @internal
 * @license Modernizr 3.0.0pre (Custom Build) | MIT
 */
function isEventSupported(eventNameSuffix, capture) {
  if (!ExecutionEnvironment.canUseDOM ||
      capture && !('addEventListener' in document)) {
    return false;
  }

  var eventName = 'on' + eventNameSuffix;
  var isSupported = eventName in document;

  if (!isSupported) {
    var element = document.createElement('div');
    element.setAttribute(eventName, 'return;');
    isSupported = typeof element[eventName] === 'function';
  }

  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {
    // This is the only way to test support for the `wheel` event in IE9+.
    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');
  }

  return isSupported;
}

module.exports = isEventSupported;

},{"./ExecutionEnvironment":20}],114:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule isNode
 * @typechecks
 */

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM node.
 */
function isNode(object) {
  return !!(object && (
    typeof Node === 'function' ? object instanceof Node :
      typeof object === 'object' &&
      typeof object.nodeType === 'number' &&
      typeof object.nodeName === 'string'
  ));
}

module.exports = isNode;

},{}],115:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule isTextInputElement
 */

"use strict";

/**
 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
 */
var supportedInputTypes = {
  'color': true,
  'date': true,
  'datetime': true,
  'datetime-local': true,
  'email': true,
  'month': true,
  'number': true,
  'password': true,
  'range': true,
  'search': true,
  'tel': true,
  'text': true,
  'time': true,
  'url': true,
  'week': true
};

function isTextInputElement(elem) {
  return elem && (
    (elem.nodeName === 'INPUT' && supportedInputTypes[elem.type]) ||
    elem.nodeName === 'TEXTAREA'
  );
}

module.exports = isTextInputElement;

},{}],116:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule isTextNode
 * @typechecks
 */

var isNode = _dereq_("./isNode");

/**
 * @param {*} object The object to check.
 * @return {boolean} Whether or not the object is a DOM text node.
 */
function isTextNode(object) {
  return isNode(object) && object.nodeType == 3;
}

module.exports = isTextNode;

},{"./isNode":114}],117:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule joinClasses
 * @typechecks static-only
 */

"use strict";

/**
 * Combines multiple className strings into one.
 * http://jsperf.com/joinclasses-args-vs-array
 *
 * @param {...?string} classes
 * @return {string}
 */
function joinClasses(className/*, ... */) {
  if (!className) {
    className = '';
  }
  var nextClass;
  var argLength = arguments.length;
  if (argLength > 1) {
    for (var ii = 1; ii < argLength; ii++) {
      nextClass = arguments[ii];
      nextClass && (className += ' ' + nextClass);
    }
  }
  return className;
}

module.exports = joinClasses;

},{}],118:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule keyMirror
 * @typechecks static-only
 */

"use strict";

var invariant = _dereq_("./invariant");

/**
 * Constructs an enumeration with keys equal to their value.
 *
 * For example:
 *
 *   var COLORS = keyMirror({blue: null, red: null});
 *   var myColor = COLORS.blue;
 *   var isColorValid = !!COLORS[myColor];
 *
 * The last line could not be performed if the values of the generated enum were
 * not equal to their keys.
 *
 *   Input:  {key1: val1, key2: val2}
 *   Output: {key1: key1, key2: key2}
 *
 * @param {object} obj
 * @return {object}
 */
var keyMirror = function(obj) {
  var ret = {};
  var key;
  ("production" !== "development" ? invariant(
    obj instanceof Object && !Array.isArray(obj),
    'keyMirror(...): Argument must be an object.'
  ) : invariant(obj instanceof Object && !Array.isArray(obj)));
  for (key in obj) {
    if (!obj.hasOwnProperty(key)) {
      continue;
    }
    ret[key] = key;
  }
  return ret;
};

module.exports = keyMirror;

},{"./invariant":112}],119:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule keyOf
 */

/**
 * Allows extraction of a minified key. Let's the build system minify keys
 * without loosing the ability to dynamically use key strings as values
 * themselves. Pass in an object with a single key/val pair and it will return
 * you the string key of that single record. Suppose you want to grab the
 * value for a key 'className' inside of an object. Key/val minification may
 * have aliased that key to be 'xa12'. keyOf({className: null}) will return
 * 'xa12' in that case. Resolve keys you want to use once at startup time, then
 * reuse those resolutions.
 */
var keyOf = function(oneKeyObj) {
  var key;
  for (key in oneKeyObj) {
    if (!oneKeyObj.hasOwnProperty(key)) {
      continue;
    }
    return key;
  }
  return null;
};


module.exports = keyOf;

},{}],120:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule memoizeStringOnly
 * @typechecks static-only
 */

"use strict";

/**
 * Memoizes the return value of a function that accepts one string argument.
 *
 * @param {function} callback
 * @return {function}
 */
function memoizeStringOnly(callback) {
  var cache = {};
  return function(string) {
    if (cache.hasOwnProperty(string)) {
      return cache[string];
    } else {
      return cache[string] = callback.call(this, string);
    }
  };
}

module.exports = memoizeStringOnly;

},{}],121:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule merge
 */

"use strict";

var mergeInto = _dereq_("./mergeInto");

/**
 * Shallow merges two structures into a return value, without mutating either.
 *
 * @param {?object} one Optional object with properties to merge from.
 * @param {?object} two Optional object with properties to merge from.
 * @return {object} The shallow extension of one by two.
 */
var merge = function(one, two) {
  var result = {};
  mergeInto(result, one);
  mergeInto(result, two);
  return result;
};

module.exports = merge;

},{"./mergeInto":123}],122:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule mergeHelpers
 *
 * requiresPolyfills: Array.isArray
 */

"use strict";

var invariant = _dereq_("./invariant");
var keyMirror = _dereq_("./keyMirror");

/**
 * Maximum number of levels to traverse. Will catch circular structures.
 * @const
 */
var MAX_MERGE_DEPTH = 36;

/**
 * We won't worry about edge cases like new String('x') or new Boolean(true).
 * Functions are considered terminals, and arrays are not.
 * @param {*} o The item/object/value to test.
 * @return {boolean} true iff the argument is a terminal.
 */
var isTerminal = function(o) {
  return typeof o !== 'object' || o === null;
};

var mergeHelpers = {

  MAX_MERGE_DEPTH: MAX_MERGE_DEPTH,

  isTerminal: isTerminal,

  /**
   * Converts null/undefined values into empty object.
   *
   * @param {?Object=} arg Argument to be normalized (nullable optional)
   * @return {!Object}
   */
  normalizeMergeArg: function(arg) {
    return arg === undefined || arg === null ? {} : arg;
  },

  /**
   * If merging Arrays, a merge strategy *must* be supplied. If not, it is
   * likely the caller's fault. If this function is ever called with anything
   * but `one` and `two` being `Array`s, it is the fault of the merge utilities.
   *
   * @param {*} one Array to merge into.
   * @param {*} two Array to merge from.
   */
  checkMergeArrayArgs: function(one, two) {
    ("production" !== "development" ? invariant(
      Array.isArray(one) && Array.isArray(two),
      'Tried to merge arrays, instead got %s and %s.',
      one,
      two
    ) : invariant(Array.isArray(one) && Array.isArray(two)));
  },

  /**
   * @param {*} one Object to merge into.
   * @param {*} two Object to merge from.
   */
  checkMergeObjectArgs: function(one, two) {
    mergeHelpers.checkMergeObjectArg(one);
    mergeHelpers.checkMergeObjectArg(two);
  },

  /**
   * @param {*} arg
   */
  checkMergeObjectArg: function(arg) {
    ("production" !== "development" ? invariant(
      !isTerminal(arg) && !Array.isArray(arg),
      'Tried to merge an object, instead got %s.',
      arg
    ) : invariant(!isTerminal(arg) && !Array.isArray(arg)));
  },

  /**
   * Checks that a merge was not given a circular object or an object that had
   * too great of depth.
   *
   * @param {number} Level of recursion to validate against maximum.
   */
  checkMergeLevel: function(level) {
    ("production" !== "development" ? invariant(
      level < MAX_MERGE_DEPTH,
      'Maximum deep merge depth exceeded. You may be attempting to merge ' +
      'circular structures in an unsupported way.'
    ) : invariant(level < MAX_MERGE_DEPTH));
  },

  /**
   * Checks that the supplied merge strategy is valid.
   *
   * @param {string} Array merge strategy.
   */
  checkArrayStrategy: function(strategy) {
    ("production" !== "development" ? invariant(
      strategy === undefined || strategy in mergeHelpers.ArrayStrategies,
      'You must provide an array strategy to deep merge functions to ' +
      'instruct the deep merge how to resolve merging two arrays.'
    ) : invariant(strategy === undefined || strategy in mergeHelpers.ArrayStrategies));
  },

  /**
   * Set of possible behaviors of merge algorithms when encountering two Arrays
   * that must be merged together.
   * - `clobber`: The left `Array` is ignored.
   * - `indexByIndex`: The result is achieved by recursively deep merging at
   *   each index. (not yet supported.)
   */
  ArrayStrategies: keyMirror({
    Clobber: true,
    IndexByIndex: true
  })

};

module.exports = mergeHelpers;

},{"./invariant":112,"./keyMirror":118}],123:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule mergeInto
 * @typechecks static-only
 */

"use strict";

var mergeHelpers = _dereq_("./mergeHelpers");

var checkMergeObjectArg = mergeHelpers.checkMergeObjectArg;

/**
 * Shallow merges two structures by mutating the first parameter.
 *
 * @param {object} one Object to be merged into.
 * @param {?object} two Optional object with properties to merge from.
 */
function mergeInto(one, two) {
  checkMergeObjectArg(one);
  if (two != null) {
    checkMergeObjectArg(two);
    for (var key in two) {
      if (!two.hasOwnProperty(key)) {
        continue;
      }
      one[key] = two[key];
    }
  }
}

module.exports = mergeInto;

},{"./mergeHelpers":122}],124:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule mixInto
 */

"use strict";

/**
 * Simply copies properties to the prototype.
 */
var mixInto = function(constructor, methodBag) {
  var methodName;
  for (methodName in methodBag) {
    if (!methodBag.hasOwnProperty(methodName)) {
      continue;
    }
    constructor.prototype[methodName] = methodBag[methodName];
  }
};

module.exports = mixInto;

},{}],125:[function(_dereq_,module,exports){
/**
 * Copyright 2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule monitorCodeUse
 */

"use strict";

var invariant = _dereq_("./invariant");

/**
 * Provides open-source compatible instrumentation for monitoring certain API
 * uses before we're ready to issue a warning or refactor. It accepts an event
 * name which may only contain the characters [a-z0-9_] and an optional data
 * object with further information.
 */

function monitorCodeUse(eventName, data) {
  ("production" !== "development" ? invariant(
    eventName && !/[^a-z0-9_]/.test(eventName),
    'You must provide an eventName using only the characters [a-z0-9_]'
  ) : invariant(eventName && !/[^a-z0-9_]/.test(eventName)));
}

module.exports = monitorCodeUse;

},{"./invariant":112}],126:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule objMap
 */

"use strict";

/**
 * For each key/value pair, invokes callback func and constructs a resulting
 * object which contains, for every key in obj, values that are the result of
 * of invoking the function:
 *
 *   func(value, key, iteration)
 *
 * @param {?object} obj Object to map keys over
 * @param {function} func Invoked for each key/val pair.
 * @param {?*} context
 * @return {?object} Result of mapping or null if obj is falsey
 */
function objMap(obj, func, context) {
  if (!obj) {
    return null;
  }
  var i = 0;
  var ret = {};
  for (var key in obj) {
    if (obj.hasOwnProperty(key)) {
      ret[key] = func.call(context, obj[key], key, i++);
    }
  }
  return ret;
}

module.exports = objMap;

},{}],127:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule objMapKeyVal
 */

"use strict";

/**
 * Behaves the same as `objMap` but invokes func with the key first, and value
 * second. Use `objMap` unless you need this special case.
 * Invokes func as:
 *
 *   func(key, value, iteration)
 *
 * @param {?object} obj Object to map keys over
 * @param {!function} func Invoked for each key/val pair.
 * @param {?*} context
 * @return {?object} Result of mapping or null if obj is falsey
 */
function objMapKeyVal(obj, func, context) {
  if (!obj) {
    return null;
  }
  var i = 0;
  var ret = {};
  for (var key in obj) {
    if (obj.hasOwnProperty(key)) {
      ret[key] = func.call(context, key, obj[key], i++);
    }
  }
  return ret;
}

module.exports = objMapKeyVal;

},{}],128:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule onlyChild
 */
"use strict";

var ReactComponent = _dereq_("./ReactComponent");

var invariant = _dereq_("./invariant");

/**
 * Returns the first child in a collection of children and verifies that there
 * is only one child in the collection. The current implementation of this
 * function assumes that a single child gets passed without a wrapper, but the
 * purpose of this helper function is to abstract away the particular structure
 * of children.
 *
 * @param {?object} children Child collection structure.
 * @return {ReactComponent} The first and only `ReactComponent` contained in the
 * structure.
 */
function onlyChild(children) {
  ("production" !== "development" ? invariant(
    ReactComponent.isValidComponent(children),
    'onlyChild must be passed a children with exactly one child.'
  ) : invariant(ReactComponent.isValidComponent(children)));
  return children;
}

module.exports = onlyChild;

},{"./ReactComponent":27,"./invariant":112}],129:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule performanceNow
 * @typechecks static-only
 */

"use strict";

var ExecutionEnvironment = _dereq_("./ExecutionEnvironment");

/**
 * Detect if we can use window.performance.now() and gracefully
 * fallback to Date.now() if it doesn't exist.
 * We need to support Firefox < 15 for now due to Facebook's webdriver
 * infrastructure.
 */
var performance = null;

if (ExecutionEnvironment.canUseDOM) {
  performance = window.performance || window.webkitPerformance;
}

if (!performance || !performance.now) {
  performance = Date;
}

var performanceNow = performance.now.bind(performance);

module.exports = performanceNow;

},{"./ExecutionEnvironment":20}],130:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule shallowEqual
 */

"use strict";

/**
 * Performs equality by iterating through keys on an object and returning
 * false when any key has values which are not strictly equal between
 * objA and objB. Returns true when the values of all keys are strictly equal.
 *
 * @return {boolean}
 */
function shallowEqual(objA, objB) {
  if (objA === objB) {
    return true;
  }
  var key;
  // Test for A's keys different from B.
  for (key in objA) {
    if (objA.hasOwnProperty(key) &&
        (!objB.hasOwnProperty(key) || objA[key] !== objB[key])) {
      return false;
    }
  }
  // Test for B'a keys missing from A.
  for (key in objB) {
    if (objB.hasOwnProperty(key) && !objA.hasOwnProperty(key)) {
      return false;
    }
  }
  return true;
}

module.exports = shallowEqual;

},{}],131:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule shouldUpdateReactComponent
 * @typechecks static-only
 */

"use strict";

/**
 * Given a `prevComponentInstance` and `nextComponent`, determines if
 * `prevComponentInstance` should be updated as opposed to being destroyed or
 * replaced by a new instance. The second argument is a descriptor. Future
 * versions of the reconciler should only compare descriptors to other
 * descriptors.
 *
 * @param {?object} prevComponentInstance
 * @param {?object} nextDescriptor
 * @return {boolean} True if `prevComponentInstance` should be updated.
 * @protected
 */
function shouldUpdateReactComponent(prevComponentInstance, nextDescriptor) {
  // TODO: Remove warning after a release.
  if (prevComponentInstance && nextDescriptor &&
      prevComponentInstance.constructor === nextDescriptor.constructor && (
        (prevComponentInstance.props && prevComponentInstance.props.key) ===
        (nextDescriptor.props && nextDescriptor.props.key)
      )) {
    if (prevComponentInstance._owner === nextDescriptor._owner) {
      return true;
    } else {
      if ("production" !== "development") {
        if (prevComponentInstance.state) {
          console.warn(
            'A recent change to React has been found to impact your code. ' +
            'A mounted component will now be unmounted and replaced by a ' +
            'component (of the same class) if their owners are different. ' +
            'Previously, ownership was not considered when updating.',
            prevComponentInstance,
            nextDescriptor
          );
        }
      }
    }
  }
  return false;
}

module.exports = shouldUpdateReactComponent;

},{}],132:[function(_dereq_,module,exports){
/**
 * Copyright 2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule toArray
 * @typechecks
 */

var invariant = _dereq_("./invariant");

/**
 * Convert array-like objects to arrays.
 *
 * This API assumes the caller knows the contents of the data type. For less
 * well defined inputs use createArrayFrom.
 *
 * @param {object|function} obj
 * @return {array}
 */
function toArray(obj) {
  var length = obj.length;

  // Some browse builtin objects can report typeof 'function' (e.g. NodeList in
  // old versions of Safari).
  ("production" !== "development" ? invariant(
    !Array.isArray(obj) &&
    (typeof obj === 'object' || typeof obj === 'function'),
    'toArray: Array-like object expected'
  ) : invariant(!Array.isArray(obj) &&
  (typeof obj === 'object' || typeof obj === 'function')));

  ("production" !== "development" ? invariant(
    typeof length === 'number',
    'toArray: Object needs a length property'
  ) : invariant(typeof length === 'number'));

  ("production" !== "development" ? invariant(
    length === 0 ||
    (length - 1) in obj,
    'toArray: Object should have keys for indices'
  ) : invariant(length === 0 ||
  (length - 1) in obj));

  // Old IE doesn't give collections access to hasOwnProperty. Assume inputs
  // without method will throw during the slice call and skip straight to the
  // fallback.
  if (obj.hasOwnProperty) {
    try {
      return Array.prototype.slice.call(obj);
    } catch (e) {
      // IE < 9 does not support Array#slice on collections objects
    }
  }

  // Fall back to copying key by key. This assumes all keys have a value,
  // so will not preserve sparsely populated inputs.
  var ret = Array(length);
  for (var ii = 0; ii < length; ii++) {
    ret[ii] = obj[ii];
  }
  return ret;
}

module.exports = toArray;

},{"./invariant":112}],133:[function(_dereq_,module,exports){
/**
 * Copyright 2013-2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule traverseAllChildren
 */

"use strict";

var ReactInstanceHandles = _dereq_("./ReactInstanceHandles");
var ReactTextComponent = _dereq_("./ReactTextComponent");

var invariant = _dereq_("./invariant");

var SEPARATOR = ReactInstanceHandles.SEPARATOR;
var SUBSEPARATOR = ':';

/**
 * TODO: Test that:
 * 1. `mapChildren` transforms strings and numbers into `ReactTextComponent`.
 * 2. it('should fail when supplied duplicate key', function() {
 * 3. That a single child and an array with one item have the same key pattern.
 * });
 */

var userProvidedKeyEscaperLookup = {
  '=': '=0',
  '.': '=1',
  ':': '=2'
};

var userProvidedKeyEscapeRegex = /[=.:]/g;

function userProvidedKeyEscaper(match) {
  return userProvidedKeyEscaperLookup[match];
}

/**
 * Generate a key string that identifies a component within a set.
 *
 * @param {*} component A component that could contain a manual key.
 * @param {number} index Index that is used if a manual key is not provided.
 * @return {string}
 */
function getComponentKey(component, index) {
  if (component && component.props && component.props.key != null) {
    // Explicit key
    return wrapUserProvidedKey(component.props.key);
  }
  // Implicit key determined by the index in the set
  return index.toString(36);
}

/**
 * Escape a component key so that it is safe to use in a reactid.
 *
 * @param {*} key Component key to be escaped.
 * @return {string} An escaped string.
 */
function escapeUserProvidedKey(text) {
  return ('' + text).replace(
    userProvidedKeyEscapeRegex,
    userProvidedKeyEscaper
  );
}

/**
 * Wrap a `key` value explicitly provided by the user to distinguish it from
 * implicitly-generated keys generated by a component's index in its parent.
 *
 * @param {string} key Value of a user-provided `key` attribute
 * @return {string}
 */
function wrapUserProvidedKey(key) {
  return '$' + escapeUserProvidedKey(key);
}

/**
 * @param {?*} children Children tree container.
 * @param {!string} nameSoFar Name of the key path so far.
 * @param {!number} indexSoFar Number of children encountered until this point.
 * @param {!function} callback Callback to invoke with each child found.
 * @param {?*} traverseContext Used to pass information throughout the traversal
 * process.
 * @return {!number} The number of children in this subtree.
 */
var traverseAllChildrenImpl =
  function(children, nameSoFar, indexSoFar, callback, traverseContext) {
    var subtreeCount = 0;  // Count of children found in the current subtree.
    if (Array.isArray(children)) {
      for (var i = 0; i < children.length; i++) {
        var child = children[i];
        var nextName = (
          nameSoFar +
          (nameSoFar ? SUBSEPARATOR : SEPARATOR) +
          getComponentKey(child, i)
        );
        var nextIndex = indexSoFar + subtreeCount;
        subtreeCount += traverseAllChildrenImpl(
          child,
          nextName,
          nextIndex,
          callback,
          traverseContext
        );
      }
    } else {
      var type = typeof children;
      var isOnlyChild = nameSoFar === '';
      // If it's the only child, treat the name as if it was wrapped in an array
      // so that it's consistent if the number of children grows
      var storageName =
        isOnlyChild ? SEPARATOR + getComponentKey(children, 0) : nameSoFar;
      if (children == null || type === 'boolean') {
        // All of the above are perceived as null.
        callback(traverseContext, null, storageName, indexSoFar);
        subtreeCount = 1;
      } else if (children.type && children.type.prototype &&
                 children.type.prototype.mountComponentIntoNode) {
        callback(traverseContext, children, storageName, indexSoFar);
        subtreeCount = 1;
      } else {
        if (type === 'object') {
          ("production" !== "development" ? invariant(
            !children || children.nodeType !== 1,
            'traverseAllChildren(...): Encountered an invalid child; DOM ' +
            'elements are not valid children of React components.'
          ) : invariant(!children || children.nodeType !== 1));
          for (var key in children) {
            if (children.hasOwnProperty(key)) {
              subtreeCount += traverseAllChildrenImpl(
                children[key],
                (
                  nameSoFar + (nameSoFar ? SUBSEPARATOR : SEPARATOR) +
                  wrapUserProvidedKey(key) + SUBSEPARATOR +
                  getComponentKey(children[key], 0)
                ),
                indexSoFar + subtreeCount,
                callback,
                traverseContext
              );
            }
          }
        } else if (type === 'string') {
          var normalizedText = new ReactTextComponent(children);
          callback(traverseContext, normalizedText, storageName, indexSoFar);
          subtreeCount += 1;
        } else if (type === 'number') {
          var normalizedNumber = new ReactTextComponent('' + children);
          callback(traverseContext, normalizedNumber, storageName, indexSoFar);
          subtreeCount += 1;
        }
      }
    }
    return subtreeCount;
  };

/**
 * Traverses children that are typically specified as `props.children`, but
 * might also be specified through attributes:
 *
 * - `traverseAllChildren(this.props.children, ...)`
 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
 *
 * The `traverseContext` is an optional argument that is passed through the
 * entire traversal. It can be used to store accumulations or anything else that
 * the callback might find relevant.
 *
 * @param {?*} children Children tree object.
 * @param {!function} callback To invoke upon traversing each child.
 * @param {?*} traverseContext Context for traversal.
 */
function traverseAllChildren(children, callback, traverseContext) {
  if (children !== null && children !== undefined) {
    traverseAllChildrenImpl(children, '', 0, callback, traverseContext);
  }
}

module.exports = traverseAllChildren;

},{"./ReactInstanceHandles":53,"./ReactTextComponent":70,"./invariant":112}],134:[function(_dereq_,module,exports){
/**
 * Copyright 2014 Facebook, Inc.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 *
 * @providesModule warning
 */

"use strict";

var emptyFunction = _dereq_("./emptyFunction");

/**
 * Similar to invariant but only logs a warning if the condition is not met.
 * This can be used to log issues in development environments in critical
 * paths. Removing the logging code for production environments will keep the
 * same logic and follow the same code paths.
 */

var warning = emptyFunction;

if ("production" !== "development") {
  warning = function(condition, format ) {var args=Array.prototype.slice.call(arguments,2);
    if (format === undefined) {
      throw new Error(
        '`warning(condition, format, ...args)` requires a warning ' +
        'message argument'
      );
    }

    if (!condition) {
      var argIndex = 0;
      console.warn('Warning: ' + format.replace(/%s/g, function()  {return args[argIndex++];}));
    }
  };
}

module.exports = warning;

},{"./emptyFunction":96}]},{},[24])
(24)
});
if(typeof Math.imul == "undefined" || (Math.imul(0xffffffff,5) == 0)) {
    Math.imul = function (a, b) {
        var ah  = (a >>> 16) & 0xffff;
        var al = a & 0xffff;
        var bh  = (b >>> 16) & 0xffff;
        var bl = b & 0xffff;
        // the shift by 0 fixes the sign on the high part
        // the final |0 converts the unsigned value into a signed value
        return ((al * bl) + (((ah * bl + al * bh) << 16) >>> 0)|0);
    }
}

;(function(){
var k,aa=aa||{},ba=this;function ca(a){a=a.split(".");for(var b=ba,c;c=a.shift();)if(null!=b[c])b=b[c];else return null;return b}function da(){}
function l(a){var b=typeof a;if("object"==b)if(a){if(a instanceof Array)return"array";if(a instanceof Object)return b;var c=Object.prototype.toString.call(a);if("[object Window]"==c)return"object";if("[object Array]"==c||"number"==typeof a.length&&"undefined"!=typeof a.splice&&"undefined"!=typeof a.propertyIsEnumerable&&!a.propertyIsEnumerable("splice"))return"array";if("[object Function]"==c||"undefined"!=typeof a.call&&"undefined"!=typeof a.propertyIsEnumerable&&!a.propertyIsEnumerable("call"))return"function"}else return"null";
else if("function"==b&&"undefined"==typeof a.call)return"object";return b}function ea(a){var b=l(a);return"array"==b||"object"==b&&"number"==typeof a.length}function fa(a){return"string"==typeof a}function ga(a){return"function"==l(a)}function ha(a){var b=typeof a;return"object"==b&&null!=a||"function"==b}var ia="closure_uid_"+(1E9*Math.random()>>>0),ja=0;function ka(a,b,c){return a.call.apply(a.bind,arguments)}
function la(a,b,c){if(!a)throw Error();if(2<arguments.length){var e=Array.prototype.slice.call(arguments,2);return function(){var c=Array.prototype.slice.call(arguments);Array.prototype.unshift.apply(c,e);return a.apply(b,c)}}return function(){return a.apply(b,arguments)}}function ma(a,b,c){ma=Function.prototype.bind&&-1!=Function.prototype.bind.toString().indexOf("native code")?ka:la;return ma.apply(null,arguments)}var na=Date.now||function(){return+new Date};
function pa(a,b){function c(){}c.prototype=b.prototype;a.Of=b.prototype;a.prototype=new c;a.prototype.constructor=a;a.Bc=function(a,c,g){return b.prototype[c].apply(a,Array.prototype.slice.call(arguments,2))}};function qa(a,b){for(var c=a.split("%s"),e="",d=Array.prototype.slice.call(arguments,1);d.length&&1<c.length;)e+=c.shift()+d.shift();return e+c.join("%s")}function ra(a){return/^[\s\xa0]*$/.test(null==a?"":String(a))}
function sa(a){if(!ta.test(a))return a;-1!=a.indexOf("\x26")&&(a=a.replace(ua,"\x26amp;"));-1!=a.indexOf("\x3c")&&(a=a.replace(va,"\x26lt;"));-1!=a.indexOf("\x3e")&&(a=a.replace(wa,"\x26gt;"));-1!=a.indexOf('"')&&(a=a.replace(xa,"\x26quot;"));-1!=a.indexOf("'")&&(a=a.replace(ya,"\x26#39;"));-1!=a.indexOf("\x00")&&(a=a.replace(za,"\x26#0;"));return a}var ua=/&/g,va=/</g,wa=/>/g,xa=/"/g,ya=/'/g,za=/\x00/g,ta=/[\x00&<>"']/;function Aa(a){return Array.prototype.join.call(arguments,"")}
function Ba(a,b){return a<b?-1:a>b?1:0};function Ca(a,b){for(var c in a)b.call(void 0,a[c],c,a)}function Da(a){var b=[],c=0,e;for(e in a)b[c++]=a[e];return b}function Ea(a){var b=[],c=0,e;for(e in a)b[c++]=e;return b}var Fa="constructor hasOwnProperty isPrototypeOf propertyIsEnumerable toLocaleString toString valueOf".split(" ");function Ga(a,b){for(var c,e,d=1;d<arguments.length;d++){e=arguments[d];for(c in e)a[c]=e[c];for(var g=0;g<Fa.length;g++)c=Fa[g],Object.prototype.hasOwnProperty.call(e,c)&&(a[c]=e[c])}}
function Ha(a){var b=arguments.length;if(1==b&&"array"==l(arguments[0]))return Ha.apply(null,arguments[0]);for(var c={},e=0;e<b;e++)c[arguments[e]]=!0;return c};function Ia(a,b){null!=a&&this.append.apply(this,arguments)}k=Ia.prototype;k.Lb="";k.set=function(a){this.Lb=""+a};k.append=function(a,b,c){this.Lb+=a;if(null!=b)for(var e=1;e<arguments.length;e++)this.Lb+=arguments[e];return this};k.clear=function(){this.Lb=""};k.toString=function(){return this.Lb};function La(a){if(Error.captureStackTrace)Error.captureStackTrace(this,La);else{var b=Error().stack;b&&(this.stack=b)}a&&(this.message=String(a))}pa(La,Error);La.prototype.name="CustomError";var Ma;function Oa(a,b){b.unshift(a);La.call(this,qa.apply(null,b));b.shift()}pa(Oa,La);Oa.prototype.name="AssertionError";function Pa(a,b){throw new Oa("Failure"+(a?": "+a:""),Array.prototype.slice.call(arguments,1));};var Ua=Array.prototype,Va=Ua.indexOf?function(a,b,c){return Ua.indexOf.call(a,b,c)}:function(a,b,c){c=null==c?0:0>c?Math.max(0,a.length+c):c;if(fa(a))return fa(b)&&1==b.length?a.indexOf(b,c):-1;for(;c<a.length;c++)if(c in a&&a[c]===b)return c;return-1},Wa=Ua.forEach?function(a,b,c){Ua.forEach.call(a,b,c)}:function(a,b,c){for(var e=a.length,d=fa(a)?a.split(""):a,g=0;g<e;g++)g in d&&b.call(c,d[g],g,a)},Xa=Ua.filter?function(a,b,c){return Ua.filter.call(a,b,c)}:function(a,b,c){for(var e=a.length,d=[],
g=0,f=fa(a)?a.split(""):a,h=0;h<e;h++)if(h in f){var m=f[h];b.call(c,m,h,a)&&(d[g++]=m)}return d},$a=Ua.some?function(a,b,c){return Ua.some.call(a,b,c)}:function(a,b,c){for(var e=a.length,d=fa(a)?a.split(""):a,g=0;g<e;g++)if(g in d&&b.call(c,d[g],g,a))return!0;return!1};function ab(a){var b;a:{b=bb;for(var c=a.length,e=fa(a)?a.split(""):a,d=0;d<c;d++)if(d in e&&b.call(void 0,e[d],d,a)){b=d;break a}b=-1}return 0>b?null:fa(a)?a.charAt(b):a[b]}function cb(a){return Ua.concat.apply(Ua,arguments)}
function db(a){var b=a.length;if(0<b){for(var c=Array(b),e=0;e<b;e++)c[e]=a[e];return c}return[]}function fb(a,b){return a>b?1:a<b?-1:0};if("undefined"===typeof gb)var gb=function(){throw Error("No *print-fn* fn set for evaluation environment");};var hb=!0,ib=null;if("undefined"===typeof jb)var jb=null;function lb(){return new n(null,5,[mb,!0,nb,!0,ob,!1,pb,!1,qb,null],null)}function s(a){return null!=a&&!1!==a}function rb(a){return null==a}function sb(a){return s(a)?!1:!0}function t(a,b){return a[l(null==b?null:b)]?!0:a._?!0:!1}function tb(a){return null==a?null:a.constructor}
function v(a,b){var c=tb(b),c=s(s(c)?c.kd:c)?c.jd:l(b);return Error(["No protocol method ",a," defined for type ",c,": ",b].join(""))}function ub(a){var b=a.jd;return s(b)?b:""+w(a)}var vb="undefined"!==typeof Symbol&&"function"===l(Symbol)?Symbol.Kg:"@@iterator";function wb(a){for(var b=a.length,c=Array(b),e=0;;)if(e<b)c[e]=a[e],e+=1;else break;return c}function xb(a){for(var b=Array(arguments.length),c=0;;)if(c<b.length)b[c]=arguments[c],c+=1;else return b}
var zb=function(){function a(a,c){function b(a,c){a.push(c);return a}var f=[];return yb.j?yb.j(b,f,c):yb.call(null,b,f,c)}function b(a){return c.e(null,a)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,0,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}(),Ab={};function Bb(a){if(a?a.za:a)return a.za(a);var b;b=Bb[l(null==a?null:a)];if(!b&&(b=Bb._,!b))throw v("ICloneable.-clone",a);return b.call(null,a)}var Cb={};
function Db(a){if(a?a.ca:a)return a.ca(a);var b;b=Db[l(null==a?null:a)];if(!b&&(b=Db._,!b))throw v("ICounted.-count",a);return b.call(null,a)}function Eb(a){if(a?a.ea:a)return a.ea(a);var b;b=Eb[l(null==a?null:a)];if(!b&&(b=Eb._,!b))throw v("IEmptyableCollection.-empty",a);return b.call(null,a)}var Fb={};function Gb(a,b){if(a?a.Y:a)return a.Y(a,b);var c;c=Gb[l(null==a?null:a)];if(!c&&(c=Gb._,!c))throw v("ICollection.-conj",a);return c.call(null,a,b)}
var Ib={},x=function(){function a(a,c,b){if(a?a.Aa:a)return a.Aa(a,c,b);var f;f=x[l(null==a?null:a)];if(!f&&(f=x._,!f))throw v("IIndexed.-nth",a);return f.call(null,a,c,b)}function b(a,c){if(a?a.s:a)return a.s(a,c);var b;b=x[l(null==a?null:a)];if(!b&&(b=x._,!b))throw v("IIndexed.-nth",a);return b.call(null,a,c)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),
Jb={};function Kb(a){if(a?a.la:a)return a.la(a);var b;b=Kb[l(null==a?null:a)];if(!b&&(b=Kb._,!b))throw v("ISeq.-first",a);return b.call(null,a)}function Lb(a){if(a?a.pa:a)return a.pa(a);var b;b=Lb[l(null==a?null:a)];if(!b&&(b=Lb._,!b))throw v("ISeq.-rest",a);return b.call(null,a)}
var Mb={},Nb={},Ob=function(){function a(a,c,b){if(a?a.J:a)return a.J(a,c,b);var f;f=Ob[l(null==a?null:a)];if(!f&&(f=Ob._,!f))throw v("ILookup.-lookup",a);return f.call(null,a,c,b)}function b(a,c){if(a?a.I:a)return a.I(a,c);var b;b=Ob[l(null==a?null:a)];if(!b&&(b=Ob._,!b))throw v("ILookup.-lookup",a);return b.call(null,a,c)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=
a;return c}(),Pb={};function Qb(a,b){if(a?a.$c:a)return a.$c(a,b);var c;c=Qb[l(null==a?null:a)];if(!c&&(c=Qb._,!c))throw v("IAssociative.-contains-key?",a);return c.call(null,a,b)}function Rb(a,b,c){if(a?a.pb:a)return a.pb(a,b,c);var e;e=Rb[l(null==a?null:a)];if(!e&&(e=Rb._,!e))throw v("IAssociative.-assoc",a);return e.call(null,a,b,c)}var Sb={};function Tb(a,b){if(a?a.Fc:a)return a.Fc(a,b);var c;c=Tb[l(null==a?null:a)];if(!c&&(c=Tb._,!c))throw v("IMap.-dissoc",a);return c.call(null,a,b)}var Ub={};
function Vb(a){if(a?a.Gc:a)return a.Gc(a);var b;b=Vb[l(null==a?null:a)];if(!b&&(b=Vb._,!b))throw v("IMapEntry.-key",a);return b.call(null,a)}function Wb(a){if(a?a.Hc:a)return a.Hc(a);var b;b=Wb[l(null==a?null:a)];if(!b&&(b=Wb._,!b))throw v("IMapEntry.-val",a);return b.call(null,a)}var Xb={};function Yb(a){if(a?a.Mb:a)return a.Mb(a);var b;b=Yb[l(null==a?null:a)];if(!b&&(b=Yb._,!b))throw v("IStack.-peek",a);return b.call(null,a)}
function $b(a){if(a?a.Nb:a)return a.Nb(a);var b;b=$b[l(null==a?null:a)];if(!b&&(b=$b._,!b))throw v("IStack.-pop",a);return b.call(null,a)}var ac={};function bc(a,b,c){if(a?a.Zb:a)return a.Zb(a,b,c);var e;e=bc[l(null==a?null:a)];if(!e&&(e=bc._,!e))throw v("IVector.-assoc-n",a);return e.call(null,a,b,c)}function cc(a){if(a?a.Wb:a)return a.Wb(a);var b;b=cc[l(null==a?null:a)];if(!b&&(b=cc._,!b))throw v("IDeref.-deref",a);return b.call(null,a)}var dc={};
function ec(a){if(a?a.P:a)return a.P(a);var b;b=ec[l(null==a?null:a)];if(!b&&(b=ec._,!b))throw v("IMeta.-meta",a);return b.call(null,a)}var fc={};function gc(a,b){if(a?a.R:a)return a.R(a,b);var c;c=gc[l(null==a?null:a)];if(!c&&(c=gc._,!c))throw v("IWithMeta.-with-meta",a);return c.call(null,a,b)}
var hc={},ic=function(){function a(a,c,b){if(a?a.na:a)return a.na(a,c,b);var f;f=ic[l(null==a?null:a)];if(!f&&(f=ic._,!f))throw v("IReduce.-reduce",a);return f.call(null,a,c,b)}function b(a,c){if(a?a.ma:a)return a.ma(a,c);var b;b=ic[l(null==a?null:a)];if(!b&&(b=ic._,!b))throw v("IReduce.-reduce",a);return b.call(null,a,c)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=
a;return c}();function jc(a,b,c){if(a?a.Ec:a)return a.Ec(a,b,c);var e;e=jc[l(null==a?null:a)];if(!e&&(e=jc._,!e))throw v("IKVReduce.-kv-reduce",a);return e.call(null,a,b,c)}function lc(a,b){if(a?a.G:a)return a.G(a,b);var c;c=lc[l(null==a?null:a)];if(!c&&(c=lc._,!c))throw v("IEquiv.-equiv",a);return c.call(null,a,b)}function mc(a){if(a?a.L:a)return a.L(a);var b;b=mc[l(null==a?null:a)];if(!b&&(b=mc._,!b))throw v("IHash.-hash",a);return b.call(null,a)}var nc={};
function oc(a){if(a?a.U:a)return a.U(a);var b;b=oc[l(null==a?null:a)];if(!b&&(b=oc._,!b))throw v("ISeqable.-seq",a);return b.call(null,a)}var pc={};function qc(a,b){if(a?a.me:a)return a.me(0,b);var c;c=qc[l(null==a?null:a)];if(!c&&(c=qc._,!c))throw v("IWriter.-write",a);return c.call(null,a,b)}var rc={};function sc(a,b,c){if(a?a.M:a)return a.M(a,b,c);var e;e=sc[l(null==a?null:a)];if(!e&&(e=sc._,!e))throw v("IPrintWithWriter.-pr-writer",a);return e.call(null,a,b,c)}
function tc(a,b,c){if(a?a.le:a)return a.le(0,b,c);var e;e=tc[l(null==a?null:a)];if(!e&&(e=tc._,!e))throw v("IWatchable.-notify-watches",a);return e.call(null,a,b,c)}function uc(a){if(a?a.qc:a)return a.qc(a);var b;b=uc[l(null==a?null:a)];if(!b&&(b=uc._,!b))throw v("IEditableCollection.-as-transient",a);return b.call(null,a)}function vc(a,b){if(a?a.Xb:a)return a.Xb(a,b);var c;c=vc[l(null==a?null:a)];if(!c&&(c=vc._,!c))throw v("ITransientCollection.-conj!",a);return c.call(null,a,b)}
function wc(a){if(a?a.Yb:a)return a.Yb(a);var b;b=wc[l(null==a?null:a)];if(!b&&(b=wc._,!b))throw v("ITransientCollection.-persistent!",a);return b.call(null,a)}function xc(a,b,c){if(a?a.Jc:a)return a.Jc(a,b,c);var e;e=xc[l(null==a?null:a)];if(!e&&(e=xc._,!e))throw v("ITransientAssociative.-assoc!",a);return e.call(null,a,b,c)}function yc(a,b,c){if(a?a.ke:a)return a.ke(0,b,c);var e;e=yc[l(null==a?null:a)];if(!e&&(e=yc._,!e))throw v("ITransientVector.-assoc-n!",a);return e.call(null,a,b,c)}
function Ac(a){if(a?a.ie:a)return a.ie();var b;b=Ac[l(null==a?null:a)];if(!b&&(b=Ac._,!b))throw v("IChunk.-drop-first",a);return b.call(null,a)}function y(a){if(a?a.Jd:a)return a.Jd(a);var b;b=y[l(null==a?null:a)];if(!b&&(b=y._,!b))throw v("IChunkedSeq.-chunked-first",a);return b.call(null,a)}function A(a){if(a?a.Kd:a)return a.Kd(a);var b;b=A[l(null==a?null:a)];if(!b&&(b=A._,!b))throw v("IChunkedSeq.-chunked-rest",a);return b.call(null,a)}
function Bc(a){if(a?a.Id:a)return a.Id(a);var b;b=Bc[l(null==a?null:a)];if(!b&&(b=Bc._,!b))throw v("IChunkedNext.-chunked-next",a);return b.call(null,a)}function Cc(a,b){if(a?a.sf:a)return a.sf(a,b);var c;c=Cc[l(null==a?null:a)];if(!c&&(c=Cc._,!c))throw v("IReset.-reset!",a);return c.call(null,a,b)}
var Dc=function(){function a(a,c,b,e,d){if(a?a.wf:a)return a.wf(a,c,b,e,d);var q;q=Dc[l(null==a?null:a)];if(!q&&(q=Dc._,!q))throw v("ISwap.-swap!",a);return q.call(null,a,c,b,e,d)}function b(a,c,b,e){if(a?a.vf:a)return a.vf(a,c,b,e);var d;d=Dc[l(null==a?null:a)];if(!d&&(d=Dc._,!d))throw v("ISwap.-swap!",a);return d.call(null,a,c,b,e)}function c(a,c,b){if(a?a.uf:a)return a.uf(a,c,b);var e;e=Dc[l(null==a?null:a)];if(!e&&(e=Dc._,!e))throw v("ISwap.-swap!",a);return e.call(null,a,c,b)}function e(a,c){if(a?
a.tf:a)return a.tf(a,c);var b;b=Dc[l(null==a?null:a)];if(!b&&(b=Dc._,!b))throw v("ISwap.-swap!",a);return b.call(null,a,c)}var d=null,d=function(d,f,h,m,p){switch(arguments.length){case 2:return e.call(this,d,f);case 3:return c.call(this,d,f,h);case 4:return b.call(this,d,f,h,m);case 5:return a.call(this,d,f,h,m,p)}throw Error("Invalid arity: "+arguments.length);};d.e=e;d.j=c;d.D=b;d.O=a;return d}();
function Ec(a){if(a?a.Dc:a)return a.Dc(a);var b;b=Ec[l(null==a?null:a)];if(!b&&(b=Ec._,!b))throw v("IIterable.-iterator",a);return b.call(null,a)}function Fc(a){this.Nf=a;this.H=0;this.A=1073741824}Fc.prototype.me=function(a,b){return this.Nf.append(b)};function Gc(a){var b=new Ia;a.M(null,new Fc(b),lb());return""+w(b)}
var Hc="undefined"!==typeof Math.imul&&0!==(Math.imul.e?Math.imul.e(4294967295,5):Math.imul.call(null,4294967295,5))?function(a,b){return Math.imul.e?Math.imul.e(a,b):Math.imul.call(null,a,b)}:function(a,b){var c=a&65535,e=b&65535;return c*e+((a>>>16&65535)*e+c*(b>>>16&65535)<<16>>>0)|0};function Ic(a){a=Hc(a,3432918353);return Hc(a<<15|a>>>-15,461845907)}function Jc(a,b){var c=a^b;return Hc(c<<13|c>>>-13,5)+3864292196}
function Kc(a,b){var c=a^b,c=Hc(c^c>>>16,2246822507),c=Hc(c^c>>>13,3266489909);return c^c>>>16}function Lc(a){var b;a:{b=1;for(var c=0;;)if(b<a.length){var e=b+2,c=Jc(c,Ic(a.charCodeAt(b-1)|a.charCodeAt(b)<<16));b=e}else{b=c;break a}b=void 0}b=1===(a.length&1)?b^Ic(a.charCodeAt(a.length-1)):b;return Kc(b,Hc(2,a.length))}var Mc={},Nc=0;
function Oc(a){255<Nc&&(Mc={},Nc=0);var b=Mc[a];if("number"!==typeof b){a:if(null!=a)if(b=a.length,0<b){for(var c=0,e=0;;)if(c<b)var d=c+1,e=Hc(31,e)+a.charCodeAt(c),c=d;else{b=e;break a}b=void 0}else b=0;else b=0;Mc[a]=b;Nc+=1}return a=b}
function Pc(a){a&&(a.A&4194304||a.Ld)?a=a.L(null):"number"===typeof a?a=(Math.floor.h?Math.floor.h(a):Math.floor.call(null,a))%2147483647:!0===a?a=1:!1===a?a=0:"string"===typeof a?(a=Oc(a),0!==a&&(a=Ic(a),a=Jc(0,a),a=Kc(a,4))):a=a instanceof Date?a.valueOf():null==a?0:mc(a);return a}function Qc(a,b){return a^b+2654435769+(a<<6)+(a>>2)}
function Rc(a,b){if(a.xa===b.xa)return 0;var c=sb(a.wa);if(s(c?b.wa:c))return-1;if(s(a.wa)){if(sb(b.wa))return 1;c=fb(a.wa,b.wa);return 0===c?fb(a.name,b.name):c}return fb(a.name,b.name)}function Sc(a,b,c,e,d){this.wa=a;this.name=b;this.xa=c;this.mc=e;this.ya=d;this.A=2154168321;this.H=4096}k=Sc.prototype;k.M=function(a,b){return qc(b,this.xa)};k.L=function(){var a=this.mc;return null!=a?a:this.mc=a=Qc(Lc(this.name),Oc(this.wa))};k.R=function(a,b){return new Sc(this.wa,this.name,this.xa,this.mc,b)};
k.P=function(){return this.ya};k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return Ob.j(c,this,null);case 3:return Ob.j(c,this,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return Ob.j(c,this,null)};a.j=function(a,c,e){return Ob.j(c,this,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return Ob.j(a,this,null)};k.e=function(a,b){return Ob.j(a,this,b)};
k.G=function(a,b){return b instanceof Sc?this.xa===b.xa:!1};k.toString=function(){return this.xa};k.equiv=function(a){return this.G(null,a)};var Tc=function(){function a(a,c){var b=null!=a?[w(a),w("/"),w(c)].join(""):c;return new Sc(a,c,b,null,null)}function b(a){return a instanceof Sc?a:c.e(null,a)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();
function B(a){if(null==a)return null;if(a&&(a.A&8388608||a.zg))return a.U(null);if(a instanceof Array||"string"===typeof a)return 0===a.length?null:new C(a,0);if(t(nc,a))return oc(a);throw Error([w(a),w(" is not ISeqable")].join(""));}function D(a){if(null==a)return null;if(a&&(a.A&64||a.Ic))return a.la(null);a=B(a);return null==a?null:Kb(a)}function F(a){return null!=a?a&&(a.A&64||a.Ic)?a.pa(null):(a=B(a))?Lb(a):G:G}function I(a){return null==a?null:a&&(a.A&128||a.ed)?a.sa(null):B(F(a))}
var Uc=function(){function a(a,c){return null==a?null==c:a===c||lc(a,c)}var b=null,c=function(){function a(b,e,h){var m=null;if(2<arguments.length){for(var m=0,p=Array(arguments.length-2);m<p.length;)p[m]=arguments[m+2],++m;m=new C(p,0)}return c.call(this,b,e,m)}function c(a,e,d){for(;;)if(b.e(a,e))if(I(d))a=e,e=D(d),d=I(d);else return b.e(e,D(d));else return!1}a.q=2;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=F(a);return c(b,e,a)};a.k=c;return a}(),b=function(b,d,g){switch(arguments.length){case 1:return!0;
case 2:return a.call(this,b,d);default:var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;f=new C(h,0)}return c.k(b,d,f)}throw Error("Invalid arity: "+arguments.length);};b.q=2;b.l=c.l;b.h=function(){return!0};b.e=a;b.k=c.k;return b}();function Vc(a){this.K=a}Vc.prototype.next=function(){if(null!=this.K){var a=D(this.K);this.K=I(this.K);return{done:!1,value:a}}return{done:!0,value:null}};function Wc(a){return new Vc(B(a))}
function Xc(a,b){var c=Ic(a),c=Jc(0,c);return Kc(c,b)}function Yc(a){var b=0,c=1;for(a=B(a);;)if(null!=a)b+=1,c=Hc(31,c)+Pc(D(a))|0,a=I(a);else return Xc(c,b)}function Zc(a){var b=0,c=0;for(a=B(a);;)if(null!=a)b+=1,c=c+Pc(D(a))|0,a=I(a);else return Xc(c,b)}Cb["null"]=!0;Db["null"]=function(){return 0};Date.prototype.G=function(a,b){return b instanceof Date&&this.toString()===b.toString()};lc.number=function(a,b){return a===b};dc["function"]=!0;ec["function"]=function(){return null};
Ab["function"]=!0;mc._=function(a){return a[ia]||(a[ia]=++ja)};function $c(a){return a+1}function ad(a){this.C=a;this.H=0;this.A=32768}ad.prototype.Wb=function(){return this.C};function bd(a){return a instanceof ad}function J(a){return cc(a)}
var cd=function(){function a(a,c,b,e){for(var m=Db(a);;)if(e<m){var p=x.e(a,e);b=c.e?c.e(b,p):c.call(null,b,p);if(bd(b))return cc(b);e+=1}else return b}function b(a,c,b){var e=Db(a),m=b;for(b=0;;)if(b<e){var p=x.e(a,b),m=c.e?c.e(m,p):c.call(null,m,p);if(bd(m))return cc(m);b+=1}else return m}function c(a,c){var b=Db(a);if(0===b)return c.F?c.F():c.call(null);for(var e=x.e(a,0),m=1;;)if(m<b){var p=x.e(a,m),e=c.e?c.e(e,p):c.call(null,e,p);if(bd(e))return cc(e);m+=1}else return e}var e=null,e=function(e,
g,f,h){switch(arguments.length){case 2:return c.call(this,e,g);case 3:return b.call(this,e,g,f);case 4:return a.call(this,e,g,f,h)}throw Error("Invalid arity: "+arguments.length);};e.e=c;e.j=b;e.D=a;return e}(),ed=function(){function a(a,c,b,e){for(var m=a.length;;)if(e<m){var p=a[e];b=c.e?c.e(b,p):c.call(null,b,p);if(bd(b))return cc(b);e+=1}else return b}function b(a,c,b){var e=a.length,m=b;for(b=0;;)if(b<e){var p=a[b],m=c.e?c.e(m,p):c.call(null,m,p);if(bd(m))return cc(m);b+=1}else return m}function c(a,
c){var b=a.length;if(0===a.length)return c.F?c.F():c.call(null);for(var e=a[0],m=1;;)if(m<b){var p=a[m],e=c.e?c.e(e,p):c.call(null,e,p);if(bd(e))return cc(e);m+=1}else return e}var e=null,e=function(e,g,f,h){switch(arguments.length){case 2:return c.call(this,e,g);case 3:return b.call(this,e,g,f);case 4:return a.call(this,e,g,f,h)}throw Error("Invalid arity: "+arguments.length);};e.e=c;e.j=b;e.D=a;return e}();function fd(a){return a?a.A&2||a.ff?!0:a.A?!1:t(Cb,a):t(Cb,a)}
function gd(a){return a?a.A&16||a.je?!0:a.A?!1:t(Ib,a):t(Ib,a)}function hd(a,b){this.o=a;this.i=b}hd.prototype.qd=function(){return this.i<this.o.length};hd.prototype.next=function(){var a=this.o[this.i];this.i+=1;return a};function C(a,b){this.o=a;this.i=b;this.A=166199550;this.H=8192}k=C.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.s=function(a,b){var c=b+this.i;return c<this.o.length?this.o[c]:null};
k.Aa=function(a,b,c){a=b+this.i;return a<this.o.length?this.o[a]:c};k.Dc=function(){return new hd(this.o,this.i)};k.za=function(){return new C(this.o,this.i)};k.sa=function(){return this.i+1<this.o.length?new C(this.o,this.i+1):null};k.ca=function(){return this.o.length-this.i};k.L=function(){return Yc(this)};k.G=function(a,b){return id.e?id.e(this,b):id.call(null,this,b)};k.ea=function(){return G};k.ma=function(a,b){return ed.D(this.o,b,this.o[this.i],this.i+1)};
k.na=function(a,b,c){return ed.D(this.o,b,c,this.i)};k.la=function(){return this.o[this.i]};k.pa=function(){return this.i+1<this.o.length?new C(this.o,this.i+1):G};k.U=function(){return this};k.Y=function(a,b){return K.e?K.e(b,this):K.call(null,b,this)};C.prototype[vb]=function(){return Wc(this)};
var jd=function(){function a(a,c){return c<a.length?new C(a,c):null}function b(a){return c.e(a,0)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}(),L=function(){function a(a,c){return jd.e(a,c)}function b(a){return jd.e(a,0)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+
arguments.length);};c.h=b;c.e=a;return c}();function kd(a,b,c){this.Cc=a;this.i=b;this.meta=c;this.A=32374990;this.H=8192}k=kd.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.za=function(){return new kd(this.Cc,this.i,this.meta)};k.sa=function(){return 0<this.i?new kd(this.Cc,this.i-1,null):null};k.ca=function(){return this.i+1};k.L=function(){return Yc(this)};
k.G=function(a,b){return id.e?id.e(this,b):id.call(null,this,b)};k.ea=function(){var a=G,b=this.meta;return ld.e?ld.e(a,b):ld.call(null,a,b)};k.ma=function(a,b){return md.e?md.e(b,this):md.call(null,b,this)};k.na=function(a,b,c){return md.j?md.j(b,c,this):md.call(null,b,c,this)};k.la=function(){return x.e(this.Cc,this.i)};k.pa=function(){return 0<this.i?new kd(this.Cc,this.i-1,null):G};k.U=function(){return this};k.R=function(a,b){return new kd(this.Cc,this.i,b)};
k.Y=function(a,b){return K.e?K.e(b,this):K.call(null,b,this)};kd.prototype[vb]=function(){return Wc(this)};lc._=function(a,b){return a===b};
var od=function(){function a(a,c){return null!=a?Gb(a,c):Gb(G,c)}var b=null,c=function(){function a(b,e,h){var m=null;if(2<arguments.length){for(var m=0,p=Array(arguments.length-2);m<p.length;)p[m]=arguments[m+2],++m;m=new C(p,0)}return c.call(this,b,e,m)}function c(a,e,d){for(;;)if(s(d))a=b.e(a,e),e=D(d),d=I(d);else return b.e(a,e)}a.q=2;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=F(a);return c(b,e,a)};a.k=c;return a}(),b=function(b,d,g){switch(arguments.length){case 0:return nd;case 1:return b;
case 2:return a.call(this,b,d);default:var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;f=new C(h,0)}return c.k(b,d,f)}throw Error("Invalid arity: "+arguments.length);};b.q=2;b.l=c.l;b.F=function(){return nd};b.h=function(a){return a};b.e=a;b.k=c.k;return b}();
function N(a){if(null!=a)if(a&&(a.A&2||a.ff))a=a.ca(null);else if(a instanceof Array)a=a.length;else if("string"===typeof a)a=a.length;else if(t(Cb,a))a=Db(a);else a:{a=B(a);for(var b=0;;){if(fd(a)){a=b+Db(a);break a}a=I(a);b+=1}a=void 0}else a=0;return a}
var pd=function(){function a(a,c,b){for(;;){if(null==a)return b;if(0===c)return B(a)?D(a):b;if(gd(a))return x.j(a,c,b);if(B(a))a=I(a),c-=1;else return b}}function b(a,c){for(;;){if(null==a)throw Error("Index out of bounds");if(0===c){if(B(a))return D(a);throw Error("Index out of bounds");}if(gd(a))return x.e(a,c);if(B(a)){var b=I(a),f=c-1;a=b;c=f}else throw Error("Index out of bounds");}}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,
c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),O=function(){function a(a,c,b){if("number"!==typeof c)throw Error("index argument to nth must be a number.");if(null==a)return b;if(a&&(a.A&16||a.je))return a.Aa(null,c,b);if(a instanceof Array||"string"===typeof a)return c<a.length?a[c]:b;if(t(Ib,a))return x.e(a,c);if(a?a.A&64||a.Ic||(a.A?0:t(Jb,a)):t(Jb,a))return pd.j(a,c,b);throw Error([w("nth not supported on this type "),w(ub(tb(a)))].join(""));}function b(a,c){if("number"!==
typeof c)throw Error("index argument to nth must be a number");if(null==a)return a;if(a&&(a.A&16||a.je))return a.s(null,c);if(a instanceof Array||"string"===typeof a)return c<a.length?a[c]:null;if(t(Ib,a))return x.e(a,c);if(a?a.A&64||a.Ic||(a.A?0:t(Jb,a)):t(Jb,a))return pd.e(a,c);throw Error([w("nth not supported on this type "),w(ub(tb(a)))].join(""));}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+
arguments.length);};c.e=b;c.j=a;return c}(),P=function(){function a(a,c,b){return null!=a?a&&(a.A&256||a.mf)?a.J(null,c,b):a instanceof Array?c<a.length?a[c]:b:"string"===typeof a?c<a.length?a[c]:b:t(Nb,a)?Ob.j(a,c,b):b:b}function b(a,c){return null==a?null:a&&(a.A&256||a.mf)?a.I(null,c):a instanceof Array?c<a.length?a[c]:null:"string"===typeof a?c<a.length?a[c]:null:t(Nb,a)?Ob.e(a,c):null}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,
c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),rd=function(){function a(a,c,b){return null!=a?Rb(a,c,b):qd([c],[b])}var b=null,c=function(){function a(b,e,h,m){var p=null;if(3<arguments.length){for(var p=0,q=Array(arguments.length-3);p<q.length;)q[p]=arguments[p+3],++p;p=new C(q,0)}return c.call(this,b,e,h,p)}function c(a,e,d,m){for(;;)if(a=b.j(a,e,d),s(m))e=D(m),d=D(I(m)),m=I(I(m));else return a}a.q=3;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var m=D(a);
a=F(a);return c(b,e,m,a)};a.k=c;return a}(),b=function(b,d,g,f){switch(arguments.length){case 3:return a.call(this,b,d,g);default:var h=null;if(3<arguments.length){for(var h=0,m=Array(arguments.length-3);h<m.length;)m[h]=arguments[h+3],++h;h=new C(m,0)}return c.k(b,d,g,h)}throw Error("Invalid arity: "+arguments.length);};b.q=3;b.l=c.l;b.j=a;b.k=c.k;return b}(),sd=function(){function a(a,c){return null==a?null:Tb(a,c)}var b=null,c=function(){function a(b,e,h){var m=null;if(2<arguments.length){for(var m=
0,p=Array(arguments.length-2);m<p.length;)p[m]=arguments[m+2],++m;m=new C(p,0)}return c.call(this,b,e,m)}function c(a,e,d){for(;;){if(null==a)return null;a=b.e(a,e);if(s(d))e=D(d),d=I(d);else return a}}a.q=2;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=F(a);return c(b,e,a)};a.k=c;return a}(),b=function(b,d,g){switch(arguments.length){case 1:return b;case 2:return a.call(this,b,d);default:var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;
f=new C(h,0)}return c.k(b,d,f)}throw Error("Invalid arity: "+arguments.length);};b.q=2;b.l=c.l;b.h=function(a){return a};b.e=a;b.k=c.k;return b}();function ud(a){var b=ga(a);return s(b)?b:a?s(s(null)?null:a.ef)?!0:a.Od?!1:t(Ab,a):t(Ab,a)}function vd(a,b){this.v=a;this.meta=b;this.H=0;this.A=393217}k=vd.prototype;
k.call=function(){function a(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa,Ka,Za){a=this.v;return Q.cd?Q.cd(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa,Ka,Za):Q.call(null,a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa,Ka,Za)}function b(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa,Ka){a=this;return a.v.Ab?a.v.Ab(c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa,Ka):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa,Ka)}function c(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa){a=this;return a.v.zb?a.v.zb(c,b,e,d,f,g,h,m,p,q,r,u,z,
H,E,M,W,X,oa):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X,oa)}function e(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X){a=this;return a.v.yb?a.v.yb(c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W,X)}function d(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W){a=this;return a.v.xb?a.v.xb(c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M,W)}function g(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M){a=this;return a.v.wb?a.v.wb(c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M):
a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E,M)}function f(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E){a=this;return a.v.vb?a.v.vb(c,b,e,d,f,g,h,m,p,q,r,u,z,H,E):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H,E)}function h(a,c,b,e,d,f,g,h,m,p,q,r,u,z,H){a=this;return a.v.ub?a.v.ub(c,b,e,d,f,g,h,m,p,q,r,u,z,H):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z,H)}function m(a,c,b,e,d,f,g,h,m,p,q,r,u,z){a=this;return a.v.tb?a.v.tb(c,b,e,d,f,g,h,m,p,q,r,u,z):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u,z)}function p(a,c,b,e,d,f,g,h,m,
p,q,r,u){a=this;return a.v.sb?a.v.sb(c,b,e,d,f,g,h,m,p,q,r,u):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r,u)}function q(a,c,b,e,d,f,g,h,m,p,q,r){a=this;return a.v.rb?a.v.rb(c,b,e,d,f,g,h,m,p,q,r):a.v.call(null,c,b,e,d,f,g,h,m,p,q,r)}function r(a,c,b,e,d,f,g,h,m,p,q){a=this;return a.v.qb?a.v.qb(c,b,e,d,f,g,h,m,p,q):a.v.call(null,c,b,e,d,f,g,h,m,p,q)}function u(a,c,b,e,d,f,g,h,m,p){a=this;return a.v.Cb?a.v.Cb(c,b,e,d,f,g,h,m,p):a.v.call(null,c,b,e,d,f,g,h,m,p)}function z(a,c,b,e,d,f,g,h,m){a=this;return a.v.Bb?
a.v.Bb(c,b,e,d,f,g,h,m):a.v.call(null,c,b,e,d,f,g,h,m)}function E(a,c,b,e,d,f,g,h){a=this;return a.v.Pa?a.v.Pa(c,b,e,d,f,g,h):a.v.call(null,c,b,e,d,f,g,h)}function H(a,c,b,e,d,f,g){a=this;return a.v.ia?a.v.ia(c,b,e,d,f,g):a.v.call(null,c,b,e,d,f,g)}function M(a,c,b,e,d,f){a=this;return a.v.O?a.v.O(c,b,e,d,f):a.v.call(null,c,b,e,d,f)}function W(a,c,b,e,d){a=this;return a.v.D?a.v.D(c,b,e,d):a.v.call(null,c,b,e,d)}function X(a,c,b,e){a=this;return a.v.j?a.v.j(c,b,e):a.v.call(null,c,b,e)}function oa(a,
c,b){a=this;return a.v.e?a.v.e(c,b):a.v.call(null,c,b)}function Ka(a,c){a=this;return a.v.h?a.v.h(c):a.v.call(null,c)}function Za(a){a=this;return a.v.F?a.v.F():a.v.call(null)}var S=null,S=function(S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td,Yd,He,Mf,Nh,hk,Rn){switch(arguments.length){case 1:return Za.call(this,S);case 2:return Ka.call(this,S,Ja);case 3:return oa.call(this,S,Ja,Na);case 4:return X.call(this,S,Ja,Na,Qa);case 5:return W.call(this,S,Ja,Na,Qa,Ra);case 6:return M.call(this,S,Ja,Na,
Qa,Ra,Ya);case 7:return H.call(this,S,Ja,Na,Qa,Ra,Ya,eb);case 8:return E.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa);case 9:return z.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta);case 10:return u.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb);case 11:return r.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb);case 12:return q.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb);case 13:return p.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc);case 14:return m.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc);case 15:return h.call(this,
S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd);case 16:return f.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td);case 17:return g.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td,Yd);case 18:return d.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td,Yd,He);case 19:return e.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td,Yd,He,Mf);case 20:return c.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td,Yd,He,Mf,Nh);case 21:return b.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,
kb,Hb,Zb,kc,zc,dd,td,Yd,He,Mf,Nh,hk);case 22:return a.call(this,S,Ja,Na,Qa,Ra,Ya,eb,Sa,Ta,kb,Hb,Zb,kc,zc,dd,td,Yd,He,Mf,Nh,hk,Rn)}throw Error("Invalid arity: "+arguments.length);};S.h=Za;S.e=Ka;S.j=oa;S.D=X;S.O=W;S.ia=M;S.Pa=H;S.Bb=E;S.Cb=z;S.qb=u;S.rb=r;S.sb=q;S.tb=p;S.ub=m;S.vb=h;S.wb=f;S.xb=g;S.yb=d;S.zb=e;S.Ab=c;S.lf=b;S.cd=a;return S}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.F=function(){return this.v.F?this.v.F():this.v.call(null)};
k.h=function(a){return this.v.h?this.v.h(a):this.v.call(null,a)};k.e=function(a,b){return this.v.e?this.v.e(a,b):this.v.call(null,a,b)};k.j=function(a,b,c){return this.v.j?this.v.j(a,b,c):this.v.call(null,a,b,c)};k.D=function(a,b,c,e){return this.v.D?this.v.D(a,b,c,e):this.v.call(null,a,b,c,e)};k.O=function(a,b,c,e,d){return this.v.O?this.v.O(a,b,c,e,d):this.v.call(null,a,b,c,e,d)};k.ia=function(a,b,c,e,d,g){return this.v.ia?this.v.ia(a,b,c,e,d,g):this.v.call(null,a,b,c,e,d,g)};
k.Pa=function(a,b,c,e,d,g,f){return this.v.Pa?this.v.Pa(a,b,c,e,d,g,f):this.v.call(null,a,b,c,e,d,g,f)};k.Bb=function(a,b,c,e,d,g,f,h){return this.v.Bb?this.v.Bb(a,b,c,e,d,g,f,h):this.v.call(null,a,b,c,e,d,g,f,h)};k.Cb=function(a,b,c,e,d,g,f,h,m){return this.v.Cb?this.v.Cb(a,b,c,e,d,g,f,h,m):this.v.call(null,a,b,c,e,d,g,f,h,m)};k.qb=function(a,b,c,e,d,g,f,h,m,p){return this.v.qb?this.v.qb(a,b,c,e,d,g,f,h,m,p):this.v.call(null,a,b,c,e,d,g,f,h,m,p)};
k.rb=function(a,b,c,e,d,g,f,h,m,p,q){return this.v.rb?this.v.rb(a,b,c,e,d,g,f,h,m,p,q):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q)};k.sb=function(a,b,c,e,d,g,f,h,m,p,q,r){return this.v.sb?this.v.sb(a,b,c,e,d,g,f,h,m,p,q,r):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r)};k.tb=function(a,b,c,e,d,g,f,h,m,p,q,r,u){return this.v.tb?this.v.tb(a,b,c,e,d,g,f,h,m,p,q,r,u):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u)};
k.ub=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z){return this.v.ub?this.v.ub(a,b,c,e,d,g,f,h,m,p,q,r,u,z):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z)};k.vb=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E){return this.v.vb?this.v.vb(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E)};k.wb=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H){return this.v.wb?this.v.wb(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H)};
k.xb=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M){return this.v.xb?this.v.xb(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M)};k.yb=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W){return this.v.yb?this.v.yb(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W)};
k.zb=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X){return this.v.zb?this.v.zb(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X)};k.Ab=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa){return this.v.Ab?this.v.Ab(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa):this.v.call(null,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa)};
k.lf=function(a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka){var Za=this.v;return Q.cd?Q.cd(Za,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka):Q.call(null,Za,a,b,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka)};k.ef=!0;k.R=function(a,b){return new vd(this.v,b)};k.P=function(){return this.meta};function ld(a,b){return ud(a)&&!(a?a.A&262144||a.yf||(a.A?0:t(fc,a)):t(fc,a))?new vd(a,b):null==a?null:gc(a,b)}function wd(a){var b=null!=a;return(b?a?a.A&131072||a.pf||(a.A?0:t(dc,a)):t(dc,a):b)?ec(a):null}
function xd(a){return null==a?!1:a?a.A&8||a.xg?!0:a.A?!1:t(Fb,a):t(Fb,a)}function yd(a){return null==a?!1:a?a.A&4096||a.Bg?!0:a.A?!1:t(Xb,a):t(Xb,a)}function zd(a){return null==a?!1:a?a.A&1024||a.nf?!0:a.A?!1:t(Sb,a):t(Sb,a)}function Ad(a){return a?a.A&16384||a.Cg?!0:a.A?!1:t(ac,a):t(ac,a)}function R(a){return a?a.H&512||a.wg?!0:!1:!1}function Bd(a){var b=[];Ca(a,function(a,b){return function(a,c){return b.push(c)}}(a,b));return b}function Cd(a,b,c,e,d){for(;0!==d;)c[e]=a[b],e+=1,d-=1,b+=1}
function Dd(a,b,c,e,d){b+=d-1;for(e+=d-1;0!==d;)c[e]=a[b],e-=1,d-=1,b-=1}var Ed={};function Fd(a){return null==a?!1:a?a.A&64||a.Ic?!0:a.A?!1:t(Jb,a):t(Jb,a)}function Gd(a){return s(a)?!0:!1}function Hd(a,b){return P.j(a,b,Ed)===Ed?!1:!0}function Id(a,b){var c;if(c=null!=a)c=a?a.A&512||a.vg?!0:a.A?!1:t(Pb,a):t(Pb,a);return c&&Hd(a,b)?new T(null,2,5,U,[b,P.e(a,b)],null):null}
function Jd(a,b){if(a===b)return 0;if(null==a)return-1;if(null==b)return 1;if(tb(a)===tb(b))return a&&(a.H&2048||a.ad)?a.bd(null,b):fb(a,b);throw Error("compare on non-nil objects of different types");}
var Kd=function(){function a(a,c,b,f){for(;;){var h=Jd(O.e(a,f),O.e(c,f));if(0===h&&f+1<b)f+=1;else return h}}function b(a,b){var g=N(a),f=N(b);return g<f?-1:g>f?1:c.D(a,b,g,0)}var c=null,c=function(c,d,g,f){switch(arguments.length){case 2:return b.call(this,c,d);case 4:return a.call(this,c,d,g,f)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.D=a;return c}(),md=function(){function a(a,c,b){for(b=B(b);;)if(b){var f=D(b);c=a.e?a.e(c,f):a.call(null,c,f);if(bd(c))return cc(c);b=I(b)}else return c}
function b(a,c){var b=B(c);if(b){var f=D(b),b=I(b);return yb.j?yb.j(a,f,b):yb.call(null,a,f,b)}return a.F?a.F():a.call(null)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),yb=function(){function a(a,c,b){return b&&(b.A&524288||b.rf)?b.na(null,a,c):b instanceof Array?ed.j(b,a,c):"string"===typeof b?ed.j(b,a,c):t(hc,b)?ic.j(b,a,c):md.j(a,c,b)}function b(a,
c){return c&&(c.A&524288||c.rf)?c.ma(null,a):c instanceof Array?ed.e(c,a):"string"===typeof c?ed.e(c,a):t(hc,c)?ic.e(c,a):md.e(a,c)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}();function Ld(a,b){var c=["^ "];return null!=b?jc(b,a,c):c}function Md(a){return a}
var Nd=function(){function a(a,c,b,f){a=a.h?a.h(c):a.call(null,c);b=yb.j(a,b,f);return a.h?a.h(b):a.call(null,b)}function b(a,b,g){return c.D(a,b,b.F?b.F():b.call(null),g)}var c=null,c=function(c,d,g,f){switch(arguments.length){case 3:return b.call(this,c,d,g);case 4:return a.call(this,c,d,g,f)}throw Error("Invalid arity: "+arguments.length);};c.j=b;c.D=a;return c}();function Od(a){return a-1}
function Pd(a){a=(a-a%2)/2;return 0<=a?Math.floor.h?Math.floor.h(a):Math.floor.call(null,a):Math.ceil.h?Math.ceil.h(a):Math.ceil.call(null,a)}function Qd(a){a-=a>>1&1431655765;a=(a&858993459)+(a>>2&858993459);return 16843009*(a+(a>>4)&252645135)>>24}function Rd(a){var b=1;for(a=B(a);;)if(a&&0<b)b-=1,a=I(a);else return a}
var w=function(){function a(a){return null==a?"":Aa(a)}var b=null,c=function(){function a(b,e){var h=null;if(1<arguments.length){for(var h=0,m=Array(arguments.length-1);h<m.length;)m[h]=arguments[h+1],++h;h=new C(m,0)}return c.call(this,b,h)}function c(a,e){for(var d=new Ia(b.h(a)),m=e;;)if(s(m))d=d.append(b.h(D(m))),m=I(m);else return d.toString()}a.q=1;a.l=function(a){var b=D(a);a=F(a);return c(b,a)};a.k=c;return a}(),b=function(b,d){switch(arguments.length){case 0:return"";case 1:return a.call(this,
b);default:var g=null;if(1<arguments.length){for(var g=0,f=Array(arguments.length-1);g<f.length;)f[g]=arguments[g+1],++g;g=new C(f,0)}return c.k(b,g)}throw Error("Invalid arity: "+arguments.length);};b.q=1;b.l=c.l;b.F=function(){return""};b.h=a;b.k=c.k;return b}(),Sd=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return a.substring(c);case 3:return a.substring(c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return a.substring(c)};a.j=function(a,c,
e){return a.substring(c,e)};return a}();function id(a,b){var c;if(b?b.A&16777216||b.Ag||(b.A?0:t(pc,b)):t(pc,b))if(fd(a)&&fd(b)&&N(a)!==N(b))c=!1;else a:{c=B(a);for(var e=B(b);;){if(null==c){c=null==e;break a}if(null!=e&&Uc.e(D(c),D(e)))c=I(c),e=I(e);else{c=!1;break a}}c=void 0}else c=null;return Gd(c)}
function Td(a){var b=0;for(a=B(a);;)if(a){var c=D(a),b=(b+(Pc(function(){var a=c;return Ud.h?Ud.h(a):Ud.call(null,a)}())^Pc(function(){var a=c;return Vd.h?Vd.h(a):Vd.call(null,a)}())))%4503599627370496;a=I(a)}else return b}function Wd(a,b,c,e,d){this.meta=a;this.first=b;this.ib=c;this.count=e;this.B=d;this.A=65937646;this.H=8192}k=Wd.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};
k.za=function(){return new Wd(this.meta,this.first,this.ib,this.count,this.B)};k.sa=function(){return 1===this.count?null:this.ib};k.ca=function(){return this.count};k.Mb=function(){return this.first};k.Nb=function(){return Lb(this)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return gc(G,this.meta)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return this.first};
k.pa=function(){return 1===this.count?G:this.ib};k.U=function(){return this};k.R=function(a,b){return new Wd(b,this.first,this.ib,this.count,this.B)};k.Y=function(a,b){return new Wd(this.meta,b,this,this.count+1,null)};Wd.prototype[vb]=function(){return Wc(this)};function Xd(a){this.meta=a;this.A=65937614;this.H=8192}k=Xd.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.za=function(){return new Xd(this.meta)};k.sa=function(){return null};
k.ca=function(){return 0};k.Mb=function(){return null};k.Nb=function(){throw Error("Can't pop empty list");};k.L=function(){return 0};k.G=function(a,b){return id(this,b)};k.ea=function(){return this};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return null};k.pa=function(){return G};k.U=function(){return null};k.R=function(a,b){return new Xd(b)};k.Y=function(a,b){return new Wd(this.meta,b,null,1,null)};var G=new Xd(null);Xd.prototype[vb]=function(){return Wc(this)};
var Zd=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b;if(a instanceof C&&0===a.i)b=a.o;else a:{for(b=[];;)if(null!=a)b.push(a.la(null)),a=a.sa(null);else break a;b=void 0}a=b.length;for(var d=G;;)if(0<a){var g=a-1,d=d.Y(null,b[a-1]);a=g}else return d}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}();
function $d(a,b,c,e){this.meta=a;this.first=b;this.ib=c;this.B=e;this.A=65929452;this.H=8192}k=$d.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.za=function(){return new $d(this.meta,this.first,this.ib,this.B)};k.sa=function(){return null==this.ib?null:B(this.ib)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};
k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return this.first};k.pa=function(){return null==this.ib?G:this.ib};k.U=function(){return this};k.R=function(a,b){return new $d(b,this.first,this.ib,this.B)};k.Y=function(a,b){return new $d(null,b,this,this.B)};$d.prototype[vb]=function(){return Wc(this)};function K(a,b){var c=null==b;return(c?c:b&&(b.A&64||b.Ic))?new $d(null,a,b,null):new $d(null,a,B(b),null)}
function ae(a,b){if(a.La===b.La)return 0;var c=sb(a.wa);if(s(c?b.wa:c))return-1;if(s(a.wa)){if(sb(b.wa))return 1;c=fb(a.wa,b.wa);return 0===c?fb(a.name,b.name):c}return fb(a.name,b.name)}function V(a,b,c,e){this.wa=a;this.name=b;this.La=c;this.mc=e;this.A=2153775105;this.H=4096}k=V.prototype;k.M=function(a,b){return qc(b,[w(":"),w(this.La)].join(""))};k.L=function(){var a=this.mc;return null!=a?a:this.mc=a=Qc(Lc(this.name),Oc(this.wa))+2654435769|0};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return P.e(c,this);case 3:return P.j(c,this,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return P.e(c,this)};a.j=function(a,c,e){return P.j(c,this,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return P.e(a,this)};k.e=function(a,b){return P.j(a,this,b)};k.G=function(a,b){return b instanceof V?this.La===b.La:!1};
k.toString=function(){return[w(":"),w(this.La)].join("")};k.equiv=function(a){return this.G(null,a)};function be(a,b){return a===b?!0:a instanceof V&&b instanceof V?a.La===b.La:!1}
var de=function(){function a(a,c){return new V(a,c,[w(s(a)?[w(a),w("/")].join(""):null),w(c)].join(""),null)}function b(a){if(a instanceof V)return a;if(a instanceof Sc){var c;if(a&&(a.H&4096||a.qf))c=a.wa;else throw Error([w("Doesn't support namespace: "),w(a)].join(""));return new V(c,ce.h?ce.h(a):ce.call(null,a),a.xa,null)}return"string"===typeof a?(c=a.split("/"),2===c.length?new V(c[0],c[1],a,null):new V(null,c[0],a,null)):null}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,
c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();function ee(a,b,c,e){this.meta=a;this.Pb=b;this.K=c;this.B=e;this.H=0;this.A=32374988}k=ee.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};function fe(a){null!=a.Pb&&(a.K=a.Pb.F?a.Pb.F():a.Pb.call(null),a.Pb=null);return a.K}k.P=function(){return this.meta};k.sa=function(){oc(this);return null==this.K?null:I(this.K)};
k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){oc(this);return null==this.K?null:D(this.K)};k.pa=function(){oc(this);return null!=this.K?F(this.K):G};k.U=function(){fe(this);if(null==this.K)return null;for(var a=this.K;;)if(a instanceof ee)a=fe(a);else return this.K=a,B(this.K)};
k.R=function(a,b){return new ee(b,this.Pb,this.K,this.B)};k.Y=function(a,b){return K(b,this)};ee.prototype[vb]=function(){return Wc(this)};function ge(a,b){this.X=a;this.end=b;this.H=0;this.A=2}ge.prototype.ca=function(){return this.end};ge.prototype.add=function(a){this.X[this.end]=a;return this.end+=1};ge.prototype.Ia=function(){var a=new he(this.X,0,this.end);this.X=null;return a};function he(a,b,c){this.o=a;this.ra=b;this.end=c;this.H=0;this.A=524306}k=he.prototype;
k.ma=function(a,b){return ed.D(this.o,b,this.o[this.ra],this.ra+1)};k.na=function(a,b,c){return ed.D(this.o,b,c,this.ra)};k.ie=function(){if(this.ra===this.end)throw Error("-drop-first of empty chunk");return new he(this.o,this.ra+1,this.end)};k.s=function(a,b){return this.o[this.ra+b]};k.Aa=function(a,b,c){return 0<=b&&b<this.end-this.ra?this.o[this.ra+b]:c};k.ca=function(){return this.end-this.ra};
var ie=function(){function a(a,c,b){return new he(a,c,b)}function b(a,c){return new he(a,c,a.length)}function c(a){return new he(a,0,a.length)}var e=null,e=function(e,g,f){switch(arguments.length){case 1:return c.call(this,e);case 2:return b.call(this,e,g);case 3:return a.call(this,e,g,f)}throw Error("Invalid arity: "+arguments.length);};e.h=c;e.e=b;e.j=a;return e}();function je(a,b,c,e){this.Ia=a;this.lb=b;this.meta=c;this.B=e;this.A=31850732;this.H=1536}k=je.prototype;k.toString=function(){return Gc(this)};
k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.sa=function(){if(1<Db(this.Ia))return new je(Ac(this.Ia),this.lb,this.meta,null);var a=oc(this.lb);return null==a?null:a};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.la=function(){return x.e(this.Ia,0)};k.pa=function(){return 1<Db(this.Ia)?new je(Ac(this.Ia),this.lb,this.meta,null):null==this.lb?G:this.lb};k.U=function(){return this};
k.Jd=function(){return this.Ia};k.Kd=function(){return null==this.lb?G:this.lb};k.R=function(a,b){return new je(this.Ia,this.lb,b,this.B)};k.Y=function(a,b){return K(b,this)};k.Id=function(){return null==this.lb?null:this.lb};je.prototype[vb]=function(){return Wc(this)};function ke(a,b){return 0===Db(a)?b:new je(a,b,null,null)}function le(a,b){a.add(b)}function me(a){for(var b=[];;)if(B(a))b.push(D(a)),a=I(a);else return b}
var ne=function(){function a(a,c){var b=Array(a);if(Fd(c))for(var f=0,h=B(c);;)if(h&&f<a)b[f]=D(h),f+=1,h=I(h);else return b;else{for(f=0;;)if(f<a)b[f]=c,f+=1;else break;return b}}function b(a){return"number"===typeof a?c.e(a,null):zb.h(a)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();
function oe(a,b){if(fd(a))return N(a);for(var c=a,e=b,d=0;;)if(0<e&&B(c))c=I(c),e-=1,d+=1;else return d}
var qe=function pe(b){return null==b?null:null==I(b)?B(D(b)):K(D(b),pe(I(b)))},re=function(){function a(a,c){return new ee(null,function(){var b=B(a);return b?R(b)?ke(y(b),e.e(A(b),c)):K(D(b),e.e(F(b),c)):c},null,null)}function b(a){return new ee(null,function(){return a},null,null)}function c(){return new ee(null,function(){return null},null,null)}var e=null,d=function(){function a(b,e,d){var g=null;if(2<arguments.length){for(var g=0,r=Array(arguments.length-2);g<r.length;)r[g]=arguments[g+2],++g;
g=new C(r,0)}return c.call(this,b,e,g)}function c(a,b,d){return function r(a,c){return new ee(null,function(){var b=B(a);return b?R(b)?ke(y(b),r(A(b),c)):K(D(b),r(F(b),c)):s(c)?r(D(c),I(c)):null},null,null)}(e.e(a,b),d)}a.q=2;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=F(a);return c(b,e,a)};a.k=c;return a}(),e=function(e,f,h){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,e);case 2:return a.call(this,e,f);default:var m=null;if(2<arguments.length){for(var m=0,p=Array(arguments.length-
2);m<p.length;)p[m]=arguments[m+2],++m;m=new C(p,0)}return d.k(e,f,m)}throw Error("Invalid arity: "+arguments.length);};e.q=2;e.l=d.l;e.F=c;e.h=b;e.e=a;e.k=d.k;return e}(),se=function(){function a(a,c,b,e){return K(a,K(c,K(b,e)))}function b(a,c,b){return K(a,K(c,b))}var c=null,e=function(){function a(b,e,d,p,q){var r=null;if(4<arguments.length){for(var r=0,u=Array(arguments.length-4);r<u.length;)u[r]=arguments[r+4],++r;r=new C(u,0)}return c.call(this,b,e,d,p,r)}function c(a,b,e,d,g){return K(a,K(b,
K(e,K(d,qe(g)))))}a.q=4;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var q=D(a);a=F(a);return c(b,e,d,q,a)};a.k=c;return a}(),c=function(c,g,f,h,m){switch(arguments.length){case 1:return B(c);case 2:return K(c,g);case 3:return b.call(this,c,g,f);case 4:return a.call(this,c,g,f,h);default:var p=null;if(4<arguments.length){for(var p=0,q=Array(arguments.length-4);p<q.length;)q[p]=arguments[p+4],++p;p=new C(q,0)}return e.k(c,g,f,h,p)}throw Error("Invalid arity: "+arguments.length);
};c.q=4;c.l=e.l;c.h=function(a){return B(a)};c.e=function(a,c){return K(a,c)};c.j=b;c.D=a;c.k=e.k;return c}();function te(a){return wc(a)}
var ue=function(){function a(){return uc(nd)}var b=null,c=function(){function a(b,e,h){var m=null;if(2<arguments.length){for(var m=0,p=Array(arguments.length-2);m<p.length;)p[m]=arguments[m+2],++m;m=new C(p,0)}return c.call(this,b,e,m)}function c(a,b,e){for(;;)if(a=vc(a,b),s(e))b=D(e),e=I(e);else return a}a.q=2;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=F(a);return c(b,e,a)};a.k=c;return a}(),b=function(b,d,g){switch(arguments.length){case 0:return a.call(this);case 1:return b;case 2:return vc(b,
d);default:var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;f=new C(h,0)}return c.k(b,d,f)}throw Error("Invalid arity: "+arguments.length);};b.q=2;b.l=c.l;b.F=a;b.h=function(a){return a};b.e=function(a,c){return vc(a,c)};b.k=c.k;return b}(),ve=function(){var a=null,b=function(){function a(c,g,f,h){var m=null;if(3<arguments.length){for(var m=0,p=Array(arguments.length-3);m<p.length;)p[m]=arguments[m+3],++m;m=new C(p,0)}return b.call(this,
c,g,f,m)}function b(a,c,e,h){for(;;)if(a=xc(a,c,e),s(h))c=D(h),e=D(I(h)),h=I(I(h));else return a}a.q=3;a.l=function(a){var c=D(a);a=I(a);var f=D(a);a=I(a);var h=D(a);a=F(a);return b(c,f,h,a)};a.k=b;return a}(),a=function(a,e,d,g){switch(arguments.length){case 3:return xc(a,e,d);default:var f=null;if(3<arguments.length){for(var f=0,h=Array(arguments.length-3);f<h.length;)h[f]=arguments[f+3],++f;f=new C(h,0)}return b.k(a,e,d,f)}throw Error("Invalid arity: "+arguments.length);};a.q=3;a.l=b.l;a.j=function(a,
b,d){return xc(a,b,d)};a.k=b.k;return a}();
function we(a,b,c){var e=B(c);if(0===b)return a.F?a.F():a.call(null);c=Kb(e);var d=Lb(e);if(1===b)return a.h?a.h(c):a.h?a.h(c):a.call(null,c);var e=Kb(d),g=Lb(d);if(2===b)return a.e?a.e(c,e):a.e?a.e(c,e):a.call(null,c,e);var d=Kb(g),f=Lb(g);if(3===b)return a.j?a.j(c,e,d):a.j?a.j(c,e,d):a.call(null,c,e,d);var g=Kb(f),h=Lb(f);if(4===b)return a.D?a.D(c,e,d,g):a.D?a.D(c,e,d,g):a.call(null,c,e,d,g);var f=Kb(h),m=Lb(h);if(5===b)return a.O?a.O(c,e,d,g,f):a.O?a.O(c,e,d,g,f):a.call(null,c,e,d,g,f);var h=Kb(m),
p=Lb(m);if(6===b)return a.ia?a.ia(c,e,d,g,f,h):a.ia?a.ia(c,e,d,g,f,h):a.call(null,c,e,d,g,f,h);var m=Kb(p),q=Lb(p);if(7===b)return a.Pa?a.Pa(c,e,d,g,f,h,m):a.Pa?a.Pa(c,e,d,g,f,h,m):a.call(null,c,e,d,g,f,h,m);var p=Kb(q),r=Lb(q);if(8===b)return a.Bb?a.Bb(c,e,d,g,f,h,m,p):a.Bb?a.Bb(c,e,d,g,f,h,m,p):a.call(null,c,e,d,g,f,h,m,p);var q=Kb(r),u=Lb(r);if(9===b)return a.Cb?a.Cb(c,e,d,g,f,h,m,p,q):a.Cb?a.Cb(c,e,d,g,f,h,m,p,q):a.call(null,c,e,d,g,f,h,m,p,q);var r=Kb(u),z=Lb(u);if(10===b)return a.qb?a.qb(c,
e,d,g,f,h,m,p,q,r):a.qb?a.qb(c,e,d,g,f,h,m,p,q,r):a.call(null,c,e,d,g,f,h,m,p,q,r);var u=Kb(z),E=Lb(z);if(11===b)return a.rb?a.rb(c,e,d,g,f,h,m,p,q,r,u):a.rb?a.rb(c,e,d,g,f,h,m,p,q,r,u):a.call(null,c,e,d,g,f,h,m,p,q,r,u);var z=Kb(E),H=Lb(E);if(12===b)return a.sb?a.sb(c,e,d,g,f,h,m,p,q,r,u,z):a.sb?a.sb(c,e,d,g,f,h,m,p,q,r,u,z):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z);var E=Kb(H),M=Lb(H);if(13===b)return a.tb?a.tb(c,e,d,g,f,h,m,p,q,r,u,z,E):a.tb?a.tb(c,e,d,g,f,h,m,p,q,r,u,z,E):a.call(null,c,e,d,g,f,h,m,
p,q,r,u,z,E);var H=Kb(M),W=Lb(M);if(14===b)return a.ub?a.ub(c,e,d,g,f,h,m,p,q,r,u,z,E,H):a.ub?a.ub(c,e,d,g,f,h,m,p,q,r,u,z,E,H):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z,E,H);var M=Kb(W),X=Lb(W);if(15===b)return a.vb?a.vb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M):a.vb?a.vb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M);var W=Kb(X),oa=Lb(X);if(16===b)return a.wb?a.wb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W):a.wb?a.wb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W);var X=
Kb(oa),Ka=Lb(oa);if(17===b)return a.xb?a.xb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X):a.xb?a.xb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X);var oa=Kb(Ka),Za=Lb(Ka);if(18===b)return a.yb?a.yb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa):a.yb?a.yb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa);Ka=Kb(Za);Za=Lb(Za);if(19===b)return a.zb?a.zb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka):a.zb?a.zb(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka):a.call(null,
c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka);var S=Kb(Za);Lb(Za);if(20===b)return a.Ab?a.Ab(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka,S):a.Ab?a.Ab(c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka,S):a.call(null,c,e,d,g,f,h,m,p,q,r,u,z,E,H,M,W,X,oa,Ka,S);throw Error("Only up to 20 arguments supported on functions");}
var Q=function(){function a(a,c,b,e,d){c=se.D(c,b,e,d);b=a.q;return a.l?(e=oe(c,b+1),e<=b?we(a,e,c):a.l(c)):a.apply(a,me(c))}function b(a,c,b,e){c=se.j(c,b,e);b=a.q;return a.l?(e=oe(c,b+1),e<=b?we(a,e,c):a.l(c)):a.apply(a,me(c))}function c(a,c,b){c=se.e(c,b);b=a.q;if(a.l){var e=oe(c,b+1);return e<=b?we(a,e,c):a.l(c)}return a.apply(a,me(c))}function e(a,c){var b=a.q;if(a.l){var e=oe(c,b+1);return e<=b?we(a,e,c):a.l(c)}return a.apply(a,me(c))}var d=null,g=function(){function a(b,e,d,f,g,z){var E=null;
if(5<arguments.length){for(var E=0,H=Array(arguments.length-5);E<H.length;)H[E]=arguments[E+5],++E;E=new C(H,0)}return c.call(this,b,e,d,f,g,E)}function c(a,b,e,d,f,g){b=K(b,K(e,K(d,K(f,qe(g)))));e=a.q;return a.l?(d=oe(b,e+1),d<=e?we(a,d,b):a.l(b)):a.apply(a,me(b))}a.q=5;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var f=D(a);a=I(a);var g=D(a);a=F(a);return c(b,e,d,f,g,a)};a.k=c;return a}(),d=function(d,h,m,p,q,r){switch(arguments.length){case 2:return e.call(this,d,h);case 3:return c.call(this,
d,h,m);case 4:return b.call(this,d,h,m,p);case 5:return a.call(this,d,h,m,p,q);default:var u=null;if(5<arguments.length){for(var u=0,z=Array(arguments.length-5);u<z.length;)z[u]=arguments[u+5],++u;u=new C(z,0)}return g.k(d,h,m,p,q,u)}throw Error("Invalid arity: "+arguments.length);};d.q=5;d.l=g.l;d.e=e;d.j=c;d.D=b;d.O=a;d.k=g.k;return d}(),xe=function(){function a(a,c){return!Uc.e(a,c)}var b=null,c=function(){function a(b,e,h){var m=null;if(2<arguments.length){for(var m=0,p=Array(arguments.length-
2);m<p.length;)p[m]=arguments[m+2],++m;m=new C(p,0)}return c.call(this,b,e,m)}function c(a,b,e){return sb(Q.D(Uc,a,b,e))}a.q=2;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=F(a);return c(b,e,a)};a.k=c;return a}(),b=function(b,d,g){switch(arguments.length){case 1:return!1;case 2:return a.call(this,b,d);default:var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;f=new C(h,0)}return c.k(b,d,f)}throw Error("Invalid arity: "+arguments.length);
};b.q=2;b.l=c.l;b.h=function(){return!1};b.e=a;b.k=c.k;return b}();function ye(a,b){for(;;){if(null==B(b))return!0;var c;c=D(b);c=a.h?a.h(c):a.call(null,c);if(s(c)){c=a;var e=I(b);a=c;b=e}else return!1}}function ze(a,b){for(;;)if(B(b)){var c;c=D(b);c=a.h?a.h(c):a.call(null,c);if(s(c))return c;c=a;var e=I(b);a=c;b=e}else return null}
function Ae(){return function(){function a(a){if(0<arguments.length)for(var c=0,e=Array(arguments.length-0);c<e.length;)e[c]=arguments[c+0],++c;return!1}a.q=0;a.l=function(a){B(a);return!1};a.k=function(){return!1};return a}()}
var Be=function(){function a(a,c,b,e){return function(){function d(p,q,r){return a.ia?a.ia(c,b,e,p,q,r):a.call(null,c,b,e,p,q,r)}function q(d,p){return a.O?a.O(c,b,e,d,p):a.call(null,c,b,e,d,p)}function r(d){return a.D?a.D(c,b,e,d):a.call(null,c,b,e,d)}function u(){return a.j?a.j(c,b,e):a.call(null,c,b,e)}var z=null,E=function(){function d(a,c,b,e){var f=null;if(3<arguments.length){for(var f=0,g=Array(arguments.length-3);f<g.length;)g[f]=arguments[f+3],++f;f=new C(g,0)}return p.call(this,a,c,b,f)}
function p(d,q,r,u){return Q.k(a,c,b,e,d,L([q,r,u],0))}d.q=3;d.l=function(a){var c=D(a);a=I(a);var b=D(a);a=I(a);var e=D(a);a=F(a);return p(c,b,e,a)};d.k=p;return d}(),z=function(a,c,b,e){switch(arguments.length){case 0:return u.call(this);case 1:return r.call(this,a);case 2:return q.call(this,a,c);case 3:return d.call(this,a,c,b);default:var f=null;if(3<arguments.length){for(var f=0,g=Array(arguments.length-3);f<g.length;)g[f]=arguments[f+3],++f;f=new C(g,0)}return E.k(a,c,b,f)}throw Error("Invalid arity: "+
arguments.length);};z.q=3;z.l=E.l;z.F=u;z.h=r;z.e=q;z.j=d;z.k=E.k;return z}()}function b(a,c,b){return function(){function e(d,m,p){return a.O?a.O(c,b,d,m,p):a.call(null,c,b,d,m,p)}function d(e,m){return a.D?a.D(c,b,e,m):a.call(null,c,b,e,m)}function q(e){return a.j?a.j(c,b,e):a.call(null,c,b,e)}function r(){return a.e?a.e(c,b):a.call(null,c,b)}var u=null,z=function(){function e(a,c,b,f){var g=null;if(3<arguments.length){for(var g=0,h=Array(arguments.length-3);g<h.length;)h[g]=arguments[g+3],++g;
g=new C(h,0)}return d.call(this,a,c,b,g)}function d(e,m,p,q){return Q.k(a,c,b,e,m,L([p,q],0))}e.q=3;e.l=function(a){var c=D(a);a=I(a);var b=D(a);a=I(a);var e=D(a);a=F(a);return d(c,b,e,a)};e.k=d;return e}(),u=function(a,c,b,f){switch(arguments.length){case 0:return r.call(this);case 1:return q.call(this,a);case 2:return d.call(this,a,c);case 3:return e.call(this,a,c,b);default:var g=null;if(3<arguments.length){for(var g=0,h=Array(arguments.length-3);g<h.length;)h[g]=arguments[g+3],++g;g=new C(h,0)}return z.k(a,
c,b,g)}throw Error("Invalid arity: "+arguments.length);};u.q=3;u.l=z.l;u.F=r;u.h=q;u.e=d;u.j=e;u.k=z.k;return u}()}function c(a,c){return function(){function b(e,d,h){return a.D?a.D(c,e,d,h):a.call(null,c,e,d,h)}function e(b,d){return a.j?a.j(c,b,d):a.call(null,c,b,d)}function d(b){return a.e?a.e(c,b):a.call(null,c,b)}function q(){return a.h?a.h(c):a.call(null,c)}var r=null,u=function(){function b(a,c,d,f){var g=null;if(3<arguments.length){for(var g=0,h=Array(arguments.length-3);g<h.length;)h[g]=
arguments[g+3],++g;g=new C(h,0)}return e.call(this,a,c,d,g)}function e(b,d,h,m){return Q.k(a,c,b,d,h,L([m],0))}b.q=3;b.l=function(a){var c=D(a);a=I(a);var b=D(a);a=I(a);var d=D(a);a=F(a);return e(c,b,d,a)};b.k=e;return b}(),r=function(a,c,f,g){switch(arguments.length){case 0:return q.call(this);case 1:return d.call(this,a);case 2:return e.call(this,a,c);case 3:return b.call(this,a,c,f);default:var r=null;if(3<arguments.length){for(var r=0,X=Array(arguments.length-3);r<X.length;)X[r]=arguments[r+3],
++r;r=new C(X,0)}return u.k(a,c,f,r)}throw Error("Invalid arity: "+arguments.length);};r.q=3;r.l=u.l;r.F=q;r.h=d;r.e=e;r.j=b;r.k=u.k;return r}()}var e=null,d=function(){function a(b,e,d,g,r){var u=null;if(4<arguments.length){for(var u=0,z=Array(arguments.length-4);u<z.length;)z[u]=arguments[u+4],++u;u=new C(z,0)}return c.call(this,b,e,d,g,u)}function c(a,b,e,d,f){return function(){function c(a){var b=null;if(0<arguments.length){for(var b=0,e=Array(arguments.length-0);b<e.length;)e[b]=arguments[b+
0],++b;b=new C(e,0)}return g.call(this,b)}function g(c){return Q.O(a,b,e,d,re.e(f,c))}c.q=0;c.l=function(a){a=B(a);return g(a)};c.k=g;return c}()}a.q=4;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var g=D(a);a=F(a);return c(b,e,d,g,a)};a.k=c;return a}(),e=function(e,f,h,m,p){switch(arguments.length){case 1:return e;case 2:return c.call(this,e,f);case 3:return b.call(this,e,f,h);case 4:return a.call(this,e,f,h,m);default:var q=null;if(4<arguments.length){for(var q=0,r=Array(arguments.length-
4);q<r.length;)r[q]=arguments[q+4],++q;q=new C(r,0)}return d.k(e,f,h,m,q)}throw Error("Invalid arity: "+arguments.length);};e.q=4;e.l=d.l;e.h=function(a){return a};e.e=c;e.j=b;e.D=a;e.k=d.k;return e}();
function Ce(a,b){return function e(b,g){return new ee(null,function(){var f=B(g);if(f){if(R(f)){for(var h=y(f),m=N(h),p=new ge(Array(m),0),q=0;;)if(q<m)le(p,function(){var e=b+q,f=x.e(h,q);return a.e?a.e(e,f):a.call(null,e,f)}()),q+=1;else break;return ke(p.Ia(),e(b+m,A(f)))}return K(function(){var e=D(f);return a.e?a.e(b,e):a.call(null,b,e)}(),e(b+1,F(f)))}return null},null,null)}(0,b)}function De(a,b,c,e){this.state=a;this.meta=b;this.Sf=c;this.Ye=e;this.A=6455296;this.H=16386}k=De.prototype;
k.L=function(){return this[ia]||(this[ia]=++ja)};k.le=function(a,b,c){for(var e=B(this.Ye),d=null,g=0,f=0;;)if(f<g){a=d.s(null,f);var h=O.j(a,0,null);a=O.j(a,1,null);var m=b,p=c;a.D?a.D(h,this,m,p):a.call(null,h,this,m,p);f+=1}else if(a=B(e))e=a,R(e)?(d=y(e),e=A(e),a=d,g=N(d),d=a):(a=D(e),h=O.j(a,0,null),a=O.j(a,1,null),d=h,g=b,f=c,a.D?a.D(d,this,g,f):a.call(null,d,this,g,f),e=I(e),d=null,g=0),f=0;else return null};k.P=function(){return this.meta};k.Wb=function(){return this.state};
k.G=function(a,b){return this===b};k.equiv=function(a){return this.G(null,a)};
var Ge=function(){function a(a){return new De(a,null,null,null)}var b=null,c=function(){function a(b,e){var h=null;if(1<arguments.length){for(var h=0,m=Array(arguments.length-1);h<m.length;)m[h]=arguments[h+1],++h;h=new C(m,0)}return c.call(this,b,h)}function c(a,b){var e=Fd(b)?Q.e(Ee,b):b,d=P.e(e,Fe),e=P.e(e,ob);return new De(a,e,d,null)}a.q=1;a.l=function(a){var b=D(a);a=F(a);return c(b,a)};a.k=c;return a}(),b=function(b,d){switch(arguments.length){case 1:return a.call(this,b);default:var g=null;
if(1<arguments.length){for(var g=0,f=Array(arguments.length-1);g<f.length;)f[g]=arguments[g+1],++g;g=new C(f,0)}return c.k(b,g)}throw Error("Invalid arity: "+arguments.length);};b.q=1;b.l=c.l;b.h=a;b.k=c.k;return b}();
function Ie(a,b){if(a instanceof De){var c=a.Sf;if(null!=c&&!s(c.h?c.h(b):c.call(null,b)))throw Error([w("Assert failed: "),w("Validator rejected reference state"),w("\n"),w(function(){var a=Zd(new Sc(null,"validate","validate",1439230700,null),new Sc(null,"new-value","new-value",-1567397401,null));return Je.h?Je.h(a):Je.call(null,a)}())].join(""));c=a.state;a.state=b;null!=a.Ye&&tc(a,c,b);return b}return Cc(a,b)}
var Ke=function(){function a(a,c,b,e){if(a instanceof De){var d=a.state;c=c.j?c.j(d,b,e):c.call(null,d,b,e);a=Ie(a,c)}else a=Dc.D(a,c,b,e);return a}function b(a,c,b){if(a instanceof De){var e=a.state;c=c.e?c.e(e,b):c.call(null,e,b);a=Ie(a,c)}else a=Dc.j(a,c,b);return a}function c(a,c){var b;a instanceof De?(b=a.state,b=c.h?c.h(b):c.call(null,b),b=Ie(a,b)):b=Dc.e(a,c);return b}var e=null,d=function(){function a(b,e,d,g,r){var u=null;if(4<arguments.length){for(var u=0,z=Array(arguments.length-4);u<
z.length;)z[u]=arguments[u+4],++u;u=new C(z,0)}return c.call(this,b,e,d,g,u)}function c(a,b,e,d,f){return a instanceof De?Ie(a,Q.O(b,a.state,e,d,f)):Dc.O(a,b,e,d,f)}a.q=4;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var g=D(a);a=F(a);return c(b,e,d,g,a)};a.k=c;return a}(),e=function(e,f,h,m,p){switch(arguments.length){case 2:return c.call(this,e,f);case 3:return b.call(this,e,f,h);case 4:return a.call(this,e,f,h,m);default:var q=null;if(4<arguments.length){for(var q=0,r=Array(arguments.length-
4);q<r.length;)r[q]=arguments[q+4],++q;q=new C(r,0)}return d.k(e,f,h,m,q)}throw Error("Invalid arity: "+arguments.length);};e.q=4;e.l=d.l;e.e=c;e.j=b;e.D=a;e.k=d.k;return e}();function Le(a){return Uc.e(a.Wb(null),!1)?(Ie(a,!0),!0):!1}function Me(a){this.state=a;this.H=0;this.A=32768}Me.prototype.Wb=function(){return this.state};Me.prototype.xf=function(a){return this.state=a};
var Ne=function(){function a(a,c,b,e){return new ee(null,function(){var g=B(c),r=B(b),u=B(e);if(g&&r&&u){var z=K,E;E=D(g);var H=D(r),M=D(u);E=a.j?a.j(E,H,M):a.call(null,E,H,M);g=z(E,d.D(a,F(g),F(r),F(u)))}else g=null;return g},null,null)}function b(a,c,b){return new ee(null,function(){var e=B(c),g=B(b);if(e&&g){var r=K,u;u=D(e);var z=D(g);u=a.e?a.e(u,z):a.call(null,u,z);e=r(u,d.j(a,F(e),F(g)))}else e=null;return e},null,null)}function c(a,c){return new ee(null,function(){var b=B(c);if(b){if(R(b)){for(var e=
y(b),g=N(e),r=new ge(Array(g),0),u=0;;)if(u<g)le(r,function(){var c=x.e(e,u);return a.h?a.h(c):a.call(null,c)}()),u+=1;else break;return ke(r.Ia(),d.e(a,A(b)))}return K(function(){var c=D(b);return a.h?a.h(c):a.call(null,c)}(),d.e(a,F(b)))}return null},null,null)}function e(a){return function(c){return function(){function b(e,d){var g=a.h?a.h(d):a.call(null,d);return c.e?c.e(e,g):c.call(null,e,g)}function e(a){return c.h?c.h(a):c.call(null,a)}function d(){return c.F?c.F():c.call(null)}var g=null,
u=function(){function b(a,c,d){var f=null;if(2<arguments.length){for(var f=0,g=Array(arguments.length-2);f<g.length;)g[f]=arguments[f+2],++f;f=new C(g,0)}return e.call(this,a,c,f)}function e(b,d,g){d=Q.j(a,d,g);return c.e?c.e(b,d):c.call(null,b,d)}b.q=2;b.l=function(a){var c=D(a);a=I(a);var b=D(a);a=F(a);return e(c,b,a)};b.k=e;return b}(),g=function(a,c,f){switch(arguments.length){case 0:return d.call(this);case 1:return e.call(this,a);case 2:return b.call(this,a,c);default:var g=null;if(2<arguments.length){for(var g=
0,h=Array(arguments.length-2);g<h.length;)h[g]=arguments[g+2],++g;g=new C(h,0)}return u.k(a,c,g)}throw Error("Invalid arity: "+arguments.length);};g.q=2;g.l=u.l;g.F=d;g.h=e;g.e=b;g.k=u.k;return g}()}}var d=null,g=function(){function a(b,e,d,f,g){var z=null;if(4<arguments.length){for(var z=0,E=Array(arguments.length-4);z<E.length;)E[z]=arguments[z+4],++z;z=new C(E,0)}return c.call(this,b,e,d,f,z)}function c(a,b,e,f,g){var h=function H(a){return new ee(null,function(){var c=d.e(B,a);return ye(Md,c)?
K(d.e(D,c),H(d.e(F,c))):null},null,null)};return d.e(function(){return function(c){return Q.e(a,c)}}(h),h(od.k(g,f,L([e,b],0))))}a.q=4;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var f=D(a);a=F(a);return c(b,e,d,f,a)};a.k=c;return a}(),d=function(d,h,m,p,q){switch(arguments.length){case 1:return e.call(this,d);case 2:return c.call(this,d,h);case 3:return b.call(this,d,h,m);case 4:return a.call(this,d,h,m,p);default:var r=null;if(4<arguments.length){for(var r=0,u=Array(arguments.length-
4);r<u.length;)u[r]=arguments[r+4],++r;r=new C(u,0)}return g.k(d,h,m,p,r)}throw Error("Invalid arity: "+arguments.length);};d.q=4;d.l=g.l;d.h=e;d.e=c;d.j=b;d.D=a;d.k=g.k;return d}(),Oe=function(){function a(a,b){return new ee(null,function(){if(0<a){var g=B(b);return g?K(D(g),c.e(a-1,F(g))):null}return null},null,null)}function b(a){return function(c){return function(a){return function(){function b(e,f){var h=cc(a),m=a.xf(a.Wb(null)-1),h=0<h?c.e?c.e(e,f):c.call(null,e,f):e;return 0<m?h:bd(h)?h:new ad(h)}
function e(a){return c.h?c.h(a):c.call(null,a)}function m(){return c.F?c.F():c.call(null)}var p=null,p=function(a,c){switch(arguments.length){case 0:return m.call(this);case 1:return e.call(this,a);case 2:return b.call(this,a,c)}throw Error("Invalid arity: "+arguments.length);};p.F=m;p.h=e;p.e=b;return p}()}(new Me(a))}}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}(),
Qe=function Pe(b,c){return K(c,new ee(null,function(){return Pe(b,b.h?b.h(c):b.call(null,c))},null,null))},Re=function(){function a(a,b){return new ee(null,function(){var g=B(b);if(g){if(R(g)){for(var f=y(g),h=N(f),m=new ge(Array(h),0),p=0;;)if(p<h){var q;q=x.e(f,p);q=a.h?a.h(q):a.call(null,q);s(q)&&(q=x.e(f,p),m.add(q));p+=1}else break;return ke(m.Ia(),c.e(a,A(g)))}f=D(g);g=F(g);return s(a.h?a.h(f):a.call(null,f))?K(f,c.e(a,g)):c.e(a,g)}return null},null,null)}function b(a){return function(c){return function(){function b(f,
g){return s(a.h?a.h(g):a.call(null,g))?c.e?c.e(f,g):c.call(null,f,g):f}function f(a){return c.h?c.h(a):c.call(null,a)}function h(){return c.F?c.F():c.call(null)}var m=null,m=function(a,c){switch(arguments.length){case 0:return h.call(this);case 1:return f.call(this,a);case 2:return b.call(this,a,c)}throw Error("Invalid arity: "+arguments.length);};m.F=h;m.h=f;m.e=b;return m}()}}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+
arguments.length);};c.h=b;c.e=a;return c}(),Se=function(){function a(a,c,b){return a&&(a.H&4||a.gf)?ld(te(Nd.D(c,ue,uc(a),b)),wd(a)):Nd.D(c,od,a,b)}function b(a,c){return null!=a?a&&(a.H&4||a.gf)?ld(te(yb.j(vc,uc(a),c)),wd(a)):yb.j(Gb,a,c):yb.j(od,G,c)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),Te=function(){function a(a,c,b,e){return Se.e(nd,Ne.D(a,
c,b,e))}function b(a,c,b){return Se.e(nd,Ne.j(a,c,b))}function c(a,c){return te(yb.j(function(c,b){return ue.e(c,a.h?a.h(b):a.call(null,b))},uc(nd),c))}var e=null,d=function(){function a(b,e,d,g,r){var u=null;if(4<arguments.length){for(var u=0,z=Array(arguments.length-4);u<z.length;)z[u]=arguments[u+4],++u;u=new C(z,0)}return c.call(this,b,e,d,g,u)}function c(a,b,e,d,f){return Se.e(nd,Q.k(Ne,a,b,e,d,L([f],0)))}a.q=4;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var g=D(a);
a=F(a);return c(b,e,d,g,a)};a.k=c;return a}(),e=function(e,f,h,m,p){switch(arguments.length){case 2:return c.call(this,e,f);case 3:return b.call(this,e,f,h);case 4:return a.call(this,e,f,h,m);default:var q=null;if(4<arguments.length){for(var q=0,r=Array(arguments.length-4);q<r.length;)r[q]=arguments[q+4],++q;q=new C(r,0)}return d.k(e,f,h,m,q)}throw Error("Invalid arity: "+arguments.length);};e.q=4;e.l=d.l;e.e=c;e.j=b;e.D=a;e.k=d.k;return e}(),Ve=function Ue(b,c,e){var d=O.j(c,0,null);return(c=Rd(c))?
rd.j(b,d,Ue(P.e(b,d),c,e)):rd.j(b,d,e)},We=function(){function a(a,c,b,e,g,r){var u=O.j(c,0,null);return(c=Rd(c))?rd.j(a,u,d.ia(P.e(a,u),c,b,e,g,r)):rd.j(a,u,function(){var c=P.e(a,u);return b.D?b.D(c,e,g,r):b.call(null,c,e,g,r)}())}function b(a,c,b,e,g){var r=O.j(c,0,null);return(c=Rd(c))?rd.j(a,r,d.O(P.e(a,r),c,b,e,g)):rd.j(a,r,function(){var c=P.e(a,r);return b.j?b.j(c,e,g):b.call(null,c,e,g)}())}function c(a,c,b,e){var g=O.j(c,0,null);return(c=Rd(c))?rd.j(a,g,d.D(P.e(a,g),c,b,e)):rd.j(a,g,function(){var c=
P.e(a,g);return b.e?b.e(c,e):b.call(null,c,e)}())}function e(a,c,b){var e=O.j(c,0,null);return(c=Rd(c))?rd.j(a,e,d.j(P.e(a,e),c,b)):rd.j(a,e,function(){var c=P.e(a,e);return b.h?b.h(c):b.call(null,c)}())}var d=null,g=function(){function a(b,e,d,f,g,z,E){var H=null;if(6<arguments.length){for(var H=0,M=Array(arguments.length-6);H<M.length;)M[H]=arguments[H+6],++H;H=new C(M,0)}return c.call(this,b,e,d,f,g,z,H)}function c(a,b,e,f,g,h,E){var H=O.j(b,0,null);return(b=Rd(b))?rd.j(a,H,Q.k(d,P.e(a,H),b,e,
f,L([g,h,E],0))):rd.j(a,H,Q.k(e,P.e(a,H),f,g,h,L([E],0)))}a.q=6;a.l=function(a){var b=D(a);a=I(a);var e=D(a);a=I(a);var d=D(a);a=I(a);var f=D(a);a=I(a);var g=D(a);a=I(a);var E=D(a);a=F(a);return c(b,e,d,f,g,E,a)};a.k=c;return a}(),d=function(d,h,m,p,q,r,u){switch(arguments.length){case 3:return e.call(this,d,h,m);case 4:return c.call(this,d,h,m,p);case 5:return b.call(this,d,h,m,p,q);case 6:return a.call(this,d,h,m,p,q,r);default:var z=null;if(6<arguments.length){for(var z=0,E=Array(arguments.length-
6);z<E.length;)E[z]=arguments[z+6],++z;z=new C(E,0)}return g.k(d,h,m,p,q,r,z)}throw Error("Invalid arity: "+arguments.length);};d.q=6;d.l=g.l;d.j=e;d.D=c;d.O=b;d.ia=a;d.k=g.k;return d}();function Xe(a,b){this.aa=a;this.o=b}function Ye(a){return new Xe(a,[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null])}function Ze(a){return new Xe(a.aa,wb(a.o))}
function $e(a){a=a.w;return 32>a?0:a-1>>>5<<5}function af(a,b,c){for(;;){if(0===b)return c;var e=Ye(a);e.o[0]=c;c=e;b-=5}}var cf=function bf(b,c,e,d){var g=Ze(e),f=b.w-1>>>c&31;5===c?g.o[f]=d:(e=e.o[f],b=null!=e?bf(b,c-5,e,d):af(null,c-5,d),g.o[f]=b);return g};function df(a,b){throw Error([w("No item "),w(a),w(" in vector of length "),w(b)].join(""));}function ef(a,b){if(b>=$e(a))return a.Q;for(var c=a.root,e=a.shift;;)if(0<e)var d=e-5,c=c.o[b>>>e&31],e=d;else return c.o}
function ff(a,b){return 0<=b&&b<a.w?ef(a,b):df(b,a.w)}var hf=function gf(b,c,e,d,g){var f=Ze(e);if(0===c)f.o[d&31]=g;else{var h=d>>>c&31;b=gf(b,c-5,e.o[h],d,g);f.o[h]=b}return f},kf=function jf(b,c,e){var d=b.w-2>>>c&31;if(5<c){b=jf(b,c-5,e.o[d]);if(null==b&&0===d)return null;e=Ze(e);e.o[d]=b;return e}if(0===d)return null;e=Ze(e);e.o[d]=null;return e};function lf(a,b,c,e,d,g){this.i=a;this.Bc=b;this.o=c;this.Ya=e;this.start=d;this.end=g}lf.prototype.qd=function(){return this.i<this.end};
lf.prototype.next=function(){32===this.i-this.Bc&&(this.o=ef(this.Ya,this.i),this.Bc+=32);var a=this.o[this.i&31];this.i+=1;return a};function T(a,b,c,e,d,g){this.meta=a;this.w=b;this.shift=c;this.root=e;this.Q=d;this.B=g;this.A=167668511;this.H=8196}k=T.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){return"number"===typeof b?x.j(this,b,c):c};
k.Ec=function(a,b,c){a=0;for(var e=c;;)if(a<this.w){var d=ef(this,a);c=d.length;a:{for(var g=0;;)if(g<c){var f=g+a,h=d[g],e=b.j?b.j(e,f,h):b.call(null,e,f,h);if(bd(e)){d=e;break a}g+=1}else{d=e;break a}d=void 0}if(bd(d))return b=d,J.h?J.h(b):J.call(null,b);a+=c;e=d}else return e};k.s=function(a,b){return ff(this,b)[b&31]};k.Aa=function(a,b,c){return 0<=b&&b<this.w?ef(this,b)[b&31]:c};
k.Zb=function(a,b,c){if(0<=b&&b<this.w)return $e(this)<=b?(a=wb(this.Q),a[b&31]=c,new T(this.meta,this.w,this.shift,this.root,a,null)):new T(this.meta,this.w,this.shift,hf(this,this.shift,this.root,b,c),this.Q,null);if(b===this.w)return Gb(this,c);throw Error([w("Index "),w(b),w(" out of bounds  [0,"),w(this.w),w("]")].join(""));};k.Dc=function(){var a=this.w;return new lf(0,0,0<N(this)?ef(this,0):null,this,0,a)};k.P=function(){return this.meta};
k.za=function(){return new T(this.meta,this.w,this.shift,this.root,this.Q,this.B)};k.ca=function(){return this.w};k.Gc=function(){return x.e(this,0)};k.Hc=function(){return x.e(this,1)};k.Mb=function(){return 0<this.w?x.e(this,this.w-1):null};
k.Nb=function(){if(0===this.w)throw Error("Can't pop empty vector");if(1===this.w)return gc(nd,this.meta);if(1<this.w-$e(this))return new T(this.meta,this.w-1,this.shift,this.root,this.Q.slice(0,-1),null);var a=ef(this,this.w-2),b=kf(this,this.shift,this.root),b=null==b?U:b,c=this.w-1;return 5<this.shift&&null==b.o[1]?new T(this.meta,c,this.shift-5,b.o[0],a,null):new T(this.meta,c,this.shift,b,a,null)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};
k.G=function(a,b){if(b instanceof T)if(this.w===N(b))for(var c=Ec(this),e=Ec(b);;)if(s(c.qd())){var d=c.next(),g=e.next();if(!Uc.e(d,g))return!1}else return!0;else return!1;else return id(this,b)};k.qc=function(){var a=this;return new mf(a.w,a.shift,function(){var b=a.root;return nf.h?nf.h(b):nf.call(null,b)}(),function(){var b=a.Q;return of.h?of.h(b):of.call(null,b)}())};k.ea=function(){return ld(nd,this.meta)};k.ma=function(a,b){return cd.e(this,b)};
k.na=function(a,b,c){a=0;for(var e=c;;)if(a<this.w){var d=ef(this,a);c=d.length;a:{for(var g=0;;)if(g<c){var f=d[g],e=b.e?b.e(e,f):b.call(null,e,f);if(bd(e)){d=e;break a}g+=1}else{d=e;break a}d=void 0}if(bd(d))return b=d,J.h?J.h(b):J.call(null,b);a+=c;e=d}else return e};k.pb=function(a,b,c){if("number"===typeof b)return bc(this,b,c);throw Error("Vector's key for assoc must be a number.");};
k.U=function(){if(0===this.w)return null;if(32>=this.w)return new C(this.Q,0);var a;a:{a=this.root;for(var b=this.shift;;)if(0<b)b-=5,a=a.o[0];else{a=a.o;break a}a=void 0}return pf.D?pf.D(this,a,0,0):pf.call(null,this,a,0,0)};k.R=function(a,b){return new T(b,this.w,this.shift,this.root,this.Q,this.B)};
k.Y=function(a,b){if(32>this.w-$e(this)){for(var c=this.Q.length,e=Array(c+1),d=0;;)if(d<c)e[d]=this.Q[d],d+=1;else break;e[c]=b;return new T(this.meta,this.w+1,this.shift,this.root,e,null)}c=(e=this.w>>>5>1<<this.shift)?this.shift+5:this.shift;e?(e=Ye(null),e.o[0]=this.root,d=af(null,this.shift,new Xe(null,this.Q)),e.o[1]=d):e=cf(this,this.shift,this.root,new Xe(null,this.Q));return new T(this.meta,this.w+1,c,e,[b],null)};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.s(null,c);case 3:return this.Aa(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.s(null,c)};a.j=function(a,c,e){return this.Aa(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.s(null,a)};k.e=function(a,b){return this.Aa(null,a,b)};
var U=new Xe(null,[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null]),nd=new T(null,0,5,U,[],0);function qf(a,b){var c=a.length,e=b?a:wb(a);if(32>c)return new T(null,c,5,U,e,null);for(var d=32,g=(new T(null,32,5,U,e.slice(0,32),null)).qc(null);;)if(d<c)var f=d+1,g=ue.e(g,e[d]),d=f;else return wc(g)}T.prototype[vb]=function(){return Wc(this)};function rf(a){return wc(yb.j(vc,uc(nd),a))}
function sf(a,b,c,e,d,g){this.Ma=a;this.Fb=b;this.i=c;this.ra=e;this.meta=d;this.B=g;this.A=32375020;this.H=1536}k=sf.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.sa=function(){if(this.ra+1<this.Fb.length){var a;a=this.Ma;var b=this.Fb,c=this.i,e=this.ra+1;a=pf.D?pf.D(a,b,c,e):pf.call(null,a,b,c,e);return null==a?null:a}return Bc(this)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};
k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(nd,this.meta)};k.ma=function(a,b){var c=this;return cd.e(function(){var a=c.Ma,b=c.i+c.ra,g=N(c.Ma);return tf.j?tf.j(a,b,g):tf.call(null,a,b,g)}(),b)};k.na=function(a,b,c){var e=this;return cd.j(function(){var a=e.Ma,c=e.i+e.ra,b=N(e.Ma);return tf.j?tf.j(a,c,b):tf.call(null,a,c,b)}(),b,c)};k.la=function(){return this.Fb[this.ra]};
k.pa=function(){if(this.ra+1<this.Fb.length){var a;a=this.Ma;var b=this.Fb,c=this.i,e=this.ra+1;a=pf.D?pf.D(a,b,c,e):pf.call(null,a,b,c,e);return null==a?G:a}return A(this)};k.U=function(){return this};k.Jd=function(){return ie.e(this.Fb,this.ra)};k.Kd=function(){var a=this.i+this.Fb.length;if(a<Db(this.Ma)){var b=this.Ma,c=ef(this.Ma,a);return pf.D?pf.D(b,c,a,0):pf.call(null,b,c,a,0)}return G};
k.R=function(a,b){var c=this.Ma,e=this.Fb,d=this.i,g=this.ra;return pf.O?pf.O(c,e,d,g,b):pf.call(null,c,e,d,g,b)};k.Y=function(a,b){return K(b,this)};k.Id=function(){var a=this.i+this.Fb.length;if(a<Db(this.Ma)){var b=this.Ma,c=ef(this.Ma,a);return pf.D?pf.D(b,c,a,0):pf.call(null,b,c,a,0)}return null};sf.prototype[vb]=function(){return Wc(this)};
var pf=function(){function a(a,c,b,e,m){return new sf(a,c,b,e,m,null)}function b(a,c,b,e){return new sf(a,c,b,e,null,null)}function c(a,c,b){return new sf(a,ff(a,c),c,b,null,null)}var e=null,e=function(e,g,f,h,m){switch(arguments.length){case 3:return c.call(this,e,g,f);case 4:return b.call(this,e,g,f,h);case 5:return a.call(this,e,g,f,h,m)}throw Error("Invalid arity: "+arguments.length);};e.j=c;e.D=b;e.O=a;return e}();
function uf(a,b,c,e,d){this.meta=a;this.Ya=b;this.start=c;this.end=e;this.B=d;this.A=166617887;this.H=8192}k=uf.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){return"number"===typeof b?x.j(this,b,c):c};k.s=function(a,b){return 0>b||this.end<=this.start+b?df(b,this.end-this.start):x.e(this.Ya,this.start+b)};k.Aa=function(a,b,c){return 0>b||this.end<=this.start+b?c:x.j(this.Ya,this.start+b,c)};
k.Zb=function(a,b,c){var e=this.start+b;a=this.meta;c=rd.j(this.Ya,e,c);b=this.start;var d=this.end,e=e+1,e=d>e?d:e;return vf.O?vf.O(a,c,b,e,null):vf.call(null,a,c,b,e,null)};k.P=function(){return this.meta};k.za=function(){return new uf(this.meta,this.Ya,this.start,this.end,this.B)};k.ca=function(){return this.end-this.start};k.Mb=function(){return x.e(this.Ya,this.end-1)};
k.Nb=function(){if(this.start===this.end)throw Error("Can't pop empty vector");var a=this.meta,b=this.Ya,c=this.start,e=this.end-1;return vf.O?vf.O(a,b,c,e,null):vf.call(null,a,b,c,e,null)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(nd,this.meta)};k.ma=function(a,b){return cd.e(this,b)};k.na=function(a,b,c){return cd.j(this,b,c)};
k.pb=function(a,b,c){if("number"===typeof b)return bc(this,b,c);throw Error("Subvec's key for assoc must be a number.");};k.U=function(){var a=this;return function(b){return function e(d){return d===a.end?null:K(x.e(a.Ya,d),new ee(null,function(){return function(){return e(d+1)}}(b),null,null))}}(this)(a.start)};k.R=function(a,b){var c=this.Ya,e=this.start,d=this.end,g=this.B;return vf.O?vf.O(b,c,e,d,g):vf.call(null,b,c,e,d,g)};
k.Y=function(a,b){var c=this.meta,e=bc(this.Ya,this.end,b),d=this.start,g=this.end+1;return vf.O?vf.O(c,e,d,g,null):vf.call(null,c,e,d,g,null)};k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.s(null,c);case 3:return this.Aa(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.s(null,c)};a.j=function(a,c,e){return this.Aa(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};
k.h=function(a){return this.s(null,a)};k.e=function(a,b){return this.Aa(null,a,b)};uf.prototype[vb]=function(){return Wc(this)};function vf(a,b,c,e,d){for(;;)if(b instanceof uf)c=b.start+c,e=b.start+e,b=b.Ya;else{var g=N(b);if(0>c||0>e||c>g||e>g)throw Error("Index out of bounds");return new uf(a,b,c,e,d)}}
var tf=function(){function a(a,c,b){return vf(null,a,c,b,null)}function b(a,b){return c.j(a,b,N(a))}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}();function wf(a,b){return a===b.aa?b:new Xe(a,wb(b.o))}function nf(a){return new Xe({},wb(a.o))}
function of(a){var b=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];Cd(a,0,b,0,a.length);return b}var yf=function xf(b,c,e,d){e=wf(b.root.aa,e);var g=b.w-1>>>c&31;if(5===c)b=d;else{var f=e.o[g];b=null!=f?xf(b,c-5,f,d):af(b.root.aa,c-5,d)}e.o[g]=b;return e};function mf(a,b,c,e){this.w=a;this.shift=b;this.root=c;this.Q=e;this.A=275;this.H=88}k=mf.prototype;
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};k.I=function(a,b){return Ob.j(this,b,null)};
k.J=function(a,b,c){return"number"===typeof b?x.j(this,b,c):c};k.s=function(a,b){if(this.root.aa)return ff(this,b)[b&31];throw Error("nth after persistent!");};k.Aa=function(a,b,c){return 0<=b&&b<this.w?x.e(this,b):c};k.ca=function(){if(this.root.aa)return this.w;throw Error("count after persistent!");};
k.ke=function(a,b,c){var e=this;if(e.root.aa){if(0<=b&&b<e.w)return $e(this)<=b?e.Q[b&31]=c:(a=function(){return function g(a,h){var m=wf(e.root.aa,h);if(0===a)m.o[b&31]=c;else{var p=b>>>a&31,q=g(a-5,m.o[p]);m.o[p]=q}return m}}(this).call(null,e.shift,e.root),e.root=a),this;if(b===e.w)return vc(this,c);throw Error([w("Index "),w(b),w(" out of bounds for TransientVector of length"),w(e.w)].join(""));}throw Error("assoc! after persistent!");};
k.Jc=function(a,b,c){if("number"===typeof b)return yc(this,b,c);throw Error("TransientVector's key for assoc! must be a number.");};
k.Xb=function(a,b){if(this.root.aa){if(32>this.w-$e(this))this.Q[this.w&31]=b;else{var c=new Xe(this.root.aa,this.Q),e=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];e[0]=b;this.Q=e;if(this.w>>>5>1<<this.shift){var e=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],d=this.shift+
5;e[0]=this.root;e[1]=af(this.root.aa,this.shift,c);this.root=new Xe(this.root.aa,e);this.shift=d}else this.root=yf(this,this.shift,this.root,c)}this.w+=1;return this}throw Error("conj! after persistent!");};k.Yb=function(){if(this.root.aa){this.root.aa=null;var a=this.w-$e(this),b=Array(a);Cd(this.Q,0,b,0,a);return new T(null,this.w,this.shift,this.root,b,null)}throw Error("persistent! called twice");};function zf(a,b,c,e){this.meta=a;this.Da=b;this.hb=c;this.B=e;this.H=0;this.A=31850572}k=zf.prototype;
k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.la=function(){return D(this.Da)};k.pa=function(){var a=I(this.Da);return a?new zf(this.meta,a,this.hb,null):null==this.hb?Eb(this):new zf(this.meta,this.hb,null,null)};k.U=function(){return this};
k.R=function(a,b){return new zf(b,this.Da,this.hb,this.B)};k.Y=function(a,b){return K(b,this)};zf.prototype[vb]=function(){return Wc(this)};function Af(a,b,c,e,d){this.meta=a;this.count=b;this.Da=c;this.hb=e;this.B=d;this.A=31858766;this.H=8192}k=Af.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.za=function(){return new Af(this.meta,this.count,this.Da,this.hb,this.B)};k.ca=function(){return this.count};k.Mb=function(){return D(this.Da)};
k.Nb=function(){if(s(this.Da)){var a=I(this.Da);return a?new Af(this.meta,this.count-1,a,this.hb,null):new Af(this.meta,this.count-1,B(this.hb),nd,null)}return this};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(Bf,this.meta)};k.la=function(){return D(this.Da)};k.pa=function(){return F(B(this))};k.U=function(){var a=B(this.hb),b=this.Da;return s(s(b)?b:a)?new zf(null,this.Da,B(a),null):null};
k.R=function(a,b){return new Af(b,this.count,this.Da,this.hb,this.B)};k.Y=function(a,b){var c;s(this.Da)?(c=this.hb,c=new Af(this.meta,this.count+1,this.Da,od.e(s(c)?c:nd,b),null)):c=new Af(this.meta,this.count+1,od.e(this.Da,b),nd,null);return c};var Bf=new Af(null,0,null,nd,0);Af.prototype[vb]=function(){return Wc(this)};function Cf(){this.H=0;this.A=2097152}Cf.prototype.G=function(){return!1};Cf.prototype.equiv=function(a){return this.G(null,a)};var Df=new Cf;
function Ef(a,b){return Gd(zd(b)?N(a)===N(b)?ye(Md,Ne.e(function(a){return Uc.e(P.j(b,D(a),Df),D(I(a)))},a)):null:null)}function Ff(a){this.K=a}Ff.prototype.next=function(){if(null!=this.K){var a=D(this.K),b=O.j(a,0,null),a=O.j(a,1,null);this.K=I(this.K);return{done:!1,value:[b,a]}}return{done:!0,value:null}};function Gf(a){return new Ff(B(a))}function Hf(a){this.K=a}Hf.prototype.next=function(){if(null!=this.K){var a=D(this.K);this.K=I(this.K);return{done:!1,value:[a,a]}}return{done:!0,value:null}};
function If(a){return new Hf(B(a))}
function Jf(a,b){var c=a.o;if(b instanceof V)a:{for(var e=c.length,d=b.La,g=0;;){if(e<=g){c=-1;break a}var f=c[g];if(f instanceof V&&d===f.La){c=g;break a}g+=2}c=void 0}else if(e=fa(b),s(s(e)?e:"number"===typeof b))a:{e=c.length;for(d=0;;){if(e<=d){c=-1;break a}if(b===c[d]){c=d;break a}d+=2}c=void 0}else if(b instanceof Sc)a:{e=c.length;d=b.xa;for(g=0;;){if(e<=g){c=-1;break a}f=c[g];if(f instanceof Sc&&d===f.xa){c=g;break a}g+=2}c=void 0}else if(null==b)a:{e=c.length;for(d=0;;){if(e<=d){c=-1;break a}if(null==
c[d]){c=d;break a}d+=2}c=void 0}else a:{e=c.length;for(d=0;;){if(e<=d){c=-1;break a}if(Uc.e(b,c[d])){c=d;break a}d+=2}c=void 0}return c}function Kf(a,b,c){this.o=a;this.i=b;this.ya=c;this.H=0;this.A=32374990}k=Kf.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.ya};k.sa=function(){return this.i<this.o.length-2?new Kf(this.o,this.i+2,this.ya):null};k.ca=function(){return(this.o.length-this.i)/2};k.L=function(){return Yc(this)};
k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.ya)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return new T(null,2,5,U,[this.o[this.i],this.o[this.i+1]],null)};k.pa=function(){return this.i<this.o.length-2?new Kf(this.o,this.i+2,this.ya):G};k.U=function(){return this};k.R=function(a,b){return new Kf(this.o,this.i,b)};k.Y=function(a,b){return K(b,this)};Kf.prototype[vb]=function(){return Wc(this)};
function Lf(a,b,c){this.o=a;this.i=b;this.w=c}Lf.prototype.qd=function(){return this.i<this.w};Lf.prototype.next=function(){var a=new T(null,2,5,U,[this.o[this.i],this.o[this.i+1]],null);this.i+=2;return a};function n(a,b,c,e){this.meta=a;this.w=b;this.o=c;this.B=e;this.A=16647951;this.H=8196}k=n.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.keys=function(){return Wc(Nf.h?Nf.h(this):Nf.call(null,this))};k.entries=function(){return Gf(B(this))};
k.values=function(){return Wc(Of.h?Of.h(this):Of.call(null,this))};k.has=function(a){return Hd(this,a)};k.get=function(a){return this.I(null,a)};k.forEach=function(a){for(var b=B(this),c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d),f=O.j(g,0,null),g=O.j(g,1,null);a.e?a.e(g,f):a.call(null,g,f);d+=1}else if(b=B(b))R(b)?(c=y(b),b=A(b),f=c,e=N(c),c=f):(c=D(b),f=O.j(c,0,null),c=g=O.j(c,1,null),a.e?a.e(c,f):a.call(null,c,f),b=I(b),c=null,e=0),d=0;else return null};k.I=function(a,b){return Ob.j(this,b,null)};
k.J=function(a,b,c){a=Jf(this,b);return-1===a?c:this.o[a+1]};k.Ec=function(a,b,c){a=this.o.length;for(var e=0;;)if(e<a){var d=this.o[e],g=this.o[e+1];c=b.j?b.j(c,d,g):b.call(null,c,d,g);if(bd(c))return b=c,J.h?J.h(b):J.call(null,b);e+=2}else return c};k.Dc=function(){return new Lf(this.o,0,2*this.w)};k.P=function(){return this.meta};k.za=function(){return new n(this.meta,this.w,this.o,this.B)};k.ca=function(){return this.w};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Zc(this)};
k.G=function(a,b){if(b&&(b.A&1024||b.nf)){var c=this.o.length;if(this.w===b.ca(null))for(var e=0;;)if(e<c){var d=b.J(null,this.o[e],Ed);if(d!==Ed)if(Uc.e(this.o[e+1],d))e+=2;else return!1;else return!1}else return!0;else return!1}else return Ef(this,b)};k.qc=function(){return new Pf({},this.o.length,wb(this.o))};k.ea=function(){return gc(Qf,this.meta)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};
k.Fc=function(a,b){if(0<=Jf(this,b)){var c=this.o.length,e=c-2;if(0===e)return Eb(this);for(var e=Array(e),d=0,g=0;;){if(d>=c)return new n(this.meta,this.w-1,e,null);Uc.e(b,this.o[d])||(e[g]=this.o[d],e[g+1]=this.o[d+1],g+=2);d+=2}}else return this};
k.pb=function(a,b,c){a=Jf(this,b);if(-1===a){if(this.w<Rf){a=this.o;for(var e=a.length,d=Array(e+2),g=0;;)if(g<e)d[g]=a[g],g+=1;else break;d[e]=b;d[e+1]=c;return new n(this.meta,this.w+1,d,null)}return gc(Rb(Se.e(Sf,this),b,c),this.meta)}if(c===this.o[a+1])return this;b=wb(this.o);b[a+1]=c;return new n(this.meta,this.w,b,null)};k.$c=function(a,b){return-1!==Jf(this,b)};k.U=function(){var a=this.o;return 0<=a.length-2?new Kf(a,0,null):null};k.R=function(a,b){return new n(b,this.w,this.o,this.B)};
k.Y=function(a,b){if(Ad(b))return Rb(this,x.e(b,0),x.e(b,1));for(var c=this,e=B(b);;){if(null==e)return c;var d=D(e);if(Ad(d))c=Rb(c,x.e(d,0),x.e(d,1)),e=I(e);else throw Error("conj on a map takes map entries or seqables of map entries");}};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};var Qf=new n(null,0,[],null),Rf=8;
function Tf(a,b,c){a=b?a:wb(a);if(c)return new n(null,a.length/2,a,null);c=a.length;b=0;for(var e=uc(Qf);;)if(b<c){var d=b+2,e=xc(e,a[b],a[b+1]);b=d}else return wc(e)}n.prototype[vb]=function(){return Wc(this)};function Pf(a,b,c){this.rc=a;this.xc=b;this.o=c;this.H=56;this.A=258}k=Pf.prototype;
k.Jc=function(a,b,c){var e=this;if(s(e.rc)){a=Jf(this,b);if(-1===a)return e.xc+2<=2*Rf?(e.xc+=2,e.o.push(b),e.o.push(c),this):ve.j(function(){var a=e.xc,c=e.o;return Uf.e?Uf.e(a,c):Uf.call(null,a,c)}(),b,c);c!==e.o[a+1]&&(e.o[a+1]=c);return this}throw Error("assoc! after persistent!");};
k.Xb=function(a,b){if(s(this.rc)){if(b?b.A&2048||b.of||(b.A?0:t(Ub,b)):t(Ub,b))return xc(this,Ud.h?Ud.h(b):Ud.call(null,b),Vd.h?Vd.h(b):Vd.call(null,b));for(var c=B(b),e=this;;){var d=D(c);if(s(d))var g=d,c=I(c),e=xc(e,function(){var a=g;return Ud.h?Ud.h(a):Ud.call(null,a)}(),function(){var a=g;return Vd.h?Vd.h(a):Vd.call(null,a)}());else return e}}else throw Error("conj! after persistent!");};
k.Yb=function(){if(s(this.rc))return this.rc=!1,new n(null,Pd(this.xc),this.o,null);throw Error("persistent! called twice");};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){if(s(this.rc))return a=Jf(this,b),-1===a?c:this.o[a+1];throw Error("lookup after persistent!");};k.ca=function(){if(s(this.rc))return Pd(this.xc);throw Error("count after persistent!");};function Uf(a,b){for(var c=uc(Sf),e=0;;)if(e<a)c=ve.j(c,b[e],b[e+1]),e+=2;else return c}function Vf(){this.C=!1}
function Wf(a,b){return a===b?!0:be(a,b)?!0:Uc.e(a,b)}var Xf=function(){function a(a,c,b,f,h){a=wb(a);a[c]=b;a[f]=h;return a}function b(a,c,b){a=wb(a);a[c]=b;return a}var c=null,c=function(c,d,g,f,h){switch(arguments.length){case 3:return b.call(this,c,d,g);case 5:return a.call(this,c,d,g,f,h)}throw Error("Invalid arity: "+arguments.length);};c.j=b;c.O=a;return c}();function Yf(a,b){var c=Array(a.length-2);Cd(a,0,c,0,2*b);Cd(a,2*(b+1),c,2*b,c.length-2*b);return c}
var Zf=function(){function a(a,c,b,f,h,m){a=a.sc(c);a.o[b]=f;a.o[h]=m;return a}function b(a,c,b,f){a=a.sc(c);a.o[b]=f;return a}var c=null,c=function(c,d,g,f,h,m){switch(arguments.length){case 4:return b.call(this,c,d,g,f);case 6:return a.call(this,c,d,g,f,h,m)}throw Error("Invalid arity: "+arguments.length);};c.D=b;c.ia=a;return c}();
function $f(a,b,c){for(var e=a.length,d=0,g=c;;)if(d<e){c=a[d];if(null!=c){var f=a[d+1];c=b.j?b.j(g,c,f):b.call(null,g,c,f)}else c=a[d+1],c=null!=c?c.ec(b,g):g;if(bd(c))return a=c,J.h?J.h(a):J.call(null,a);d+=2;g=c}else return g}function ag(a,b,c){this.aa=a;this.fa=b;this.o=c}k=ag.prototype;k.sc=function(a){if(a===this.aa)return this;var b=Qd(this.fa),c=Array(0>b?4:2*(b+1));Cd(this.o,0,c,0,2*b);return new ag(a,this.fa,c)};k.Oc=function(){var a=this.o;return bg.h?bg.h(a):bg.call(null,a)};
k.ec=function(a,b){return $f(this.o,a,b)};k.Qb=function(a,b,c,e){var d=1<<(b>>>a&31);if(0===(this.fa&d))return e;var g=Qd(this.fa&d-1),d=this.o[2*g],g=this.o[2*g+1];return null==d?g.Qb(a+5,b,c,e):Wf(c,d)?g:e};
k.fb=function(a,b,c,e,d,g){var f=1<<(c>>>b&31),h=Qd(this.fa&f-1);if(0===(this.fa&f)){var m=Qd(this.fa);if(2*m<this.o.length){var p=this.sc(a),q=p.o;g.C=!0;Dd(q,2*h,q,2*(h+1),2*(m-h));q[2*h]=e;q[2*h+1]=d;p.fa|=f;return p}if(16<=m){f=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];f[c>>>b&31]=cg.fb(a,b+5,c,e,d,g);for(p=h=0;;)if(32>h)0!==(this.fa>>>h&1)&&(f[h]=null!=this.o[p]?cg.fb(a,b+5,
Pc(this.o[p]),this.o[p],this.o[p+1],g):this.o[p+1],p+=2),h+=1;else break;return new dg(a,m+1,f)}q=Array(2*(m+4));Cd(this.o,0,q,0,2*h);q[2*h]=e;q[2*h+1]=d;Cd(this.o,2*h,q,2*(h+1),2*(m-h));g.C=!0;p=this.sc(a);p.o=q;p.fa|=f;return p}var r=this.o[2*h],u=this.o[2*h+1];if(null==r)return m=u.fb(a,b+5,c,e,d,g),m===u?this:Zf.D(this,a,2*h+1,m);if(Wf(e,r))return d===u?this:Zf.D(this,a,2*h+1,d);g.C=!0;return Zf.ia(this,a,2*h,null,2*h+1,function(){var f=b+5;return eg.Pa?eg.Pa(a,f,r,u,c,e,d):eg.call(null,a,f,r,
u,c,e,d)}())};
k.eb=function(a,b,c,e,d){var g=1<<(b>>>a&31),f=Qd(this.fa&g-1);if(0===(this.fa&g)){var h=Qd(this.fa);if(16<=h){g=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];g[b>>>a&31]=cg.eb(a+5,b,c,e,d);for(var m=f=0;;)if(32>f)0!==(this.fa>>>f&1)&&(g[f]=null!=this.o[m]?cg.eb(a+5,Pc(this.o[m]),this.o[m],this.o[m+1],d):this.o[m+1],m+=2),f+=1;else break;return new dg(null,h+1,g)}m=Array(2*(h+1));Cd(this.o,
0,m,0,2*f);m[2*f]=c;m[2*f+1]=e;Cd(this.o,2*f,m,2*(f+1),2*(h-f));d.C=!0;return new ag(null,this.fa|g,m)}var p=this.o[2*f],q=this.o[2*f+1];if(null==p)return h=q.eb(a+5,b,c,e,d),h===q?this:new ag(null,this.fa,Xf.j(this.o,2*f+1,h));if(Wf(c,p))return e===q?this:new ag(null,this.fa,Xf.j(this.o,2*f+1,e));d.C=!0;return new ag(null,this.fa,Xf.O(this.o,2*f,null,2*f+1,function(){var d=a+5;return eg.ia?eg.ia(d,p,q,b,c,e):eg.call(null,d,p,q,b,c,e)}()))};
k.Pc=function(a,b,c){var e=1<<(b>>>a&31);if(0===(this.fa&e))return this;var d=Qd(this.fa&e-1),g=this.o[2*d],f=this.o[2*d+1];return null==g?(a=f.Pc(a+5,b,c),a===f?this:null!=a?new ag(null,this.fa,Xf.j(this.o,2*d+1,a)):this.fa===e?null:new ag(null,this.fa^e,Yf(this.o,d))):Wf(c,g)?new ag(null,this.fa^e,Yf(this.o,d)):this};var cg=new ag(null,0,[]);function dg(a,b,c){this.aa=a;this.w=b;this.o=c}k=dg.prototype;k.sc=function(a){return a===this.aa?this:new dg(a,this.w,wb(this.o))};
k.Oc=function(){var a=this.o;return fg.h?fg.h(a):fg.call(null,a)};k.ec=function(a,b){for(var c=this.o.length,e=0,d=b;;)if(e<c){var g=this.o[e];if(null!=g&&(d=g.ec(a,d),bd(d)))return c=d,J.h?J.h(c):J.call(null,c);e+=1}else return d};k.Qb=function(a,b,c,e){var d=this.o[b>>>a&31];return null!=d?d.Qb(a+5,b,c,e):e};k.fb=function(a,b,c,e,d,g){var f=c>>>b&31,h=this.o[f];if(null==h)return a=Zf.D(this,a,f,cg.fb(a,b+5,c,e,d,g)),a.w+=1,a;b=h.fb(a,b+5,c,e,d,g);return b===h?this:Zf.D(this,a,f,b)};
k.eb=function(a,b,c,e,d){var g=b>>>a&31,f=this.o[g];if(null==f)return new dg(null,this.w+1,Xf.j(this.o,g,cg.eb(a+5,b,c,e,d)));a=f.eb(a+5,b,c,e,d);return a===f?this:new dg(null,this.w,Xf.j(this.o,g,a))};
k.Pc=function(a,b,c){var e=b>>>a&31,d=this.o[e];if(null!=d){a=d.Pc(a+5,b,c);if(a===d)e=this;else if(null==a)if(8>=this.w)a:{d=this.o;a=d.length;b=Array(2*(this.w-1));c=0;for(var g=1,f=0;;)if(c<a)c!==e&&null!=d[c]&&(b[g]=d[c],g+=2,f|=1<<c),c+=1;else{e=new ag(null,f,b);break a}e=void 0}else e=new dg(null,this.w-1,Xf.j(this.o,e,a));else e=new dg(null,this.w,Xf.j(this.o,e,a));return e}return this};function gg(a,b,c){b*=2;for(var e=0;;)if(e<b){if(Wf(c,a[e]))return e;e+=2}else return-1}
function hg(a,b,c,e){this.aa=a;this.Db=b;this.w=c;this.o=e}k=hg.prototype;k.sc=function(a){if(a===this.aa)return this;var b=Array(2*(this.w+1));Cd(this.o,0,b,0,2*this.w);return new hg(a,this.Db,this.w,b)};k.Oc=function(){var a=this.o;return bg.h?bg.h(a):bg.call(null,a)};k.ec=function(a,b){return $f(this.o,a,b)};k.Qb=function(a,b,c,e){a=gg(this.o,this.w,c);return 0>a?e:Wf(c,this.o[a])?this.o[a+1]:e};
k.fb=function(a,b,c,e,d,g){if(c===this.Db){b=gg(this.o,this.w,e);if(-1===b){if(this.o.length>2*this.w)return a=Zf.ia(this,a,2*this.w,e,2*this.w+1,d),g.C=!0,a.w+=1,a;c=this.o.length;b=Array(c+2);Cd(this.o,0,b,0,c);b[c]=e;b[c+1]=d;g.C=!0;g=this.w+1;a===this.aa?(this.o=b,this.w=g,a=this):a=new hg(this.aa,this.Db,g,b);return a}return this.o[b+1]===d?this:Zf.D(this,a,b+1,d)}return(new ag(a,1<<(this.Db>>>b&31),[null,this,null,null])).fb(a,b,c,e,d,g)};
k.eb=function(a,b,c,e,d){return b===this.Db?(a=gg(this.o,this.w,c),-1===a?(a=2*this.w,b=Array(a+2),Cd(this.o,0,b,0,a),b[a]=c,b[a+1]=e,d.C=!0,new hg(null,this.Db,this.w+1,b)):Uc.e(this.o[a],e)?this:new hg(null,this.Db,this.w,Xf.j(this.o,a+1,e))):(new ag(null,1<<(this.Db>>>a&31),[null,this])).eb(a,b,c,e,d)};k.Pc=function(a,b,c){a=gg(this.o,this.w,c);return-1===a?this:1===this.w?null:new hg(null,this.Db,this.w-1,Yf(this.o,Pd(a)))};
var eg=function(){function a(a,c,b,f,h,m,p){var q=Pc(b);if(q===h)return new hg(null,q,2,[b,f,m,p]);var r=new Vf;return cg.fb(a,c,q,b,f,r).fb(a,c,h,m,p,r)}function b(a,c,b,f,h,m){var p=Pc(c);if(p===f)return new hg(null,p,2,[c,b,h,m]);var q=new Vf;return cg.eb(a,p,c,b,q).eb(a,f,h,m,q)}var c=null,c=function(c,d,g,f,h,m,p){switch(arguments.length){case 6:return b.call(this,c,d,g,f,h,m);case 7:return a.call(this,c,d,g,f,h,m,p)}throw Error("Invalid arity: "+arguments.length);};c.ia=b;c.Pa=a;return c}();
function ig(a,b,c,e,d){this.meta=a;this.Rb=b;this.i=c;this.K=e;this.B=d;this.H=0;this.A=32374860}k=ig.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};
k.la=function(){return null==this.K?new T(null,2,5,U,[this.Rb[this.i],this.Rb[this.i+1]],null):D(this.K)};k.pa=function(){if(null==this.K){var a=this.Rb,b=this.i+2;return bg.j?bg.j(a,b,null):bg.call(null,a,b,null)}var a=this.Rb,b=this.i,c=I(this.K);return bg.j?bg.j(a,b,c):bg.call(null,a,b,c)};k.U=function(){return this};k.R=function(a,b){return new ig(b,this.Rb,this.i,this.K,this.B)};k.Y=function(a,b){return K(b,this)};ig.prototype[vb]=function(){return Wc(this)};
var bg=function(){function a(a,c,b){if(null==b)for(b=a.length;;)if(c<b){if(null!=a[c])return new ig(null,a,c,null,null);var f=a[c+1];if(s(f)&&(f=f.Oc(),s(f)))return new ig(null,a,c+2,f,null);c+=2}else return null;else return new ig(null,a,c,b,null)}function b(a){return c.j(a,0,null)}var c=null,c=function(c,d,g){switch(arguments.length){case 1:return b.call(this,c);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.j=a;return c}();
function jg(a,b,c,e,d){this.meta=a;this.Rb=b;this.i=c;this.K=e;this.B=d;this.H=0;this.A=32374860}k=jg.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return D(this.K)};
k.pa=function(){var a=this.Rb,b=this.i,c=I(this.K);return fg.D?fg.D(null,a,b,c):fg.call(null,null,a,b,c)};k.U=function(){return this};k.R=function(a,b){return new jg(b,this.Rb,this.i,this.K,this.B)};k.Y=function(a,b){return K(b,this)};jg.prototype[vb]=function(){return Wc(this)};
var fg=function(){function a(a,c,b,f){if(null==f)for(f=c.length;;)if(b<f){var h=c[b];if(s(h)&&(h=h.Oc(),s(h)))return new jg(a,c,b+1,h,null);b+=1}else return null;else return new jg(a,c,b,f,null)}function b(a){return c.D(null,a,0,null)}var c=null,c=function(c,d,g,f){switch(arguments.length){case 1:return b.call(this,c);case 4:return a.call(this,c,d,g,f)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.D=a;return c}();
function kg(a,b,c,e,d,g){this.meta=a;this.w=b;this.root=c;this.ta=e;this.Ea=d;this.B=g;this.A=16123663;this.H=8196}k=kg.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.keys=function(){return Wc(Nf.h?Nf.h(this):Nf.call(null,this))};k.entries=function(){return Gf(B(this))};k.values=function(){return Wc(Of.h?Of.h(this):Of.call(null,this))};k.has=function(a){return Hd(this,a)};k.get=function(a){return this.I(null,a)};
k.forEach=function(a){for(var b=B(this),c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d),f=O.j(g,0,null),g=O.j(g,1,null);a.e?a.e(g,f):a.call(null,g,f);d+=1}else if(b=B(b))R(b)?(c=y(b),b=A(b),f=c,e=N(c),c=f):(c=D(b),f=O.j(c,0,null),c=g=O.j(c,1,null),a.e?a.e(c,f):a.call(null,c,f),b=I(b),c=null,e=0),d=0;else return null};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){return null==b?this.ta?this.Ea:c:null==this.root?c:this.root.Qb(0,Pc(b),b,c)};
k.Ec=function(a,b,c){this.ta&&(a=this.Ea,c=b.j?b.j(c,null,a):b.call(null,c,null,a));return bd(c)?J.h?J.h(c):J.call(null,c):null!=this.root?this.root.ec(b,c):c};k.P=function(){return this.meta};k.za=function(){return new kg(this.meta,this.w,this.root,this.ta,this.Ea,this.B)};k.ca=function(){return this.w};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Zc(this)};k.G=function(a,b){return Ef(this,b)};k.qc=function(){return new lg({},this.root,this.w,this.ta,this.Ea)};
k.ea=function(){return gc(Sf,this.meta)};k.Fc=function(a,b){if(null==b)return this.ta?new kg(this.meta,this.w-1,this.root,!1,null,null):this;if(null==this.root)return this;var c=this.root.Pc(0,Pc(b),b);return c===this.root?this:new kg(this.meta,this.w-1,c,this.ta,this.Ea,null)};
k.pb=function(a,b,c){if(null==b)return this.ta&&c===this.Ea?this:new kg(this.meta,this.ta?this.w:this.w+1,this.root,!0,c,null);a=new Vf;b=(null==this.root?cg:this.root).eb(0,Pc(b),b,c,a);return b===this.root?this:new kg(this.meta,a.C?this.w+1:this.w,b,this.ta,this.Ea,null)};k.$c=function(a,b){return null==b?this.ta:null==this.root?!1:this.root.Qb(0,Pc(b),b,Ed)!==Ed};k.U=function(){if(0<this.w){var a=null!=this.root?this.root.Oc():null;return this.ta?K(new T(null,2,5,U,[null,this.Ea],null),a):a}return null};
k.R=function(a,b){return new kg(b,this.w,this.root,this.ta,this.Ea,this.B)};k.Y=function(a,b){if(Ad(b))return Rb(this,x.e(b,0),x.e(b,1));for(var c=this,e=B(b);;){if(null==e)return c;var d=D(e);if(Ad(d))c=Rb(c,x.e(d,0),x.e(d,1)),e=I(e);else throw Error("conj on a map takes map entries or seqables of map entries");}};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};var Sf=new kg(null,0,null,!1,null,0);
function qd(a,b){for(var c=a.length,e=0,d=uc(Sf);;)if(e<c)var g=e+1,d=d.Jc(null,a[e],b[e]),e=g;else return wc(d)}kg.prototype[vb]=function(){return Wc(this)};function lg(a,b,c,e,d){this.aa=a;this.root=b;this.count=c;this.ta=e;this.Ea=d;this.H=56;this.A=258}k=lg.prototype;k.Jc=function(a,b,c){return mg(this,b,c)};k.Xb=function(a,b){return ng(this,b)};k.Yb=function(){var a;if(this.aa)this.aa=null,a=new kg(null,this.count,this.root,this.ta,this.Ea,null);else throw Error("persistent! called twice");return a};
k.I=function(a,b){return null==b?this.ta?this.Ea:null:null==this.root?null:this.root.Qb(0,Pc(b),b)};k.J=function(a,b,c){return null==b?this.ta?this.Ea:c:null==this.root?c:this.root.Qb(0,Pc(b),b,c)};k.ca=function(){if(this.aa)return this.count;throw Error("count after persistent!");};
function ng(a,b){if(a.aa){if(b?b.A&2048||b.of||(b.A?0:t(Ub,b)):t(Ub,b))return mg(a,Ud.h?Ud.h(b):Ud.call(null,b),Vd.h?Vd.h(b):Vd.call(null,b));for(var c=B(b),e=a;;){var d=D(c);if(s(d))var g=d,c=I(c),e=mg(e,function(){var a=g;return Ud.h?Ud.h(a):Ud.call(null,a)}(),function(){var a=g;return Vd.h?Vd.h(a):Vd.call(null,a)}());else return e}}else throw Error("conj! after persistent");}
function mg(a,b,c){if(a.aa){if(null==b)a.Ea!==c&&(a.Ea=c),a.ta||(a.count+=1,a.ta=!0);else{var e=new Vf;b=(null==a.root?cg:a.root).fb(a.aa,0,Pc(b),b,c,e);b!==a.root&&(a.root=b);e.C&&(a.count+=1)}return a}throw Error("assoc! after persistent!");}function og(a,b,c){for(var e=b;;)if(null!=a)b=c?a.left:a.right,e=od.e(e,a),a=b;else return e}function pg(a,b,c,e,d){this.meta=a;this.stack=b;this.Wc=c;this.w=e;this.B=d;this.H=0;this.A=32374862}k=pg.prototype;k.toString=function(){return Gc(this)};
k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.meta};k.ca=function(){return 0>this.w?N(I(this))+1:this.w};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){var a=this.stack;return null==a?null:Yb(a)};
k.pa=function(){var a=D(this.stack),a=og(this.Wc?a.right:a.left,I(this.stack),this.Wc);return null!=a?new pg(null,a,this.Wc,this.w-1,null):G};k.U=function(){return this};k.R=function(a,b){return new pg(b,this.stack,this.Wc,this.w,this.B)};k.Y=function(a,b){return K(b,this)};pg.prototype[vb]=function(){return Wc(this)};
function qg(a,b,c,e){return c instanceof Y?c.left instanceof Y?new Y(c.key,c.C,c.left.ob(),new rg(a,b,c.right,e,null),null):c.right instanceof Y?new Y(c.right.key,c.right.C,new rg(c.key,c.C,c.left,c.right.left,null),new rg(a,b,c.right.right,e,null),null):new rg(a,b,c,e,null):new rg(a,b,c,e,null)}
function sg(a,b,c,e){return e instanceof Y?e.right instanceof Y?new Y(e.key,e.C,new rg(a,b,c,e.left,null),e.right.ob(),null):e.left instanceof Y?new Y(e.left.key,e.left.C,new rg(a,b,c,e.left.left,null),new rg(e.key,e.C,e.left.right,e.right,null),null):new rg(a,b,c,e,null):new rg(a,b,c,e,null)}
function tg(a,b,c,e){if(c instanceof Y)return new Y(a,b,c.ob(),e,null);if(e instanceof rg)return sg(a,b,c,e.Tc());if(e instanceof Y&&e.left instanceof rg)return new Y(e.left.key,e.left.C,new rg(a,b,c,e.left.left,null),sg(e.key,e.C,e.left.right,e.right.Tc()),null);throw Error("red-black tree invariant violation");}
var vg=function ug(b,c,e){e=null!=b.left?ug(b.left,c,e):e;if(bd(e))return J.h?J.h(e):J.call(null,e);var d=b.key,g=b.C;e=c.j?c.j(e,d,g):c.call(null,e,d,g);if(bd(e))return J.h?J.h(e):J.call(null,e);b=null!=b.right?ug(b.right,c,e):e;return bd(b)?J.h?J.h(b):J.call(null,b):b};function rg(a,b,c,e,d){this.key=a;this.C=b;this.left=c;this.right=e;this.B=d;this.H=0;this.A=32402207}k=rg.prototype;k.de=function(a){return a.fe(this)};k.Tc=function(){return new Y(this.key,this.C,this.left,this.right,null)};
k.ob=function(){return this};k.ce=function(a){return a.ee(this)};k.replace=function(a,b,c,e){return new rg(a,b,c,e,null)};k.ee=function(a){return new rg(a.key,a.C,this,a.right,null)};k.fe=function(a){return new rg(a.key,a.C,a.left,this,null)};k.ec=function(a,b){return vg(this,a,b)};k.I=function(a,b){return x.j(this,b,null)};k.J=function(a,b,c){return x.j(this,b,c)};k.s=function(a,b){return 0===b?this.key:1===b?this.C:null};k.Aa=function(a,b,c){return 0===b?this.key:1===b?this.C:c};
k.Zb=function(a,b,c){return(new T(null,2,5,U,[this.key,this.C],null)).Zb(null,b,c)};k.P=function(){return null};k.ca=function(){return 2};k.Gc=function(){return this.key};k.Hc=function(){return this.C};k.Mb=function(){return this.C};k.Nb=function(){return new T(null,1,5,U,[this.key],null)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return nd};k.ma=function(a,b){return cd.e(this,b)};k.na=function(a,b,c){return cd.j(this,b,c)};
k.pb=function(a,b,c){return rd.j(new T(null,2,5,U,[this.key,this.C],null),b,c)};k.U=function(){return Gb(Gb(G,this.C),this.key)};k.R=function(a,b){return ld(new T(null,2,5,U,[this.key,this.C],null),b)};k.Y=function(a,b){return new T(null,3,5,U,[this.key,this.C,b],null)};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};rg.prototype[vb]=function(){return Wc(this)};
function Y(a,b,c,e,d){this.key=a;this.C=b;this.left=c;this.right=e;this.B=d;this.H=0;this.A=32402207}k=Y.prototype;k.de=function(a){return new Y(this.key,this.C,this.left,a,null)};k.Tc=function(){throw Error("red-black tree invariant violation");};k.ob=function(){return new rg(this.key,this.C,this.left,this.right,null)};k.ce=function(a){return new Y(this.key,this.C,a,this.right,null)};k.replace=function(a,b,c,e){return new Y(a,b,c,e,null)};
k.ee=function(a){return this.left instanceof Y?new Y(this.key,this.C,this.left.ob(),new rg(a.key,a.C,this.right,a.right,null),null):this.right instanceof Y?new Y(this.right.key,this.right.C,new rg(this.key,this.C,this.left,this.right.left,null),new rg(a.key,a.C,this.right.right,a.right,null),null):new rg(a.key,a.C,this,a.right,null)};
k.fe=function(a){return this.right instanceof Y?new Y(this.key,this.C,new rg(a.key,a.C,a.left,this.left,null),this.right.ob(),null):this.left instanceof Y?new Y(this.left.key,this.left.C,new rg(a.key,a.C,a.left,this.left.left,null),new rg(this.key,this.C,this.left.right,this.right,null),null):new rg(a.key,a.C,a.left,this,null)};k.ec=function(a,b){return vg(this,a,b)};k.I=function(a,b){return x.j(this,b,null)};k.J=function(a,b,c){return x.j(this,b,c)};
k.s=function(a,b){return 0===b?this.key:1===b?this.C:null};k.Aa=function(a,b,c){return 0===b?this.key:1===b?this.C:c};k.Zb=function(a,b,c){return(new T(null,2,5,U,[this.key,this.C],null)).Zb(null,b,c)};k.P=function(){return null};k.ca=function(){return 2};k.Gc=function(){return this.key};k.Hc=function(){return this.C};k.Mb=function(){return this.C};k.Nb=function(){return new T(null,1,5,U,[this.key],null)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};
k.G=function(a,b){return id(this,b)};k.ea=function(){return nd};k.ma=function(a,b){return cd.e(this,b)};k.na=function(a,b,c){return cd.j(this,b,c)};k.pb=function(a,b,c){return rd.j(new T(null,2,5,U,[this.key,this.C],null),b,c)};k.U=function(){return Gb(Gb(G,this.C),this.key)};k.R=function(a,b){return ld(new T(null,2,5,U,[this.key,this.C],null),b)};k.Y=function(a,b){return new T(null,3,5,U,[this.key,this.C,b],null)};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};Y.prototype[vb]=function(){return Wc(this)};
var xg=function wg(b,c,e,d,g){if(null==c)return new Y(e,d,null,null,null);var f;f=c.key;f=b.e?b.e(e,f):b.call(null,e,f);if(0===f)return g[0]=c,null;if(0>f)return b=wg(b,c.left,e,d,g),null!=b?c.ce(b):null;b=wg(b,c.right,e,d,g);return null!=b?c.de(b):null},zg=function yg(b,c){if(null==b)return c;if(null==c)return b;if(b instanceof Y){if(c instanceof Y){var e=yg(b.right,c.left);return e instanceof Y?new Y(e.key,e.C,new Y(b.key,b.C,b.left,e.left,null),new Y(c.key,c.C,e.right,c.right,null),null):new Y(b.key,
b.C,b.left,new Y(c.key,c.C,e,c.right,null),null)}return new Y(b.key,b.C,b.left,yg(b.right,c),null)}if(c instanceof Y)return new Y(c.key,c.C,yg(b,c.left),c.right,null);e=yg(b.right,c.left);return e instanceof Y?new Y(e.key,e.C,new rg(b.key,b.C,b.left,e.left,null),new rg(c.key,c.C,e.right,c.right,null),null):tg(b.key,b.C,b.left,new rg(c.key,c.C,e,c.right,null))},Bg=function Ag(b,c,e,d){if(null!=c){var g;g=c.key;g=b.e?b.e(e,g):b.call(null,e,g);if(0===g)return d[0]=c,zg(c.left,c.right);if(0>g)return b=
Ag(b,c.left,e,d),null!=b||null!=d[0]?c.left instanceof rg?tg(c.key,c.C,b,c.right):new Y(c.key,c.C,b,c.right,null):null;b=Ag(b,c.right,e,d);if(null!=b||null!=d[0])if(c.right instanceof rg)if(d=c.key,e=c.C,c=c.left,b instanceof Y)c=new Y(d,e,c,b.ob(),null);else if(c instanceof rg)c=qg(d,e,c.Tc(),b);else if(c instanceof Y&&c.right instanceof rg)c=new Y(c.right.key,c.right.C,qg(c.key,c.C,c.left.Tc(),c.right.left),new rg(d,e,c.right.right,b,null),null);else throw Error("red-black tree invariant violation");
else c=new Y(c.key,c.C,c.left,b,null);else c=null;return c}return null},Dg=function Cg(b,c,e,d){var g=c.key,f=b.e?b.e(e,g):b.call(null,e,g);return 0===f?c.replace(g,d,c.left,c.right):0>f?c.replace(g,c.C,Cg(b,c.left,e,d),c.right):c.replace(g,c.C,c.left,Cg(b,c.right,e,d))};function Eg(a,b,c,e,d){this.Ja=a;this.Jb=b;this.w=c;this.meta=e;this.B=d;this.A=418776847;this.H=8192}k=Eg.prototype;
k.forEach=function(a){for(var b=B(this),c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d),f=O.j(g,0,null),g=O.j(g,1,null);a.e?a.e(g,f):a.call(null,g,f);d+=1}else if(b=B(b))R(b)?(c=y(b),b=A(b),f=c,e=N(c),c=f):(c=D(b),f=O.j(c,0,null),c=g=O.j(c,1,null),a.e?a.e(c,f):a.call(null,c,f),b=I(b),c=null,e=0),d=0;else return null};k.get=function(a){return this.I(null,a)};k.entries=function(){return Gf(B(this))};k.toString=function(){return Gc(this)};k.keys=function(){return Wc(Nf.h?Nf.h(this):Nf.call(null,this))};
k.values=function(){return Wc(Of.h?Of.h(this):Of.call(null,this))};k.equiv=function(a){return this.G(null,a)};function Fg(a,b){for(var c=a.Jb;;)if(null!=c){var e;e=c.key;e=a.Ja.e?a.Ja.e(b,e):a.Ja.call(null,b,e);if(0===e)return c;c=0>e?c.left:c.right}else return null}k.has=function(a){return Hd(this,a)};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){a=Fg(this,b);return null!=a?a.C:c};k.Ec=function(a,b,c){return null!=this.Jb?vg(this.Jb,b,c):c};k.P=function(){return this.meta};
k.za=function(){return new Eg(this.Ja,this.Jb,this.w,this.meta,this.B)};k.ca=function(){return this.w};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Zc(this)};k.G=function(a,b){return Ef(this,b)};k.ea=function(){return new Eg(this.Ja,null,0,this.meta,0)};k.Fc=function(a,b){var c=[null],e=Bg(this.Ja,this.Jb,b,c);return null==e?null==O.e(c,0)?this:new Eg(this.Ja,null,0,this.meta,null):new Eg(this.Ja,e.ob(),this.w-1,this.meta,null)};
k.pb=function(a,b,c){a=[null];var e=xg(this.Ja,this.Jb,b,c,a);return null==e?(a=O.e(a,0),Uc.e(c,a.C)?this:new Eg(this.Ja,Dg(this.Ja,this.Jb,b,c),this.w,this.meta,null)):new Eg(this.Ja,e.ob(),this.w+1,this.meta,null)};k.$c=function(a,b){return null!=Fg(this,b)};k.U=function(){var a;0<this.w?(a=this.w,a=new pg(null,og(this.Jb,null,!0),!0,a,null)):a=null;return a};k.R=function(a,b){return new Eg(this.Ja,this.Jb,this.w,b,this.B)};
k.Y=function(a,b){if(Ad(b))return Rb(this,x.e(b,0),x.e(b,1));for(var c=this,e=B(b);;){if(null==e)return c;var d=D(e);if(Ad(d))c=Rb(c,x.e(d,0),x.e(d,1)),e=I(e);else throw Error("conj on a map takes map entries or seqables of map entries");}};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};Eg.prototype[vb]=function(){return Wc(this)};
var Ee=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){a=B(a);for(var b=uc(Sf);;)if(a){var d=I(I(a)),b=ve.j(b,D(a),D(I(a)));a=d}else return wc(b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),Gg=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){return Tf(Q.e(xb,a),!0,!1)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}();function Hg(a,b){this.va=a;this.ya=b;this.H=0;this.A=32374988}k=Hg.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.ya};k.sa=function(){var a=this.va,a=(a?a.A&128||a.ed||(a.A?0:t(Mb,a)):t(Mb,a))?this.va.sa(null):I(this.va);return null==a?null:new Hg(a,this.ya)};k.L=function(){return Yc(this)};
k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.ya)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return this.va.la(null).Gc(null)};k.pa=function(){var a=this.va,a=(a?a.A&128||a.ed||(a.A?0:t(Mb,a)):t(Mb,a))?this.va.sa(null):I(this.va);return null!=a?new Hg(a,this.ya):G};k.U=function(){return this};k.R=function(a,b){return new Hg(this.va,b)};k.Y=function(a,b){return K(b,this)};Hg.prototype[vb]=function(){return Wc(this)};
function Nf(a){return(a=B(a))?new Hg(a,null):null}function Ud(a){return Vb(a)}function Ig(a,b){this.va=a;this.ya=b;this.H=0;this.A=32374988}k=Ig.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.P=function(){return this.ya};k.sa=function(){var a=this.va,a=(a?a.A&128||a.ed||(a.A?0:t(Mb,a)):t(Mb,a))?this.va.sa(null):I(this.va);return null==a?null:new Ig(a,this.ya)};k.L=function(){return Yc(this)};k.G=function(a,b){return id(this,b)};
k.ea=function(){return ld(G,this.ya)};k.ma=function(a,b){return md.e(b,this)};k.na=function(a,b,c){return md.j(b,c,this)};k.la=function(){return this.va.la(null).Hc(null)};k.pa=function(){var a=this.va,a=(a?a.A&128||a.ed||(a.A?0:t(Mb,a)):t(Mb,a))?this.va.sa(null):I(this.va);return null!=a?new Ig(a,this.ya):G};k.U=function(){return this};k.R=function(a,b){return new Ig(this.va,b)};k.Y=function(a,b){return K(b,this)};Ig.prototype[vb]=function(){return Wc(this)};
function Of(a){return(a=B(a))?new Ig(a,null):null}function Vd(a){return Wb(a)}var Jg=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){return s(ze(Md,a))?yb.e(function(a,c){return od.e(s(a)?a:Qf,c)},a):null}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}();function Kg(a,b,c){this.meta=a;this.cc=b;this.B=c;this.A=15077647;this.H=8196}k=Kg.prototype;
k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.keys=function(){return Wc(B(this))};k.entries=function(){return If(B(this))};k.values=function(){return Wc(B(this))};k.has=function(a){return Hd(this,a)};
k.forEach=function(a){for(var b=B(this),c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d),f=O.j(g,0,null),g=O.j(g,1,null);a.e?a.e(g,f):a.call(null,g,f);d+=1}else if(b=B(b))R(b)?(c=y(b),b=A(b),f=c,e=N(c),c=f):(c=D(b),f=O.j(c,0,null),c=g=O.j(c,1,null),a.e?a.e(c,f):a.call(null,c,f),b=I(b),c=null,e=0),d=0;else return null};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){return Qb(this.cc,b)?b:c};k.P=function(){return this.meta};k.za=function(){return new Kg(this.meta,this.cc,this.B)};k.ca=function(){return Db(this.cc)};
k.L=function(){var a=this.B;return null!=a?a:this.B=a=Zc(this)};k.G=function(a,b){return yd(b)&&N(this)===N(b)&&ye(function(a){return function(b){return Hd(a,b)}}(this),b)};k.qc=function(){return new Lg(uc(this.cc))};k.ea=function(){return ld(Mg,this.meta)};k.U=function(){return Nf(this.cc)};k.R=function(a,b){return new Kg(b,this.cc,this.B)};k.Y=function(a,b){return new Kg(this.meta,rd.j(this.cc,b,null),null)};
k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};var Mg=new Kg(null,Qf,0);Kg.prototype[vb]=function(){return Wc(this)};
function Lg(a){this.Ib=a;this.A=259;this.H=136}k=Lg.prototype;k.call=function(){function a(a,c,b){return Ob.j(this.Ib,c,Ed)===Ed?b:c}function b(a,c){return Ob.j(this.Ib,c,Ed)===Ed?null:c}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};
k.h=function(a){return Ob.j(this.Ib,a,Ed)===Ed?null:a};k.e=function(a,b){return Ob.j(this.Ib,a,Ed)===Ed?b:a};k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){return Ob.j(this.Ib,b,Ed)===Ed?c:b};k.ca=function(){return N(this.Ib)};k.Xb=function(a,b){this.Ib=ve.j(this.Ib,b,null);return this};k.Yb=function(){return new Kg(null,wc(this.Ib),null)};function Ng(a,b,c){this.meta=a;this.kc=b;this.B=c;this.A=417730831;this.H=8192}k=Ng.prototype;k.toString=function(){return Gc(this)};
k.equiv=function(a){return this.G(null,a)};k.keys=function(){return Wc(B(this))};k.entries=function(){return If(B(this))};k.values=function(){return Wc(B(this))};k.has=function(a){return Hd(this,a)};k.forEach=function(a){for(var b=B(this),c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d),f=O.j(g,0,null),g=O.j(g,1,null);a.e?a.e(g,f):a.call(null,g,f);d+=1}else if(b=B(b))R(b)?(c=y(b),b=A(b),f=c,e=N(c),c=f):(c=D(b),f=O.j(c,0,null),c=g=O.j(c,1,null),a.e?a.e(c,f):a.call(null,c,f),b=I(b),c=null,e=0),d=0;else return null};
k.I=function(a,b){return Ob.j(this,b,null)};k.J=function(a,b,c){a=Fg(this.kc,b);return null!=a?a.key:c};k.P=function(){return this.meta};k.za=function(){return new Ng(this.meta,this.kc,this.B)};k.ca=function(){return N(this.kc)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Zc(this)};k.G=function(a,b){return yd(b)&&N(this)===N(b)&&ye(function(a){return function(b){return Hd(a,b)}}(this),b)};k.ea=function(){return new Ng(this.meta,Eb(this.kc),0)};k.U=function(){return Nf(this.kc)};
k.R=function(a,b){return new Ng(b,this.kc,this.B)};k.Y=function(a,b){return new Ng(this.meta,rd.j(this.kc,b,null),null)};k.call=function(){var a=null,a=function(a,c,e){switch(arguments.length){case 2:return this.I(null,c);case 3:return this.J(null,c,e)}throw Error("Invalid arity: "+arguments.length);};a.e=function(a,c){return this.I(null,c)};a.j=function(a,c,e){return this.J(null,c,e)};return a}();k.apply=function(a,b){return this.call.apply(this,[this].concat(wb(b)))};
k.h=function(a){return this.I(null,a)};k.e=function(a,b){return this.J(null,a,b)};Ng.prototype[vb]=function(){return Wc(this)};
var Og=function(){function a(a,c){if(Ad(c)){var b=N(c);return yb.j(function(){return function(c,b){var d=Id(a,O.e(c,b));return s(d)?rd.j(c,b,D(I(d))):c}}(b),c,Oe.e(b,Qe($c,0)))}return Ne.e(function(c){var b=Id(a,c);return s(b)?D(I(b)):c},c)}function b(a){return Ne.h(function(c){var b=Id(a,c);return s(b)?Wb(b):c})}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();
function ce(a){if(a&&(a.H&4096||a.qf))return a.name;if("string"===typeof a)return a;throw Error([w("Doesn't support name: "),w(a)].join(""));}function Pg(a,b){for(var c=uc(Qf),e=B(a),d=B(b);;)if(e&&d)c=ve.j(c,D(e),D(d)),e=I(e),d=I(d);else return wc(c)}function Qg(a,b,c){this.i=a;this.end=b;this.step=c}Qg.prototype.qd=function(){return 0<this.step?this.i<this.end:this.i>this.end};Qg.prototype.next=function(){var a=this.i;this.i+=this.step;return a};
function Rg(a,b,c,e,d){this.meta=a;this.start=b;this.end=c;this.step=e;this.B=d;this.A=32375006;this.H=8192}k=Rg.prototype;k.toString=function(){return Gc(this)};k.equiv=function(a){return this.G(null,a)};k.s=function(a,b){if(b<Db(this))return this.start+b*this.step;if(this.start>this.end&&0===this.step)return this.start;throw Error("Index out of bounds");};k.Aa=function(a,b,c){return b<Db(this)?this.start+b*this.step:this.start>this.end&&0===this.step?this.start:c};
k.Dc=function(){return new Qg(this.start,this.end,this.step)};k.P=function(){return this.meta};k.za=function(){return new Rg(this.meta,this.start,this.end,this.step,this.B)};k.sa=function(){return 0<this.step?this.start+this.step<this.end?new Rg(this.meta,this.start+this.step,this.end,this.step,null):null:this.start+this.step>this.end?new Rg(this.meta,this.start+this.step,this.end,this.step,null):null};
k.ca=function(){if(sb(oc(this)))return 0;var a=(this.end-this.start)/this.step;return Math.ceil.h?Math.ceil.h(a):Math.ceil.call(null,a)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Yc(this)};k.G=function(a,b){return id(this,b)};k.ea=function(){return ld(G,this.meta)};k.ma=function(a,b){return cd.e(this,b)};k.na=function(a,b,c){for(a=this.start;;)if(0<this.step?a<this.end:a>this.end){var e=a;c=b.e?b.e(c,e):b.call(null,c,e);if(bd(c))return b=c,J.h?J.h(b):J.call(null,b);a+=this.step}else return c};
k.la=function(){return null==oc(this)?null:this.start};k.pa=function(){return null!=oc(this)?new Rg(this.meta,this.start+this.step,this.end,this.step,null):G};k.U=function(){return 0<this.step?this.start<this.end?this:null:this.start>this.end?this:null};k.R=function(a,b){return new Rg(b,this.start,this.end,this.step,this.B)};k.Y=function(a,b){return K(b,this)};Rg.prototype[vb]=function(){return Wc(this)};
var Sg=function(){function a(a,c,b){return new Rg(null,a,c,b,null)}function b(a,c){return d.j(a,c,1)}function c(a){return d.j(0,a,1)}function e(){return d.j(0,Number.MAX_VALUE,1)}var d=null,d=function(d,f,h){switch(arguments.length){case 0:return e.call(this);case 1:return c.call(this,d);case 2:return b.call(this,d,f);case 3:return a.call(this,d,f,h)}throw Error("Invalid arity: "+arguments.length);};d.F=e;d.h=c;d.e=b;d.j=a;return d}(),Tg=function(){function a(a,c){for(;;)if(B(c)&&0<a){var b=a-1,f=
I(c);a=b;c=f}else return null}function b(a){for(;;)if(B(a))a=I(a);else return null}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}(),Ug=function(){function a(a,c){Tg.e(a,c);return c}function b(a){Tg.h(a);return a}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);
};c.h=b;c.e=a;return c}();function Vg(a,b){if("string"===typeof b){var c=a.exec(b);return Uc.e(D(c),b)?1===N(c)?D(c):rf(c):null}throw new TypeError("re-matches must match against a string.");}function Wg(a,b){if("string"===typeof b){var c=a.exec(b);return null==c?null:1===N(c)?D(c):rf(c)}throw new TypeError("re-find must match against a string.");}
function Xg(a){if(a instanceof RegExp)return a;var b=Wg(/^(?:\(\?([idmsux]*)\))?(.*)/,a);O.j(b,0,null);a=O.j(b,1,null);b=O.j(b,2,null);return new RegExp(b,a)}
function Yg(a,b,c,e,d,g,f){var h=ib;try{ib=null==ib?null:ib-1;if(null!=ib&&0>ib)return qc(a,"#");qc(a,c);if(B(f)){var m=D(f);b.j?b.j(m,a,g):b.call(null,m,a,g)}for(var p=I(f),q=qb.h(g)-1;;)if(!p||null!=q&&0===q){B(p)&&0===q&&(qc(a,e),qc(a,"..."));break}else{qc(a,e);var r=D(p);c=a;f=g;b.j?b.j(r,c,f):b.call(null,r,c,f);var u=I(p);c=q-1;p=u;q=c}return qc(a,d)}finally{ib=h}}
var Zg=function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){for(var d=B(b),g=null,f=0,h=0;;)if(h<f){var m=g.s(null,h);qc(a,m);h+=1}else if(d=B(d))g=d,R(g)?(d=y(g),f=A(g),g=d,m=N(d),d=f,f=m):(m=D(g),qc(a,m),d=I(g),g=null,f=0),h=0;else return null}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}(),$g={'"':'\\"',"\\":"\\\\","\b":"\\b","\f":"\\f",
"\n":"\\n","\r":"\\r","\t":"\\t"};function ah(a){return[w('"'),w(a.replace(RegExp('[\\\\"\b\f\n\r\t]',"g"),function(a){return $g[a]})),w('"')].join("")}
var dh=function bh(b,c,e){if(null==b)return qc(c,"nil");if(void 0===b)return qc(c,"#\x3cundefined\x3e");s(function(){var c=P.e(e,ob);return s(c)?(c=b?b.A&131072||b.pf?!0:b.A?!1:t(dc,b):t(dc,b))?wd(b):c:c}())&&(qc(c,"^"),bh(wd(b),c,e),qc(c," "));if(null==b)return qc(c,"nil");if(b.kd)return b.Nd(b,c,e);if(b&&(b.A&2147483648||b.Z))return b.M(null,c,e);if(tb(b)===Boolean||"number"===typeof b)return qc(c,""+w(b));if(null!=b&&b.constructor===Object){qc(c,"#js ");var d=Ne.e(function(c){return new T(null,
2,5,U,[de.h(c),b[c]],null)},Bd(b));return ch.D?ch.D(d,bh,c,e):ch.call(null,d,bh,c,e)}return b instanceof Array?Yg(c,bh,"#js ["," ","]",e,b):s(fa(b))?s(nb.h(e))?qc(c,ah(b)):qc(c,b):ud(b)?Zg.k(c,L(["#\x3c",""+w(b),"\x3e"],0)):b instanceof Date?(d=function(c,b){for(var e=""+w(c);;)if(N(e)<b)e=[w("0"),w(e)].join("");else return e},Zg.k(c,L(['#inst "',""+w(b.getUTCFullYear()),"-",d(b.getUTCMonth()+1,2),"-",d(b.getUTCDate(),2),"T",d(b.getUTCHours(),2),":",d(b.getUTCMinutes(),2),":",d(b.getUTCSeconds(),
2),".",d(b.getUTCMilliseconds(),3),"-",'00:00"'],0))):b instanceof RegExp?Zg.k(c,L(['#"',b.source,'"'],0)):(b?b.A&2147483648||b.Z||(b.A?0:t(rc,b)):t(rc,b))?sc(b,c,e):Zg.k(c,L(["#\x3c",""+w(b),"\x3e"],0))};
function eh(a,b){var c;if(null==a||sb(B(a)))c="";else{c=w;var e=new Ia;a:{var d=new Fc(e);dh(D(a),d,b);for(var g=B(I(a)),f=null,h=0,m=0;;)if(m<h){var p=f.s(null,m);qc(d," ");dh(p,d,b);m+=1}else if(g=B(g))f=g,R(f)?(g=y(f),h=A(f),f=g,p=N(g),g=h,h=p):(p=D(f),qc(d," "),dh(p,d,b),g=I(f),f=null,h=0),m=0;else break a}c=""+c(e)}return c}
var Je=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){return eh(a,lb())}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),fh=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=rd.j(lb(),nb,!1);a=eh(a,b);gb.h?gb.h(a):
gb.call(null,a);s(hb)?(a=lb(),gb.h?gb.h("\n"):gb.call(null,"\n"),a=(P.e(a,mb),null)):a=null;return a}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}();function ch(a,b,c,e){return Yg(c,function(a,c,e){var h=Vb(a);b.j?b.j(h,c,e):b.call(null,h,c,e);qc(c," ");a=Wb(a);return b.j?b.j(a,c,e):b.call(null,a,c,e)},"{",", ","}",e,B(a))}Me.prototype.Z=!0;Me.prototype.M=function(a,b,c){qc(b,"#\x3cVolatile: ");dh(this.state,b,c);return qc(b,"\x3e")};C.prototype.Z=!0;
C.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};ee.prototype.Z=!0;ee.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};pg.prototype.Z=!0;pg.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};ig.prototype.Z=!0;ig.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};rg.prototype.Z=!0;rg.prototype.M=function(a,b,c){return Yg(b,dh,"["," ","]",c,this)};Kf.prototype.Z=!0;Kf.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};
Ng.prototype.Z=!0;Ng.prototype.M=function(a,b,c){return Yg(b,dh,"#{"," ","}",c,this)};sf.prototype.Z=!0;sf.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};$d.prototype.Z=!0;$d.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};kd.prototype.Z=!0;kd.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};kg.prototype.Z=!0;kg.prototype.M=function(a,b,c){return ch(this,dh,b,c)};jg.prototype.Z=!0;jg.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};
uf.prototype.Z=!0;uf.prototype.M=function(a,b,c){return Yg(b,dh,"["," ","]",c,this)};Eg.prototype.Z=!0;Eg.prototype.M=function(a,b,c){return ch(this,dh,b,c)};Kg.prototype.Z=!0;Kg.prototype.M=function(a,b,c){return Yg(b,dh,"#{"," ","}",c,this)};je.prototype.Z=!0;je.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};De.prototype.Z=!0;De.prototype.M=function(a,b,c){qc(b,"#\x3cAtom: ");dh(this.state,b,c);return qc(b,"\x3e")};Ig.prototype.Z=!0;
Ig.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};Y.prototype.Z=!0;Y.prototype.M=function(a,b,c){return Yg(b,dh,"["," ","]",c,this)};T.prototype.Z=!0;T.prototype.M=function(a,b,c){return Yg(b,dh,"["," ","]",c,this)};zf.prototype.Z=!0;zf.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};Xd.prototype.Z=!0;Xd.prototype.M=function(a,b){return qc(b,"()")};Af.prototype.Z=!0;Af.prototype.M=function(a,b,c){return Yg(b,dh,"#queue ["," ","]",c,B(this))};n.prototype.Z=!0;
n.prototype.M=function(a,b,c){return ch(this,dh,b,c)};Rg.prototype.Z=!0;Rg.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};Hg.prototype.Z=!0;Hg.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};Wd.prototype.Z=!0;Wd.prototype.M=function(a,b,c){return Yg(b,dh,"("," ",")",c,this)};T.prototype.ad=!0;T.prototype.bd=function(a,b){return Kd.e(this,b)};uf.prototype.ad=!0;uf.prototype.bd=function(a,b){return Kd.e(this,b)};V.prototype.ad=!0;
V.prototype.bd=function(a,b){return ae(this,b)};Sc.prototype.ad=!0;Sc.prototype.bd=function(a,b){return Rc(this,b)};var gh={};function hh(a){if(a?a.kf:a)return a.kf(a);var b;b=hh[l(null==a?null:a)];if(!b&&(b=hh._,!b))throw v("IEncodeJS.-clj-\x3ejs",a);return b.call(null,a)}function ih(a){return(a?s(s(null)?null:a.jf)||(a.Od?0:t(gh,a)):t(gh,a))?hh(a):"string"===typeof a||"number"===typeof a||a instanceof V||a instanceof Sc?jh.h?jh.h(a):jh.call(null,a):Je.k(L([a],0))}
var jh=function kh(b){if(null==b)return null;if(b?s(s(null)?null:b.jf)||(b.Od?0:t(gh,b)):t(gh,b))return hh(b);if(b instanceof V)return ce(b);if(b instanceof Sc)return""+w(b);if(zd(b)){var c={};b=B(b);for(var e=null,d=0,g=0;;)if(g<d){var f=e.s(null,g),h=O.j(f,0,null),f=O.j(f,1,null);c[ih(h)]=kh(f);g+=1}else if(b=B(b))R(b)?(d=y(b),b=A(b),e=d,d=N(d)):(d=D(b),e=O.j(d,0,null),d=O.j(d,1,null),c[ih(e)]=kh(d),b=I(b),e=null,d=0),g=0;else break;return c}if(xd(b)){c=[];b=B(Ne.e(kh,b));e=null;for(g=d=0;;)if(g<
d)h=e.s(null,g),c.push(h),g+=1;else if(b=B(b))e=b,R(e)?(b=y(e),g=A(e),e=b,d=N(b),b=g):(b=D(e),c.push(b),b=I(e),e=null,d=0),g=0;else break;return c}return b},lh={};function mh(a,b){if(a?a.hf:a)return a.hf(a,b);var c;c=mh[l(null==a?null:a)];if(!c&&(c=mh._,!c))throw v("IEncodeClojure.-js-\x3eclj",a);return c.call(null,a,b)}
var oh=function(){function a(a){return b.k(a,L([new n(null,1,[nh,!1],null)],0))}var b=null,c=function(){function a(b,e){var h=null;if(1<arguments.length){for(var h=0,m=Array(arguments.length-1);h<m.length;)m[h]=arguments[h+1],++h;h=new C(m,0)}return c.call(this,b,h)}function c(a,b){var e=Fd(b)?Q.e(Ee,b):b,d=P.e(e,nh);return function(a,c,e,d){return function E(g){return(g?s(s(null)?null:g.yg)||(g.Od?0:t(lh,g)):t(lh,g))?mh(g,Q.e(Gg,b)):Fd(g)?Ug.h(Ne.e(E,g)):xd(g)?Se.e(null==g?null:Eb(g),Ne.e(E,g)):
g instanceof Array?rf(Ne.e(E,g)):tb(g)===Object?Se.e(Qf,function(){return function(a,c,b,e){return function Za(d){return new ee(null,function(a,c,b,e){return function(){for(;;){var a=B(d);if(a){if(R(a)){var c=y(a),b=N(c),f=new ge(Array(b),0);return function(){for(var a=0;;)if(a<b){var d=x.e(c,a),h=f,m=U,p;p=d;p=e.h?e.h(p):e.call(null,p);d=new T(null,2,5,m,[p,E(g[d])],null);h.add(d);a+=1}else return!0}()?ke(f.Ia(),Za(A(a))):ke(f.Ia(),null)}var h=D(a);return K(new T(null,2,5,U,[function(){var a=h;return e.h?
e.h(a):e.call(null,a)}(),E(g[h])],null),Za(F(a)))}return null}}}(a,c,b,e),null,null)}}(a,c,e,d)(Bd(g))}()):g}}(b,e,d,s(d)?de:w)(a)}a.q=1;a.l=function(a){var b=D(a);a=F(a);return c(b,a)};a.k=c;return a}(),b=function(b,d){switch(arguments.length){case 1:return a.call(this,b);default:var g=null;if(1<arguments.length){for(var g=0,f=Array(arguments.length-1);g<f.length;)f[g]=arguments[g+1],++g;g=new C(f,0)}return c.k(b,g)}throw Error("Invalid arity: "+arguments.length);};b.q=1;b.l=c.l;b.h=a;b.k=c.k;return b}();
function ph(a){this.Ub=a;this.H=0;this.A=2153775104}k=ph.prototype;k.L=function(){for(var a=Je.k(L([this],0)),b=0,c=0;c<a.length;++c)b=31*b+a.charCodeAt(c),b%=4294967296;return b};k.M=function(a,b){return qc(b,[w('#uuid "'),w(this.Ub),w('"')].join(""))};k.G=function(a,b){return b instanceof ph&&this.Ub===b.Ub};k.toString=function(){return this.Ub};k.equiv=function(a){return this.G(null,a)};var qh=new V(null,"response","response",-1068424192),rh=new V(null,"input-chan","input-chan",-1487255360),sh=new V(null,"thead","thead",-291875296),th=new V(null,"path","path",-188191168),uh=new V(null,"del","del",574975584),vh=new V(null,"encoding","encoding",1728578272),wh=new V(null,"fieldset","fieldset",-1949770816),xh=new V(null,"q","q",689001697),yh=new V(null,"figure","figure",-561394079),zh=new V(null,"aside","aside",1414397537),Ah=new V(null,"figcaption","figcaption",-1790122047),Bh=new V(null,
"video","video",156888130),Ch=new V(null,"address","address",559499426),Dh=new V(null,"caption","caption",-855383902),Eh=new V(null,"definitions","definitions",167529986),Fh=new V(null,"bdi","bdi",-1323025886),Gh=new V(null,"rp","rp",2006698083),Hh=new V(null,"get","get",1683182755),Ih=new V(null,"hr","hr",1377740067),Jh=new V(null,"dd","dd",-1340437629),Kh=new V(null,"json-params","json-params",-1112693596),ob=new V(null,"meta","meta",1499536964),Lh=new V(null,"tbody","tbody",-80678300),Mh=new V(null,
"table","table",-564943036),Oh=new V(null,"ul","ul",-1349521403),pb=new V(null,"dup","dup",556298533),Ph=new V(null,"pre","pre",2118456869),Qh=new V(null,"patch","patch",380775109),Rh=new V(null,"placeholder","placeholder",-104873083),Sh=new V(null,"daily","daily",-697831483),Th=new V(null,"sup","sup",-2039492346),Uh=new V(null,"script","script",-1304443801),Vh=new V(null,"big","big",903550151),Wh=new V(null,"button","button",1456579943),Xh=new V(null,"dfn","dfn",241908167),Yh=new V(null,"sub","sub",
-2093760025),Zh=new V(null,"password","password",417022471),$h=new V(null,"add-new-word","add-new-word",-298964409),ai=new V(null,"transit-params","transit-params",357261095),bi=new V(null,"dictionary","dictionary",-2013643865),Fe=new V(null,"validator","validator",-1966190681),ci=new V(null,"mark","mark",-373816345),di=new V(null,"method","method",55703592),ei=new V(null,"search-term","search-term",356193544),fi=new V(null,"finally-block","finally-block",832982472),gi=new V(null,"wbr","wbr",228661800),
hi=new V(null,"strong","strong",269529E3),ii=new V(null,"name","name",1843675177),ji=new V(null,"td","td",1479933353),ki=new V(null,"li","li",723558921),li=new V(null,"encoding-opts","encoding-opts",-1805664631),mi=new V(null,"dt","dt",-368444759),ni=new V(null,"value","value",305978217),oi=new V(null,"section","section",-300141526),pi=new V(null,"th","th",-545608566),qi=new V(null,"time","time",1385887882),ri=new V(null,"optgroup","optgroup",1738282218),si=new V(null,"onSubmit","onSubmit",761425194),
ti=new V(null,"iframe","iframe",884422026),ui=new V(null,"file","file",-1269645878),vi=new V(null,"tr","tr",-1424774646),wi=new V(null,"username","username",1605666410),xi=new V(null,"functions","functions",184951466),yi=new V(null,"circle","circle",1903212362),zi=new V(null,"definition-added","definition-added",195281834),Ai=new V(null,"end-column","end-column",1425389514),Bi=new V(null,"mode","mode",654403691),Ci=new V(null,"article","article",-21685045),Di=new V(null,"legend","legend",-1027192245),
Ei=new V(null,"em","em",707813035),Fi=new V(null,"kbd","kbd",316156875),Gi=new V(null,"channel","channel",734187692),Z=new V(null,"recur","recur",-437573268),Hi=new V(null,"type","type",1174270348),Ii=new V(null,"abbr","abbr",2088591884),Ji=new V(null,"catch-block","catch-block",1175212748),Ki=new V(null,"delete","delete",-1768633620),Li=new V(null,"state","state",-1988618099),Mi=new V(null,"source","source",-433931539),Ni=new V(null,"output","output",-1105869043),Oi=new V(null,"handlers","handlers",
79528781),mb=new V(null,"flush-on-newline","flush-on-newline",-151457939),Pi=new V(null,"ins","ins",-1021983570),Qi=new V(null,"footer","footer",1606445390),Ri=new V(null,"header","header",119441134),Si=new V(null,"datalist","datalist",-1235043474),Ti=new V(null,"tfoot","tfoot",-701599890),Ui=new V(null,"s","s",1705939918),Vi=new V(null,"h5","h5",-1829156625),Wi=new V(null,"className","className",-1983287057),Xi=new V(null,"canvas","canvas",-1798817489),Yi=new V(null,"param","param",2013631823),Zi=
new V(null,"title","title",636505583),$i=new V(null,"column","column",2078222095),aj=new V(null,"headers","headers",-835030129),bj=new V(null,"server-port","server-port",663745648),cj=new V(null,"small","small",2133478704),dj=new V(null,"style","style",-496642736),ej=new V(null,"textarea","textarea",-650375824),fj=new V(null,"div","div",1057191632),gj=new V(null,"option","option",65132272),nb=new V(null,"readably","readably",1129599760),hj=new V(null,"summary","summary",380847952),ij=new V(null,"samp",
"samp",1506141136),jj=new V(null,"h4","h4",2004862993),kj=new V(null,"head","head",-771383919),lj=new V(null,"g","g",1738089905),mj=new V(null,"with-credentials?","with-credentials?",-1773202222),nj=new V(null,"success","success",1890645906),oj=new V(null,"ol","ol",932524051),pj=new V(null,"details","details",1956795411),qj=new V(null,"form-params","form-params",1884296467),rj=new V(null,"line","line",212345235),sj=new V(null,"list","list",765357683),tj=new V(null,"var","var",-769682797),uj=new V(null,
"status","status",-1997798413),vj=new V(null,"h6","h6",557293780),qb=new V(null,"print-length","print-length",1931866356),wj=new V(null,"link","link",-1769163468),xj=new V(null,"col","col",-1959363084),yj=new V(null,"label","label",1718410804),zj=new V(null,"id","id",-1388402092),Aj=new V(null,"rt","rt",623480692),Bj=new V(null,"decoding-opts","decoding-opts",1050289140),Cj=new V(null,"colgroup","colgroup",651118645),Dj=new V(null,"catch-exception","catch-exception",-1997306795),Ej=new V(null,"meter",
"meter",-187641611),Fj=new V(null,"answer","answer",-742633163),Gj=new V(null,"prev","prev",-1597069226),Hj=new V(null,"svg","svg",856789142),Ij=new V(null,"url","url",276297046),Jj=new V(null,"code","code",1586293142),Kj=new V(null,"continue-block","continue-block",-1852047850),Lj=new V(null,"bdo","bdo",-2131148202),Mj=new V(null,"query-params","query-params",900640534),Nj=new V(null,"content-type","content-type",-508222634),Oj=new V(null,"b","b",1482224470),Pj=new V(null,"end-line","end-line",1837326455),
Qj=new V(null,"http","http",382524695),Rj=new V(null,"oauth-token","oauth-token",311415191),Sj=new V(null,"right","right",-452581833),Tj=new V(null,"post","post",269697687),Uj=new V(null,"entry","entry",505168823),Vj=new V(null,"noscript","noscript",-704777289),Wj=new V(null,"action","action",-811238024),Xj=new V(null,"h2","h2",-372662728),Yj=new V(null,"area","area",472007256),Zj=new V(null,"br","br",934104792),ak=new V(null,"cancel","cancel",-1964088360),bk=new V(null,"uri","uri",-774711847),ck=
new V(null,"form","form",-1624062471),dk=new V(null,"audio","audio",1819127321),ek=new V(null,"tag","tag",-1290361223),fk=new V(null,"decoding","decoding",-568180903),gk=new V(null,"input","input",556931961),ik=new V(null,"server-name","server-name",-1012104295),jk=new V(null,"menuitem","menuitem",1004047353),kk=new V(null,"put","put",1299772570),lk=new V(null,"json","json",1279968570),mk=new V(null,"timeout","timeout",-318625318),nk=new V(null,"base","base",185279322),ok=new V(null,"h1","h1",-1896887462),
pk=new V(null,"transit-opts","transit-opts",1104386010),qk=new V(null,"query-string","query-string",-1018845061),rk=new V(null,"progress","progress",244323547),sk=new V(null,"main","main",-2117802661),tk=new V(null,"embed","embed",-1354913349),uk=new V(null,"h3","h3",2067611163),vk=new V(null,"body","body",-2049205669),wk=new V(null,"keygen","keygen",-571693253),xk=new V(null,"polyline","polyline",-1731551044),yk=new V(null,"cite","cite",1909439996),zk=new V(null,"edn-params","edn-params",894273052),
nh=new V(null,"keywordize-keys","keywordize-keys",1310784252),Ak=new V(null,"rect","rect",-108902628),Bk=new V(null,"basic-auth","basic-auth",-673163332),Ck=new V(null,"p","p",151049309),Dk=new V(null,"multipart-params","multipart-params",-1033508707),Ek=new V(null,"nav","nav",719540477),Fk=new V(null,"ruby","ruby",2000737661),Gk=new V(null,"map","map",1371690461),Hk=new V(null,"scheme","scheme",90199613),Ik=new V(null,"object","object",1474613949),Jk=new V(null,"i","i",-1386841315),Kk=new V(null,
"trace-redirects","trace-redirects",-1149427907),Lk=new V(null,"search-page","search-page",-705609891),Mk=new V(null,"onClick","onClick",-1991238530),Nk=new V(null,"menu","menu",352255198),Ok=new V(null,"blockquote","blockquote",372264190),Pk=new V(null,"img","img",1442687358),Qk=new V(null,"request-method","request-method",1764796830),Rk=new V(null,"a","a",-2123407586),Sk=new V(null,"dl","dl",-2140151713),Tk=new V(null,"select","select",1147833503),Uk=new V(null,"polygon","polygon",837053759),Vk=
new V(null,"html","html",-998796897),Wk=new V(null,"accept","accept",1874130431),Xk=new V(null,"text","text",-1790561697),Yk=new V(null,"span","span",1394872991),Zk=new V(null,"track","track",195787487),$k=new V(null,"data","data",-232669377),al=new V(null,"u","u",-1156634785);var bl;function cl(a,b){if(a?a.Md:a)return a.Md(0,b);var c;c=cl[l(null==a?null:a)];if(!c&&(c=cl._,!c))throw v("ReadPort.take!",a);return c.call(null,a,b)}function dl(a,b,c){if(a?a.hd:a)return a.hd(0,b,c);var e;e=dl[l(null==a?null:a)];if(!e&&(e=dl._,!e))throw v("WritePort.put!",a);return e.call(null,a,b,c)}function el(a){if(a?a.gd:a)return a.gd();var b;b=el[l(null==a?null:a)];if(!b&&(b=el._,!b))throw v("Channel.close!",a);return b.call(null,a)}
function fl(a){if(a?a.pe:a)return!0;var b;b=fl[l(null==a?null:a)];if(!b&&(b=fl._,!b))throw v("Handler.active?",a);return b.call(null,a)}function gl(a){if(a?a.qe:a)return a.Ra;var b;b=gl[l(null==a?null:a)];if(!b&&(b=gl._,!b))throw v("Handler.commit",a);return b.call(null,a)}function hl(a,b){if(a?a.oe:a)return a.oe(0,b);var c;c=hl[l(null==a?null:a)];if(!c&&(c=hl._,!c))throw v("Buffer.add!*",a);return c.call(null,a,b)}
var il=function(){function a(a,b){if(null==b)throw Error([w("Assert failed: "),w(Je.k(L([Zd(new Sc(null,"not","not",1044554643,null),Zd(new Sc(null,"nil?","nil?",1612038930,null),new Sc(null,"itm","itm",-713282527,null)))],0)))].join(""));return hl(a,b)}var b=null,b=function(c,b){switch(arguments.length){case 1:return c;case 2:return a.call(this,c,b)}throw Error("Invalid arity: "+arguments.length);};b.h=function(a){return a};b.e=a;return b}();function jl(a,b,c,e,d){for(var g=0;;)if(g<d)c[e+g]=a[b+g],g+=1;else break}function kl(a,b,c,e){this.head=a;this.Q=b;this.length=c;this.o=e}kl.prototype.pop=function(){if(0===this.length)return null;var a=this.o[this.Q];this.o[this.Q]=null;this.Q=(this.Q+1)%this.o.length;this.length-=1;return a};kl.prototype.unshift=function(a){this.o[this.head]=a;this.head=(this.head+1)%this.o.length;this.length+=1;return null};function ll(a,b){a.length+1===a.o.length&&a.resize();a.unshift(b)}
kl.prototype.resize=function(){var a=Array(2*this.o.length);return this.Q<this.head?(jl(this.o,this.Q,a,0,this.length),this.Q=0,this.head=this.length,this.o=a):this.Q>this.head?(jl(this.o,this.Q,a,0,this.o.length-this.Q),jl(this.o,0,a,this.o.length-this.Q,this.head),this.Q=0,this.head=this.length,this.o=a):this.Q===this.head?(this.head=this.Q=0,this.o=a):null};function ml(a,b){for(var c=a.length,e=0;;)if(e<c){var d=a.pop(),g;g=d;g=b.h?b.h(g):b.call(null,g);s(g)&&a.unshift(d);e+=1}else break}
function nl(a){if(!(0<a))throw Error([w("Assert failed: "),w("Can't create a ring buffer of size 0"),w("\n"),w(Je.k(L([Zd(new Sc(null,"\x3e","\x3e",1085014381,null),new Sc(null,"n","n",-2092305744,null),0)],0)))].join(""));return new kl(0,0,0,Array(a))}function ol(a,b){this.X=a;this.Kf=b;this.H=0;this.A=2}ol.prototype.ca=function(){return this.X.length};function pl(a){return a.X.length===a.Kf}ol.prototype.fd=function(){return this.X.pop()};ol.prototype.oe=function(a,b){ll(this.X,b);return this};
function ql(a){return new ol(nl(a),a)};function rl(a){ba.setTimeout(function(){throw a;},0)}function sl(a){ga(ba.setImmediate)?ba.setImmediate(a):(tl||(tl=ul()),tl(a))}var tl;
function ul(){var a=ba.MessageChannel;"undefined"===typeof a&&"undefined"!==typeof window&&window.postMessage&&window.addEventListener&&(a=function(){var a=document.createElement("iframe");a.style.display="none";a.src="";document.documentElement.appendChild(a);var c=a.contentWindow,a=c.document;a.open();a.write("");a.close();var b="callImmediate"+Math.random(),e="file:"==c.location.protocol?"*":c.location.protocol+"//"+c.location.host,a=ma(function(a){if(a.origin==e||a.data==b)this.port1.onmessage()},
this);c.addEventListener("message",a,!1);this.port1={};this.port2={postMessage:function(){c.postMessage(b,e)}}});if("undefined"!==typeof a){var b=new a,c={},e=c;b.port1.onmessage=function(){c=c.next;var a=c.he;c.he=null;a()};return function(a){e.next={he:a};e=e.next;b.port2.postMessage(0)}}return"undefined"!==typeof document&&"onreadystatechange"in document.createElement("script")?function(a){var c=document.createElement("script");c.onreadystatechange=function(){c.onreadystatechange=null;c.parentNode.removeChild(c);
c=null;a();a=null};document.documentElement.appendChild(c)}:function(a){ba.setTimeout(a,0)}};var vl=nl(32),wl=!1,xl=!1;function yl(){wl=!0;xl=!1;for(var a=0;;){var b=vl.pop();if(null!=b&&(b.F?b.F():b.call(null),1024>a)){a+=1;continue}break}wl=!1;return 0<vl.length?zl.F?zl.F():zl.call(null):null}function zl(){var a=xl;if(s(s(a)?wl:a))return null;xl=!0;return sl(yl)}function Al(a){ll(vl,a);zl()};var Bl,Dl=function Cl(b){"undefined"===typeof Bl&&(Bl=function(c,b,d){this.C=c;this.bf=b;this.If=d;this.H=0;this.A=425984},Bl.prototype.Wb=function(){return this.C},Bl.prototype.P=function(){return this.If},Bl.prototype.R=function(c,b){return new Bl(this.C,this.bf,b)},Bl.kd=!0,Bl.jd="cljs.core.async.impl.channels/t30216",Bl.Nd=function(c,b){return qc(b,"cljs.core.async.impl.channels/t30216")});return new Bl(b,Cl,new n(null,5,[Ai,22,Pj,18,$i,3,rj,17,ui,"/Users/monte/Developer/speicher/target/base+system+user+dev/cljsbuild-compiler-1/cljs/core/async/impl/channels.cljs"],
null))};function El(a,b){this.Fa=a;this.C=b}function Fl(a){return fl(a.Fa)}function Gl(a){if(a?a.ne:a)return a.ne();var b;b=Gl[l(null==a?null:a)];if(!b&&(b=Gl._,!b))throw v("MMC.abort",a);return b.call(null,a)}function Hl(a,b,c,e,d,g,f){this.ic=a;this.md=b;this.Sb=c;this.ld=e;this.X=d;this.closed=g;this.Na=f}
Hl.prototype.gd=function(){var a=this;if(!a.closed){a.closed=!0;if(s(function(){var c=a.X;return s(c)?0===a.Sb.length:c}())){var b=a.X;a.Na.h?a.Na.h(b):a.Na.call(null,b)}for(;b=a.ic.pop(),null!=b;){var c=b.Ra,e=s(function(){var c=a.X;return s(c)?0<N(a.X):c}())?a.X.fd():null;Al(function(a,c){return function(){return a.h?a.h(c):a.call(null,c)}}(c,e,b,this))}}return null};
Hl.prototype.Md=function(a,b){var c=this;if(null!=c.X&&0<N(c.X)){for(var e=b.Ra,d=Dl(c.X.fd());;){if(!s(pl(c.X))){var g=c.Sb.pop();if(null!=g){var f=g.Fa,h=g.C;Al(function(a){return function(){return a.h?a.h(!0):a.call(null,!0)}}(f.Ra,f,h,g,e,d,this));bd(function(){var a=c.X,b=h;return c.Na.e?c.Na.e(a,b):c.Na.call(null,a,b)}())&&Gl(this);continue}}break}return d}e=function(){for(;;){var a=c.Sb.pop();if(s(a)){if(fl(a.Fa))return a}else return null}}();if(s(e))return d=gl(e.Fa),Al(function(a){return function(){return a.h?
a.h(!0):a.call(null,!0)}}(d,e,this)),Dl(e.C);if(s(c.closed))return s(c.X)&&(e=c.X,c.Na.h?c.Na.h(e):c.Na.call(null,e)),s(s(!0)?b.Ra:!0)?(e=function(){var a=c.X;return s(a)?0<N(c.X):a}(),e=s(e)?c.X.fd():null,Dl(e)):null;64<c.md?(c.md=0,ml(c.ic,fl)):c.md+=1;if(!(1024>c.ic.length))throw Error([w("Assert failed: "),w([w("No more than "),w(1024),w(" pending takes are allowed on a single channel.")].join("")),w("\n"),w(Je.k(L([Zd(new Sc(null,"\x3c","\x3c",993667236,null),Zd(new Sc(null,".-length",".-length",
-280799999,null),new Sc(null,"takes","takes",298247964,null)),new Sc("impl","MAX-QUEUE-SIZE","impl/MAX-QUEUE-SIZE",1508600732,null))],0)))].join(""));ll(c.ic,b);return null};
Hl.prototype.hd=function(a,b,c){var e=this;if(null==b)throw Error([w("Assert failed: "),w("Can't put nil in on a channel"),w("\n"),w(Je.k(L([Zd(new Sc(null,"not","not",1044554643,null),Zd(new Sc(null,"nil?","nil?",1612038930,null),new Sc(null,"val","val",1769233139,null)))],0)))].join(""));if(a=e.closed)return Dl(!a);if(s(function(){var a=e.X;return s(a)?sb(pl(e.X)):a}())){for(c=bd(function(){var a=e.X;return e.Na.e?e.Na.e(a,b):e.Na.call(null,a,b)}());;){if(0<e.ic.length&&0<N(e.X)){var d=e.ic.pop(),
g=d.Ra,f=e.X.fd();Al(function(a,c){return function(){return a.h?a.h(c):a.call(null,c)}}(g,f,d,c,a,this))}break}c&&Gl(this);return Dl(!0)}d=function(){for(;;){var a=e.ic.pop();if(s(a)){if(s(!0))return a}else return null}}();if(s(d))return c=gl(d),Al(function(a){return function(){return a.h?a.h(b):a.call(null,b)}}(c,d,a,this)),Dl(!0);64<e.ld?(e.ld=0,ml(e.Sb,Fl)):e.ld+=1;if(!(1024>e.Sb.length))throw Error([w("Assert failed: "),w([w("No more than "),w(1024),w(" pending puts are allowed on a single channel."),
w(" Consider using a windowed buffer.")].join("")),w("\n"),w(Je.k(L([Zd(new Sc(null,"\x3c","\x3c",993667236,null),Zd(new Sc(null,".-length",".-length",-280799999,null),new Sc(null,"puts","puts",-1883877054,null)),new Sc("impl","MAX-QUEUE-SIZE","impl/MAX-QUEUE-SIZE",1508600732,null))],0)))].join(""));ll(e.Sb,new El(c,b));return null};
Hl.prototype.ne=function(){for(;;){var a=this.Sb.pop();if(null!=a){var b=a.Fa;Al(function(a){return function(){return a.h?a.h(!0):a.call(null,!0)}}(b.Ra,b,a.C,a,this))}break}ml(this.Sb,Ae());return el(this)};function Il(a){console.log(a);return null}function Jl(a,b,c){b=(s(b)?b:Il).call(null,c);return null==b?a:il.e(a,b)}
var Kl=function(){function a(a,c,b){return new Hl(nl(32),0,nl(32),0,a,!1,function(){return function(a){return function(){function c(e,d){try{return a.e?a.e(e,d):a.call(null,e,d)}catch(g){return Jl(e,b,g)}}function e(c){try{return a.h?a.h(c):a.call(null,c)}catch(d){return Jl(c,b,d)}}var d=null,d=function(a,b){switch(arguments.length){case 1:return e.call(this,a);case 2:return c.call(this,a,b)}throw Error("Invalid arity: "+arguments.length);};d.h=e;d.e=c;return d}()}(s(c)?c.h?c.h(il):c.call(null,il):
il)}())}function b(a,c){return e.j(a,c,null)}function c(a){return e.e(a,null)}var e=null,e=function(e,g,f){switch(arguments.length){case 1:return c.call(this,e);case 2:return b.call(this,e,g);case 3:return a.call(this,e,g,f)}throw Error("Invalid arity: "+arguments.length);};e.h=c;e.e=b;e.j=a;return e}();var Ll,Nl=function Ml(b){"undefined"===typeof Ll&&(Ll=function(c,b,d){this.Ra=c;this.Sd=b;this.Hf=d;this.H=0;this.A=393216},Ll.prototype.pe=function(){return!0},Ll.prototype.qe=function(){return this.Ra},Ll.prototype.P=function(){return this.Hf},Ll.prototype.R=function(c,b){return new Ll(this.Ra,this.Sd,b)},Ll.kd=!0,Ll.jd="cljs.core.async.impl.ioc-helpers/t30095",Ll.Nd=function(c,b){return qc(b,"cljs.core.async.impl.ioc-helpers/t30095")});return new Ll(b,Ml,new n(null,5,[Ai,19,Pj,30,$i,3,rj,27,ui,
"/Users/monte/Developer/speicher/target/base+system+user+dev/cljsbuild-compiler-1/cljs/core/async/impl/ioc_helpers.cljs"],null))};function Ol(a){try{return a[0].call(null,a)}catch(b){throw b instanceof Object&&a[6].gd(),b;}}function Pl(a,b,c){c=c.Md(0,Nl(function(c){a[2]=c;a[1]=b;return Ol(a)}));return s(c)?(a[2]=J.h?J.h(c):J.call(null,c),a[1]=b,Z):null}function Ql(a,b,c,e){c=c.hd(0,e,Nl(function(c){a[2]=c;a[1]=b;return Ol(a)}));return s(c)?(a[2]=J.h?J.h(c):J.call(null,c),a[1]=b,Z):null}
function Rl(a,b){var c=a[6];null!=b&&c.hd(0,b,Nl(function(){return function(){return null}}(c)));c.gd();return c}function Sl(a,b,c,e,d,g,f,h){this.Za=a;this.$a=b;this.cb=c;this.bb=e;this.gb=d;this.jb=g;this.Ga=f;this.B=h;this.A=2229667594;this.H=8192}k=Sl.prototype;k.I=function(a,b){return Ob.j(this,b,null)};
k.J=function(a,b,c){switch(b instanceof V?b.La:null){case "prev":return this.gb;case "continue-block":return this.bb;case "finally-block":return this.cb;case "catch-exception":return this.$a;case "catch-block":return this.Za;default:return P.j(this.Ga,b,c)}};
k.M=function(a,b,c){return Yg(b,function(){return function(a){return Yg(b,dh,""," ","",c,a)}}(this),"#cljs.core.async.impl.ioc-helpers.ExceptionFrame{",", ","}",c,re.e(new T(null,5,5,U,[new T(null,2,5,U,[Ji,this.Za],null),new T(null,2,5,U,[Dj,this.$a],null),new T(null,2,5,U,[fi,this.cb],null),new T(null,2,5,U,[Kj,this.bb],null),new T(null,2,5,U,[Gj,this.gb],null)],null),this.Ga))};k.P=function(){return this.jb};
k.za=function(){return new Sl(this.Za,this.$a,this.cb,this.bb,this.gb,this.jb,this.Ga,this.B)};k.ca=function(){return 5+N(this.Ga)};k.L=function(){var a=this.B;return null!=a?a:this.B=a=Td(this)};k.G=function(a,b){return s(s(b)?this.constructor===b.constructor&&Ef(this,b):b)?!0:!1};
k.Fc=function(a,b){var c;if(Hd(new Kg(null,new n(null,5,[fi,null,Ji,null,Dj,null,Gj,null,Kj,null],null),null),b))c=sd.e(ld(Se.e(Qf,this),this.jb),b);else{c=this.Za;var e=this.$a,d=this.cb,g=this.bb,f=this.gb,h=this.jb,m;m=sd.e(this.Ga,b);m=B(m)?m:null;c=new Sl(c,e,d,g,f,h,m,null)}return c};
k.pb=function(a,b,c){return s(be.e?be.e(Ji,b):be.call(null,Ji,b))?new Sl(c,this.$a,this.cb,this.bb,this.gb,this.jb,this.Ga,null):s(be.e?be.e(Dj,b):be.call(null,Dj,b))?new Sl(this.Za,c,this.cb,this.bb,this.gb,this.jb,this.Ga,null):s(be.e?be.e(fi,b):be.call(null,fi,b))?new Sl(this.Za,this.$a,c,this.bb,this.gb,this.jb,this.Ga,null):s(be.e?be.e(Kj,b):be.call(null,Kj,b))?new Sl(this.Za,this.$a,this.cb,c,this.gb,this.jb,this.Ga,null):s(be.e?be.e(Gj,b):be.call(null,Gj,b))?new Sl(this.Za,this.$a,this.cb,
this.bb,c,this.jb,this.Ga,null):new Sl(this.Za,this.$a,this.cb,this.bb,this.gb,this.jb,rd.j(this.Ga,b,c),null)};k.U=function(){return B(re.e(new T(null,5,5,U,[new T(null,2,5,U,[Ji,this.Za],null),new T(null,2,5,U,[Dj,this.$a],null),new T(null,2,5,U,[fi,this.cb],null),new T(null,2,5,U,[Kj,this.bb],null),new T(null,2,5,U,[Gj,this.gb],null)],null),this.Ga))};k.R=function(a,b){return new Sl(this.Za,this.$a,this.cb,this.bb,this.gb,b,this.Ga,this.B)};
k.Y=function(a,b){return Ad(b)?Rb(this,x.e(b,0),x.e(b,1)):yb.j(Gb,this,b)};
function Tl(a){for(;;){var b=a[4],c=Ji.h(b),e=Dj.h(b),d=a[5];if(s(function(){var a=d;return s(a)?sb(b):a}()))throw d;if(s(function(){var a=d;return s(a)?(a=c,s(a)?d instanceof e:a):a}())){a[1]=c;a[2]=d;a[5]=null;a[4]=rd.k(b,Ji,null,L([Dj,null],0));break}if(s(function(){var a=d;return s(a)?sb(c)&&sb(fi.h(b)):a}()))a[4]=Gj.h(b);else{if(s(function(){var a=d;return s(a)?(a=sb(c))?fi.h(b):a:a}())){a[1]=fi.h(b);a[4]=rd.j(b,fi,null);break}if(s(function(){var a=sb(d);return a?fi.h(b):a}())){a[1]=fi.h(b);
a[4]=rd.j(b,fi,null);break}if(sb(d)&&sb(fi.h(b))){a[1]=Kj.h(b);a[4]=Gj.h(b);break}throw Error("No matching clause");}}};function Ul(a,b,c){this.key=a;this.C=b;this.forward=c;this.H=0;this.A=2155872256}Ul.prototype.M=function(a,b,c){return Yg(b,dh,"["," ","]",c,this)};Ul.prototype.U=function(){return Gb(Gb(G,this.C),this.key)};
(function(){function a(a,c,b){b=Array(b+1);for(var f=0;;)if(f<b.length)b[f]=null,f+=1;else break;return new Ul(a,c,b)}function b(a){return c.j(null,null,a)}var c=null,c=function(c,d,g){switch(arguments.length){case 1:return b.call(this,c);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.j=a;return c})().h(0);var Wl=function Vl(b){"undefined"===typeof bl&&(bl=function(c,b,d){this.Ra=c;this.Sd=b;this.Gf=d;this.H=0;this.A=393216},bl.prototype.pe=function(){return!0},bl.prototype.qe=function(){return this.Ra},bl.prototype.P=function(){return this.Gf},bl.prototype.R=function(c,b){return new bl(this.Ra,this.Sd,b)},bl.kd=!0,bl.jd="cljs.core.async/t26685",bl.Nd=function(c,b){return qc(b,"cljs.core.async/t26685")});return new bl(b,Vl,new n(null,5,[Ai,20,Pj,16,$i,3,rj,13,ui,"/Users/monte/Developer/speicher/target/base+system+user+dev/cljsbuild-compiler-1/cljs/core/async.cljs"],
null))},Xl=function(){function a(a,c,b){a=Uc.e(a,0)?null:a;if(s(c)&&!s(a))throw Error([w("Assert failed: "),w("buffer must be supplied when transducer is"),w("\n"),w(Je.k(L([new Sc(null,"buf-or-n","buf-or-n",-1646815050,null)],0)))].join(""));return Kl.j("number"===typeof a?ql(a):a,c,b)}function b(a,c){return d.j(a,c,null)}function c(a){return d.j(a,null,null)}function e(){return d.h(null)}var d=null,d=function(d,f,h){switch(arguments.length){case 0:return e.call(this);case 1:return c.call(this,d);
case 2:return b.call(this,d,f);case 3:return a.call(this,d,f,h)}throw Error("Invalid arity: "+arguments.length);};d.F=e;d.h=c;d.e=b;d.j=a;return d}(),Yl=function(){function a(a,c,b){a=cl(a,Wl(c));if(s(a)){var f=J.h?J.h(a):J.call(null,a);s(b)?c.h?c.h(f):c.call(null,f):Al(function(a){return function(){return c.h?c.h(a):c.call(null,a)}}(f,a))}return null}function b(a,b){return c.j(a,b,!0)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,
d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),Zl=Wl(function(){return null}),$l=function(){function a(a,c,b,e){a=dl(a,c,Wl(b));return s(a)?(c=J.h?J.h(a):J.call(null,a),s(e)?b.h?b.h(c):b.call(null,c):Al(function(a){return function(){return b.h?b.h(a):b.call(null,a)}}(c,a,a)),c):!0}function b(a,c,b){return e.D(a,c,b,!0)}function c(a,c){var b=dl(a,c,Zl);return s(b)?J.h?J.h(b):J.call(null,b):!0}var e=null,e=function(e,g,f,h){switch(arguments.length){case 2:return c.call(this,
e,g);case 3:return b.call(this,e,g,f);case 4:return a.call(this,e,g,f,h)}throw Error("Invalid arity: "+arguments.length);};e.e=c;e.j=b;e.D=a;return e}(),am=function(){function a(a,c,b){var f=Xl.h(1);Al(function(f){return function(){var m=function(){return function(a){return function(){function c(b){for(;;){var e;a:{try{for(;;){var d=a(b);if(!be(d,Z)){e=d;break a}}}catch(f){if(f instanceof Object){b[5]=f;Tl(b);e=Z;break a}throw f;}e=void 0}if(!be(e,Z))return e}}function b(){var a=[null,null,null,null,
null,null,null,null];a[0]=e;a[1]=1;return a}var e=null,e=function(a){switch(arguments.length){case 0:return b.call(this);case 1:return c.call(this,a)}throw Error("Invalid arity: "+arguments.length);};e.F=b;e.h=c;return e}()}(function(){return function(f){var h=f[1];return 7===h?(h=f,h[2]=f[2],h[1]=3,Z):1===h?(f[2]=null,f[1]=2,Z):4===h?(h=f[7],h=f[2],f[7]=h,f[1]=s(null==h)?5:6,Z):13===h?(f[2]=null,f[1]=14,Z):6===h?(h=f[7],Ql(f,11,c,h)):3===h?(h=f[2],Rl(f,h)):12===h?(f[2]=null,f[1]=2,Z):2===h?Pl(f,
4,a):11===h?(h=f[2],f[1]=s(h)?12:13,Z):9===h?(f[2]=null,f[1]=10,Z):5===h?(f[1]=s(b)?8:9,Z):14===h||10===h?(h=f[2],f[2]=h,f[1]=7,Z):8===h?(h=el(c),f[2]=h,f[1]=10,Z):null}}(f),f)}(),p=function(){var a=m.F?m.F():m.call(null);a[6]=f;return a}();return Ol(p)}}(f));return c}function b(a,b){return c.j(a,b,!0)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}(),bm=function(){function a(a,
c,b){c=rf(c);b=Xl.h(b);var f=N(c),h=ne.h(f),m=Xl.h(1),p=Ge.h?Ge.h(null):Ge.call(null,null),q=Te.e(function(a,c,b,e,d,f){return function(g){return function(a,c,b,e,d,f){return function(a){e[g]=a;return 0===Ke.e(f,Od)?$l.e(d,e.slice(0)):null}}(a,c,b,e,d,f)}}(c,b,f,h,m,p),Sg.h(f)),r=Xl.h(1);Al(function(c,b,d,f,g,h,m,p){return function(){var q=function(){return function(a){return function(){function c(b){for(;;){var e;a:{try{for(;;){var d=a(b);if(!be(d,Z)){e=d;break a}}}catch(f){if(f instanceof Object){b[5]=
f;Tl(b);e=Z;break a}throw f;}e=void 0}if(!be(e,Z))return e}}function b(){var a=[null,null,null,null,null,null,null,null,null,null,null,null,null,null];a[0]=e;a[1]=1;return a}var e=null,e=function(a){switch(arguments.length){case 0:return b.call(this);case 1:return c.call(this,a)}throw Error("Invalid arity: "+arguments.length);};e.F=b;e.h=c;return e}()}(function(c,b,d,f,g,h,m,p){return function(c){var g=c[1];if(7===g)return c[2]=null,c[1]=8,Z;if(1===g)return c[2]=null,c[1]=2,Z;if(4===g){var q=c[7],
g=q<f;c[1]=s(g)?6:7;return Z}if(15===g)return g=c[2],c[2]=g,c[1]=3,Z;if(13===g)return g=el(d),c[2]=g,c[1]=15,Z;if(6===g)return c[2]=null,c[1]=11,Z;if(3===g)return g=c[2],Rl(c,g);if(12===g){var g=c[8],g=c[2],r=ze(rb,g);c[8]=g;c[1]=s(r)?13:14;return Z}return 2===g?(g=Ie.e?Ie.e(m,f):Ie.call(null,m,f),q=0,c[9]=g,c[7]=q,c[2]=null,c[1]=4,Z):11===g?(q=c[7],c[4]=new Sl(10,Object,null,9,c[4],null,null,null),g=function(){var a=q;return b.h?b.h(a):b.call(null,a)}(),r=function(){var a=q;return p.h?p.h(a):p.call(null,
a)}(),g=Yl.e(g,r),c[2]=g,Tl(c),Z):9===g?(q=c[7],c[10]=c[2],c[7]=q+1,c[2]=null,c[1]=4,Z):5===g?(c[11]=c[2],Pl(c,12,h)):14===g?(g=c[8],g=Q.e(a,g),Ql(c,16,d,g)):16===g?(c[12]=c[2],c[2]=null,c[1]=2,Z):10===g?(r=c[2],g=Ke.e(m,Od),c[13]=r,c[2]=g,Tl(c),Z):8===g?(g=c[2],c[2]=g,c[1]=5,Z):null}}(c,b,d,f,g,h,m,p),c,b,d,f,g,h,m,p)}(),r=function(){var a=q.F?q.F():q.call(null);a[6]=c;return a}();return Ol(r)}}(r,c,b,f,h,m,p,q));return b}function b(a,b){return c.j(a,b,null)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,
c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}();var $=function cm(b){if(zd(b)){var c={};b=B(b);for(var e=null,d=0,g=0;;)if(g<d){var f=e.s(null,g),h=O.j(f,0,null),f=O.j(f,1,null);c[ce(h)]=cm(f);g+=1}else if(b=B(b))R(b)?(d=y(b),b=A(b),e=d,d=N(d)):(d=D(b),e=O.j(d,0,null),d=O.j(d,1,null),c[ce(e)]=cm(d),b=I(b),e=null,d=0),g=0;else break;return c}return b};
function dm(a){return function(a){return function(){function c(a,c){var b=null;if(1<arguments.length){for(var b=0,h=Array(arguments.length-1);b<h.length;)h[b]=arguments[b+1],++b;b=new C(h,0)}return e.call(this,a,b)}function e(c,e){var f={statics:e,value:c};return a.h?a.h(f):a.call(null,f)}c.q=1;c.l=function(a){var c=D(a);a=F(a);return e(c,a)};c.k=e;return c}()}(React.createClass({render:function(){return Q.j(a,this.props.value,this.props.statics)},shouldComponentUpdate:function(a){return xe.e(this.props.value,
a.value)}}))}
React.createClass({componentWillUnmount:function(){var a=this.props.onWillUnmount;return s(a)?a.F?a.F():a.call(null):null},componentWillUpdate:function(){var a=this.props.onWillUpdate;return s(a)?a.F?a.F():a.call(null):null},componentWillMount:function(){var a=this.props.onWillMount;return s(a)?a.F?a.F():a.call(null):null},componentDidMount:function(){var a=this.props.onMount;if(s(a)){var b=this.getDOMNode();return a.h?a.h(b):a.call(null,b)}return null},componentDidUpdate:function(){var a=this.props.onUpdate;
if(s(a)){var b=this.getDOMNode();return a.h?a.h(b):a.call(null,b)}return null},render:function(){return this.props.wrappee}});var em=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.br.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),fm=function(){function a(a){var e=
null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.button.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),gm=function(){function a(a){var e=null;if(0<
arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.div.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),hm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=
0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.fieldset.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),im=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-
0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.form.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),jm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=
arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.input.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),km=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+
0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.legend.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),lm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=
new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.p.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),mm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.table.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),nm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=
[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.tbody.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),om=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));
a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.td.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),pm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=
null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.th.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),qm=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<
g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.tr.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}();
qd([sh,th,uh,wh,xh,yh,zh,Ah,Bh,Ch,Dh,Fh,Gh,Ih,Jh,ob,Lh,Mh,Oh,Ph,Th,Uh,Vh,Wh,Xh,Yh,ci,gi,hi,ji,ki,mi,oi,pi,qi,ri,ti,vi,yi,Ci,Di,Ei,Fi,Ii,Mi,Ni,Pi,Qi,Ri,Si,Ti,Ui,Vi,Xi,Yi,Zi,cj,dj,ej,fj,gj,hj,ij,jj,kj,lj,oj,pj,rj,tj,vj,wj,xj,yj,Aj,Cj,Ej,Hj,Jj,Lj,Oj,Vj,Xj,Yj,Zj,ck,dk,gk,jk,nk,ok,rk,sk,tk,uk,vk,wk,xk,yk,Ak,Ck,Ek,Fk,Gk,Ik,Jk,Nk,Ok,Pk,Rk,Sk,Tk,Uk,Vk,Xk,Yk,Zk,$k,al],[function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,
0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.thead.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.path.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=
[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Eg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),hm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));
a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Wg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=
null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Ig.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<
g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.qg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,
f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Hg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=
1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.video.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=
B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.address.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?
(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.caption.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=
A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.rg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),
a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Yg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),
a=I(d),d=null,g=0),f=0;else break;return React.DOM.hr.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=
0),f=0;else break;return React.DOM.dd.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;
return React.DOM.meta.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),nm,mm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.ul.apply(null,
b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.pre.apply(null,b)}a.q=0;a.l=function(a){a=
B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.sup.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};
a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.script.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),
function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.big.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),fm,function(){function a(a){var e=
null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Gg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=
0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.sub.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-
0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Og.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=
arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.wbr.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],
++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.strong.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),om,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,
0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.li.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.dt.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=
[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.section.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),pm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));
a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.time.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=
null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.optgroup.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,
f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.iframe.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),qm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<
g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.circle.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,
f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.article.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),km,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);
f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.em.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=
B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Lg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=
y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.abbr.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=
a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.source.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=
f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Ug.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),
a=I(d),d=null,g=0),f=0;else break;return React.DOM.Jg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=
0),f=0;else break;return React.DOM.footer.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;
return React.DOM.header.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Dg.apply(null,
b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.tfoot.apply(null,b)}a.q=0;a.l=
function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.K.apply(null,b)}a.q=0;a.l=function(a){a=B(a);
return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.h5.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=
b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.canvas.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),
function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.param.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=
null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.title.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=
0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.small.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-
0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.style.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=
arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.textarea.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),gm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+
0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.option.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,
0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.summary.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.ah.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=
[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.h4.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));
a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.head.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=
null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.g.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<
g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.ol.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,
f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Fg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=
1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.line.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=
a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.dh.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),
f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.h6.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),
a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.link.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),
b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.col.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),
d=null,g=0),f=0;else break;return React.DOM.label.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),
f=0;else break;return React.DOM.Zg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;
return React.DOM.colgroup.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Rg.apply(null,
b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.svg.apply(null,b)}a.q=0;a.l=function(a){a=
B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.code.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};
a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.sg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),
function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.b.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=
null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Tg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=
0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.h2.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-
0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.pg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),em,im,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=
arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.audio.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),jm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+
0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Qg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,
0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Bc.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.h1.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=
[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Vg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));
a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Ng.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=
null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.embed.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<
g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.h3.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,
f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.body.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);
f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Mg.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=
B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.polyline.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,
R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.cite.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),
f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.rect.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),lm,function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,
g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.nav.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=
D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.$g.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),
a=I(d),d=null,g=0),f=0;else break;return React.DOM.map.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,
g=0),f=0;else break;return React.DOM.object.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;
return React.DOM.i.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.Pg.apply(null,
b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.tg.apply(null,b)}a.q=0;a.l=function(a){a=
B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.img.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};
a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.a.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),
function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.dl.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=
null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.select.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=
0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.polygon.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-
0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.html.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=
arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.text.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],
++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.span.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,
0)}return b.call(this,e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.track.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,
e)}function b(a){var b=[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.data.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}(),function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){var b=
[];b.push($(D(a)));a=B(F(a));for(var d=null,g=0,f=0;;)if(f<g){var h=d.s(null,f);b.push(h);f+=1}else if(a=B(a))d=a,R(d)?(a=y(d),f=A(d),d=a,g=N(a),a=f):(a=D(d),b.push(a),a=I(d),d=null,g=0),f=0;else break;return React.DOM.u.apply(null,b)}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}()]);var rm=function(){function a(a,b){for(var c=new Ia,f=B(b);;)if(f)c.append(""+w(D(f))),f=I(f),null!=f&&c.append(a);else return c.toString()}function b(a){var b=new Ia;for(a=B(a);;)if(a)b=b.append(""+w(D(a))),a=I(a);else return b.toString()}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();function sm(a){return a.toUpperCase()}
function tm(a){return a.toLowerCase()}function um(a){return 2>N(a)?sm(a):[w(sm(Sd.j(a,0,1))),w(tm(Sd.e(a,1)))].join("")}function vm(a,b){if(0>=b||b>=2+N(a))return od.e(rf(K("",Ne.e(w,B(a)))),"");if(s(Uc.e?Uc.e(1,b):Uc.call(null,1,b)))return new T(null,1,5,U,[a],null);if(s(Uc.e?Uc.e(2,b):Uc.call(null,2,b)))return new T(null,2,5,U,["",a],null);var c=b-2;return od.e(rf(K("",tf.j(rf(Ne.e(w,B(a))),0,c))),Sd.e(a,c))}
var wm=function(){function a(a,b,c){if(Uc.e(""+w(b),"/(?:)/"))b=vm(a,c);else if(1>c)b=rf((""+w(a)).split(b));else a:{for(var f=c,h=nd;;){if(Uc.e(f,1)){b=od.e(h,a);break a}var m=Wg(b,a);if(s(m)){var p=m,m=a.indexOf(p),p=a.substring(m+N(p)),f=f-1,h=od.e(h,a.substring(0,m));a=p}else{b=od.e(h,a);break a}}b=void 0}if(Uc.e(0,c))a:{for(c=b;;)if(Uc.e("",null==c?null:Yb(c)))c=null==c?null:$b(c);else break a;c=void 0}else c=b;return c}function b(a,b){return c.j(a,b,0)}var c=null,c=function(c,d,g){switch(arguments.length){case 2:return b.call(this,
c,d);case 3:return a.call(this,c,d,g)}throw Error("Invalid arity: "+arguments.length);};c.e=b;c.j=a;return c}();function xm(a){if(a?a.re:a)return a.re();var b;b=xm[l(null==a?null:a)];if(!b&&(b=xm._,!b))throw v("PushbackReader.read-char",a);return b.call(null,a)}function ym(a,b){if(a?a.se:a)return a.se(0,b);var c;c=ym[l(null==a?null:a)];if(!c&&(c=ym._,!c))throw v("PushbackReader.unread",a);return c.call(null,a,b)}function zm(a,b,c){this.K=a;this.buffer=b;this.S=c}zm.prototype.re=function(){return 0===this.buffer.length?(this.S+=1,this.K[this.S]):this.buffer.pop()};zm.prototype.se=function(a,b){return this.buffer.push(b)};
function Am(a){var b=!/[^\t\n\r ]/.test(a);return s(b)?b:","===a}function Bm(a,b){var c;!(c=!/[^0-9]/.test(b))&&(c="+"===b||"-"===b)&&(c=xm(a),ym(a,c),c=!/[^0-9]/.test(c));return c}var Cm=function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,0,d)}function b(a,b){throw Error(Q.e(w,b));}a.q=1;a.l=function(a){D(a);a=F(a);return b(0,a)};a.k=b;return a}();
function Dm(a,b){for(var c=new Ia(b),e=xm(a);;){var d;if(!(d=null==e||Am(e))){d=e;var g="#"!==d;d=g?(g="'"!==d)?(g=":"!==d)?Em.h?Em.h(d):Em.call(null,d):g:g:g}if(d)return ym(a,e),c.toString();c.append(e);e=xm(a)}}function Fm(a){for(;;){var b=xm(a);if("\n"===b||"\r"===b||null==b)return a}}var Gm=Xg("^([-+]?)(?:(0)|([1-9][0-9]*)|0[xX]([0-9A-Fa-f]+)|0([0-7]+)|([1-9][0-9]?)[rR]([0-9A-Za-z]+))(N)?$"),Hm=Xg("^([-+]?[0-9]+)/([0-9]+)$"),Im=Xg("^([-+]?[0-9]+(\\.[0-9]*)?([eE][-+]?[0-9]+)?)(M)?$"),Jm=Xg("^[:]?([^0-9/].*/)?([^0-9/][^/]*)$");
function Km(a,b){var c=a.exec(b);return null!=c&&c[0]===b?1===c.length?c[0]:c:null}function Lm(a){if(s(Km(Gm,a))){a=Km(Gm,a);var b=a[2];if(null!=(Uc.e(b,"")?null:b))a=0;else{var b=s(a[3])?[a[3],10]:s(a[4])?[a[4],16]:s(a[5])?[a[5],8]:s(a[6])?[a[7],parseInt(a[6],10)]:[null,null],c=b[0];null==c?a=null:(b=parseInt(c,b[1]),a="-"===a[1]?-b:b)}}else s(Km(Hm,a))?(a=Km(Hm,a),a=parseInt(a[1],10)/parseInt(a[2],10)):a=s(Km(Im,a))?parseFloat(a):null;return a}var Mm=Xg("^[0-9A-Fa-f]{2}$"),Nm=Xg("^[0-9A-Fa-f]{4}$");
function Om(a,b,c,e){return s(Vg(a,e))?e:Cm.k(b,L(["Unexpected unicode escape \\",c,e],0))}function Pm(a){return String.fromCharCode(parseInt(a,16))}
function Qm(a){var b=xm(a),c="t"===b?"\t":"r"===b?"\r":"n"===b?"\n":"\\"===b?"\\":'"'===b?'"':"b"===b?"\b":"f"===b?"\f":null;s(c)?a=c:"x"===b?(c=(new Ia(xm(a),xm(a))).toString(),a=Pm(Om(Mm,a,b,c))):"u"===b?(c=(new Ia(xm(a),xm(a),xm(a),xm(a))).toString(),a=Pm(Om(Nm,a,b,c))):a=/[^0-9]/.test(b)?Cm.k(a,L(["Unexpected unicode escape \\",b],0)):String.fromCharCode(b);return a}function Rm(a){for(var b=xm(a);;){var c;c=b;c=Am.h?Am.h(c):Am.call(null,c);if(s(c))b=xm(a);else return b}}
function Sm(a,b){for(var c=uc(nd);;){var e=Rm(b);s(e)||Cm.k(b,L(["EOF while reading"],0));if(a===e)return wc(c);var d=function(){var a=e;return Em.h?Em.h(a):Em.call(null,a)}();if(s(d))var g=d,d=function(){var a=e;return g.e?g.e(b,a):g.call(null,b,a)}();else ym(b,e),d=Tm.D?Tm.D(b,!0,null,!0):Tm.call(null,b,!0,null);c=d===b?c:ue.e(c,d)}}function Um(a,b){return Cm.k(a,L(["Reader for ",b," not implemented yet"],0))}
function Vm(a,b){var c=xm(a),e=Wm.h?Wm.h(c):Wm.call(null,c);if(s(e))return e.e?e.e(a,b):e.call(null,a,b);e=Xm.e?Xm.e(a,c):Xm.call(null,a,c);return s(e)?e:Cm.k(a,L(["No dispatch macro for ",c],0))}function Ym(a,b){return Cm.k(a,L(["Unmatched delimiter ",b],0))}function Zm(a){return Q.e(Zd,Sm(")",a))}function $m(a){return Sm("]",a)}
function an(a){var b=Sm("}",a);var c=N(b);if("number"!==typeof c||!sb(isNaN(c))||Infinity===c||parseFloat(c)!==parseInt(c,10))throw Error([w("Argument must be an integer: "),w(c)].join(""));0!==(c&1)&&Cm.k(a,L(["Map literal must contain an even number of forms"],0));return Q.e(Ee,b)}
function bn(a,b){for(var c=new Ia(b),e=xm(a);;){if(s(function(){var a=null==e;if(a||(a=Am(e)))return a;a=e;return Em.h?Em.h(a):Em.call(null,a)}())){ym(a,e);var d=c.toString(),c=Lm(d);return s(c)?c:Cm.k(a,L(["Invalid number format [",d,"]"],0))}c.append(e);e=d=xm(a)}}function cn(a){for(var b=new Ia,c=xm(a);;){if(null==c)return Cm.k(a,L(["EOF while reading"],0));if("\\"===c)b.append(Qm(a));else{if('"'===c)return b.toString();b.append(c)}c=xm(a)}}
function dn(a){for(var b=new Ia,c=xm(a);;){if(null==c)return Cm.k(a,L(["EOF while reading"],0));if("\\"===c){b.append(c);var e=xm(a);if(null==e)return Cm.k(a,L(["EOF while reading"],0));var d=function(){var a=b;a.append(e);return a}(),g=xm(a)}else{if('"'===c)return b.toString();d=function(){var a=b;a.append(c);return a}();g=xm(a)}b=d;c=g}}
function en(a,b){var c=Dm(a,b),e=-1!=c.indexOf("/");s(s(e)?1!==c.length:e)?c=Tc.e(Sd.j(c,0,c.indexOf("/")),Sd.j(c,c.indexOf("/")+1,c.length)):(e=Tc.h(c),c="nil"===c?null:"true"===c?!0:"false"===c?!1:"/"===c?new Sc(null,"/","/",-1371932971,null):e);return c}
function fn(a){var b=Dm(a,xm(a)),c=Km(Jm,b),b=c[0],e=c[1],c=c[2];return void 0!==e&&":/"===e.substring(e.length-2,e.length)||":"===c[c.length-1]||-1!==b.indexOf("::",1)?Cm.k(a,L(["Invalid token: ",b],0)):null!=e&&0<e.length?de.e(e.substring(0,e.indexOf("/")),c):de.h(b)}function gn(a){return function(b){return Gb(Gb(G,Tm.D?Tm.D(b,!0,null,!0):Tm.call(null,b,!0,null)),a)}}function hn(){return function(a){return Cm.k(a,L(["Unreadable form"],0))}}
function jn(a){var b;b=Tm.D?Tm.D(a,!0,null,!0):Tm.call(null,a,!0,null);b=b instanceof Sc?new n(null,1,[ek,b],null):"string"===typeof b?new n(null,1,[ek,b],null):b instanceof V?new Tf([b,!0],!0,!1):b;zd(b)||Cm.k(a,L(["Metadata must be Symbol,Keyword,String or Map"],0));var c=Tm.D?Tm.D(a,!0,null,!0):Tm.call(null,a,!0,null);return(c?c.A&262144||c.yf||(c.A?0:t(fc,c)):t(fc,c))?ld(c,Jg.k(L([wd(c),b],0))):Cm.k(a,L(["Metadata can only be applied to IWithMetas"],0))}
function kn(a){a:if(a=Sm("}",a),a=B(a),null==a)a=Mg;else if(a instanceof C&&0===a.i){a=a.o;b:{for(var b=0,c=uc(Mg);;)if(b<a.length)var e=b+1,c=c.Xb(null,a[b]),b=e;else{a=c;break b}a=void 0}a=a.Yb(null)}else{for(e=uc(Mg);;)if(null!=a)b=a.sa(null),e=e.Xb(null,a.la(null)),a=b;else{a=e.Yb(null);break a}a=void 0}return a}function ln(a){return Xg(dn(a))}function mn(a){Tm.D?Tm.D(a,!0,null,!0):Tm.call(null,a,!0,null);return a}
function Em(a){return'"'===a?cn:":"===a?fn:";"===a?Fm:"'"===a?gn(new Sc(null,"quote","quote",1377916282,null)):"@"===a?gn(new Sc(null,"deref","deref",1494944732,null)):"^"===a?jn:"`"===a?Um:"~"===a?Um:"("===a?Zm:")"===a?Ym:"["===a?$m:"]"===a?Ym:"{"===a?an:"}"===a?Ym:"\\"===a?xm:"#"===a?Vm:null}function Wm(a){return"{"===a?kn:"\x3c"===a?hn():'"'===a?ln:"!"===a?Fm:"_"===a?mn:null}
function Tm(a,b,c){for(;;){var e=xm(a);if(null==e)return s(b)?Cm.k(a,L(["EOF while reading"],0)):c;if(!Am(e))if(";"===e){var d=function(){var b=a,c=e;return Fm.e?Fm.e(b,c):Fm.call(null,b)}();a=d}else{var g=Em(e),d=s(g)?function(){var b=a,c=e;return g.e?g.e(b,c):g.call(null,b,c)}():Bm(a,e)?bn(a,e):en(a,e);if(d!==a)return d}}}function nn(a){return Tm(new zm(a,[],-1),!1,null)}
var on=function(a,b){return function(c,e){return P.e(s(e)?b:a,c)}}(new T(null,13,5,U,[null,31,28,31,30,31,30,31,31,30,31,30,31],null),new T(null,13,5,U,[null,31,29,31,30,31,30,31,31,30,31,30,31],null)),pn=/(\d\d\d\d)(?:-(\d\d)(?:-(\d\d)(?:[T](\d\d)(?::(\d\d)(?::(\d\d)(?:[.](\d+))?)?)?)?)?)?(?:[Z]|([-+])(\d\d):(\d\d))?/;function qn(a){a=parseInt(a,10);return sb(isNaN(a))?a:null}
function rn(a,b,c,e){a<=b&&b<=c||Cm.k(null,L([[w(e),w(" Failed:  "),w(a),w("\x3c\x3d"),w(b),w("\x3c\x3d"),w(c)].join("")],0));return b}
function sn(a){var b=Vg(pn,a);O.j(b,0,null);var c=O.j(b,1,null),e=O.j(b,2,null),d=O.j(b,3,null),g=O.j(b,4,null),f=O.j(b,5,null),h=O.j(b,6,null),m=O.j(b,7,null),p=O.j(b,8,null),q=O.j(b,9,null),r=O.j(b,10,null);if(sb(b))return Cm.k(null,L([[w("Unrecognized date/time syntax: "),w(a)].join("")],0));var u=qn(c),z=function(){var a=qn(e);return s(a)?a:1}();a=function(){var a=qn(d);return s(a)?a:1}();var b=function(){var a=qn(g);return s(a)?a:0}(),c=function(){var a=qn(f);return s(a)?a:0}(),E=function(){var a=
qn(h);return s(a)?a:0}(),H=function(){var a;a:if(Uc.e(3,N(m)))a=m;else if(3<N(m))a=Sd.j(m,0,3);else{for(a=new Ia(m);;)if(3>a.Lb.length)a=a.append("0");else{a=a.toString();break a}a=void 0}a=qn(a);return s(a)?a:0}(),p=(Uc.e(p,"-")?-1:1)*(60*function(){var a=qn(q);return s(a)?a:0}()+function(){var a=qn(r);return s(a)?a:0}());return new T(null,8,5,U,[u,rn(1,z,12,"timestamp month field must be in range 1..12"),rn(1,a,function(){var a;if(a=0===(u%4+4)%4)a=0!==(u%100+100)%100||0===(u%400+400)%400;return on.e?
on.e(z,a):on.call(null,z,a)}(),"timestamp day field must be in range 1..last day in month"),rn(0,b,23,"timestamp hour field must be in range 0..23"),rn(0,c,59,"timestamp minute field must be in range 0..59"),rn(0,E,Uc.e(c,59)?60:59,"timestamp second field must be in range 0..60"),rn(0,H,999,"timestamp millisecond field must be in range 0..999"),p],null)}
var tn,un=new n(null,4,["inst",function(a){var b;if("string"===typeof a)if(b=sn(a),s(b)){a=O.j(b,0,null);var c=O.j(b,1,null),e=O.j(b,2,null),d=O.j(b,3,null),g=O.j(b,4,null),f=O.j(b,5,null),h=O.j(b,6,null);b=O.j(b,7,null);b=new Date(Date.UTC(a,c-1,e,d,g,f,h)-6E4*b)}else b=Cm.k(null,L([[w("Unrecognized date/time syntax: "),w(a)].join("")],0));else b=Cm.k(null,L(["Instance literal expects a string for its timestamp."],0));return b},"uuid",function(a){return"string"===typeof a?new ph(a):Cm.k(null,L(["UUID literal expects a string as its representation."],
0))},"queue",function(a){return Ad(a)?Se.e(Bf,a):Cm.k(null,L(["Queue literal expects a vector for its elements."],0))},"js",function(a){if(Ad(a)){var b=[];a=B(a);for(var c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d);b.push(g);d+=1}else if(a=B(a))c=a,R(c)?(a=y(c),d=A(c),c=a,e=N(a),a=d):(a=D(c),b.push(a),a=I(c),c=null,e=0),d=0;else break;return b}if(zd(a)){b={};a=B(a);c=null;for(d=e=0;;)if(d<e){var f=c.s(null,d),g=O.j(f,0,null),f=O.j(f,1,null);b[ce(g)]=f;d+=1}else if(a=B(a))R(a)?(e=y(a),a=A(a),c=e,e=N(e)):
(e=D(a),c=O.j(e,0,null),e=O.j(e,1,null),b[ce(c)]=e,a=I(a),c=null,e=0),d=0;else break;return b}return Cm.k(null,L([[w("JS literal expects a vector or map containing "),w("only string or unqualified keyword keys")].join("")],0))}],null);tn=Ge.h?Ge.h(un):Ge.call(null,un);var vn=Ge.h?Ge.h(null):Ge.call(null,null);
function Xm(a,b){var c=en(a,b),e=P.e(J.h?J.h(tn):J.call(null,tn),""+w(c)),d=J.h?J.h(vn):J.call(null,vn);return s(e)?(c=Tm(a,!0,null),e.h?e.h(c):e.call(null,c)):s(d)?(e=Tm(a,!0,null),d.e?d.e(c,e):d.call(null,c,e)):Cm.k(a,L(["Could not find tag parser for ",""+w(c)," in ",Je.k(L([Nf(J.h?J.h(tn):J.call(null,tn))],0))],0))};var xn=function wn(b,c){var e;e=Be.e(wn,b);Fd(c)?(e=Ug.h(Ne.e(e,c)),e=b.h?b.h(e):b.call(null,e)):xd(c)?(e=Se.e(null==c?null:Eb(c),Ne.e(e,c)),e=b.h?b.h(e):b.call(null,e)):e=b.h?b.h(c):b.call(null,c);return e};function yn(a){return xn(function(a){return function(c){return zd(c)?Se.e(Qf,Ne.e(a,c)):c}}(function(a){var c=O.j(a,0,null);a=O.j(a,1,null);return"string"===typeof c?new T(null,2,5,U,[de.h(c),a],null):new T(null,2,5,U,[c,a],null)}),a)};function zn(a){return oh.h(jh(nd).slice.call(a))}function An(a){return yn(yb.j(function(a,c){return rd.j(a,c.name,c.value)},Qf,Re.e(function(a){return a.checked},zn(a.target.elements))))}
dm(function(a,b){return gm.k(L([Qf,lm.k(L([new n(null,1,[Wi,"german"],null),"German Word"],0)),fm.k(L([new n(null,2,[zj,"clear-completed",Mk,function(){var a=Xl.h(1);Al(function(a){return function(){var c=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(g){if(g instanceof Object){c[5]=g;Tl(c);d=Z;break a}throw g;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null];a[0]=d;a[1]=1;
return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,a)}throw Error("Invalid arity: "+arguments.length);};d.F=c;d.h=b;return d}()}(function(){return function(a){var c=a[1];return 2===c?Rl(a,a[2]):1===c?(c=new T(null,2,5,U,[Sj,null],null),Ql(a,2,b,c)):null}}(a),a)}(),g=function(){var b=c.F?c.F():c.call(null);b[6]=a;return b}();return Ol(g)}}(a));return a}],null),"Recognized it"],0))],0))});
var Bn=dm(function(a){return qm.k(L([Qf,om.k(L([Qf,D(a)],0)),om.k(L([Qf,D(I(a))],0))],0))}),Cn=dm(function(a,b){return Q.j(mm,Qf,Ne.e(function(a){return Bn.e?Bn.e(a,b):Bn.call(null,a,b)},sj.h(a)))});
Zd("lernen | lernend | gelernt | ich lerne | du lernst | er/sie lernt | ich/er/sie lernte | er/sie hat/hatte gelernt | deutsch lernen :: to learn {learned, learnt; learned, learnt} | learning | learned; learnt | I learn | you learn | he/she learns | I/he/she learned; I/he/she learnt | he/she has/had learned; he/she has/had learnt | to learn German","lernen; sich aneignen; aufschnappen :: to pick up");
function Dn(a){var b=document.getElementById("term").value,c=Xl.h(1);Al(function(b,c){return function(){var g=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(f){if(f instanceof Object){c[5]=f;Tl(c);d=Z;break a}throw f;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,
a)}throw Error("Invalid arity: "+arguments.length);};d.F=c;d.h=b;return d}()}(function(b,c){return function(b){var d=b[1];return 2===d?Rl(b,b[2]):1===d?(d=new T(null,2,5,U,[ei,c],null),Ql(b,2,a,d)):null}}(b,c),b,c)}(),f=function(){var a=g.F?g.F():g.call(null);a[6]=b;return a}();return Ol(f)}}(c,b));return!1}
function En(a,b){var c=An(b),e=Xl.h(1);Al(function(b,c){return function(){var e=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(f){if(f instanceof Object){c[5]=f;Tl(c);d=Z;break a}throw f;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,a)}throw Error("Invalid arity: "+
arguments.length);};d.F=c;d.h=b;return d}()}(function(b,c){return function(b){var d=b[1];return 2===d?Rl(b,b[2]):1===d?(d=new T(null,2,5,U,[$h,c],null),Ql(b,2,a,d)):null}}(b,c),b,c)}(),h=function(){var a=e.F?e.F():e.call(null);a[6]=b;return a}();return Ol(h)}}(e,c));return!1}function Fn(a){a=wm.e(a,/ :: /);return Ne.j(function(){return function(a,c){return new T(null,2,5,U,[a,c],null)}}(a),wm.e(D(a),/ \|/),wm.e(D(I(a)),/ \|/))}
var Gn=dm(function(a,b,c,e){return qm.k(L([Qf,om.k(L([new n(null,1,[Wi,"check-column"],null),s(b)?jm.k(L([new n(null,3,[Hi,"radio",ii,"entry",ni,""+w(a)],null)],0)):null],0)),om.k(L([Qf,c],0)),om.k(L([Qf,e],0))],0))}),Hn=dm(function(a){var b=Fd(a)?Q.e(Ee,a):a,c=P.e(b,bi),e=P.e(b,rh),d=Be.e(Dn,e),g=Be.e(En,e);return gm.k(L([Qf,im.k(L([Qf,jm.k(L([new n(null,3,[ii,"search",zj,"term",Rh,"enter new word"],null)],0)),fm.k(L([new n(null,1,[Mk,d],null),"Submit"],0))],0)),em(),s(c)?im.k(L([new n(null,2,[Wj,
"#",si,g],null),hm.k(L([Qf,km.k(L([Qf,"Pick the definition which fits best"],0))],0)),mm.k(L([Qf,qm.k(L([Qf,pm.k(L([Qf,"Pick One"],0)),pm.k(L([Qf,"German"],0)),pm.k(L([Qf,"English"],0))],0)),Q.j(gm,Qf,Ce(function(a,b,c,d,e,g){return function(u,z){return Q.j(nm,Qf,Ce(function(){return function(a,b){var c=O.j(b,0,null),d=O.j(b,1,null),e=Uc.e(0,a);return Gn.D?Gn.D(u,e,c,d):Gn.call(null,u,e,c,d)}}(a,b,c,d,e,g),z))}}(d,g,a,b,c,e),Ne.e(Fn,c)))],0)),fm.k(L([new n(null,1,[Hi,"submit"],null),"Submit"],0))],
0)):null],0))}),In=dm(function(a,b){return gm.k(L([Qf,function(){var c=Bi.h(a);return s(Uc.e?Uc.e(Lk,c):Uc.call(null,Lk,c))?Hn.h?Hn.h(a):Hn.call(null,a):Cn.e?Cn.e(a,b):Cn.call(null,a,b)}()],0))}),Jn=function(a){return function(b){return Le(a)?window.requestAnimationFrame(function(a){return function(){var e;e=rh.h(b);e=In.e?In.e(b,e):In.call(null,b,e);React.renderComponent(e,document.getElementById("speicher"));return Ie.e?Ie.e(a,!1):Ie.call(null,a,!1)}}(a)):null}}(Ge.h?Ge.h(!1):Ge.call(null,!1));var Kn;a:{var Ln=ba.navigator;if(Ln){var Mn=Ln.userAgent;if(Mn){Kn=Mn;break a}}Kn=""};var Nn=-1!=Kn.indexOf("Opera")||-1!=Kn.indexOf("OPR"),On=-1!=Kn.indexOf("Trident")||-1!=Kn.indexOf("MSIE"),Pn=-1!=Kn.indexOf("Gecko")&&-1==Kn.toLowerCase().indexOf("webkit")&&!(-1!=Kn.indexOf("Trident")||-1!=Kn.indexOf("MSIE")),Qn=-1!=Kn.toLowerCase().indexOf("webkit");function Sn(){var a=ba.document;return a?a.documentMode:void 0}
var Tn=function(){var a="",b;if(Nn&&ba.opera)return a=ba.opera.version,ga(a)?a():a;Pn?b=/rv\:([^\);]+)(\)|;)/:On?b=/\b(?:MSIE|rv)[: ]([^\);]+)(\)|;)/:Qn&&(b=/WebKit\/(\S+)/);b&&(a=(a=b.exec(Kn))?a[1]:"");return On&&(b=Sn(),b>parseFloat(a))?String(b):a}(),Un={};
function Vn(a){var b;if(!(b=Un[a])){b=0;for(var c=String(Tn).replace(/^[\s\xa0]+|[\s\xa0]+$/g,"").split("."),e=String(a).replace(/^[\s\xa0]+|[\s\xa0]+$/g,"").split("."),d=Math.max(c.length,e.length),g=0;0==b&&g<d;g++){var f=c[g]||"",h=e[g]||"",m=RegExp("(\\d*)(\\D*)","g"),p=RegExp("(\\d*)(\\D*)","g");do{var q=m.exec(f)||["","",""],r=p.exec(h)||["","",""];if(0==q[0].length&&0==r[0].length)break;b=Ba(0==q[1].length?0:parseInt(q[1],10),0==r[1].length?0:parseInt(r[1],10))||Ba(0==q[2].length,0==r[2].length)||
Ba(q[2],r[2])}while(0==b)}b=Un[a]=0<=b}return b}var Wn=ba.document,Xn=Wn&&On?Sn()||("CSS1Compat"==Wn.compatMode?parseInt(Tn,10):5):void 0;var Yn=!Pn&&!On||On&&On&&9<=Xn||Pn&&Vn("1.9.1");On&&Vn("9");function Zn(a,b,c){function e(c){c&&b.appendChild(fa(c)?a.createTextNode(c):c)}for(var d=1;d<c.length;d++){var g=c[d];!ea(g)||ha(g)&&0<g.nodeType?e(g):Wa($n(g)?db(g):g,e)}}function $n(a){if(a&&"number"==typeof a.length){if(ha(a))return"function"==typeof a.item||"string"==typeof a.item;if(ga(a))return"function"==typeof a.item}return!1}function ao(a){this.Ae=a||ba.document||document}k=ao.prototype;k.createElement=function(a){return this.Ae.createElement(a)};k.createTextNode=function(a){return this.Ae.createTextNode(String(a))};
k.appendChild=function(a,b){a.appendChild(b)};k.append=function(a,b){Zn(9==a.nodeType?a:a.ownerDocument||a.document,a,arguments)};k.Ee=function(a){return Yn&&void 0!=a.children?a.children:Xa(a.childNodes,function(a){return 1==a.nodeType})};function bo(a){if("function"==typeof a.Eb)return a.Eb();if(fa(a))return a.split("");if(ea(a)){for(var b=[],c=a.length,e=0;e<c;e++)b.push(a[e]);return b}return Da(a)}
function co(a,b){if("function"==typeof a.forEach)a.forEach(b,void 0);else if(ea(a)||fa(a))Wa(a,b,void 0);else{var c;if("function"==typeof a.Sa)c=a.Sa();else if("function"!=typeof a.Eb)if(ea(a)||fa(a)){c=[];for(var e=a.length,d=0;d<e;d++)c.push(d)}else c=Ea(a);else c=void 0;for(var e=bo(a),d=e.length,g=0;g<d;g++)b.call(void 0,e[g],c&&c[g],a)}};function eo(a,b){this.kb={};this.ua=[];this.ja=0;var c=arguments.length;if(1<c){if(c%2)throw Error("Uneven number of arguments");for(var e=0;e<c;e+=2)this.set(arguments[e],arguments[e+1])}else if(a){a instanceof eo?(c=a.Sa(),e=a.Eb()):(c=Ea(a),e=Da(a));for(var d=0;d<c.length;d++)this.set(c[d],e[d])}}k=eo.prototype;k.Fe=function(){return this.ja};k.Eb=function(){fo(this);for(var a=[],b=0;b<this.ua.length;b++)a.push(this.kb[this.ua[b]]);return a};k.Sa=function(){fo(this);return this.ua.concat()};
k.Kc=function(a){return go(this.kb,a)};k.Ca=function(a,b){if(this===a)return!0;if(this.ja!=a.Fe())return!1;var c=b||ho;fo(this);for(var e,d=0;e=this.ua[d];d++)if(!c(this.get(e),a.get(e)))return!1;return!0};function ho(a,b){return a===b}k.Wd=function(){return 0==this.ja};k.clear=function(){this.kb={};this.ja=this.ua.length=0};k.remove=function(a){return go(this.kb,a)?(delete this.kb[a],this.ja--,this.ua.length>2*this.ja&&fo(this),!0):!1};
function fo(a){if(a.ja!=a.ua.length){for(var b=0,c=0;b<a.ua.length;){var e=a.ua[b];go(a.kb,e)&&(a.ua[c++]=e);b++}a.ua.length=c}if(a.ja!=a.ua.length){for(var d={},c=b=0;b<a.ua.length;)e=a.ua[b],go(d,e)||(a.ua[c++]=e,d[e]=1),b++;a.ua.length=c}}k.get=function(a,b){return go(this.kb,a)?this.kb[a]:b};k.set=function(a,b){go(this.kb,a)||(this.ja++,this.ua.push(a));this.kb[a]=b};k.forEach=function(a,b){for(var c=this.Sa(),e=0;e<c.length;e++){var d=c[e],g=this.get(d);a.call(b,g,d,this)}};k.clone=function(){return new eo(this)};
function go(a,b){return Object.prototype.hasOwnProperty.call(a,b)};function io(a){var b;b||(b=jo(a||arguments.callee.caller,[]));return b}
function jo(a,b){var c=[];if(0<=Va(b,a))c.push("[...circular reference...]");else if(a&&50>b.length){c.push(ko(a)+"(");for(var e=a.arguments,d=0;e&&d<e.length;d++){0<d&&c.push(", ");var g;g=e[d];switch(typeof g){case "object":g=g?"object":"null";break;case "string":break;case "number":g=String(g);break;case "boolean":g=g?"true":"false";break;case "function":g=(g=ko(g))?g:"[fn]";break;default:g=typeof g}40<g.length&&(g=g.substr(0,40)+"...");c.push(g)}b.push(a);c.push(")\n");try{c.push(jo(a.caller,
b))}catch(f){c.push("[exception trying to get caller]\n")}}else a?c.push("[...long stack...]"):c.push("[end]");return c.join("")}function ko(a){if(lo[a])return lo[a];a=String(a);if(!lo[a]){var b=/function ([^\(]+)/.exec(a);lo[a]=b?b[1]:"[Anonymous]"}return lo[a]}var lo={};function mo(a,b,c,e,d){this.reset(a,b,c,e,d)}mo.prototype.De=null;mo.prototype.Ce=null;var no=0;mo.prototype.reset=function(a,b,c,e,d){"number"==typeof d||no++;e||na();this.Sc=a;this.Jf=b;delete this.De;delete this.Ce};mo.prototype.Te=function(a){this.Sc=a};function oo(a){this.Lf=a;this.He=this.Hd=this.Sc=this.Ba=null}function po(a,b){this.name=a;this.value=b}po.prototype.toString=function(){return this.name};var qo=new po("SEVERE",1E3),ro=new po("INFO",800),so=new po("CONFIG",700),to=new po("FINE",500),uo=new po("FINEST",300);k=oo.prototype;k.getParent=function(){return this.Ba};k.Ee=function(){this.Hd||(this.Hd={});return this.Hd};k.Te=function(a){this.Sc=a};
function vo(a){if(a.Sc)return a.Sc;if(a.Ba)return vo(a.Ba);Pa("Root logger has no level set.");return null}k.log=function(a,b,c){if(a.value>=vo(this).value)for(ga(b)&&(b=b()),a=this.Ge(a,b,c,oo.prototype.log),b="log:"+a.Jf,ba.console&&(ba.console.timeStamp?ba.console.timeStamp(b):ba.console.markTimeline&&ba.console.markTimeline(b)),ba.msWriteProfilerMark&&ba.msWriteProfilerMark(b),b=this;b;){c=b;var e=a;if(c.He)for(var d=0,g=void 0;g=c.He[d];d++)g(e);b=b.getParent()}};
k.Ge=function(a,b,c,e){a=new mo(a,String(b),this.Lf);if(c){a.De=c;var d;e=e||oo.prototype.Ge;try{var g;var f=ca("window.location.href");if(fa(c))g={message:c,name:"Unknown error",lineNumber:"Not available",fileName:f,stack:"Not available"};else{var h,m;b=!1;try{h=c.lineNumber||c.line||"Not available"}catch(p){h="Not available",b=!0}try{m=c.fileName||c.filename||c.sourceURL||ba.$googDebugFname||f}catch(q){m="Not available",b=!0}g=!b&&c.lineNumber&&c.fileName&&c.stack&&c.message&&c.name?c:{message:c.message||
"Not available",name:c.name||"UnknownError",lineNumber:h,fileName:m,stack:c.stack||"Not available"}}d="Message: "+sa(g.message)+'\nUrl: \x3ca href\x3d"view-source:'+g.fileName+'" target\x3d"_new"\x3e'+g.fileName+"\x3c/a\x3e\nLine: "+g.lineNumber+"\n\nBrowser stack:\n"+sa(g.stack+"-\x3e ")+"[end]\n\nJS stack traversal:\n"+sa(io(e)+"-\x3e ")}catch(r){d="Exception trying to expose exception! You win, we lose. "+r}a.Ce=d}return a};k.info=function(a,b){this.log(ro,a,b)};var wo={},xo=null;
function yo(a){xo||(xo=new oo(""),wo[""]=xo,xo.Te(so));var b;if(!(b=wo[a])){b=new oo(a);var c=a.lastIndexOf("."),e=a.substr(c+1),c=yo(a.substr(0,c));c.Ee()[e]=b;b.Ba=c;wo[a]=b}return b};function zo(a){var b=Ao;b&&b.info(a,void 0)}function Bo(a,b){a&&a.log(to,b,void 0)};var Ao=yo("goog.net.xpc");function Co(){0!=Do&&(this[ia]||(this[ia]=++ja))}var Do=0;Co.prototype.Bf=!1;function Eo(a){Co.call(this);a||Ma||(Ma=new ao)}pa(Eo,Co);function Fo(a,b){Eo.call(this,b);this.df=a;this.xd=[]}var Go;pa(Fo,Eo);Fo.prototype.Tb=0;Fo.prototype.Xe=!1;Fo.prototype.Vc=3800;Fo.prototype.send=function(a,b){var c=a+":"+b;if(!On||b.length<=this.Vc)this.xd.push("|"+c);else for(var e=b.length,d=Math.ceil(e/this.Vc),g=0,f=1;g<e;)this.xd.push(","+f+"/"+d+"|"+c.substr(g,this.Vc)),f++,g+=this.Vc;!this.Xe&&this.xd.length&&(c=this.xd.shift(),++this.Tb,this.Sg.send(this.Tb+c),(e=Ao)&&e.log(uo,"msg sent: "+this.Tb+c,void 0),this.Xe=!0)};
var Ho=[],Io=ma(function(){var a,b=!1;try{for(var c=0;a=Ho[c];c++){var e;if(!(e=b)){var d=a,g=d.Xg.location.href;if(g!=d.zf){d.zf=g;var f=g.split("#")[1];f&&(f=f.substr(1),d.ug(decodeURIComponent(f)));e=!0}else e=!1}b=e}}catch(h){if(zo("receive_() failed: "+h),a=a.bh.df,zo("Transport Error"),a.close(),!Ho.length)return}a=na();b&&(Go=a);window.setTimeout(Io,1E3>a-Go?10:100)},Fo);var Jo=!On||On&&9<=Xn,Ko=On&&!Vn("9");!Qn||Vn("528");Pn&&Vn("1.9b")||On&&Vn("8")||Nn&&Vn("9.5")||Qn&&Vn("528");Pn&&!Vn("8")||On&&Vn("9");function Lo(a,b){this.type=a;this.currentTarget=this.target=b;this.defaultPrevented=this.yc=!1;this.Se=!0}Lo.prototype.preventDefault=function(){this.defaultPrevented=!0;this.Se=!1};function Mo(a){Mo[" "](a);return a}Mo[" "]=da;function No(a,b){Lo.call(this,a?a.type:"");this.relatedTarget=this.currentTarget=this.target=null;this.charCode=this.keyCode=this.button=this.screenY=this.screenX=this.clientY=this.clientX=this.offsetY=this.offsetX=0;this.metaKey=this.shiftKey=this.altKey=this.ctrlKey=!1;this.Be=this.state=null;a&&this.vc(a,b)}pa(No,Lo);
No.prototype.vc=function(a,b){var c=this.type=a.type;this.target=a.target||a.srcElement;this.currentTarget=b;var e=a.relatedTarget;if(e){if(Pn){var d;a:{try{Mo(e.nodeName);d=!0;break a}catch(g){}d=!1}d||(e=null)}}else"mouseover"==c?e=a.fromElement:"mouseout"==c&&(e=a.toElement);this.relatedTarget=e;this.offsetX=Qn||void 0!==a.offsetX?a.offsetX:a.layerX;this.offsetY=Qn||void 0!==a.offsetY?a.offsetY:a.layerY;this.clientX=void 0!==a.clientX?a.clientX:a.pageX;this.clientY=void 0!==a.clientY?a.clientY:
a.pageY;this.screenX=a.screenX||0;this.screenY=a.screenY||0;this.button=a.button;this.keyCode=a.keyCode||0;this.charCode=a.charCode||("keypress"==c?a.keyCode:0);this.ctrlKey=a.ctrlKey;this.altKey=a.altKey;this.shiftKey=a.shiftKey;this.metaKey=a.metaKey;this.state=a.state;this.Be=a;a.defaultPrevented&&this.preventDefault()};
No.prototype.preventDefault=function(){No.Of.preventDefault.call(this);var a=this.Be;if(a.preventDefault)a.preventDefault();else if(a.returnValue=!1,Ko)try{if(a.ctrlKey||112<=a.keyCode&&123>=a.keyCode)a.keyCode=-1}catch(b){}};var Oo="closure_listenable_"+(1E6*Math.random()|0),Po=0;function Qo(a,b,c,e,d){this.fc=a;this.vd=null;this.src=b;this.type=c;this.Yc=!!e;this.Fa=d;this.key=++Po;this.zc=this.Xc=!1}function Ro(a){a.zc=!0;a.fc=null;a.vd=null;a.src=null;a.Fa=null};function So(a){this.src=a;this.Ua={};this.zd=0}So.prototype.add=function(a,b,c,e,d){var g=a.toString();a=this.Ua[g];a||(a=this.Ua[g]=[],this.zd++);var f=To(a,b,e,d);-1<f?(b=a[f],c||(b.Xc=!1)):(b=new Qo(b,this.src,g,!!e,d),b.Xc=c,a.push(b));return b};So.prototype.remove=function(a,b,c,e){a=a.toString();if(!(a in this.Ua))return!1;var d=this.Ua[a];b=To(d,b,c,e);return-1<b?(Ro(d[b]),Ua.splice.call(d,b,1),0==d.length&&(delete this.Ua[a],this.zd--),!0):!1};
function Uo(a,b){var c=b.type;if(c in a.Ua){var e=a.Ua[c],d=Va(e,b),g;(g=0<=d)&&Ua.splice.call(e,d,1);g&&(Ro(b),0==a.Ua[c].length&&(delete a.Ua[c],a.zd--))}}So.prototype.Td=function(a,b,c,e){a=this.Ua[a.toString()];var d=-1;a&&(d=To(a,b,c,e));return-1<d?a[d]:null};function To(a,b,c,e){for(var d=0;d<a.length;++d){var g=a[d];if(!g.zc&&g.fc==b&&g.Yc==!!c&&g.Fa==e)return d}return-1};var Vo="closure_lm_"+(1E6*Math.random()|0),Wo={},Xo=0;function Yo(a,b,c,e,d){if("array"==l(b))for(var g=0;g<b.length;g++)Yo(a,b[g],c,e,d);else if(c=Zo(c),a&&a[Oo])a.Ke(b,c,e,d);else{if(!b)throw Error("Invalid event type");var g=!!e,f=$o(a);f||(a[Vo]=f=new So(a));c=f.add(b,c,!1,e,d);c.vd||(e=ap(),c.vd=e,e.src=a,e.fc=c,a.addEventListener?a.addEventListener(b.toString(),e,g):a.attachEvent(bp(b.toString()),e),Xo++)}}
function ap(){var a=cp,b=Jo?function(c){return a.call(b.src,b.fc,c)}:function(c){c=a.call(b.src,b.fc,c);if(!c)return c};return b}function dp(a,b,c,e,d){if("array"==l(b))for(var g=0;g<b.length;g++)dp(a,b[g],c,e,d);else c=Zo(c),a&&a[Oo]?a.Qf(b,c,e,d):a&&(a=$o(a))&&(b=a.Td(b,c,!!e,d))&&ep(b)}
function ep(a){if("number"!=typeof a&&a&&!a.zc){var b=a.src;if(b&&b[Oo])Uo(b.tc,a);else{var c=a.type,e=a.vd;b.removeEventListener?b.removeEventListener(c,e,a.Yc):b.detachEvent&&b.detachEvent(bp(c),e);Xo--;(c=$o(b))?(Uo(c,a),0==c.zd&&(c.src=null,b[Vo]=null)):Ro(a)}}}function bp(a){return a in Wo?Wo[a]:Wo[a]="on"+a}function fp(a,b,c,e){var d=1;if(a=$o(a))if(b=a.Ua[b.toString()])for(b=b.concat(),a=0;a<b.length;a++){var g=b[a];g&&g.Yc==c&&!g.zc&&(d&=!1!==gp(g,e))}return Boolean(d)}
function gp(a,b){var c=a.fc,e=a.Fa||a.src;a.Xc&&ep(a);return c.call(e,b)}
function cp(a,b){if(a.zc)return!0;if(!Jo){var c=b||ca("window.event"),e=new No(c,this),d=!0;if(!(0>c.keyCode||void 0!=c.returnValue)){a:{var g=!1;if(0==c.keyCode)try{c.keyCode=-1;break a}catch(f){g=!0}if(g||void 0==c.returnValue)c.returnValue=!0}c=[];for(g=e.currentTarget;g;g=g.parentNode)c.push(g);for(var g=a.type,h=c.length-1;!e.yc&&0<=h;h--)e.currentTarget=c[h],d&=fp(c[h],g,!0,e);for(h=0;!e.yc&&h<c.length;h++)e.currentTarget=c[h],d&=fp(c[h],g,!1,e)}return d}return gp(a,new No(b,this))}
function $o(a){a=a[Vo];return a instanceof So?a:null}var hp="__closure_events_fn_"+(1E9*Math.random()>>>0);function Zo(a){if(ga(a))return a;a[hp]||(a[hp]=function(b){return a.handleEvent(b)});return a[hp]};function ip(){Co.call(this);this.tc=new So(this);this.$e=this;this.Qe=null}pa(ip,Co);ip.prototype[Oo]=!0;k=ip.prototype;k.addEventListener=function(a,b,c,e){Yo(this,a,b,c,e)};k.removeEventListener=function(a,b,c,e){dp(this,a,b,c,e)};
k.dispatchEvent=function(a){var b,c=this.Qe;if(c)for(b=[];c;c=c.Qe)b.push(c);var c=this.$e,e=a.type||a;if(fa(a))a=new Lo(a,c);else if(a instanceof Lo)a.target=a.target||c;else{var d=a;a=new Lo(e,c);Ga(a,d)}var d=!0,g;if(b)for(var f=b.length-1;!a.yc&&0<=f;f--)g=a.currentTarget=b[f],d=jp(g,e,!0,a)&&d;a.yc||(g=a.currentTarget=c,d=jp(g,e,!0,a)&&d,a.yc||(d=jp(g,e,!1,a)&&d));if(b)for(f=0;!a.yc&&f<b.length;f++)g=a.currentTarget=b[f],d=jp(g,e,!1,a)&&d;return d};
k.Ke=function(a,b,c,e){this.tc.add(String(a),b,!1,c,e)};k.Qf=function(a,b,c,e){this.tc.remove(String(a),b,c,e)};function jp(a,b,c,e){b=a.tc.Ua[String(b)];if(!b)return!0;b=b.concat();for(var d=!0,g=0;g<b.length;++g){var f=b[g];if(f&&!f.zc&&f.Yc==c){var h=f.fc,m=f.Fa||f.src;f.Xc&&Uo(a.tc,f);d=!1!==h.call(m,e)&&d}}return d&&!1!=e.Se}k.Td=function(a,b,c,e){return this.tc.Td(String(a),b,c,e)};function kp(a,b,c){if(ga(a))c&&(a=ma(a,c));else if(a&&"function"==typeof a.handleEvent)a=ma(a.handleEvent,a);else throw Error("Invalid listener argument");return 2147483647<b?-1:ba.setTimeout(a,b||0)};function lp(a){a.prototype.then=a.prototype.then;a.prototype.$goog_Thenable=!0}function mp(a){if(!a)return!1;try{return!!a.$goog_Thenable}catch(b){return!1}};function np(a,b){op||pp();qp||(op(),qp=!0);rp.push(new sp(a,b))}var op;function pp(){if(ba.Promise&&ba.Promise.resolve){var a=ba.Promise.resolve();op=function(){a.then(tp)}}else op=function(){sl(tp)}}var qp=!1,rp=[];function tp(){for(;rp.length;){var a=rp;rp=[];for(var b=0;b<a.length;b++){var c=a[b];try{c.Pb.call(c.scope)}catch(e){rl(e)}}}qp=!1}function sp(a,b){this.Pb=a;this.scope=b};function up(a,b){this.Xa=vp;this.nb=void 0;this.Oa=this.Ba=null;this.pd=this.Rd=!1;try{var c=this;a.call(b,function(a){wp(c,xp,a)},function(a){wp(c,yp,a)})}catch(e){wp(this,yp,e)}}var vp=0,xp=2,yp=3;up.prototype.then=function(a,b,c){return zp(this,ga(a)?a:null,ga(b)?b:null,c)};lp(up);up.prototype.cancel=function(a){this.Xa==vp&&np(function(){var b=new Ap(a);Bp(this,b)},this)};
function Bp(a,b){if(a.Xa==vp)if(a.Ba){var c=a.Ba;if(c.Oa){for(var e=0,d=-1,g=0,f;f=c.Oa[g];g++)if(f=f.Zc)if(e++,f==a&&(d=g),0<=d&&1<e)break;0<=d&&(c.Xa==vp&&1==e?Bp(c,b):(e=c.Oa.splice(d,1)[0],Cp(c,e,yp,b)))}}else wp(a,yp,b)}function Dp(a,b){a.Oa&&a.Oa.length||a.Xa!=xp&&a.Xa!=yp||Ep(a);a.Oa||(a.Oa=[]);a.Oa.push(b)}
function zp(a,b,c,e){var d={Zc:null,Ne:null,Pe:null};d.Zc=new up(function(a,f){d.Ne=b?function(c){try{var d=b.call(e,c);a(d)}catch(p){f(p)}}:a;d.Pe=c?function(b){try{var d=c.call(e,b);void 0===d&&b instanceof Ap?f(b):a(d)}catch(p){f(p)}}:f});d.Zc.Ba=a;Dp(a,d);return d.Zc}up.prototype.Ve=function(a){this.Xa=vp;wp(this,xp,a)};up.prototype.We=function(a){this.Xa=vp;wp(this,yp,a)};
function wp(a,b,c){if(a.Xa==vp){if(a==c)b=yp,c=new TypeError("Promise cannot resolve to itself");else{if(mp(c)){a.Xa=1;c.then(a.Ve,a.We,a);return}if(ha(c))try{var e=c.then;if(ga(e)){Fp(a,c,e);return}}catch(d){b=yp,c=d}}a.nb=c;a.Xa=b;Ep(a);b!=yp||c instanceof Ap||Gp(a,c)}}function Fp(a,b,c){function e(b){g||(g=!0,a.We(b))}function d(b){g||(g=!0,a.Ve(b))}a.Xa=1;var g=!1;try{c.call(b,d,e)}catch(f){e(f)}}function Ep(a){a.Rd||(a.Rd=!0,np(a.Df,a))}
up.prototype.Df=function(){for(;this.Oa&&this.Oa.length;){var a=this.Oa;this.Oa=[];for(var b=0;b<a.length;b++)Cp(this,a[b],this.Xa,this.nb)}this.Rd=!1};function Cp(a,b,c,e){if(c==xp)b.Ne(e);else{for(;a&&a.pd;a=a.Ba)a.pd=!1;b.Pe(e)}}function Gp(a,b){a.pd=!0;np(function(){a.pd&&Hp.call(null,b)})}var Hp=rl;function Ap(a){La.call(this,a)}pa(Ap,La);Ap.prototype.name="cancel";/*
 Portions of this code are from MochiKit, received by
 The Closure Authors under the MIT license. All other code is Copyright
 2005-2009 The Closure Authors. All Rights Reserved.
*/
function Ip(a,b){this.Tb=[];this.Me=a;this.ze=b||null;this.uc=this.$b=!1;this.nb=void 0;this.ae=this.af=this.Fd=!1;this.Ad=0;this.Ba=null;this.Gd=0}Ip.prototype.cancel=function(a){if(this.$b)this.nb instanceof Ip&&this.nb.cancel();else{if(this.Ba){var b=this.Ba;delete this.Ba;a?b.cancel(a):(b.Gd--,0>=b.Gd&&b.cancel())}this.Me?this.Me.call(this.ze,this):this.ae=!0;this.$b||this.Cf()}};Ip.prototype.ye=function(a,b){this.Fd=!1;this.$b=!0;this.nb=b;this.uc=!a;Jp(this)};
Ip.prototype.Cf=function(){var a=new Kp;if(this.$b){if(!this.ae)throw new Lp;this.ae=!1}this.$b=!0;this.nb=a;this.uc=!0;Jp(this)};function Mp(a,b,c){a.Tb.push([b,c,void 0]);a.$b&&Jp(a)}Ip.prototype.then=function(a,b,c){var e,d,g=new up(function(a,b){e=a;d=b});Mp(this,e,function(a){a instanceof Kp?g.cancel():d(a)});return g.then(a,b,c)};lp(Ip);function Np(a){return $a(a.Tb,function(a){return ga(a[1])})}
function Jp(a){if(a.Ad&&a.$b&&Np(a)){var b=a.Ad,c=Op[b];c&&(ba.clearTimeout(c.rd),delete Op[b]);a.Ad=0}a.Ba&&(a.Ba.Gd--,delete a.Ba);for(var b=a.nb,e=c=!1;a.Tb.length&&!a.Fd;){var d=a.Tb.shift(),g=d[0],f=d[1],d=d[2];if(g=a.uc?f:g)try{var h=g.call(d||a.ze,b);void 0!==h&&(a.uc=a.uc&&(h==b||h instanceof Error),a.nb=b=h);mp(b)&&(e=!0,a.Fd=!0)}catch(m){b=m,a.uc=!0,Np(a)||(c=!0)}}a.nb=b;e&&(h=ma(a.ye,a,!0),e=ma(a.ye,a,!1),b instanceof Ip?(Mp(b,h,e),b.af=!0):b.then(h,e));c&&(b=new Pp(b),Op[b.rd]=b,a.Ad=
b.rd)}function Lp(){La.call(this)}pa(Lp,La);Lp.prototype.message="Deferred has already fired";Lp.prototype.name="AlreadyCalledError";function Kp(){La.call(this)}pa(Kp,La);Kp.prototype.message="Deferred was canceled";Kp.prototype.name="CanceledError";function Pp(a){this.rd=ba.setTimeout(ma(this.Pf,this),0);this.nd=a}Pp.prototype.Pf=function(){delete Op[this.rd];throw this.nd;};var Op={};var Qp="undefined"!=typeof Object.keys?function(a){return Object.keys(a)}:function(a){return Ea(a)},Rp="undefined"!=typeof Array.isArray?function(a){return Array.isArray(a)}:function(a){return"array"===l(a)};function Sp(){return Math.round(15*Math.random()).toString(16)};function Tp(a,b){if(3<a.length){if(b)return!0;var c=a.charAt(1);return"~"===a.charAt(0)?":"===c||"$"===c||"#"===c:!1}return!1}function Up(a){var b=Math.floor(a/44);a=String.fromCharCode(a%44+48);return 0===b?"^"+a:"^"+String.fromCharCode(b+48)+a}function Vp(){this.cf=this.Mc=this.S=0;this.Ha={}}
Vp.prototype.write=function(a,b){if(Tp(a,b)){4096===this.cf?(this.clear(),this.Mc=0,this.Ha={}):1936===this.S&&this.clear();var c=this.Ha[a];return null==c?(this.Ha[a]=[Up(this.S),this.Mc],this.S++,a):c[1]!=this.Mc?(c[1]=this.Mc,c[0]=Up(this.S),this.S++,a):c[0]}return a};Vp.prototype.clear=function(){this.S=0;this.Mc++};function Wp(){this.S=0;this.Ha=[]}Wp.prototype.write=function(a){1936==this.S&&(this.S=0);this.Ha[this.S]=a;this.S++;return a};
Wp.prototype.wd=function(a){return this.Ha[2===a.length?a.charCodeAt(1)-48:44*(a.charCodeAt(1)-48)+(a.charCodeAt(2)-48)]};Wp.prototype.clear=function(){this.S=0};var Xp=1;function Yp(a,b){if(null==a)return null==b;if(a===b)return!0;if("object"===typeof a){if(Rp(a)){if(Rp(b)&&a.length===b.length){for(var c=0;c<a.length;c++)if(!Yp(a[c],b[c]))return!1;return!0}return!1}if(a.Qa)return a.Qa(b);if(null!=b&&"object"===typeof b){if(b.Qa)return b.Qa(a);var c=0,e=Qp(b).length,d;for(d in a)if(a.hasOwnProperty(d)&&(c++,!b.hasOwnProperty(d)||!Yp(a[d],b[d])))return!1;return c===e}}return!1}function Zp(a,b){return a^b+2654435769+(a<<6)+(a>>2)}var $p={},aq=0;
function bq(a){var b=0;if(null!=a.forEach)a.forEach(function(a,c){b=(b+(cq(c)^cq(a)))%4503599627370496});else for(var c=Qp(a),e=0;e<c.length;e++)var d=c[e],g=a[d],b=(b+(cq(d)^cq(g)))%4503599627370496;return b}function dq(a){var b=0;if(Rp(a))for(var c=0;c<a.length;c++)b=Zp(b,cq(a[c]));else a.forEach&&a.forEach(function(a){b=Zp(b,cq(a))});return b}
function cq(a){if(null==a)return 0;switch(typeof a){case "number":return a;case "boolean":return!0===a?1:0;case "string":var b=$p[a];if(null==b){for(var c=b=0;c<a.length;++c)b=31*b+a.charCodeAt(c),b%=4294967296;aq++;256<=aq&&($p={},aq=1);$p[a]=b}a=b;return a;case "function":return b=a.transit$hashCode$,b||(b=Xp,"undefined"!=typeof Object.defineProperty?Object.defineProperty(a,"transit$hashCode$",{value:b,enumerable:!1}):a.transit$hashCode$=b,Xp++),b;default:return a instanceof Date?a.valueOf():Rp(a)?
dq(a):a.ab?a.ab():bq(a)}};function eq(a,b){this.ha=a|0;this.V=b|0}var fq={};function gq(a){if(-128<=a&&128>a){var b=fq[a];if(b)return b}b=new eq(a|0,0>a?-1:0);-128<=a&&128>a&&(fq[a]=b);return b}function hq(a){return isNaN(a)||!isFinite(a)?iq:a<=-jq?kq:a+1>=jq?lq:0>a?mq(hq(-a)):new eq(a%nq|0,a/nq|0)}function oq(a,b){return new eq(a,b)}
function pq(a,b){if(0==a.length)throw Error("number format error: empty string");var c=b||10;if(2>c||36<c)throw Error("radix out of range: "+c);if("-"==a.charAt(0))return mq(pq(a.substring(1),c));if(0<=a.indexOf("-"))throw Error('number format error: interior "-" character: '+a);for(var e=hq(Math.pow(c,8)),d=iq,g=0;g<a.length;g+=8){var f=Math.min(8,a.length-g),h=parseInt(a.substring(g,g+f),c);8>f?(f=hq(Math.pow(c,f)),d=d.multiply(f).add(hq(h))):(d=d.multiply(e),d=d.add(hq(h)))}return d}
var nq=4294967296,jq=nq*nq/2,iq=gq(0),qq=gq(1),rq=gq(-1),lq=oq(-1,2147483647),kq=oq(0,-2147483648),sq=gq(16777216);function tq(a){return a.V*nq+(0<=a.ha?a.ha:nq+a.ha)}k=eq.prototype;
k.toString=function(a){a=a||10;if(2>a||36<a)throw Error("radix out of range: "+a);if(uq(this))return"0";if(0>this.V){if(this.Ca(kq)){var b=hq(a),c=this.div(b),b=vq(c.multiply(b),this);return c.toString(a)+b.ha.toString(a)}return"-"+mq(this).toString(a)}for(var c=hq(Math.pow(a,6)),b=this,e="";;){var d=b.div(c),g=vq(b,d.multiply(c)).ha.toString(a),b=d;if(uq(b))return g+e;for(;6>g.length;)g="0"+g;e=""+g+e}};function uq(a){return 0==a.V&&0==a.ha}k.Ca=function(a){return this.V==a.V&&this.ha==a.ha};
k.compare=function(a){if(this.Ca(a))return 0;var b=0>this.V,c=0>a.V;return b&&!c?-1:!b&&c?1:0>vq(this,a).V?-1:1};function mq(a){return a.Ca(kq)?kq:oq(~a.ha,~a.V).add(qq)}k.add=function(a){var b=this.V>>>16,c=this.V&65535,e=this.ha>>>16,d=a.V>>>16,g=a.V&65535,f=a.ha>>>16,h;h=0+((this.ha&65535)+(a.ha&65535));a=0+(h>>>16);a+=e+f;e=0+(a>>>16);e+=c+g;c=0+(e>>>16);c=c+(b+d)&65535;return oq((a&65535)<<16|h&65535,c<<16|e&65535)};function vq(a,b){return a.add(mq(b))}
k.multiply=function(a){if(uq(this)||uq(a))return iq;if(this.Ca(kq))return 1==(a.ha&1)?kq:iq;if(a.Ca(kq))return 1==(this.ha&1)?kq:iq;if(0>this.V)return 0>a.V?mq(this).multiply(mq(a)):mq(mq(this).multiply(a));if(0>a.V)return mq(this.multiply(mq(a)));if(0>this.compare(sq)&&0>a.compare(sq))return hq(tq(this)*tq(a));var b=this.V>>>16,c=this.V&65535,e=this.ha>>>16,d=this.ha&65535,g=a.V>>>16,f=a.V&65535,h=a.ha>>>16;a=a.ha&65535;var m,p,q,r;r=0+d*a;q=0+(r>>>16);q+=e*a;p=0+(q>>>16);q=(q&65535)+d*h;p+=q>>>
16;q&=65535;p+=c*a;m=0+(p>>>16);p=(p&65535)+e*h;m+=p>>>16;p&=65535;p+=d*f;m+=p>>>16;p&=65535;m=m+(b*a+c*h+e*f+d*g)&65535;return oq(q<<16|r&65535,m<<16|p)};
k.div=function(a){if(uq(a))throw Error("division by zero");if(uq(this))return iq;if(this.Ca(kq)){if(a.Ca(qq)||a.Ca(rq))return kq;if(a.Ca(kq))return qq;var b;b=1;if(0==b)b=this;else{var c=this.V;b=32>b?oq(this.ha>>>b|c<<32-b,c>>b):oq(c>>b-32,0<=c?0:-1)}b=b.div(a).shiftLeft(1);if(b.Ca(iq))return 0>a.V?qq:rq;c=vq(this,a.multiply(b));return b.add(c.div(a))}if(a.Ca(kq))return iq;if(0>this.V)return 0>a.V?mq(this).div(mq(a)):mq(mq(this).div(a));if(0>a.V)return mq(this.div(mq(a)));for(var e=iq,c=this;0<=
c.compare(a);){b=Math.max(1,Math.floor(tq(c)/tq(a)));for(var d=Math.ceil(Math.log(b)/Math.LN2),d=48>=d?1:Math.pow(2,d-48),g=hq(b),f=g.multiply(a);0>f.V||0<f.compare(c);)b-=d,g=hq(b),f=g.multiply(a);uq(g)&&(g=qq);e=e.add(g);c=vq(c,f)}return e};k.shiftLeft=function(a){a&=63;if(0==a)return this;var b=this.ha;return 32>a?oq(b<<a,this.V<<a|b>>>32-a):oq(0,b<<a-32)};function wq(a,b){b&=63;if(0==b)return a;var c=a.V;return 32>b?oq(a.ha>>>b|c<<32-b,c>>>b):32==b?oq(c,0):oq(c>>>b-32,0)};function xq(a,b){this.tag=a;this.N=b;this.ba=-1}xq.prototype.toString=function(){return"[TaggedValue: "+this.tag+", "+this.N+"]"};xq.prototype.equiv=function(a){return Yp(this,a)};xq.prototype.equiv=xq.prototype.equiv;xq.prototype.Qa=function(a){return a instanceof xq?this.tag===a.tag&&Yp(this.N,a.N):!1};xq.prototype.ab=function(){-1===this.ba&&(this.ba=Zp(cq(this.tag),cq(this.N)));return this.ba};function yq(a,b){return new xq(a,b)}var zq=pq("9007199254740992"),Aq=pq("-9007199254740992");
eq.prototype.equiv=function(a){return Yp(this,a)};eq.prototype.equiv=eq.prototype.equiv;eq.prototype.Qa=function(a){return a instanceof eq&&this.Ca(a)};eq.prototype.ab=function(){return this.ha};function Bq(a){this.name=a;this.ba=-1}Bq.prototype.toString=function(){return":"+this.name};Bq.prototype.equiv=function(a){return Yp(this,a)};Bq.prototype.equiv=Bq.prototype.equiv;Bq.prototype.Qa=function(a){return a instanceof Bq&&this.name==a.name};
Bq.prototype.ab=function(){-1===this.ba&&(this.ba=cq(this.name));return this.ba};function Cq(a){this.name=a;this.ba=-1}Cq.prototype.toString=function(){return"[Symbol: "+this.name+"]"};Cq.prototype.equiv=function(a){return Yp(this,a)};Cq.prototype.equiv=Cq.prototype.equiv;Cq.prototype.Qa=function(a){return a instanceof Cq&&this.name==a.name};Cq.prototype.ab=function(){-1===this.ba&&(this.ba=cq(this.name));return this.ba};
function Dq(a,b,c){var e="";c=c||b+1;for(var d=8*(7-b),g=gq(255).shiftLeft(d);b<c;b++,d-=8,g=wq(g,8)){var f=wq(oq(a.ha&g.ha,a.V&g.V),d).toString(16);1==f.length&&(f="0"+f);e+=f}return e}function Eq(a,b){this.Ud=a;this.Xd=b;this.ba=-1}Eq.prototype.toString=function(a){var b=this.Ud,c=this.Xd;a=""+(Dq(b,0,4)+"-");a+=Dq(b,4,6)+"-";a+=Dq(b,6,8)+"-";a+=Dq(c,0,2)+"-";return a+=Dq(c,2,8)};Eq.prototype.equiv=function(a){return Yp(this,a)};Eq.prototype.equiv=Eq.prototype.equiv;
Eq.prototype.Qa=function(a){return a instanceof Eq&&this.Ud.Ca(a.Ud)&&this.Xd.Ca(a.Xd)};Eq.prototype.ab=function(){-1===this.ba&&(this.ba=cq(this.toString()));return this.ba};Date.prototype.Qa=function(a){return a instanceof Date?this.valueOf()===a.valueOf():!1};Date.prototype.ab=function(){return this.valueOf()};function Fq(a,b){this.entries=a;this.type=b||0;this.S=0}
Fq.prototype.next=function(){if(this.S<this.entries.length){var a=null,a=0===this.type?this.entries[this.S]:1===this.type?this.entries[this.S+1]:[this.entries[this.S],this.entries[this.S+1]],a={value:a,done:!1};this.S+=2;return a}return{value:null,done:!0}};Fq.prototype.next=Fq.prototype.next;function Gq(a,b){this.map=a;this.type=b||0;this.keys=this.map.Sa();this.S=0;this.Vb=null;this.Kb=0}
Gq.prototype.next=function(){if(this.S<this.map.size){null!=this.Vb&&this.Kb<this.Vb.length||(this.Vb=this.map.map[this.keys[this.S]],this.Kb=0);var a=null,a=0===this.type?this.Vb[this.Kb]:1===this.type?this.Vb[this.Kb+1]:[this.Vb[this.Kb],this.Vb[this.Kb+1]],a={value:a,done:!1};this.S++;this.Kb+=2;return a}return{value:null,done:!0}};Gq.prototype.next=Gq.prototype.next;
function Hq(a,b){if((b instanceof Iq||b instanceof Jq)&&a.size===b.size){for(var c in a.map)for(var e=a.map[c],d=0;d<e.length;d+=2)if(!Yp(e[d+1],b.get(e[d])))return!1;return!0}if(null!=b&&"object"===typeof b&&(c=Qp(b),e=c.length,a.size===e)){for(d=0;d<e;d++){var g=c[d];if(!a.has(g)||!Yp(b[g],a.get(g)))return!1}return!0}return!1}function Jq(a){this.da=a;this.T=null;this.ba=-1;this.size=a.length/2;this.be=0}Jq.prototype.toString=function(){return"[TransitArrayMap]"};
function Kq(a){if(a.T)throw Error("Invalid operation, already converted");if(8>a.size)return!1;a.be++;return 32<a.be?(a.T=Lq(a.da,!0),a.da=[],!0):!1}Jq.prototype.clear=function(){this.ba=-1;this.T?this.T.clear():this.da=[];this.size=0};Jq.prototype.clear=Jq.prototype.clear;Jq.prototype.keys=function(){return this.T?this.T.keys():new Fq(this.da,0)};Jq.prototype.keys=Jq.prototype.keys;
Jq.prototype.dc=function(){if(this.T)return this.T.dc();for(var a=[],b=0,c=0;c<this.da.length;b++,c+=2)a[b]=this.da[c];return a};Jq.prototype.keySet=Jq.prototype.dc;Jq.prototype.entries=function(){return this.T?this.T.entries():new Fq(this.da,2)};Jq.prototype.entries=Jq.prototype.entries;Jq.prototype.values=function(){return this.T?this.T.values():new Fq(this.da,1)};Jq.prototype.values=Jq.prototype.values;
Jq.prototype.forEach=function(a){if(this.T)this.T.forEach(a);else for(var b=0;b<this.da.length;b+=2)a(this.da[b+1],this.da[b])};Jq.prototype.forEach=Jq.prototype.forEach;Jq.prototype.get=function(a,b){if(this.T)return this.T.get(a);if(Kq(this))return this.get(a);for(var c=0;c<this.da.length;c+=2)if(Yp(this.da[c],a))return this.da[c+1];return b};Jq.prototype.get=Jq.prototype.get;
Jq.prototype.has=function(a){if(this.T)return this.T.has(a);if(Kq(this))return this.has(a);for(var b=0;b<this.da.length;b+=2)if(Yp(this.da[b],a))return!0;return!1};Jq.prototype.has=Jq.prototype.has;Jq.prototype.set=function(a,b){this.ba=-1;if(this.T)this.T.set(a,b),this.size=this.T.size;else{for(var c=0;c<this.da.length;c+=2)if(Yp(this.da[c],a)){this.da[c+1]=b;return}this.da.push(a);this.da.push(b);this.size++;32<this.size&&(this.T=Lq(this.da,!0),this.da=null)}};Jq.prototype.set=Jq.prototype.set;
Jq.prototype["delete"]=function(a){this.ba=-1;if(this.T)this.T["delete"](a),this.size=this.T.size;else for(var b=0;b<this.da.length;b+=2)if(Yp(this.da[b],a)){this.da.splice(b,2);this.size--;break}};Jq.prototype.ab=function(){if(this.T)return this.T.ab();-1===this.ba&&(this.ba=bq(this));return this.ba};Jq.prototype.Qa=function(a){return this.T?Hq(this.T,a):Hq(this,a)};function Iq(a,b,c){this.map=b||{};this.nc=a||[];this.size=c||0;this.ba=-1}Iq.prototype.toString=function(){return"[TransitMap]"};
Iq.prototype.clear=function(){this.ba=-1;this.map={};this.nc=[];this.size=0};Iq.prototype.clear=Iq.prototype.clear;Iq.prototype.Sa=function(){return null!=this.nc?this.nc:Qp(this.map)};Iq.prototype["delete"]=function(a){this.ba=-1;this.nc=null;for(var b=cq(a),c=this.map[b],e=0;e<c.length;e+=2)if(Yp(a,c[e])){c.splice(e,2);0===c.length&&delete this.map[b];this.size--;break}};Iq.prototype.entries=function(){return new Gq(this,2)};Iq.prototype.entries=Iq.prototype.entries;
Iq.prototype.forEach=function(a){for(var b=this.Sa(),c=0;c<b.length;c++)for(var e=this.map[b[c]],d=0;d<e.length;d+=2)a(e[d+1],e[d],this)};Iq.prototype.forEach=Iq.prototype.forEach;Iq.prototype.get=function(a,b){var c=cq(a),c=this.map[c];if(null!=c)for(var e=0;e<c.length;e+=2){if(Yp(a,c[e]))return c[e+1]}else return b};Iq.prototype.get=Iq.prototype.get;Iq.prototype.has=function(a){var b=cq(a),b=this.map[b];if(null!=b)for(var c=0;c<b.length;c+=2)if(Yp(a,b[c]))return!0;return!1};Iq.prototype.has=Iq.prototype.has;
Iq.prototype.keys=function(){return new Gq(this,0)};Iq.prototype.keys=Iq.prototype.keys;Iq.prototype.dc=function(){for(var a=this.Sa(),b=[],c=0;c<a.length;c++)for(var e=this.map[a[c]],d=0;d<e.length;d+=2)b.push(e[d]);return b};Iq.prototype.keySet=Iq.prototype.dc;Iq.prototype.set=function(a,b){this.ba=-1;var c=cq(a),e=this.map[c];if(null==e)this.nc&&this.nc.push(c),this.map[c]=[a,b],this.size++;else{for(var c=!0,d=0;d<e.length;d+=2)if(Yp(b,e[d])){c=!1;e[d]=b;break}c&&(e.push(a),e.push(b),this.size++)}};
Iq.prototype.set=Iq.prototype.set;Iq.prototype.values=function(){return new Gq(this,1)};Iq.prototype.values=Iq.prototype.values;Iq.prototype.ab=function(){-1===this.ba&&(this.ba=bq(this));return this.ba};Iq.prototype.Qa=function(a){return Hq(this,a)};
function Lq(a,b){var c=!1;a=a||[];c=!1===c?c:!0;if((!0!==b||!b)&&64>=a.length){if(c){var e=a;a=[];for(c=0;c<e.length;c+=2){for(var d=!1,g=0;g<a.length;g+=2)if(Yp(a[g],e[c])){a[g+1]=e[c+1];d=!0;break}d||(a.push(e[c]),a.push(e[c+1]))}}return new Jq(a)}for(var e={},d=[],f=0,c=0;c<a.length;c+=2){var g=cq(a[c]),h=e[g];if(null==h)d.push(g),e[g]=[a[c],a[c+1]],f++;else{for(var m=!0,g=0;g<h.length;g+=2)if(Yp(h[g],a[c])){h[g+1]=a[c+1];m=!1;break}m&&(h.push(a[c]),h.push(a[c+1]),f++)}}return new Iq(d,e,f)}
function Mq(a){this.map=a;this.size=a.size}Mq.prototype.toString=function(){return"[TransitSet]"};Mq.prototype.add=function(a){this.map.set(a,a);this.size=this.map.size};Mq.prototype.add=Mq.prototype.add;Mq.prototype.clear=function(){this.map=new Iq;this.size=0};Mq.prototype.clear=Mq.prototype.clear;Mq.prototype["delete"]=function(a){this.map["delete"](a);this.size=this.map.size};Mq.prototype.entries=function(){return this.map.entries()};Mq.prototype.entries=Mq.prototype.entries;
Mq.prototype.forEach=function(a){var b=this;this.map.forEach(function(c,e){a(e,b)})};Mq.prototype.forEach=Mq.prototype.forEach;Mq.prototype.has=function(a){return this.map.has(a)};Mq.prototype.has=Mq.prototype.has;Mq.prototype.keys=function(){return this.map.keys()};Mq.prototype.keys=Mq.prototype.keys;Mq.prototype.dc=function(){return this.map.dc()};Mq.prototype.keySet=Mq.prototype.dc;Mq.prototype.values=function(){return this.map.values()};Mq.prototype.values=Mq.prototype.values;
Mq.prototype.Qa=function(a){if(a instanceof Mq){if(this.size===a.size)return Yp(this.map,a.map)}else return!1};Mq.prototype.ab=function(){return cq(this.map)};function Nq(a){this.xa=a}
function Oq(a){this.options=a||{};this.qa={};for(var b in this.Lc.qa)this.qa[b]=this.Lc.qa[b];for(b in this.options.handlers){a:{switch(b){case "_":case "s":case "?":case "i":case "d":case "b":case "'":case "array":case "map":a=!0;break a}a=!1}if(a)throw Error('Cannot override handler for ground type "'+b+'"');this.qa[b]=this.options.handlers[b]}this.ud=null!=this.options.preferStrings?this.options.preferStrings:this.Lc.ud;this.Zd=null!=this.options.preferBuffers?this.options.preferBuffers:this.Lc.Zd;
this.Pd=this.options.defaultHandler||this.Lc.Pd;this.Wa=this.options.mapBuilder;this.pc=this.options.arrayBuilder}
Oq.prototype.Lc={qa:{_:function(){return null},"?":function(a){return"t"===a},b:function(a,b){var c;if(b&&!1===b.Zd||"undefined"==typeof Buffer)if("undefined"!=typeof Uint8Array){if("undefined"!=typeof atob)c=atob(a);else{c=String(a).replace(/=+$/,"");if(1==c.length%4)throw Error("'atob' failed: The string to be decoded is not correctly encoded.");for(var e=0,d,g,f=0,h="";g=c.charAt(f++);~g&&(d=e%4?64*d+g:g,e++%4)?h+=String.fromCharCode(255&d>>(-2*e&6)):0)g="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/\x3d".indexOf(g);
c=h}e=c.length;d=new Uint8Array(e);for(g=0;g<e;g++)d[g]=c.charCodeAt(g);c=d}else c=yq("b",a);else c=new Buffer(a,"base64");return c},i:function(a){"number"===typeof a||a instanceof eq||(a=pq(a,10),a=0<a.compare(zq)||0>a.compare(Aq)?a:tq(a));return a},n:function(a){return yq("n",a)},d:function(a){return parseFloat(a)},f:function(a){return yq("f",a)},c:function(a){return a},":":function(a){return new Bq(a)},$:function(a){return new Cq(a)},r:function(a){return yq("r",a)},z:function(a){a:switch(a){case "-INF":a=
-Infinity;break a;case "INF":a=Infinity;break a;case "NaN":a=NaN;break a;default:throw Error("Invalid special double value "+a);}return a},"'":function(a){return a},m:function(a){a="number"===typeof a?a:parseInt(a,10);return new Date(a)},t:function(a){return new Date(a)},u:function(a){a=a.replace(/-/g,"");for(var b=null,c=null,e=c=0,d=24,g=0,g=c=0,d=24;8>g;g+=2,d-=8)c|=parseInt(a.substring(g,g+2),16)<<d;e=0;g=8;for(d=24;16>g;g+=2,d-=8)e|=parseInt(a.substring(g,g+2),16)<<d;b=oq(e,c);c=0;g=16;for(d=
24;24>g;g+=2,d-=8)c|=parseInt(a.substring(g,g+2),16)<<d;e=0;for(d=g=24;32>g;g+=2,d-=8)e|=parseInt(a.substring(g,g+2),16)<<d;c=oq(e,c);return new Eq(b,c)},set:function(a){a=a||[];for(var b={},c=[],e=0,d=0;d<a.length;d++){var g=cq(a[d]),f=b[g];if(null==f)c.push(g),b[g]=[a[d],a[d]],e++;else{for(var g=!0,h=0;h<f.length;h+=2)if(Yp(f[h],a[d])){g=!1;break}g&&(f.push(a[d]),f.push(a[d]),e++)}}return new Mq(new Iq(c,b,e))},list:function(a){return yq("list",a)},link:function(a){return yq("link",a)},cmap:function(a){return Lq(a)}},
Pd:function(a,b){return yq(a,b)},ud:!0,Zd:!0};
Oq.prototype.ka=function(a,b,c,e){if(null==a)return null;switch(typeof a){case "string":return Tp(a,c)?(a=Pq(this,a),b&&b.write(a,c),b=a):b="^"===a.charAt(0)&&" "!==a.charAt(1)?b.wd(a,c):Pq(this,a),b;case "object":if(Rp(a))if("^ "===a[0])if(this.Wa)if(17>a.length&&this.Wa.bc){e=[];for(c=1;c<a.length;c+=2)e.push(this.ka(a[c],b,!0,!1)),e.push(this.ka(a[c+1],b,!1,!1));b=this.Wa.bc(e,a)}else{e=this.Wa.vc(a);for(c=1;c<a.length;c+=2)e=this.Wa.add(e,this.ka(a[c],b,!0,!1),this.ka(a[c+1],b,!1,!1),a);b=this.Wa.od(e,
a)}else{e=[];for(c=1;c<a.length;c+=2)e.push(this.ka(a[c],b,!0,!1)),e.push(this.ka(a[c+1],b,!1,!1));b=Lq(e)}else b=Qq(this,a,b,c,e);else{c=Qp(a);var d=c[0];if((e=1==c.length?this.ka(d,b,!1,!1):null)&&e instanceof Nq)a=a[d],c=this.qa[e.xa],b=null!=c?c(this.ka(a,b,!1,!0),this):yq(e.xa,this.ka(a,b,!1,!1));else if(this.Wa)if(16>c.length&&this.Wa.bc){var g=[];for(e=0;e<c.length;e++)d=c[e],g.push(this.ka(d,b,!0,!1)),g.push(this.ka(a[d],b,!1,!1));b=this.Wa.bc(g,a)}else{g=this.Wa.vc(a);for(e=0;e<c.length;e++)d=
c[e],g=this.Wa.add(g,this.ka(d,b,!0,!1),this.ka(a[d],b,!1,!1),a);b=this.Wa.od(g,a)}else{g=[];for(e=0;e<c.length;e++)d=c[e],g.push(this.ka(d,b,!0,!1)),g.push(this.ka(a[d],b,!1,!1));b=Lq(g)}}return b}return a};Oq.prototype.decode=Oq.prototype.ka;
function Qq(a,b,c,e,d){if(d){var g=[];for(d=0;d<b.length;d++)g.push(a.ka(b[d],c,e,!1));return g}g=c&&c.S;if(2===b.length&&"string"===typeof b[0]&&(d=a.ka(b[0],c,!1,!1))&&d instanceof Nq)return b=b[1],g=a.qa[d.xa],null!=g?g=g(a.ka(b,c,e,!0),a):yq(d.xa,a.ka(b,c,e,!1));c&&g!=c.S&&(c.S=g);if(a.pc){if(32>=b.length&&a.pc.bc){g=[];for(d=0;d<b.length;d++)g.push(a.ka(b[d],c,e,!1));return a.pc.bc(g,b)}g=a.pc.vc();for(d=0;d<b.length;d++)g=a.pc.add(g,a.ka(b[d],c,e,!1),b);return a.pc.od(g,b)}g=[];for(d=0;d<b.length;d++)g.push(a.ka(b[d],
c,e,!1));return g}function Pq(a,b){if("~"===b.charAt(0)){var c=b.charAt(1);if("~"===c||"^"===c||"`"===c)return b.substring(1);if("#"===c)return new Nq(b.substring(2));var e=a.qa[c];return null==e?a.Pd(c,b.substring(2)):e(b.substring(2),a)}return b};function Rq(a){this.Af=new Oq(a)}function Sq(a,b){this.Rf=a;this.options=b||{};this.Ha=this.options.cache?this.options.cache:new Wp}Sq.prototype.wd=function(a){var b=this.Ha;a=this.Rf.Af.ka(JSON.parse(a),b);this.Ha.clear();return a};Sq.prototype.read=Sq.prototype.wd;function Tq(){}Tq.prototype.ge=null;function Uq(a){var b;(b=a.ge)||(b={},Vq(a)&&(b[0]=!0,b[1]=!0),b=a.ge=b);return b};var Wq;function Xq(){}pa(Xq,Tq);function Yq(a){return(a=Vq(a))?new ActiveXObject(a):new XMLHttpRequest}function Vq(a){if(!a.Ie&&"undefined"==typeof XMLHttpRequest&&"undefined"!=typeof ActiveXObject){for(var b=["MSXML2.XMLHTTP.6.0","MSXML2.XMLHTTP.3.0","MSXML2.XMLHTTP","Microsoft.XMLHTTP"],c=0;c<b.length;c++){var e=b[c];try{return new ActiveXObject(e),a.Ie=e}catch(d){}}throw Error("Could not create ActiveXObject. ActiveX might be disabled, or MSXML might not be installed");}return a.Ie}Wq=new Xq;var Zq=/^(?:([^:/?#.]+):)?(?:\/\/(?:([^/?#]*)@)?([^/#?]*?)(?::([0-9]+))?(?=[/#?]|$))?([^?#]+)?(?:\?([^#]*))?(?:#(.*))?$/;function $q(a){if(ar){ar=!1;var b=ba.location;if(b){var c=b.href;if(c&&(c=(c=$q(c)[3]||null)&&decodeURIComponent(c))&&c!=b.hostname)throw ar=!0,Error();}}return a.match(Zq)}var ar=Qn;function br(a){ip.call(this);this.headers=new eo;this.Ed=a||null;this.oc=!1;this.Dd=this.W=null;this.Je=this.Rc="";this.wc=0;this.Qc="";this.Nc=this.Vd=this.sd=this.Qd=!1;this.jc=0;this.yd=null;this.Re=cr;this.Cd=this.Ze=!1}pa(br,ip);var cr="";br.prototype.Va=yo("goog.net.XhrIo");var dr=/^https?$/i,er=["POST","PUT"];k=br.prototype;
k.send=function(a,b,c,e){if(this.W)throw Error("[goog.net.XhrIo] Object is active with another request\x3d"+this.Rc+"; newUri\x3d"+a);b=b?b.toUpperCase():"GET";this.Rc=a;this.Qc="";this.wc=0;this.Je=b;this.Qd=!1;this.oc=!0;this.W=this.Ed?Yq(this.Ed):Yq(Wq);this.Dd=this.Ed?Uq(this.Ed):Uq(Wq);this.W.onreadystatechange=ma(this.Oe,this);try{Bo(this.Va,fr(this,"Opening Xhr")),this.Vd=!0,this.W.open(b,String(a),!0),this.Vd=!1}catch(d){Bo(this.Va,fr(this,"Error opening Xhr: "+d.message));this.nd(5,d);return}a=
c||"";var g=this.headers.clone();e&&co(e,function(a,b){g.set(b,a)});e=ab(g.Sa());c=ba.FormData&&a instanceof ba.FormData;!(0<=Va(er,b))||e||c||g.set("Content-Type","application/x-www-form-urlencoded;charset\x3dutf-8");g.forEach(function(a,b){this.W.setRequestHeader(b,a)},this);this.Re&&(this.W.responseType=this.Re);"withCredentials"in this.W&&(this.W.withCredentials=this.Ze);try{gr(this),0<this.jc&&(this.Cd=hr(this.W),Bo(this.Va,fr(this,"Will abort after "+this.jc+"ms if incomplete, xhr2 "+this.Cd)),
this.Cd?(this.W.timeout=this.jc,this.W.ontimeout=ma(this.Ue,this)):this.yd=kp(this.Ue,this.jc,this)),Bo(this.Va,fr(this,"Sending request")),this.sd=!0,this.W.send(a),this.sd=!1}catch(f){Bo(this.Va,fr(this,"Send error: "+f.message)),this.nd(5,f)}};function hr(a){return On&&Vn(9)&&"number"==typeof a.timeout&&void 0!==a.ontimeout}function bb(a){return"content-type"==a.toLowerCase()}
k.Ue=function(){"undefined"!=typeof aa&&this.W&&(this.Qc="Timed out after "+this.jc+"ms, aborting",this.wc=8,Bo(this.Va,fr(this,this.Qc)),this.dispatchEvent("timeout"),this.abort(8))};k.nd=function(a,b){this.oc=!1;this.W&&(this.Nc=!0,this.W.abort(),this.Nc=!1);this.Qc=b;this.wc=a;ir(this);jr(this)};function ir(a){a.Qd||(a.Qd=!0,a.dispatchEvent("complete"),a.dispatchEvent("error"))}
k.abort=function(a){this.W&&this.oc&&(Bo(this.Va,fr(this,"Aborting")),this.oc=!1,this.Nc=!0,this.W.abort(),this.Nc=!1,this.wc=a||7,this.dispatchEvent("complete"),this.dispatchEvent("abort"),jr(this))};k.Oe=function(){this.Bf||(this.Vd||this.sd||this.Nc?kr(this):this.Mf())};k.Mf=function(){kr(this)};
function kr(a){if(a.oc&&"undefined"!=typeof aa)if(a.Dd[1]&&4==lr(a)&&2==mr(a))Bo(a.Va,fr(a,"Local request error detected and ignored"));else if(a.sd&&4==lr(a))kp(a.Oe,0,a);else if(a.dispatchEvent("readystatechange"),4==lr(a)){Bo(a.Va,fr(a,"Request complete"));a.oc=!1;try{if(nr(a))a.dispatchEvent("complete"),a.dispatchEvent("success");else{a.wc=6;var b;try{b=2<lr(a)?a.W.statusText:""}catch(c){Bo(a.Va,"Can not get status: "+c.message),b=""}a.Qc=b+" ["+mr(a)+"]";ir(a)}}finally{jr(a)}}}
function jr(a){if(a.W){gr(a);var b=a.W,c=a.Dd[0]?da:null;a.W=null;a.Dd=null;a.dispatchEvent("ready");try{b.onreadystatechange=c}catch(e){(a=a.Va)&&a.log(qo,"Problem encountered resetting onreadystatechange: "+e.message,void 0)}}}function gr(a){a.W&&a.Cd&&(a.W.ontimeout=null);"number"==typeof a.yd&&(ba.clearTimeout(a.yd),a.yd=null)}
function nr(a){var b=mr(a),c;a:switch(b){case 200:case 201:case 202:case 204:case 206:case 304:case 1223:c=!0;break a;default:c=!1}if(!c){if(b=0===b)a=$q(String(a.Rc))[1]||null,!a&&self.location&&(a=self.location.protocol,a=a.substr(0,a.length-1)),b=!dr.test(a?a.toLowerCase():"");c=b}return c}function lr(a){return a.W?a.W.readyState:0}function mr(a){try{return 2<lr(a)?a.W.status:-1}catch(b){return-1}}k.getAllResponseHeaders=function(){return this.W&&4==lr(this)?this.W.getAllResponseHeaders():""};
function fr(a,b){return b+" ["+a.Je+" "+a.Rc+" "+mr(a)+"]"};Ha("area base br col command embed hr img input keygen link meta param source track wbr".split(" "));Ha("action","cite","data","formaction","href","manifest","poster","src");Ha("link","script","style");var or=0,pr=(8|3&Math.round(14*Math.random())).toString(16),qr="transit$guid$"+(Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+"-"+Sp()+Sp()+Sp()+Sp()+"-4"+Sp()+Sp()+Sp()+"-"+pr+Sp()+Sp()+Sp()+"-"+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp()+Sp());
function rr(a){if(null==a)return"null";if(a===String)return"string";if(a===Boolean)return"boolean";if(a===Number)return"number";if(a===Array)return"array";if(a===Object)return"map";var b=a[qr];null==b&&("undefined"!=typeof Object.defineProperty?(b=++or,Object.defineProperty(a,qr,{value:b,enumerable:!1})):a[qr]=b=++or);return b}function sr(a,b){for(var c=a.toString(),e=c.length;e<b;e++)c="0"+c;return c}function tr(){}tr.prototype.tag=function(){return"_"};tr.prototype.N=function(){return null};
tr.prototype.ga=function(){return"null"};function ur(){}ur.prototype.tag=function(){return"s"};ur.prototype.N=function(a){return a};ur.prototype.ga=function(a){return a};function vr(){}vr.prototype.tag=function(){return"i"};vr.prototype.N=function(a){return a};vr.prototype.ga=function(a){return a.toString()};function wr(){}wr.prototype.tag=function(){return"i"};wr.prototype.N=function(a){return a.toString()};wr.prototype.ga=function(a){return a.toString()};function xr(){}xr.prototype.tag=function(){return"?"};
xr.prototype.N=function(a){return a};xr.prototype.ga=function(a){return a.toString()};function yr(){}yr.prototype.tag=function(){return"array"};yr.prototype.N=function(a){return a};yr.prototype.ga=function(){return null};function zr(){}zr.prototype.tag=function(){return"map"};zr.prototype.N=function(a){return a};zr.prototype.ga=function(){return null};function Ar(){}Ar.prototype.tag=function(){return"t"};
Ar.prototype.N=function(a){return a.getUTCFullYear()+"-"+sr(a.getUTCMonth()+1,2)+"-"+sr(a.getUTCDate(),2)+"T"+sr(a.getUTCHours(),2)+":"+sr(a.getUTCMinutes(),2)+":"+sr(a.getUTCSeconds(),2)+"."+sr(a.getUTCMilliseconds(),3)+"Z"};Ar.prototype.ga=function(a,b){return b.N(a)};function Br(){}Br.prototype.tag=function(){return"m"};Br.prototype.N=function(a){return a.valueOf()};Br.prototype.ga=function(a){return a.valueOf().toString()};function Cr(){}Cr.prototype.tag=function(){return"u"};Cr.prototype.N=function(a){return a.toString()};
Cr.prototype.ga=function(a){return a.toString()};function Dr(){}Dr.prototype.tag=function(){return":"};Dr.prototype.N=function(a){return a.name};Dr.prototype.ga=function(a,b){return b.N(a)};function Er(){}Er.prototype.tag=function(){return"$"};Er.prototype.N=function(a){return a.name};Er.prototype.ga=function(a,b){return b.N(a)};function Fr(){}Fr.prototype.tag=function(a){return a.tag};Fr.prototype.N=function(a){return a.N};Fr.prototype.ga=function(){return null};function Gr(){}Gr.prototype.tag=function(){return"set"};
Gr.prototype.N=function(a){var b=[];a.forEach(function(a){b.push(a)});return yq("array",b)};Gr.prototype.ga=function(){return null};function Hr(){}Hr.prototype.tag=function(){return"map"};Hr.prototype.N=function(a){return a};Hr.prototype.ga=function(){return null};function Ir(){}Ir.prototype.tag=function(){return"map"};Ir.prototype.N=function(a){return a};Ir.prototype.ga=function(){return null};function Jr(){}Jr.prototype.tag=function(){return"b"};Jr.prototype.N=function(a){return a.toString("base64")};
Jr.prototype.ga=function(){return null};function Kr(){}Kr.prototype.tag=function(){return"b"};
Kr.prototype.N=function(a){for(var b=0,c=a.length,e="",d=null;b<c;)d=a.subarray(b,Math.min(b+32768,c)),e+=String.fromCharCode.apply(null,d),b+=32768;var g;a=e;if("undefined"!=typeof btoa)g=btoa(a);else{a=String(a);c=0;e="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/\x3d";for(d="";a.charAt(c|0)||(e="\x3d",c%1);d+=e.charAt(63&g>>8-c%1*8)){b=a.charCodeAt(c+=.75);if(255<b)throw Error("'btoa' failed: The string to be encoded contains characters outside of the Latin1 range.");g=g<<8|
b}g=d}return g};Kr.prototype.ga=function(){return null};
function Lr(){this.qa={};this.set(null,new tr);this.set(String,new ur);this.set(Number,new vr);this.set(eq,new wr);this.set(Boolean,new xr);this.set(Array,new yr);this.set(Object,new zr);this.set(Date,new Br);this.set(Eq,new Cr);this.set(Bq,new Dr);this.set(Cq,new Er);this.set(xq,new Fr);this.set(Mq,new Gr);this.set(Jq,new Hr);this.set(Iq,new Ir);"undefined"!=typeof Buffer&&this.set(Buffer,new Jr);"undefined"!=typeof Uint8Array&&this.set(Uint8Array,new Kr)}
Lr.prototype.get=function(a){var b=null,b="string"===typeof a?this.qa[a]:this.qa[rr(a)];return null!=b?b:this.qa["default"]};Lr.prototype.set=function(a,b){var c;if(c="string"===typeof a)a:{switch(a){case "null":case "string":case "boolean":case "number":case "array":case "map":c=!1;break a}c=!0}c?this.qa[a]=b:this.qa[rr(a)]=b};function Mr(a){this.gc=a||{};this.ud=null!=this.gc.preferStrings?this.gc.preferStrings:!0;this.Le=this.gc.objectBuilder||null;this.qa=new Lr;if(a=this.gc.handlers){if(Rp(a)||!a.forEach)throw Error('transit writer "handlers" option must be a map');var b=this;a.forEach(function(a,e){b.qa.set(e,a)})}this.Bd=this.gc.unpack||function(a){return a instanceof Jq&&null===a.T?a.da:!1};this.Uc=this.gc&&this.gc.verbose||!1}
Mr.prototype.Fa=function(a){var b=this.qa.get(null==a?null:a.constructor);return null!=b?b:(a=a&&a.transitTag)?this.qa.get(a):null};function Nr(a,b,c,e,d){a=a+b+c;return d?d.write(a,e):a}function Or(a,b,c){var e=[];if(Rp(b))for(var d=0;d<b.length;d++)e.push(Pr(a,b[d],!1,c));else b.forEach(function(b){e.push(Pr(a,b,!1,c))});return e}function Qr(a,b){if("string"!==typeof b){var c=a.Fa(b);return c&&1===c.tag(b).length}return!0}
function Rr(a,b){var c=a.Bd(b),e=!0;if(c){for(var d=0;d<c.length&&(e=Qr(a,c[d]),e);d+=2);return e}if(b.keys&&(c=b.keys(),c.next)){for(step=c.next();!step.done;){e=Qr(a,step.value);if(!e)break;step=c.next()}return e}if(b.forEach)return b.forEach(function(b,c){e=e&&Qr(a,c)}),e;throw Error("Cannot walk keys of object type "+(null==b?null:b.constructor).name);}
function Sr(a,b,c){if(b.constructor===Object||null!=b.forEach){if(a.Uc){if(null!=b.forEach)if(Rr(a,b)){var e={};b.forEach(function(b,d){e[Pr(a,d,!0,!1)]=Pr(a,b,!1,c)})}else{var d=a.Bd(b),g=[],f=Nr("~#","cmap","",!0,c);if(d)for(var h=0;h<d.length;h+=2)g.push(Pr(a,d[h],!0,!1)),g.push(Pr(a,d[h+1],!1,c));else b.forEach(function(b,d){g.push(Pr(a,d,!0,!1));g.push(Pr(a,b,!1,c))});e={};e[f]=g}else for(e={},d=Qp(b),h=0;h<d.length;h++)e[Pr(a,d[h],!0,!1)]=Pr(a,b[d[h]],!1,c);return e}if(null!=b.forEach){if(Rr(a,
b)){d=a.Bd(b);e=["^ "];if(d)for(h=0;h<d.length;h+=2)e.push(Pr(a,d[h],!0,c)),e.push(Pr(a,d[h+1],!1,c));else b.forEach(function(b,d){e.push(Pr(a,d,!0,c));e.push(Pr(a,b,!1,c))});return e}d=a.Bd(b);g=[];f=Nr("~#","cmap","",!0,c);if(d)for(h=0;h<d.length;h+=2)g.push(Pr(a,d[h],!0,c)),g.push(Pr(a,d[h+1],!1,c));else b.forEach(function(b,d){g.push(Pr(a,d,!0,c));g.push(Pr(a,b,!1,c))});return[f,g]}e=["^ "];d=Qp(b);for(h=0;h<d.length;h++)e.push(Pr(a,d[h],!0,c)),e.push(Pr(a,b[d[h]],!1,c));return e}if(null!=a.Le)return a.Le(b,
function(b){return Pr(a,b,!0,c)},function(b){return Pr(a,b,!1,c)});h=(null==b?null:b.constructor).name;d=Error("Cannot write "+h);d.data={Yd:b,type:h};throw d;}
function Pr(a,b,c,e){var d=a.Fa(b),g=d?d.tag(b):null,f=d?d.N(b):null;if(null!=d&&null!=g)switch(g){case "_":return c?Nr("~","_","",c,e):null;case "s":return 0<f.length?(a=f.charAt(0),a="~"===a||"^"===a||"`"===a?"~"+f:f):a=f,Nr("","",a,c,e);case "?":return c?Nr("~","?",f.toString()[0],c,e):f;case "i":return Infinity===f?Nr("~","z","INF",c,e):-Infinity===f?Nr("~","z","-INF",c,e):isNaN(f)?Nr("~","z","NaN",c,e):c||"string"===typeof f||f instanceof eq?Nr("~","i",f.toString(),c,e):f;case "d":return c?Nr(f.Yf,
"d",f,c,e):f;case "b":return Nr("~","b",f,c,e);case "'":return a.Uc?(b={},c=Nr("~#","'","",!0,e),b[c]=Pr(a,f,!1,e),e=b):e=[Nr("~#","'","",!0,e),Pr(a,f,!1,e)],e;case "array":return Or(a,f,e);case "map":return Sr(a,f,e);default:a:{if(1===g.length){if("string"===typeof f){e=Nr("~",g,f,c,e);break a}if(c||a.ud){(a=a.Uc&&new Ar)?(g=a.tag(b),f=a.ga(b,a)):f=d.ga(b,d);if(null!==f){e=Nr("~",g,f,c,e);break a}e=Error('Tag "'+g+'" cannot be encoded as string');e.data={tag:g,N:f,Yd:b};throw e;}}b=g;c=f;a.Uc?(f=
{},f[Nr("~#",b,"",!0,e)]=Pr(a,c,!1,e),e=f):e=[Nr("~#",b,"",!0,e),Pr(a,c,!1,e)]}return e}else throw e=(null==b?null:b.constructor).name,a=Error("Cannot write "+e),a.data={Yd:b,type:e},a;}function Tr(a,b){var c=a.Fa(b);if(null!=c)return 1===c.tag(b).length?yq("'",b):b;var c=(null==b?null:b.constructor).name,e=Error("Cannot write "+c);e.data={Yd:b,type:c};throw e;}function Ur(a,b){this.Ac=a;this.options=b||{};this.Ha=!1===this.options.cache?null:this.options.cache?this.options.cache:new Vp}
Ur.prototype.Ff=function(){return this.Ac};Ur.prototype.marshaller=Ur.prototype.Ff;Ur.prototype.write=function(a,b){var c=null,e=b||{},c=e.asMapKey||!1,d=this.Ac.Uc?!1:this.Ha;!1===e.marshalTop?c=Pr(this.Ac,a,c,d):(e=this.Ac,c=JSON.stringify(Pr(e,Tr(e,a),c,d)));null!=this.Ha&&this.Ha.clear();return c};Ur.prototype.write=Ur.prototype.write;Ur.prototype.register=function(a,b){this.Ac.qa.set(a,b)};Ur.prototype.register=Ur.prototype.register;function Vr(a,b){if("json"===a||"json-verbose"===a||null==a){var c=new Rq(b);return new Sq(c,b)}throw Error("Cannot create reader of type "+a);}function Wr(a,b){if("json"===a||"json-verbose"===a||null==a){"json-verbose"===a&&(null==b&&(b={}),b.verbose=!0);var c=new Mr(b);return new Ur(c,b)}c=Error('Type must be "json"');c.data={type:a};throw c;};ph.prototype.G=function(a,b){return b instanceof ph?this.Ub===b.Ub:b instanceof Eq?this.Ub===b.toString():!1};xq.prototype.G=function(a,b){return this.equiv(b)};Eq.prototype.G=function(a,b){return b instanceof ph?lc(b,this):this.equiv(b)};eq.prototype.G=function(a,b){return this.equiv(b)};xq.prototype.Ld=!0;xq.prototype.L=function(){return cq.h?cq.h(this):cq.call(null,this)};Eq.prototype.Ld=!0;Eq.prototype.L=function(){return cq.h?cq.h(this):cq.call(null,this)};eq.prototype.Ld=!0;
eq.prototype.L=function(){return cq.h?cq.h(this):cq.call(null,this)};function Xr(a,b){for(var c=B(Bd(b)),e=null,d=0,g=0;;)if(g<d){var f=e.s(null,g);a[f]=b[f];g+=1}else if(c=B(c))e=c,R(e)?(c=y(e),g=A(e),e=c,d=N(c),c=g):(c=D(e),a[c]=b[c],c=I(e),e=null,d=0),g=0;else break;return a}function Yr(){}Yr.prototype.vc=function(){return uc(Qf)};Yr.prototype.add=function(a,b,c){return ve.j(a,b,c)};Yr.prototype.od=function(a){return wc(a)};
Yr.prototype.bc=function(a){return Tf.j?Tf.j(a,!0,!0):Tf.call(null,a,!0,!0)};function Zr(){}Zr.prototype.vc=function(){return uc(nd)};Zr.prototype.add=function(a,b){return ue.e(a,b)};Zr.prototype.od=function(a){return wc(a)};Zr.prototype.bc=function(a){return qf.e?qf.e(a,!0):qf.call(null,a,!0)};
var $r=function(){function a(a,b){var c=ce(a),f=Xr({prefersStrings:!1,arrayBuilder:new Zr,mapBuilder:new Yr,handlers:jh(Jg.k(L([new n(null,5,["$",function(){return function(a){return Tc.h(a)}}(c),":",function(){return function(a){return de.h(a)}}(c),"set",function(){return function(a){return Se.e(Mg,a)}}(c),"list",function(){return function(a){return Se.e(G,a.reverse())}}(c),"cmap",function(){return function(a){for(var b=0,c=uc(Qf);;)if(b<a.length)var d=b+2,c=ve.j(c,a[b],a[b+1]),b=d;else return wc(c)}}(c)],
null),Oi.h(b)],0)))},jh(sd.e(b,Oi)));return Vr.e?Vr.e(c,f):Vr.call(null,c,f)}function b(a){return c.e(a,null)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();function as(){}as.prototype.tag=function(){return":"};as.prototype.N=function(a){return a.La};as.prototype.ga=function(a){return a.La};function bs(){}bs.prototype.tag=function(){return"$"};bs.prototype.N=function(a){return a.xa};
bs.prototype.ga=function(a){return a.xa};function cs(){}cs.prototype.tag=function(){return"list"};cs.prototype.N=function(a){var b=[];a=B(a);for(var c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d);b.push(g);d+=1}else if(a=B(a))c=a,R(c)?(a=y(c),d=A(c),c=a,e=N(a),a=d):(a=D(c),b.push(a),a=I(c),c=null,e=0),d=0;else break;return yq.e?yq.e("array",b):yq.call(null,"array",b)};cs.prototype.ga=function(){return null};function ds(){}ds.prototype.tag=function(){return"map"};ds.prototype.N=function(a){return a};
ds.prototype.ga=function(){return null};function es(){}es.prototype.tag=function(){return"set"};es.prototype.N=function(a){var b=[];a=B(a);for(var c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d);b.push(g);d+=1}else if(a=B(a))c=a,R(c)?(a=y(c),d=A(c),c=a,e=N(a),a=d):(a=D(c),b.push(a),a=I(c),c=null,e=0),d=0;else break;return yq.e?yq.e("array",b):yq.call(null,"array",b)};es.prototype.ga=function(){return null};function fs(){}fs.prototype.tag=function(){return"array"};
fs.prototype.N=function(a){var b=[];a=B(a);for(var c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d);b.push(g);d+=1}else if(a=B(a))c=a,R(c)?(a=y(c),d=A(c),c=a,e=N(a),a=d):(a=D(c),b.push(a),a=I(c),c=null,e=0),d=0;else break;return b};fs.prototype.ga=function(){return null};function gs(){}gs.prototype.tag=function(){return"u"};gs.prototype.N=function(a){return a.Ub};gs.prototype.ga=function(a){return this.N(a)};
var hs=function(){function a(a,b){var c=new as,f=new bs,h=new cs,m=new ds,p=new es,q=new fs,r=new gs,u=Jg.k(L([qd([kg,$d,n,ig,Af,C,V,Xd,ee,uf,zf,jg,Ig,Kf,T,Wd,kd,Kg,Eg,Hg,sf,Ng,je,Sc,ph,Rg,pg],[m,h,m,h,h,h,c,h,h,q,h,h,h,h,q,h,h,p,m,h,h,p,h,f,r,h,h]),Oi.h(b)],0)),z=ce(a),E=Xr({unpack:function(){return function(a){return a instanceof n?a.o:!1}}(z,c,f,h,m,p,q,r,u),handlers:function(){var a=Bb(u);a.forEach=function(){return function(a){for(var b=B(this),c=null,d=0,e=0;;)if(e<d){var f=c.s(null,e),g=O.j(f,
0,null),f=O.j(f,1,null);a.e?a.e(f,g):a.call(null,f,g);e+=1}else if(b=B(b))R(b)?(c=y(b),b=A(b),g=c,d=N(c),c=g):(c=D(b),g=O.j(c,0,null),c=f=O.j(c,1,null),a.e?a.e(c,g):a.call(null,c,g),b=I(b),c=null,d=0),e=0;else return null}}(a,z,c,f,h,m,p,q,r,u);return a}(),objectBuilder:function(a,b,c,d,e,f,g,h,m){return function(p,q,r){return Ld(function(){return function(a,b,c){a.push(q.h?q.h(b):q.call(null,b),r.h?r.h(c):r.call(null,c));return a}}(a,b,c,d,e,f,g,h,m),p)}}(z,c,f,h,m,p,q,r,u)},jh(sd.e(b,Oi)));return Wr.e?
Wr.e(z,E):Wr.call(null,z,E)}function b(a){return c.e(a,null)}var c=null,c=function(c,d){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,d)}throw Error("Invalid arity: "+arguments.length);};c.h=b;c.e=a;return c}();var is=null,js=Pn||Qn||Nn||"function"==typeof ba.atob;function ks(a){var b;if(s(a))if(js)b=ba.btoa(a);else{b=[];for(var c=0,e=0;e<a.length;e++){for(var d=a.charCodeAt(e);255<d;)b[c++]=d&255,d>>=8;b[c++]=d}if(!ea(b))throw Error("encodeByteArray takes an array as a parameter");if(!is)for(is={},a=0;65>a;a++)is[a]="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/\x3d".charAt(a);a=is;c=[];for(e=0;e<b.length;e+=3){var g=b[e],f=(d=e+1<b.length)?b[e+1]:0,h=e+2<b.length,m=h?b[e+2]:0,p=g>>2,g=(g&3)<<4|f>>4,f=(f&15)<<2|m>>6,m=m&63;h||(m=64,d||(f=
64));c.push(a[p],a[g],a[f],a[m])}b=c.join("")}else b=null;return b}
var ls=function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){O.j(b,0,null);var d;s(a)?(d=""+w(a),d=encodeURIComponent(d).replace(new RegExp("*".replace(/([-()\[\]{}+?*.$\^|,:#<!\\])/g,"\\$1").replace(/\x08/g,"\\x08"),"g"),"%2A")):d=null;return d}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}(),ms=function(){function a(a,e){var d=null;if(1<
arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){O.j(b,0,null);return s(a)?decodeURIComponent(a):null}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}();qd("TKGMYZEBP".split(""),[Math.pow(1024,4),Math.pow(1024,1),Math.pow(1024,3),Math.pow(1024,2),Math.pow(1024,8),Math.pow(1024,7),Math.pow(1024,6),Math.pow(1024,0),Math.pow(1024,5)]);function ns(a,b){var c;if(a instanceof ns)this.Ta=void 0!==b?b:a.Ta,os(this,a.Hb),c=a.lc,ps(this),this.lc=c,qs(this,a.Ob),rs(this,a.hc),ss(this,a.Gb),ts(this,a.mb.clone()),c=a.ac,ps(this),this.ac=c;else if(a&&(c=$q(String(a)))){this.Ta=!!b;os(this,c[1]||"",!0);var e=c[2]||"";ps(this);this.lc=us(e);qs(this,c[3]||"",!0);rs(this,c[4]);ss(this,c[5]||"",!0);ts(this,c[6]||"",!0);c=c[7]||"";ps(this);this.ac=us(c)}else this.Ta=!!b,this.mb=new vs(null,0,this.Ta)}k=ns.prototype;k.Hb="";k.lc="";k.Ob="";
k.hc=null;k.Gb="";k.ac="";k.Ef=!1;k.Ta=!1;k.toString=function(){var a=[],b=this.Hb;b&&a.push(ws(b,xs),":");if(b=this.Ob){a.push("//");var c=this.lc;c&&a.push(ws(c,xs),"@");a.push(encodeURIComponent(String(b)));b=this.hc;null!=b&&a.push(":",String(b))}if(b=this.Gb)this.Ob&&"/"!=b.charAt(0)&&a.push("/"),a.push(ws(b,"/"==b.charAt(0)?ys:zs));(b=this.mb.toString())&&a.push("?",b);(b=this.ac)&&a.push("#",ws(b,As));return a.join("")};
k.resolve=function(a){var b=this.clone(),c=!!a.Hb;c?os(b,a.Hb):c=!!a.lc;if(c){var e=a.lc;ps(b);b.lc=e}else c=!!a.Ob;c?qs(b,a.Ob):c=null!=a.hc;e=a.Gb;if(c)rs(b,a.hc);else if(c=!!a.Gb){if("/"!=e.charAt(0))if(this.Ob&&!this.Gb)e="/"+e;else{var d=b.Gb.lastIndexOf("/");-1!=d&&(e=b.Gb.substr(0,d+1)+e)}d=e;if(".."==d||"."==d)e="";else if(-1!=d.indexOf("./")||-1!=d.indexOf("/.")){for(var e=0==d.lastIndexOf("/",0),d=d.split("/"),g=[],f=0;f<d.length;){var h=d[f++];"."==h?e&&f==d.length&&g.push(""):".."==h?
((1<g.length||1==g.length&&""!=g[0])&&g.pop(),e&&f==d.length&&g.push("")):(g.push(h),e=!0)}e=g.join("/")}else e=d}c?ss(b,e):c=""!==a.mb.toString();c?ts(b,us(a.mb.toString())):c=!!a.ac;c&&(a=a.ac,ps(b),b.ac=a);return b};k.clone=function(){return new ns(this)};function os(a,b,c){ps(a);a.Hb=c?us(b):b;a.Hb&&(a.Hb=a.Hb.replace(/:$/,""))}function qs(a,b,c){ps(a);a.Ob=c?us(b):b}function rs(a,b){ps(a);if(b){b=Number(b);if(isNaN(b)||0>b)throw Error("Bad port number "+b);a.hc=b}else a.hc=null}
function ss(a,b,c){ps(a);a.Gb=c?us(b):b}function ts(a,b,c){ps(a);b instanceof vs?(a.mb=b,a.mb.$d(a.Ta)):(c||(b=ws(b,Bs)),a.mb=new vs(b,0,a.Ta))}function ps(a){if(a.Ef)throw Error("Tried to modify a read-only Uri");}k.$d=function(a){this.Ta=a;this.mb&&this.mb.$d(a);return this};function us(a){return a?decodeURIComponent(a):""}function ws(a,b){return fa(a)?encodeURI(a).replace(b,Cs):null}function Cs(a){a=a.charCodeAt(0);return"%"+(a>>4&15).toString(16)+(a&15).toString(16)}
var xs=/[#\/\?@]/g,zs=/[\#\?:]/g,ys=/[\#\?]/g,Bs=/[\#\?@]/g,As=/#/g;function vs(a,b,c){this.Ka=a||null;this.Ta=!!c}function Ds(a){if(!a.oa&&(a.oa=new eo,a.ja=0,a.Ka))for(var b=a.Ka.split("\x26"),c=0;c<b.length;c++){var e=b[c].indexOf("\x3d"),d=null,g=null;0<=e?(d=b[c].substring(0,e),g=b[c].substring(e+1)):d=b[c];d=decodeURIComponent(d.replace(/\+/g," "));d=Es(a,d);a.add(d,g?decodeURIComponent(g.replace(/\+/g," ")):"")}}k=vs.prototype;k.oa=null;k.ja=null;k.Fe=function(){Ds(this);return this.ja};
k.add=function(a,b){Ds(this);this.Ka=null;a=Es(this,a);var c=this.oa.get(a);c||this.oa.set(a,c=[]);c.push(b);this.ja++;return this};k.remove=function(a){Ds(this);a=Es(this,a);return this.oa.Kc(a)?(this.Ka=null,this.ja-=this.oa.get(a).length,this.oa.remove(a)):!1};k.clear=function(){this.oa=this.Ka=null;this.ja=0};k.Wd=function(){Ds(this);return 0==this.ja};k.Kc=function(a){Ds(this);a=Es(this,a);return this.oa.Kc(a)};
k.Sa=function(){Ds(this);for(var a=this.oa.Eb(),b=this.oa.Sa(),c=[],e=0;e<b.length;e++)for(var d=a[e],g=0;g<d.length;g++)c.push(b[e]);return c};k.Eb=function(a){Ds(this);var b=[];if(fa(a))this.Kc(a)&&(b=cb(b,this.oa.get(Es(this,a))));else{a=this.oa.Eb();for(var c=0;c<a.length;c++)b=cb(b,a[c])}return b};k.set=function(a,b){Ds(this);this.Ka=null;a=Es(this,a);this.Kc(a)&&(this.ja-=this.oa.get(a).length);this.oa.set(a,[b]);this.ja++;return this};
k.get=function(a,b){var c=a?this.Eb(a):[];return 0<c.length?String(c[0]):b};k.toString=function(){if(this.Ka)return this.Ka;if(!this.oa)return"";for(var a=[],b=this.oa.Sa(),c=0;c<b.length;c++)for(var e=b[c],d=encodeURIComponent(String(e)),e=this.Eb(e),g=0;g<e.length;g++){var f=d;""!==e[g]&&(f+="\x3d"+encodeURIComponent(String(e[g])));a.push(f)}return this.Ka=a.join("\x26")};k.clone=function(){var a=new vs;a.Ka=this.Ka;this.oa&&(a.oa=this.oa.clone(),a.ja=this.ja);return a};
function Es(a,b){var c=String(b);a.Ta&&(c=c.toLowerCase());return c}k.$d=function(a){a&&!this.Ta&&(Ds(this),this.Ka=null,this.oa.forEach(function(a,c){var e=c.toLowerCase();c!=e&&(this.remove(c),this.remove(e),0<a.length&&(this.Ka=null,this.oa.set(Es(this,e),db(a)),this.ja+=a.length))},this));this.Ta=a};function Fs(a){var b=Fd(a)?Q.e(Ee,a):a;a=P.e(b,qk);var c=P.e(b,bk),e=P.e(b,bj),d=P.e(b,ik),b=P.e(b,Hk),g=w,f=new ns;os(f,ce(s(b)?b:Qj));qs(f,d);rs(f,e);ss(f,c);ts(f,a,!0);return""+g(f)}function Gs(a){return rm.e("-",Ne.e(um,wm.e(""+w(a),/-/)))}function Hs(a){return jh(Pg(Ne.e(Gs,Nf(a)),Of(a)))}function Is(a,b,c){return hs.e(b,c).write(a)}function Js(a){a=sb(ra(a))?JSON.parse(a):null;return s(a)?oh.k(a,L([nh,!0],0)):null}function Ks(a){a=jh(a);return JSON.stringify(a)}
function Ls(a){return yb.j(function(a,c){var e=wm.e(c,/:\s+/),d=O.j(e,0,null),e=O.j(e,1,null),g=ra(d);return s(s(g)?g:ra(e))?a:rd.j(a,tm(d),e)},Qf,wm.e(s(a)?a:"",/(\n)|(\r)|(\r\n)|(\n\r)/))};var Ms,Ns=Qf;Ms=Ge.h?Ge.h(Ns):Ge.call(null,Ns);yo("goog.messaging.AbstractChannel");Se.e(Qf,Ne.e(function(a){var b=O.j(a,0,null);a=O.j(a,1,null);return new T(null,2,5,U,[de.h(b.toLowerCase()),a],null)},Jg.k(L([oh.h({Wf:"complete",ng:"success",ERROR:"error",Tf:"abort",jg:"ready",kg:"readystatechange",TIMEOUT:"timeout",$f:"incrementaldata",ig:"progress"})],0))));
var Os=function(){function a(a,b,c,d,e,g){if(a?a.xe:a)return a.xe(0,b,c,d,e,g);var u;u=Os[l(null==a?null:a)];if(!u&&(u=Os._,!u))throw v("IConnection.transmit",a);return u.call(null,a,b,c,d,e,g)}function b(a,b,c,d,e){if(a?a.we:a)return a.we(0,b,c,d,e);var g;g=Os[l(null==a?null:a)];if(!g&&(g=Os._,!g))throw v("IConnection.transmit",a);return g.call(null,a,b,c,d,e)}function c(a,b,c,d){if(a?a.ve:a)return a.ve(0,b,c,d);var e;e=Os[l(null==a?null:a)];if(!e&&(e=Os._,!e))throw v("IConnection.transmit",a);return e.call(null,
a,b,c,d)}function e(a,b,c){if(a?a.ue:a)return a.ue(0,b,c);var d;d=Os[l(null==a?null:a)];if(!d&&(d=Os._,!d))throw v("IConnection.transmit",a);return d.call(null,a,b,c)}function d(a,b){if(a?a.te:a)return a.te(0,b);var c;c=Os[l(null==a?null:a)];if(!c&&(c=Os._,!c))throw v("IConnection.transmit",a);return c.call(null,a,b)}var g=null,g=function(f,g,m,p,q,r){switch(arguments.length){case 2:return d.call(this,f,g);case 3:return e.call(this,f,g,m);case 4:return c.call(this,f,g,m,p);case 5:return b.call(this,
f,g,m,p,q);case 6:return a.call(this,f,g,m,p,q,r)}throw Error("Invalid arity: "+arguments.length);};g.e=d;g.j=e;g.D=c;g.O=b;g.ia=a;return g}();k=br.prototype;k.te=function(a,b){return Os.ia(this,b,"GET",null,null,1E4)};k.ue=function(a,b,c){return Os.ia(this,b,c,null,null,1E4)};k.ve=function(a,b,c,e){return Os.ia(this,b,c,e,null,1E4)};k.we=function(a,b,c,e,d){return Os.ia(this,b,c,e,d,1E4)};k.xe=function(a,b,c,e,d,g){this.jc=Math.max(0,g);return this.send(b,c,e,d)};
Se.e(Qf,Ne.e(function(a){var b=O.j(a,0,null);a=O.j(a,1,null);return new T(null,2,5,U,[de.h(b.toLowerCase()),a],null)},oh.h({Vf:"cn",Uf:"at",lg:"rat",hg:"pu",Zf:"ifrid",og:"tp",bg:"lru",gg:"pru",ag:"lpu",fg:"ppu",eg:"ph",dg:"osh",mg:"role",cg:"nativeProtocolVersion",Xf:"directSyncMode"})));function Ps(a){return sb(ra(a))?yb.j(function(a,c){var e=wm.e(c,/=/),d=O.j(e,0,null),e=O.j(e,1,null);return rd.j(a,de.h(ms(d)),ms(e))},Qf,wm.e(""+w(a),/&/)):null}function Qs(a){if(sb(ra(a))){a=a instanceof ns?a.clone():new ns(a,void 0);var b=a.mb,c=de.h(a.Hb),e=a.hc;return new n(null,6,[Hk,c,ik,a.Ob,bj,s(s(e)?0<e:e)?e:null,bk,a.Gb,qk,sb(b.Wd())?""+w(b):null,Mj,sb(b.Wd())?Ps(""+w(b)):null],null)}return null}function Rs(a,b){return[w(ls(ce(a))),w("\x3d"),w(ls(""+w(b)))].join("")}
function Ss(a,b){return rm.e("\x26",Ne.e(function(b){return Rs(a,b)},b))}function Ts(a){var b=O.j(a,0,null);a=O.j(a,1,null);return xd(a)?Ss(b,a):Rs(b,a)}var Us=Pg("()*\x26^%$#!+",Ne.e(function(a){return[w("\\"),w(a)].join("")},"()*\x26^%$#!+"));function Vs(a,b,c,e){c=(e=xe.e(kj,e))?Wg(Xg([w("(?i)"),w(yb.e(w,Og.e(Us,c)))].join("")),""+w(P.j(aj.h(a),"content-type",""))):e;return s(c)?We.j(a,new T(null,1,5,U,[vk],null),b):a}var Ws=new n(null,4,[vh,lk,li,Qf,fk,lk,Bj,Qf],null);
function Xs(a){var b=new FormData;a=B(a);for(var c=null,e=0,d=0;;)if(d<e){var g=c.s(null,d),f=O.j(g,0,null),g=O.j(g,1,null);b.append(ce(f),g);d+=1}else if(a=B(a))R(a)?(e=y(a),a=A(a),c=e,e=N(e)):(e=D(a),c=O.j(e,0,null),e=O.j(e,1,null),b.append(ce(c),e),a=I(a),c=null,e=0),d=0;else break;return b}var Ys;
Ys=function(a){return function(b){var c=Gi.h(b);return s(c)?am.e(a.h?a.h(b):a.call(null,b),c):a.h?a.h(b):a.call(null,b)}}(function(a){return function(b){var c=Fd(b)?Q.e(Ee,b):b,e=P.e(c,Mj),d=Qs(Ij.h(c));return s(d)?(b=We.j(sd.e(Jg.k(L([c,d],0)),Ij),new T(null,1,5,U,[Mj],null),function(a,b,c,d,e,q){return function(a){return Jg.k(L([a,q],0))}}(d,d,b,c,c,e)),a.h?a.h(b):a.call(null,b)):a.h?a.h(c):a.call(null,c)}}(function(a){return function(b){var c=di.h(b);s(c)&&(b=rd.j(sd.e(b,di),Qk,c));return a.h?
a.h(b):a.call(null,b)}}(function(a){return function(b){var c=Rj.h(b);s(c)&&(b=Ve(sd.e(b,Rj),new T(null,2,5,U,[aj,"authorization"],null),[w("Bearer "),w(c)].join("")));return a.h?a.h(b):a.call(null,b)}}(function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){var d=O.j(b,0,null);return function(b,d){return function(b){var e,g=Bk.h(b);e=s(g)?g:d;if(null==e||sb(B(e)))return a.h?
a.h(b):a.call(null,b);b=sd.e(b,Bk);g=new T(null,2,5,U,[aj,"authorization"],null);if(s(e)){var q=zd(e)?Ne.e(e,new T(null,2,5,U,[wi,Zh],null)):e;e=O.j(q,0,null);q=O.j(q,1,null);e=[w("Basic "),w(ks([w(e),w(":"),w(q)].join("")))].join("")}else e=null;b=Ve(b,g,e);return a.h?a.h(b):a.call(null,b)}}(b,d)}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}()(function(a){return function(b){b=Fd(b)?Q.e(Ee,b):b;var c=P.e(b,Mj);s(c)&&(b=rd.j(sd.e(b,Mj),qk,rm.e("\x26",Ne.e(Ts,c))));return a.h?
a.h(b):a.call(null,b)}}(function(a){return function(b){return bm.e(function(a){return Vs(a,Js,"application/json",Qk.h(b))},new T(null,1,5,U,[a.h?a.h(b):a.call(null,b)],null))}}(function(a){return function(b){var c=Kh.h(b);s(c)&&(b=Ve(rd.j(sd.e(b,Kh),vk,Ks(c)),new T(null,2,5,U,[aj,"content-type"],null),"application/json"));return a.h?a.h(b):a.call(null,b)}}(function(a){return function(b){var c=Jg.k(L([Ws,pk.h(b)],0)),e=Fd(c)?Q.e(Ee,c):c,d=P.e(e,Bj),g=P.e(e,fk);return bm.e(function(a,c,d,e,g){return function(a){return Vs(a,
g,"application/transit+json",Qk.h(b))}}(c,e,d,g,function(a,b,c,d){return function(a){return $r.e(d,c).wd(a)}}(c,e,d,g)),new T(null,1,5,U,[a.h?a.h(b):a.call(null,b)],null))}}(function(a){return function(b){var c=ai.h(b);if(s(c)){var e=Jg.k(L([Ws,pk.h(b)],0)),d=Fd(e)?Q.e(Ee,e):e,e=P.e(d,li),d=P.e(d,vh);b=Ve(rd.j(sd.e(b,ai),vk,Is(c,d,e)),new T(null,2,5,U,[aj,"content-type"],null),"application/transit+json")}return a.h?a.h(b):a.call(null,b)}}(function(a){return function(b){return bm.e(function(a){return Vs(a,
nn,"application/edn",Qk.h(b))},new T(null,1,5,U,[a.h?a.h(b):a.call(null,b)],null))}}(function(a){return function(b){var c=zk.h(b);s(c)&&(b=Ve(rd.j(sd.e(b,zk),vk,Je.k(L([c],0))),new T(null,2,5,U,[aj,"content-type"],null),"application/edn"));return a.h?a.h(b):a.call(null,b)}}(function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){var d=O.j(b,0,null);return function(b,
d){return function(b){var e;e=Nj.h(b);e=s(e)?e:d;s(e)&&(b=Ve(b,new T(null,2,5,U,[aj,"content-type"],null),e));return a.h?a.h(b):a.call(null,b)}}(b,d)}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}()(function(a){return function(b){b=Fd(b)?Q.e(Ee,b):b;var c=P.e(b,Qk),e=P.e(b,Dk);s(s(e)?(new Kg(null,new n(null,4,[Qh,null,Ki,null,Tj,null,kk,null],null),null)).call(null,c):e)&&(b=Ve(rd.j(sd.e(b,Dk),vk,Xs(e)),new T(null,2,5,U,[aj,"content-type"],null),"multipart/form-data"));return a.h?
a.h(b):a.call(null,b)}}(function(a){return function(b){b=Fd(b)?Q.e(Ee,b):b;var c=P.e(b,Qk),e=P.e(b,qj);s(s(e)?(new Kg(null,new n(null,4,[Qh,null,Ki,null,Tj,null,kk,null],null),null)).call(null,c):e)&&(b=Ve(rd.j(sd.e(b,qj),vk,rm.e("\x26",Ne.e(Ts,e))),new T(null,2,5,U,[aj,"content-type"],null),"application/x-www-form-urlencoded"));return a.h?a.h(b):a.call(null,b)}}(function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;
d=new C(g,0)}return b.call(this,a,d)}function b(a,b){var d=O.j(b,0,null);return function(b,d){return function(b){var e;e=Wk.h(b);e=s(e)?e:d;s(e)&&(b=Ve(b,new T(null,2,5,U,[aj,"accept"],null),e));return a.h?a.h(b):a.call(null,b)}}(b,d)}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}()(function(a){var b=Fd(a)?Q.e(Ee,a):a,c=P.e(b,ak),e=P.e(b,mj),d=P.e(b,vk),g=P.e(b,aj),f=P.e(b,Qk),h=Xl.F(),m=Fs(b),p=ce(s(f)?f:Hh),q=function(){var a=mk.h(b);return s(a)?a:0}(),r=Hs(g),u=null==e?
!0:e,z=function(){var a=new br;a.jc=Math.max(0,q);a.Ze=u;return a}();Ke.D(Ms,rd,h,z);z.Ke("complete",function(a,b,c,d,e,f,g,h,m,p,q){return function(c){c=c.target;var d=mr(c),e=nr(c),f;try{f=c.W?c.W.responseText:""}catch(h){Bo(c.Va,"Can not get responseText: "+h.message),f=""}f=new n(null,5,[uj,d,nj,e,vk,f,aj,Ls(c.getAllResponseHeaders()),Kk,new T(null,2,5,U,[b,String(c.Rc)],null)],null);Uc.e(g.wc,7)||$l.e(a,f);Ke.j(Ms,sd,a);s(q)&&el(q);return el(a)}}(h,m,p,q,r,u,z,a,b,b,c,e,d,g,f));z.send(m,p,d,
r);if(s(c)){var E=Xl.h(1);Al(function(a,b,c,d,e,f,g,h,m,p,q,r,u,z,E,Sa){return function(){var Ta=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(f){if(f instanceof Object){c[5]=f;Tl(c);d=Z;break a}throw f;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,
a)}throw Error("Invalid arity: "+arguments.length);};d.F=c;d.h=b;return d}()}(function(a,b,c,d,e,f,g,h,m,p,q,r){return function(a){var b=a[1];return 5===b?Rl(a,a[2]):4===b?(a[2]=null,a[1]=5,Z):3===b?(b=h.abort(),a[2]=b,a[1]=5,Z):2===b?(b=sb(4==lr(h)),a[7]=a[2],a[1]=b?3:4,Z):1===b?Pl(a,2,r):null}}(a,b,c,d,e,f,g,h,m,p,q,r,u,z,E,Sa),a,b,c,d,e,f,g,h,m,p,q,r,u,z,E,Sa)}(),kb=function(){var b=Ta.F?Ta.F():Ta.call(null);b[6]=a;return b}();return Ol(kb)}}(E,h,m,p,q,r,u,z,a,b,b,c,e,d,g,f))}return h}))))))))))))))));
var Zs=function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){var d=O.j(b,0,null),d=Jg.k(L([d,new n(null,2,[di,Hh,Ij,a],null)],0));return Ys.h?Ys.h(d):Ys.call(null,d)}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}(),$s=function(){function a(a,e){var d=null;if(1<arguments.length){for(var d=0,g=Array(arguments.length-1);d<g.length;)g[d]=arguments[d+
1],++d;d=new C(g,0)}return b.call(this,a,d)}function b(a,b){var d=O.j(b,0,null),d=Jg.k(L([d,new n(null,2,[di,Tj,Ij,a],null)],0));return Ys.h?Ys.h(d):Ys.call(null,d)}a.q=1;a.l=function(a){var e=D(a);a=F(a);return b(e,a)};a.k=b;return a}();Ge.h?Ge.h(null):Ge.call(null,null);Ge.h?Ge.h(0):Ge.call(null,0);hb=!1;gb=function(){function a(a){var e=null;if(0<arguments.length){for(var e=0,d=Array(arguments.length-0);e<d.length;)d[e]=arguments[e+0],++e;e=new C(d,0)}return b.call(this,e)}function b(a){return console.log.apply(console,zb.h?zb.h(a):zb.call(null,a))}a.q=0;a.l=function(a){a=B(a);return b(a)};a.k=b;return a}();
function at(a){var b=Xl.h(1);Al(function(b){return function(){var e=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(f){if(f instanceof Object){c[5]=f;Tl(c);d=Z;break a}throw f;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,a)}throw Error("Invalid arity: "+
arguments.length);};d.F=c;d.h=b;return d}()}(function(){return function(b){var c=b[1];if(3===c)return Rl(b,b[2]);if(2===c){var c=b[7],d=new T(null,2,5,U,[qh,b[2]],null);return Ql(b,3,c,d)}if(1===c){var c=rh.h(a),d=[w("/list/"),w(Sh)].join(""),e=[zk],p=[qd([sj],[Sh])],e=qd(e,p),d=Zs.k(d,L([e],0));b[7]=c;return Pl(b,2,d)}return null}}(b),b)}(),d=function(){var a=e.F?e.F():e.call(null);a[6]=b;return a}();return Ol(d)}}(b));return a}
function bt(a,b){var c=Xl.h(1);Al(function(c){return function(){var d=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(g){if(g instanceof Object){c[5]=g;Tl(c);d=Z;break a}throw g;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,a)}throw Error("Invalid arity: "+
arguments.length);};d.F=c;d.h=b;return d}()}(function(){return function(c){var d=c[1];if(3===d)return Rl(c,c[2]);if(2===d){var d=c[7],e=new T(null,2,5,U,[Eh,c[2]],null);return Ql(c,3,d,e)}return 1===d?(d=rh.h(a),e=[w("/search/"),w(b)].join(""),e=Zs(e),c[7]=d,Pl(c,2,e)):null}}(c),c)}(),g=function(){var a=d.F?d.F():d.call(null);a[6]=c;return a}();return Ol(g)}}(c));return a}
function ct(a,b){var c=Xl.h(1);Al(function(c){return function(){var d=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(g){if(g instanceof Object){c[5]=g;Tl(c);d=Z;break a}throw g;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);case 1:return b.call(this,a)}throw Error("Invalid arity: "+
arguments.length);};d.F=c;d.h=b;return d}()}(function(){return function(c){var d=c[1];if(3===d){var d=c[2],e=sd.e(a,bi);c[7]=d;return Rl(c,e)}if(2===d)return d=c[8],e=new T(null,2,5,U,[zi,c[2]],null),Ql(c,3,d,e);if(1===d){var d=bi.h(a),d=rf(d),e=Uj.h(b),e=nn(e),d=P.e(d,e),d=Fn(d),g=D(d),d=rh.h(a),e=[qj],g=[qd([Uj],[g])],e=qd(e,g),e=$s.k("/add",L([e],0));c[8]=d;return Pl(c,2,e)}return null}}(c),c)}(),g=function(){var a=d.F?d.F():d.call(null);a[6]=c;return a}();return Ol(g)}}(c));return c}
function dt(a,b){var c=Fd(b)?Q.e(Ee,b):b,e=P.e(c,vk);P.e(c,uj);return rd.j(a,sj,nn(e))}function et(a,b){var c=Fd(b)?Q.e(Ee,b):b,c=P.e(c,vk);return rd.j(a,bi,c)}
function ft(a){var b=Fd(a)?Q.e(Ee,a):a,c=P.e(b,xi),e=P.e(b,Li),d=rh.h(J.h?J.h(e):J.call(null,e)),g=Xl.h(1);Al(function(a,b,c,d,e,g){return function(){var u=function(){return function(a){return function(){function b(c){for(;;){var d;a:{try{for(;;){var e=a(c);if(!be(e,Z)){d=e;break a}}}catch(f){if(f instanceof Object){c[5]=f;Tl(c);d=Z;break a}throw f;}d=void 0}if(!be(d,Z))return d}}function c(){var a=[null,null,null,null,null,null,null,null,null,null];a[0]=d;a[1]=1;return a}var d=null,d=function(a){switch(arguments.length){case 0:return c.call(this);
case 1:return b.call(this,a)}throw Error("Invalid arity: "+arguments.length);};d.F=c;d.h=b;return d}()}(function(a,b,c,d,e,f){return function(a){var c=a[1];if(7===c){var c=a[2],d=O.j(c,0,null),g=O.j(c,1,null),c=[w("on channel ["),w(d),w("], received value ["),w(g),w("]")].join(""),c=console.log(c),d=P.e(e,d),d=Ke.j(f,d,g),g=J.h?J.h(f):J.call(null,f),g=Jn(g);a[7]=g;a[8]=d;a[9]=c;a[2]=null;a[1]=2;return Z}return 6===c?(c=a[2],a[2]=c,a[1]=3,Z):5===c?(a[2]=null,a[1]=6,Z):4===c?Pl(a,7,b):3===c?(c=a[2],
Rl(a,c)):2===c?(a[1]=4,Z):1===c?(a[2]=null,a[1]=2,Z):null}}(a,b,c,d,e,g),a,b,c,d,e,g)}(),z=function(){var b=u.F?u.F():u.call(null);b[6]=a;return b}();return Ol(z)}}(g,d,a,b,c,e))}function gt(a,b){fh.k(L([b],0));return a}function ht(){var a;a=new n(null,2,[rh,Xl.F(),Bi,Lk],null);a=Ge.h?Ge.h(a):Ge.call(null,a);a=new n(null,2,[Li,a,xi,new n(null,7,[Ek,gt,$h,ct,Eh,et,zi,gt,qh,dt,ei,bt,Fj,gt],null)],null);ft(a);a=Li.h(a);a=J.h?J.h(a):J.call(null,a);return at(a)}var it=["client","main"],jt=ba;
it[0]in jt||!jt.execScript||jt.execScript("var "+it[0]);for(var kt;it.length&&(kt=it.shift());)it.length||void 0===ht?jt=jt[kt]?jt[kt]:jt[kt]={}:jt[kt]=ht;
})();
